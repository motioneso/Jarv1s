// external-modules/job-search/src/web/screens/monitors.tsx (placeholder —
// replaced by the real Monitors screen in a later task)
import { h, type ReactNodeLike } from "../runtime";

export function MonitorsScreen(): ReactNodeLike {
  return <p>Monitors arrive in a later task.</p>;
}

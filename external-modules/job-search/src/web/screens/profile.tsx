// external-modules/job-search/src/web/screens/profile.tsx (placeholder —
// replaced by the real Profile & resume screen in a later task)
import { h, type ReactNodeLike } from "../runtime";
import type { HostActions } from "../root";

export function ProfileScreen(_props: { hostActions: HostActions }): ReactNodeLike {
  return <p>Profile arrives in a later task.</p>;
}

// external-modules/job-search/src/web/screens/opportunities.tsx (placeholder —
// replaced by the real Opportunities shell in a later task)
import { h, type ReactNodeLike } from "../runtime";

export function OpportunitiesScreen(_props: { path: string }): ReactNodeLike {
  return <p>Opportunities arrive in a later task.</p>;
}

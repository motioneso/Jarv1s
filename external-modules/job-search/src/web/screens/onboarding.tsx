// external-modules/job-search/src/web/screens/onboarding.tsx (placeholder —
// replaced by the real Onboarding screen in a later task)
import { h, type ReactNodeLike } from "../runtime";
import type { HostActions } from "../root";

export function OnboardingScreen(_props: { hostActions: HostActions }): ReactNodeLike {
  return <p>Onboarding arrives in a later task.</p>;
}

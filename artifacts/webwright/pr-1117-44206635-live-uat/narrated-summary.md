# Narrated final pass

The exact-head run began with a real seeded-owner login and moved through Today, Tasks, Calendar,
News, Sports, and Wellness before opening ordinary chat, private-chat disclosure, history, and the
full personal/admin Settings rails. The core surfaces were visually coherent on desktop, and the
five built-in accents remained independent from Light/Dark mode across all 10 combinations.

The deeper News pass reached configuration through the real Modules route. Curated topic keyboard
add/remove, publisher validation retention, excluded-publisher add/remove, the source-empty state,
refresh persistence, narrow layout, and a real outbound article link worked. Freeform topics were
honestly gated by missing web search, no feedback controls appeared, and the attempted image-failure
simulation did not produce valid failure evidence.

A fresh `solo-admin` owner then exercised first-time onboarding on desktop and narrow layouts,
including the skip warning, Back/Continue, unavailable provider truth, optional connector skip, and
Finish. The final visible `Go to settings` action incorrectly opened Today.

The narrow pass preserved Dark+Dusk and rendered Tasks and Appearance, but Today compressed its
lead copy into a one-word-per-line column. Activity also remained `Loading…` after a bounded wait.
The microphone path could not proceed because Firefox permission grant was unsupported and no
transcription model was configured. Destructive export/deletion and other prerequisite-dependent
flows were not claimed.

Final narrated verdict: **RED / BLOCKED**. The exact-head appearance matrix and broad route coverage
are positive, but onboarding destination, Activity loading, narrow Today layout, and explicit proof
gaps prevent closing acceptance.

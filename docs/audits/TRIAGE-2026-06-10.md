# Audit Triage — Consolidating the Four 2026-06-10 Runs

**Purpose:** One map across the four independent audit runs (Fable, PLO, PLOO, otnr),
their GitHub issues, their revalidation status against live `origin/main`, cross-run
duplicates, and the findings that are **not yet filed anywhere**.

**Key context:** All four ran on a stale local `main` (`1601240`) that was **34 ahead /
8 behind** `origin/main` (`240de7e`). The substantive staleness was the **8 missing merged
PRs** (~9,500 insertions, incl. PR #93 multi-user lifecycle → migrations 0047/0050/0051/0052).

---

## 1. The four runs at a glance

| Run | Model | Scope | Findings | Issues filed | Revalidated vs origin? |
| --- | --- | --- | --- | --- | --- |
| **otnr** | Opus | 29-phase (per-module + cross-cutting) | 337 (0 CRIT / 35 HIGH / 117 MED / 109 LOW / 76 INFO) | **#113–#171** (`[OTNR-P*]`, 59 issues) | ✅ **Yes** @ `240de7e` — 0 fixed, 32 line-shift, 90 stand |
| **PLOO** | Opus | 8-phase | 0 CRIT / 2 HIGH headline (+ MED/LOW) | **#95–#112 + #172** (`[audit-P*]`)¹ | ✅ **Yes** @ migration 0052 — closed #103, 3 fixed/partial, rest stand, filed #172 |
| **Fable** | — | 8-phase | 0 CRIT / ~8 HIGH | Absorbed into **#95–#111** (`[audit-P*]`)¹ | ❌ No (some findings already stale, e.g. "P2 lifecycle absent" = fixed by #93) |
| **PLO** | Sonnet | 28-file (4 sec dims + modules + infra + tests) | 6→**4** CRIT, 47 HIGH (after its own Opus verification) | ❌ **No filed issues** — findings only | ⚠️ Self-verified vs **cited files on the stale tree**, NOT vs origin |

¹ The `[audit-P*]` issue series is the **shared home** for both the Fable 8-phase findings
and PLOO's additions. Fable's report docs don't cite issue numbers because the issues were
filed separately under that label.

**Headline:** No run found a true CRIT. PLO's 6 "CRIT" collapsed to 4 after its own Opus
verification, and those 4 are severity-inflated versions of findings the two Opus runs filed
as HIGH (or did not consider CRIT-worthy). Treat the **filed HIGH severity as authoritative.**

---

## 2. Revalidation status — the stale-branch question is mostly answered

- **otnr (#113–#171):** fully re-judged against `origin/main @ 240de7e`. Verdict: **0
  findings fixed**, 32 need line-number/partial updates, 90 stand verbatim. All 59 issues
  remain valid. (`docs/audit/otnr/revalidation-vs-origin.md`)
- **PLOO/audit series (#95–#112):** fully re-validated against `origin/main @ 0052` with
  per-claim adversarial verification. **Closed #103** (P2 lifecycle — fixed by PR #93);
  #108/#110 sub-findings partially resolved; 8 HIGH + others still real; filed **#172**;
  refuted 3 claims (not filed). (`docs/audits/PLOO Audit/00-summary-triage.md`)
- **Fable & PLO:** never self-revalidated. Fable's findings are covered by the
  PLOO-revalidated `[audit-P*]` series, so they inherit that revalidation. **PLO is the only
  run whose unique findings have neither been filed nor checked against origin.**

➡️ **Remaining verification work is small and targeted** (Section 4), not a full re-run.

---

## 3. Cross-run agreement — high-confidence findings (filed in BOTH issue series)

These were found independently by multiple runs → highest confidence, but currently filed
as **duplicate issues across the two series**. Fixing each closes both.

| Finding | `[audit-P*]` issue | `[OTNR-P*]` issue | Notes |
| --- | --- | --- | --- |
| SettingsRepository raw Kysely / no actor GUC (DataContextDb invariant) | **#95** | **#155, #156** | otnr frames via `/api/me` raw-Kysely read |
| `assertDataContextDb` missing in memory/structured-state | **#102** | **#146, #154** (MED batches) | |
| structured-state caller-supplied `ownerUserId` | **#99** | (P17 batch #154) | DB `WITH CHECK` mitigates → likely MED |
| `users` self-UPDATE no column restriction (admin self-escalation) | **#97** | (P1 batch #117) | |
| Worker has grants but no RLS policy on memory tables | **#98** | (P12 batch #146) | breaks chat episodic recall (fails closed) |
| Admin tables (`workspaces`/`memberships`/`resource_grants`/`instance_settings`) no RLS | (#104 P1 batch) | **#115, #116** (+ #117 batch) | PLO called this CRIT-01 |
| `handleRouteError` collapses to 401 (calendar/email/notifications) | (#111 P8 batch) | **#149** | systemic consolidation target |
| No rate limit on AI/chat/MCP | (#106 P3 batch) | **#118, #121, #162** | PLO H-03 |
| `delete-user-data` orphans vault files | **#96** | (P29 #170 related: export omits) | PLO H-04 |
| Resource-grants admin path is a dead no-op | (#108 P5 batch) | **#153** | |

**Action:** dedup the two series — pick the better-cited issue as canonical, close the
other as duplicate with a cross-link. ~10 HIGH-level duplicate pairs.

---

## 4. PLO-unique findings — NOT filed in either series (the real gap)

PLO (Sonnet) was the only run not turned into issues. Most of its findings overlap the
filed set, but a handful appear **nowhere on GitHub** and were confirmed by PLO's own Opus
verification pass. These are the **net-new candidates to verify against origin/main and
file**:

| PLO ref | Finding | File (corrected by PLO's own verify) | Verify priority |
| --- | --- | --- | --- |
| CRIT-02 | **pg-boss schema has no RLS** — cross-user job *metadata* enumeration (both runtime roles full DML, zero policies) | `pgboss.*` | **HIGH** — check vs origin; reword "payloads"→"metadata" |
| CRIT-06 | **`audit-release-hardening` omits ≥13 FORCE-RLS tables** — false-confidence gate (`passed:true` even if a policy is dropped) | `scripts/audit-release-hardening.ts` | **HIGH** — gate integrity; likely still present |
| H-05 | `eval` over operator-controlled env var in backup script | `scripts/backup-full.sh:114` | MED — needs host/env control |
| H-06 | smoke-compose polls trivial `/health`, not `/health/ready` | `scripts/smoke-compose.ts:27` | MED |
| H-08 | Vault files plaintext at rest (no encryption) | `packages/vault/src/vault-ops.ts` | check — may be intentional/spec'd |
| H-07 | Vault files written with no explicit mode (not `0o600`) | `packages/vault/src/vault-ops.ts:19` | MED |

PLO findings to **drop** (refuted by its own verification): H-02 (Google API key in URL —
file doesn't exist), H-19 (confirmation-bridge race — keyed per `actionRequestId`, no race).
PLO findings to **downgrade**: CRIT-03→HIGH (worker silent-write, not disclosure), CRIT-04→HIGH,
H-13→MED, H-14→MED.

---

## 5. Other singly-sourced findings worth noting

- **#112 (PLOO-unique HIGH): hardcoded `BETTER_AUTH` dev secret** signs sessions whenever
  `NODE_ENV != "production"` → session forgery on LAN self-host. Not caught by Fable/otnr.
  Filed + PLOO-revalidated → treat as real, action via security-tier PR.
- **#100 (Fable-unique HIGH): no HTTP security headers** (CSP/X-Frame/HSTS). Filed.
- **otnr-unique HIGH cluster** (not in `[audit-P*]`): bearer-token bypass #113/#126, dead
  workspaces subsystem #120, vault symlink-escape #129/#130, REST tool-invoke bypasses
  gateway #132, incognito not trigger-enforced #135, task cross-owner write-escalation
  #137/#139/#140, blank ToolContext in briefings #148, structured-state contribute/manage
  unenforceable #152, export:user omits private content #170.

---

## 6. Recommended next steps (after this triage)

1. **Dedup GitHub** — close ~10 cross-series duplicate HIGH pairs (Section 3), canonicalize
   the better-cited issue. Cheap, high-clarity.
2. **Verify + file PLO-unique gaps** (Section 4) — small targeted pass against `origin/main`,
   especially CRIT-02 (pg-boss RLS) and CRIT-06 (audit-script coverage gap).
3. **Apply otnr/PLOO line-number updates** to the ~32+ issues whose citations shifted (both
   runs already produced the corrected `file:line` evidence).
4. Everything else is already filed and revalidated — proceed to fix-prioritization
   (the otnr exec summary §7 fix-order is a good spine).

**Bottom line:** the four runs converge, not conflict. ~77 audit issues are filed; the two
Opus runs already revalidated their halves against live `origin/main` with zero findings
eliminated. The outstanding work is **dedup + a small PLO-gap verification pass**, not a
re-audit.

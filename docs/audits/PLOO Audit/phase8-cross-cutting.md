# PLOO Audit — Phase 8 — Cross-cutting sweep (module isolation + invariants)

> Independent Opus pass — 2026-06-10. Read-only review; findings only (no fixes applied). Did not read the parallel Fable audit findings.

## Severity counts
CRIT: 0 | HIGH: 0 | MED: 2 | LOW: 3 | INFO: 5   (refuted: 0)

## Findings

#### [MED] Worker-role grant/policy pairing is consistent across all modules EXCEPT memory_chunks/memory_file_index — the lone unpaired grant (worker chat-embed writes RLS-denied)
**File:** packages/memory/sql/0040_memory_chat_source.sql:15
**Invariant violated / concern:** Defense-in-depth via RLS + least-privilege roles: every runtime role granted DML on a FORCE-RLS table must also have an applicable RLS policy, or it is denied all rows.
**Detail:** Cross-cutting sweep of every `GRANT ... TO jarvis_worker_runtime` in packages/*/sql confirms a consistent codebase pattern: each module that grants the worker DML also creates a worker-scoped RLS policy. chat/sql/0036_chat_worker_runtime_grants.sql grants worker DML AND its policies say `TO jarvis_app_runtime, jarvis_worker_runtime` (lines 24/38/57); ai/sql/0037 likewise (lines 19/29); briefings/sql/0015 (policies 155/173/186/195) and 0026 (16/37/56/72) include the worker on every policy; tasks/sql/0003 grants and policies both cover the worker; memory/sql/0041_memory_facts.sql:45 grants the worker and its policies are role-agnostic (apply to all roles). The SINGLE outlier is memory/sql/0040_memory_chat_source.sql:15-16, which grants the worker SELECT/INSERT/UPDATE/DELETE on app.memory_chunks and app.memory_file_index but adds NO worker-applicable policy — every policy on those two tables is scoped `TO jarvis_app_runtime` only (memory/sql/0030_memory_index.sql:44-63, memory/sql/0032_memory_embedding_768.sql:42-61) under FORCE RLS. The chat embed-turn worker (registered onto the jarvis_worker_runtime connection via module-registry/src/index.ts) calls MemoryRepository.upsertFileChunks/upsertFileIndex (packages/chat/src/jobs.ts:61-93), which are RLS-denied, so chat episodic memory is silently never persisted (fails closed — no cross-user leak). Because the rest of the codebase pairs grants with policies correctly, this is a genuine isolated regression, not a design choice. Corroborates the phase-4/phase-6 MED from the cross-cutting angle: the inconsistency, not the bug in isolation, is the signal.
**Verification:** confirmed (MED). packages/memory/sql/0040_memory_chat_source.sql:15-16 grants jarvis_worker_runtime DML on app.memory_chunks and app.memory_file_index but adds no policy; the only policies on those tables are scoped TO jarvis_app_runtime (0030_memory_index.sql:44-63 under FORCE RLS 0030:39-40; 0032_memory_embedding_768.sql:42-61 under FORCE RLS 0032:39-40), and a grep of all packages/*/sql confirms no later policy includes the worker. The chat embed-turn worker runs as jarvis_worker_runtime (apps/worker/src/worker.ts:13-17 builds the runner on urls.worker = postgres://jarvis_worker_runtime per packages/db/src/urls.ts:27-30; worker.ts:36 → registerBuiltInModuleWorkers → module-registry/src/index.ts:152-153); registerDataContextWorker only sets app.actor_user_id via set_config and never changes the session role (pg-boss.ts:96 → data-context.ts:31,67), and that role is NOBYPASSRLS/NOINHERIT and non-owner (0000_roles.sql:45-51). handleEmbedTurnJob is fully implemented and calls upsertFileChunks/upsertFileIndex (chat/src/jobs.ts:74-93), so under FORCE RLS with no applicable worker policy those writes are denied. Rationale: real, fail-closed (writes silently denied, episodic memory never persisted, no cross-user leak, no secret escape) and a clean violation of the codebase's grant↔policy defense-in-depth invariant — correctly MED.
**Suggested fix:** In a NEW migration (never edit applied 0030/0032/0040), widen the memory_chunks and memory_file_index SELECT/INSERT/UPDATE policies to `TO jarvis_app_runtime, jarvis_worker_runtime`, preserving the `owner_user_id = app.current_actor_user_id()` USING/WITH CHECK verbatim — exactly the pattern chat 0036, ai 0037, and briefings 0026 already use. Alternatively REVOKE the worker DML in 0040 if app-runtime-only is intended, so the grant surface matches the policy surface.

#### [MED] ModuleJobManifest metadataOnly/payloadSchema is a dead declarative contract — never enforced by the shared worker harness
**File:** packages/module-sdk/src/index.ts:67
**Invariant violated / concern:** Metadata-only job payloads (pg-boss payloads must contain only actor/resource IDs, job kind, idempotency key, and small command params — never private content, prompts, or secrets).
**Detail:** The ModuleJobManifest type declares `payloadSchema?: JsonSchema` and `metadataOnly?: boolean` (packages/module-sdk/src/index.ts:67-68), and two modules dutifully populate them: briefings (packages/briefings/src/manifest.ts:127-128, metadataOnly: true + briefingRunPayloadSchema) and tasks (packages/tasks/src/manifest.ts:222-223, metadataOnly: true + deferredTaskStatusPayloadSchema). A repo-wide grep for `metadataOnly`/`payloadSchema` outside the type declaration and these two producers returns NOTHING — no code in packages/module-registry, packages/jobs, apps/worker, or apps/api ever READS these fields. The shared worker harness `registerDataContextWorker` (packages/jobs/src/pg-boss.ts:84-98) wraps every module job worker but only calls `toAccessContext` (lines 100-109), which checks for actorUserId presence and nothing else — it never validates the dequeued payload against the manifest's declared payloadSchema, nor asserts metadataOnly. The result: the SDK's own metadata-only enforcement mechanism is inert. tasks and briefings instead each hand-roll a private guard (isDeferredTaskStatusPayloadMetadataOnly, isBriefingRunPayloadMetadataOnly) at enqueue time, and chat's two queues (packages/chat/src/jobs.ts:16-21, CHAT_EMBED_TURN_QUEUE / CHAT_EXTRACT_FACTS_QUEUE) have NO guard at all and no manifest jobs entry. This is the missing SHARED guardrail the phase notes warned about: the invariant is enforced ad hoc per module (or not at all) rather than structurally at the one chokepoint every job passes through. The first auditor's LOW flagged only chat's two queues; the systemic root cause — a declared SDK contract that no runtime path consumes — is broader and is what lets the chat gap (and any future module's) exist undetected.
**Verification:** confirmed (MED). Both fields declared at module-sdk/src/index.ts:67-68; repo-wide grep for `metadataOnly` returns only the type decl, the two producers (tasks/manifest.ts:223, briefings/manifest.ts:128) and two test assertions (tasks.test.ts:186, briefings.test.ts:199); `payloadSchema` returns only the type decl plus the same two producers. The only references to `manifest.jobs` anywhere are two `toMatchObject` assertions — no runtime consumer. registerDataContextWorker (jobs/src/pg-boss.ts:84-98) only calls toAccessContext (100-109), which validates actorUserId presence and nothing else; module-registry routes job queues via queueDefinitions (index.ts:84,111,142,158,190), a path separate from the SDK manifest.jobs contract. tasks/briefings hand-roll guards (tasks/src/jobs.ts:52-56 enforced at routes.ts:265 and jobs.ts:72-75; briefings equivalent at jobs.ts:55,72), neither consulting the manifest; chat's two queues (chat/src/jobs.ts:16-22) registered at lines 128-153 with no guard and no jobs manifest array. Rationale: the declared guardrail is genuinely inert and enforcement is ad hoc, but no invariant is broken on a live path today (tasks/briefings guard at both ends; chat payloads structurally carry only IDs), so the exposure is a missing-by-default structural protection — exactly MED, and the systemic root cause underlying the narrower chat-only LOW.
**Suggested fix:** Make the harness the enforcement chokepoint: in registerDataContextWorker (packages/jobs/src/pg-boss.ts), look up the queue's ModuleJobManifest entry and, when metadataOnly is set, validate the dequeued payload against payloadSchema (reject/dead-letter on mismatch) before invoking the handler. Then delete the per-module hand-rolled is*PayloadMetadataOnly guards in favor of the shared check, and require a manifest jobs entry (with metadataOnly + payloadSchema) for chat's CHAT_EMBED_TURN_QUEUE and CHAT_EXTRACT_FACTS_QUEUE so they are covered too.

#### [LOW] Chat's two pg-boss queues lack the runtime metadata-only payload guard that tasks and briefings enforce at both enqueue and worker — missing shared guardrail
**File:** packages/chat/src/live/persistence.ts:115
**Invariant violated / concern:** Metadata-only job payloads: pg-boss payloads contain only actor/resource IDs, job kind, idempotency key, and small command params — never private content, prompts, or secrets.
**Detail:** A cross-cutting comparison of all four actor-scoped job queues shows two distinct enforcement postures. Tasks (packages/tasks/src/jobs.ts:52 isDeferredTaskStatusPayloadMetadataOnly, enforced in-worker at jobs.ts:73 AND at enqueue in routes.ts:265) and briefings (packages/briefings/src/jobs.ts:55 isBriefingRunPayloadMetadataOnly, enforced in-worker at jobs.ts:72 AND at enqueue in routes.ts:132) each define an allowed-keys predicate and assert it at BOTH the boss.send site and the worker handler. Chat's two queues do NOT: persistence.ts:106-116 calls `this.boss.send(CHAT_EMBED_TURN_QUEUE, embedPayload)` and `boss.send(CHAT_EXTRACT_FACTS_QUEUE, extractPayload)` with no shape assertion, and the worker handlers (packages/chat/src/jobs.ts) have no equivalent guard. The payloads are metadata-only by construction today — EmbedTurnJobPayload = {actorUserId, threadId, messageId}, ExtractFactsJobPayload = {actorUserId, threadId} (packages/chat/src/jobs.ts:26-33) — so there is no live leak. This is a missing regression tripwire on the chat memory-write path: the codebase HAS the pattern (twice) but chat did not adopt it, so a future edit that adds a `text`/`body`/`embedding` field to a chat payload would not be caught, whereas the same mistake on tasks/briefings throws. Reported once as the cross-cutting confirmation that the guardrail exists but was not applied uniformly.
**Verification:** confirmed (LOW). Tasks predicate at jobs.ts:52, enforced in-worker at jobs.ts:72-76 and at enqueue at routes.ts:264-268 (before boss.send at 270); briefings predicate at jobs.ts:55, enforced at jobs.ts:72-74 and routes.ts:132-134 (before boss.send at 136). Chat persistence.ts:115-116 sends both payloads with NO assertion; worker handlers at chat/src/jobs.ts:128-153 have none; `grep "MetadataOnly|PAYLOAD_KEYS|allowedKeys" packages/chat/src/` returns zero matches. The shared registerDataContextWorker (jobs/src/pg-boss.ts:84-98) provides no built-in guard, confirming it is per-module opt-in. Payloads are metadata-only by construction (EmbedTurnJobPayload IDs only at jobs.ts:26-29, ExtractFactsJobPayload at 31-33), so no live leak. Rationale: invariant holds at runtime today; the gap is a missing regression tripwire on a currently-safe path — exactly LOW (no risky pattern active, nothing exposed).
**Suggested fix:** Add CHAT_EMBED_TURN_PAYLOAD_KEYS / CHAT_EXTRACT_FACTS_PAYLOAD_KEYS allowed-keys predicates mirroring isDeferredTaskStatusPayloadMetadataOnly, and assert them at both the boss.send sites in persistence.ts:115-116 and the top of the worker handlers in chat/src/jobs.ts. Better still, lift a single `assertMetadataOnlyPayload(keys, payload)` helper into @jarv1s/jobs so all queues share one guardrail and cannot drift.

#### [LOW] AES-256-GCM secret cipher is duplicated byte-for-byte across the ai and connectors modules instead of shared, so envelope/crypto hardening must be applied twice
**File:** packages/ai/src/crypto.ts:11
**Invariant violated / concern:** Defense-in-depth / maintainability: a single security-critical primitive (at-rest secret encryption) should have one implementation so a hardening fix or correction propagates everywhere and the two copies cannot silently diverge.
**Detail:** packages/ai/src/crypto.ts (AiSecretCipher) and packages/connectors/src/crypto.ts (ConnectorSecretCipher) are structurally identical: same `aes-256-gcm` createCipheriv/createDecipheriv, same {version:1, algorithm, keyId, iv, ciphertext, authTag} envelope, same legacy-candidate authentication fallback loop (both lines 44-56), both importing the shared `resolveKeyring`/`Keyring` from @jarv1s/db (both line 3). A comment-stripped diff shows the ONLY differences are the type/class names, the human-readable error strings, and the env-var names + hardcoded dev-default literal passed to resolveKeyring (`jarv1s-development-ai-secret` vs `jarv1s-development-connector-secret`). The keyring itself is correctly shared in packages/db/src/keyring.ts, but the cipher/envelope layer is copy-pasted. Consequence: any future hardening to the envelope (e.g. adding AAD binding, an envelope v2, or fixing the no-salt KDF concern that lives below this layer) must be made in two places and can drift between modules; and both copies independently embed a hardcoded dev-default secret string, echoing the phase-2 silent-dev-default concern across both secret-bearing modules. No active leak — purely a duplicated-critical-primitive maintainability/defense-in-depth gap.
**Verification:** confirmed (LOW). ai/src/crypto.ts and connectors/src/crypto.ts are byte-for-byte identical in logic: identical envelope interface (lines 5-12), identical encryptJson (17-32: 12-byte random IV + createCipheriv("aes-256-gcm") + getAuthTag), identical decryptJson (34-79) with the same legacy-candidate fallback loop (51-61) and per-keyId path (66-70), both importing { type Keyring, resolveKeyring } from "@jarv1s/db" at line 3. Differences limited to class/type names, error wording, and factory env vars + dev-default literals (ai/crypto.ts:88 "jarv1s-development-ai-secret" vs connectors/crypto.ts:88 "jarv1s-development-connector-secret"). Keyring correctly shared at db/src/keyring.ts:20; a repo-wide grep for createCipheriv/aes-256-gcm/SecretCipher returns only these two files plus route consumers — no shared abstraction. keyring.ts:29-31 throws if the key is absent under NODE_ENV==="production", so the dev-defaults are genuinely dev-only. Rationale: a real duplicated security-critical primitive (hardening must be applied twice, copies can drift), but encryption is sound and no secret reaches a response/log/payload — a maintainability/defense-in-depth hygiene gap with speculative-future risk, precisely LOW.
**Suggested fix:** Extract a single generic `SecretCipher` (parameterized by envelope label + the keyring) into @jarv1s/db alongside resolveKeyring, and have both ai and connectors construct it with their own env-var names. One implementation of the GCM envelope and legacy-candidate logic eliminates drift and gives a single chokepoint for crypto hardening.

#### [LOW] DataContextDb tripwire (assertDataContextDb) is opt-in per repository, not structural — skipped in memory, structured-state, and chat repos
**File:** packages/db/src/data-context.ts:52
**Invariant violated / concern:** DataContextDb only: repositories accept only a branded DataContextDb handle (never a root Kysely instance), and the transaction-local app.actor_user_id GUC must be set for every scoped query.
**Detail:** assertDataContextDb (packages/db/src/data-context.ts:52-60) is the ONLY runtime check that a handle actually came from withDataContext — it verifies the non-exported Symbol marker `[dataContextBrand]: true` that withDataContext sets (line 36). The brand itself is just a property on a plain `{ db, [dataContextBrand]: true }` object, so the protection is real only where a repository explicitly CALLS assertDataContextDb before touching `handle.db`. A repo-wide scan shows multiple repositories that accept a DataContextDb-typed handle and execute against `scopedDb.db` directly WITHOUT ever calling the tripwire: packages/memory/src/repository.ts, packages/memory/src/facts-repository.ts, packages/memory/src/retrieval.ts, packages/memory/src/ingest.ts, packages/structured-state/src/entities-repository.ts, packages/structured-state/src/commitments-repository.ts, packages/structured-state/src/preferences-repository.ts, and packages/chat/src/memory-settings-repository.ts (the last executes raw sql against scopedDb.db at lines 36 and 64 with no assertion). Because the type-level brand is erased at runtime, any caller that constructs or forwards a non-branded `{ db }` (e.g. handing one of these repos a root-pool transaction that never ran setLocal) would execute with app.actor_user_id unset — and the intended clear-error tripwire ('Repository access requires withDataContext') would never fire. Practical exposure is moderated by FORCE RLS (a GUC-unset query sees a NULL actor and denies all rows = fail-closed, not a leak) and by the brand Symbol not being exported, so this is a missing defensive tripwire rather than an open data path — but it spans THREE modules, which signals a missing shared guardrail rather than three isolated oversights. The prior phase-6 pass noted memory/structured-state; the NEW element is chat's memory-settings-repository.ts plus the framing that the brand offers no structural guarantee — enforcement is voluntary per call site, contradicting the first auditor's INFO that 'the DataContextDb brand is sound.'
**Verification:** downgraded MED→LOW. The factual core is confirmed: assertDataContextDb (data-context.ts:52-60) is the established repository-entry tripwire used in ~10 modules (chat/src/repository.ts:26,37,47,97,114,147,183; ai/src/repository.ts 12 sites; notifications:26,43,54,86,117; email:22,33,46; connectors 11 sites; tasks:55,66,76,159,312,330,342; briefings:59,73,86,113,144,160; calendar:22,33,46; db/src/sharing/shares-repository.ts; db/src/probes/rls-probe-repository.ts), and the named outliers genuinely omit it and run directly against scopedDb.db (memory/src/repository.ts, facts-repository.ts; structured-state entities/commitments/preferences repos; chat/src/memory-settings-repository.ts:36,64). BUT two finding claims are wrong/loose: the brand symbol IS exported (db/src/index.ts:2 `export * from "./data-context.js"` re-exports dataContextBrand at data-context.ts:12), and memory/src/retrieval.ts and ingest.ts execute nothing directly — they forward scopedDb to MemoryRepository methods (service layers, not repos). No runtime path forwards a non-branded {db}: a grep for `as DataContextDb` / bare `{ db }` construction in non-test source returned zero hits, and the only producer of a branded handle is withDataContext, which always runs setLocal(app.actor_user_id) (data-context.ts:31). Rationale: the primary protection is FORCE RLS + the per-transaction GUC, which fails closed (NULL actor denies all rows); assertDataContextDb is a redundant secondary guard, so its inconsistent application is hardening/hygiene with no exploitable path — a real multi-module consistency gap in a redundant defensive guard fits LOW, not MED.
**Suggested fix:** Make brand enforcement structural rather than opt-in: have every repository method that reads scopedDb.db route through a single accessor (e.g. a `db(scopedDb)` helper that calls assertDataContextDb internally and returns scopedDb.db), and add a lint rule forbidding direct `.db`/`scopedDb.db` member access outside that helper. At minimum, add the missing assertDataContextDb call at the top of each public method in packages/chat/src/memory-settings-repository.ts and the memory/structured-state repositories listed above.

#### [INFO] Module isolation holds: every cross-module import goes through the package's public barrel; no module reaches into another module's internals or src subpaths
**File:** packages/memory/src/index.ts:1
**Invariant violated / concern:** Module isolation — modules collaborate only through declared public APIs/events; no cross-module internal imports or direct internal table queries.
**Detail:** Verified key questions 1 and 2 across the whole packages/ tree. (a) Zero deep relative imports crossing package boundaries (grep for `../../../` in packages/*/src returns nothing) and zero `@jarv1s/<pkg>/src|dist|internal` subpath imports. (b) Every package.json exports map exposes only the barrel: e.g. memory/ai/tasks/structured-state/chat all declare `exports: { ".": "./src/index.ts" }` (db additionally exposes `./probes`). (c) The only module-to-module dependencies are chat -> @jarv1s/memory + @jarv1s/ai and briefings -> @jarv1s/ai; chat imports MemoryRepository and ChatMemoryFactsRepository, which ARE exported from packages/memory/src/index.ts (the declared public API), and uses them via method calls — it never issues a raw query against memory_chunks/chat_memory_facts (those names appear only in chat comments). briefings consumes findAssistantToolFromManifests/listAssistantToolsFromManifests, both exported from the ai barrel. No module imports another feature module's repository internals, jobs internals, or SQL. No defect.
**Verification:** confirmed (INFO). All packages expose only their barrel (`{".":"./src/index.ts"}`; db additionally `./probes`); grep for `../../../`/`../../` in packages/*/src and for `@jarv1s/<pkg>/(src|dist|internal)` return no matches. The only feature-module-to-feature-module edges are chat→{memory,ai} and briefings→ai; the imported symbols (MemoryRepository, ChatMemoryFactsRepository at memory/src/index.ts:19-23; find/listAssistantToolFromManifests defined in ai/src/assistant-tools.ts:4,21 and re-exported via `export * from "./assistant-tools.js"` at ai/src/index.ts:1) are genuinely exported from the target barrel. chat's memory persistence in jobs.ts goes exclusively through MemoryRepository method calls (getFileIndex:61, upsertFileChunks:74, upsertFileIndex:84); "memory_chunks"/"chat_memory_facts" appear only in comments, and a grep for insertInto/selectFrom/updateTable/deleteFrom against memory tables in chat/src returns nothing. Rationale: the module-isolation invariant holds across the tree; INFO is correct (no defect).
**Suggested fix:** No change required — recorded as a positive verification that the module-isolation invariant holds across the tree.

#### [INFO] No console/logger path emits private content, credentials, or tokens; AI prompt/response surfaces and adapter errors carry no secrets
**File:** apps/worker/src/worker.ts:38
**Invariant violated / concern:** Secrets/private data never escape to logs; credentials/tokens/prompts never reach logs or error messages.
**Detail:** Verified key question 3 tree-wide. The only console.* calls in non-test source are apps/worker/src/worker.ts:38 (logs only the static queue name) and worker.ts:53 (a fatal-crash handler logging `String(err)` + a label — diagnostic, no data path). The API server logs via Fastify with `logger: true` and no custom body serializer / no `disableRequestLogging` override, so default pino logging emits request metadata (method/url) but not request bodies, so chat content and credentials in POST bodies are not logged; the one explicit server log is server.ts:132 `server.log.error({ err, label }, ...)`, an error-object crash log. The AI prompt/response surfaces are clean: the HTTP adapters (packages/ai/src/adapters/http-api.ts) and CLI engine (packages/chat/src/live/) contain no logging of prompts or responses, and adapter error messages embed only HTTP status codes / structural descriptions (e.g. http-api.ts:54 `HTTP ${response.status}`, :129 'No text block in Anthropic response') — never the prompt, the response body, or the API key. (Observed but out-of-scope-as-leak: http-api.ts:100 places the Google API key in the outbound request URL query string, which is the legitimate Google API destination, not an inbound-logged or response-bound surface.)
**Verification:** confirmed (INFO). A tree-wide grep for `console.(log|error|warn|info|debug|trace)` across packages+apps (excluding tests/dist) returns ONLY worker.ts:38 (static queue name) and worker.ts:53-55 (a `String(err)` crash diagnostic, no private-data path). apps/api/src/server.ts:48 sets `logger: options.logger ?? true` with NO disableRequestLogging, NO serializers, NO redact anywhere, so default pino logs request metadata, not bodies; the only `.log.` call is server.ts:132 (error object + label). request.body at server.ts:268-269 is part of the toWebRequest better-auth forwarding helper, not a log sink. packages/ai/src and packages/chat/src contain zero console.*/.log( calls; adapter errors carry only status codes / structural strings (http-api.ts:54, :129); apiKey is used only in outbound surfaces (x-api-key/Bearer headers, Google ?key= URL at http-api.ts:74/90/100), never logged or response-bound; the CLI engine writes the prompt to a temp file and pastes into tmux (cli-chat-engine.ts submit()) — a local FS/IPC seam, not a log. Rationale: positive verification of the no-secret-in-logs invariant — an observation, not a vulnerability — INFO is correct.
**Suggested fix:** No change required — positive verification of the no-secret-in-logs invariant. Optional hardening: consider an explicit pino redact list and confirm outbound provider-request logging is never enabled, so a future log addition cannot surface the Google key-in-URL.

#### [INFO] DataContextDb brand is sound: no string/cast bypass exists anywhere; the recurring bypasses are the already-tracked raw-Kysely repos, which cluster into two fixable families
**File:** packages/db/src/data-context.ts:52
**Invariant violated / concern:** DataContextDb only — every repository accepts only a branded DataContextDb handle, never a raw Kysely instance; the brand cannot be forged.
**Detail:** Verified key question 4 cross-cutting. A tree-wide grep for `as DataContextDb` / `as unknown as DataContextDb` returns ZERO hits, so no code passes a plain string or unbranded object where DataContextDb is required — the brand (a unique Symbol minted only inside DataContextRunner.withDataContext, data-context.ts:11-38) is structurally unforgeable at compile time, and assertDataContextDb (data-context.ts:52) is the runtime backstop. The recurring deviations from the invariant cluster into exactly two families, both already captured by earlier phases: (1) raw-Kysely-constructor repositories — only SettingsRepository (packages/settings/src/repository.ts:64) and AuthSessionResolver (packages/db/src/auth-session.ts:9) take `Kysely<JarvisDatabase>` and run outside any withDataContext (phases 1/5/7); note AuthSessionResolver is defensible — it routes through the SECURITY DEFINER app.resolve_auth_session and returns only user_id, no secret. (2) branded-type-but-no-runtime-guard repos — memory/* and structured-state/* omit the per-method assertDataContextDb tripwire that all 12 other module repos enforce (phase 6). The connectors requireAdmin (routes.ts:258) and chat resolveUserName (persistence.ts:139) read app.users directly; chat does so inside withDataContext, connectors on the raw appDb but only for the caller's own is_instance_admin flag. There is no new bypass beyond these tracked families.
**Verification:** confirmed (INFO). data-context.ts:12 mints `dataContextBrand: unique symbol`, set only inside withDataContext (:36), with assertDataContextDb backstop at :52-60. A tree-wide `git grep "as DataContextDb|as unknown as DataContextDb"` over runtime files returns ZERO hits (only 3 in a test fixture). The two deviation families are accurate: raw-Kysely-constructor repos AuthSessionResolver (auth-session.ts:9, routes through SECURITY DEFINER app.resolve_auth_session returning only user_id) and SettingsRepository (settings/src/repository.ts:64); memory/* and structured-state/* take DataContextDb-typed params but have 0 assertDataContextDb calls vs tasks 33 / ai 13 / connectors 12. connectors requireAdmin (routes.ts:258-262) reads only the caller's own row (id + is_instance_admin); chat resolveUserName reads app.users inside withDataContext (persistence.ts:136-138) and itself calls assertDataContextDb. All runtime roles NOBYPASSRLS (0000_roles.sql:35,43,51,59). Minor cosmetic imprecisions (chat line :136/:138 not :139; the "12 other module repos" count is loose) do not affect substance. Rationale: the brand invariant holds with no exploitable cast bypass; the genuine gaps are already captured by earlier phases — INFO is correct.
**Suggested fix:** No new finding. Track the existing remediations: route SettingsRepository through withDataContext (phase 5/7) and add per-method assertDataContextDb to memory/structured-state repos (phase 6). A shared lint/grep gate forbidding `Kysely<` in any *Repository constructor outside @jarv1s/db would make the invariant structural.

#### [INFO] requireAdmin reads app.users.is_instance_admin on a raw root pool in both settings and connectors — a shared admin-gate pattern on a foundation table that has no DataContext-routed owning repository
**File:** packages/connectors/src/routes.ts:258
**Invariant violated / concern:** No admin private-data bypass / DataContextDb only — admin authorization and identity reads should ideally run under the data-context layer with a DB-level backstop, not solely as an app-layer convention on a raw pool.
**Detail:** Both packages/connectors/src/routes.ts:253-272 requireAdmin and packages/settings/src/routes.ts requireAdmin authorize admins by reading users.is_instance_admin directly off the root appDb (jarvis_app_runtime) with no withDataContext / actor GUC. This is consistent cross-cutting behavior, not a one-off: the foundation app.users table has no encapsulating repository exposed as a module public API (the closest owner is AuthSessionResolver, which only resolves sessions), so every admin gate hand-queries it. Each read is narrowly self-scoped (`where id = accessContext.actorUserId`, selecting only id + is_instance_admin) and app.users has a deliberate `USING(true)` SELECT policy (migration 0045), so no cross-user data is exposed and no exploit exists today. It is recorded here as the cross-cutting framing of the phase-1/5/7 'admin tables have no RLS / SettingsRepository bypasses DataContext' findings: the admin trust decision currently rests entirely on an app-layer flag read from a raw pool, with no DB-level admin policy backstop — the natural place to harden when the not-yet-landed current_actor_is_admin() / migrations 0050-0052 arrive.
**Verification:** confirmed (INFO). connectors/src/routes.ts:257-262 reads `appDb.selectFrom("app.users").select(["id","is_instance_admin"]).where("id","=",actorUserId)` on the raw root pool with no withDataContext, while the other handlers in the same file DO wrap reads in dataContext.withDataContext (lines 69,92,108,126,145,170,196,217) — a deliberate exception. settings/src/routes.ts:331-344 requireAdmin calls requireKnownUser → SettingsRepository.getUserById (repository.ts:73-75), and SettingsRepository is `new SettingsRepository(dependencies.appDb)` on the raw pool (routes.ts:75; constructor repository.ts:64) — also a raw-pool read. appDb is the root Kysely pool on getJarvisDatabaseUrls().app (server.ts:32-35,53). 0045_auth_secret_rls.sql:82-87 confirms users_app_runtime_select is `FOR SELECT TO jarvis_app_runtime USING (true)` (all rows visible, no admin backstop; writes self-row only at 94,101-102). Latest migration is 0046; current_actor_is_admin()/0050-0052 do not exist. The only imprecision: the settings read passes through SettingsRepository.getUserById, but that repo is itself built on raw appDb, so the substance (raw-pool, no GUC, no DB-level admin policy) holds. Rationale: a cross-cutting observation and defense-in-depth note, not an exploitable invariant break — INFO is correct.
**Suggested fix:** When current_actor_is_admin() lands, route is_instance_admin reads through a single shared admin-check helper backed by withDataContext (or the SECURITY DEFINER function), and enable RLS on the admin/identity tables with an admin-scoped policy, so the admin trust boundary is structural rather than a convention duplicated across modules.

#### [INFO] Not-yet-landed audit targets (migrations 0050-0052, current_actor_is_admin()) are absent from the tree — N/A
**File:** infra/postgres/migrations/0046_auth_sessions_rls.sql:1
**Invariant violated / concern:** Scope boundary for this audit pass — do not invent findings for code that does not exist.
**Detail:** Per the audit instructions, the highest in-tree app migration is 0046_auth_sessions_rls.sql and there is no app.current_actor_is_admin() SQL function (grep across infra/ and packages/*/sql returns zero hits for current_actor_is_admin and for 0050/0051/0052). Admin authorization is currently enforced application-side via requireAdmin() reading users.is_instance_admin (packages/settings/src/routes.ts, packages/connectors/src/routes.ts:267). No cross-cutting finding is invented for the not-yet-landed admin-RLS work; the no-admin-bypass invariant should be re-audited against all module RLS once those land.
**Verification:** confirmed (INFO). infra/postgres/migrations/ ls shows the highest app migration is 0046_auth_sessions_rls.sql (read in full — the auth_sessions/auth_verifications RLS lockdown). `grep -rln "current_actor_is_admin" infra/ packages/` → EXIT=1 (zero matches); `find -name "005[0-2]*.sql"` → no output. Application-side enforcement confirmed: connectors/src/routes.ts:253-272 (line 267 `if (!user.is_instance_admin) throw new HttpError(403, ...)`) and the parallel settings requireAdmin (routes.ts:331-339). The DB-level half of the no-admin-bypass invariant is already intact: 0000_roles.sql sets NOBYPASSRLS on all runtime roles (lines 35,43,51,59). Rationale: a correct scope-boundary observation that declines to invent findings for not-yet-landed code — INFO is correct.
**Suggested fix:** Re-run the cross-cutting sweep when migrations 0050-0052 and current_actor_is_admin() land; confirm no module policy ever ORs in an admin predicate that could become a private-data bypass, and address the no-RLS-on-admin-tables / requireAdmin-on-raw-pool items at that time.

## Refuted (audit trail)
None.

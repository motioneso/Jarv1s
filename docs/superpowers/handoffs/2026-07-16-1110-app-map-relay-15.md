# #1110 app-map relay — checkpoint 15

Worktree: `~/Jarv1s/.claude/worktrees/build-1110-app-map`, branch `build/1110-app-map`, HEAD `9db4ec93` (unchanged — nothing committed yet).
Coordinator label: `Coord-1109-1110-g3`. Never merge/board/close — report PR URL only.

## What relay-14's fix accomplished (done, verified)

- `tests/uat/seed/chunks/ai.ts`: applied prescribed fix — `bindNews:false` now seeds **zero**
  provider at all (early return), not just an unbound one. Verified correct per
  `resolveDefaultProviderId`'s implicit-default-provider fallback.
- `tests/uat/specs/app-map-grounding.uat.spec.ts` (new, untracked): 3 tests. Rewrote tests 1+2 —
  the corrected seed makes `hasJsonModel()` genuinely false, which **also** drives the frontend's
  `customSourceByUrlEnabled` gate, so the add-source form is legitimately hidden by design (not a
  bug). Tests now assert the UI gate directly AND call `POST /api/news/sources/preview` via
  `page.request.post()` (authenticated via shared browser-context cookie) to reach the backend
  error paths the hidden form can no longer drive. **`pnpm test:uat -- app-map-grounding` passes 3/3.**

## New this session: two pre-existing, unrelated test:unit blockers found + one fixed

Both confirmed via `git stash push -u -- <task-8 files>` + rerun on bare HEAD (9db4ec93) — **fully
pre-existing, NOT caused by Task 8's diff.**

1. **FIXED** — `vitest.config.ts` was missing an alias for the `@jarv1s/module-sdk/ai-capabilities`
   subpath export (added to `package.json` in 34457186 but the matching vitest alias was never
   added — this repo resolves `@jarv1s/*` in tests via the alias map, not package.json `exports`;
   see the `core-version`/`module-registry/node` precedent right next to it). Missing alias caused
   `Cannot find package '@jarv1s/module-sdk/ai-capabilities'`, cascading to **203/419 test files**
   failing at import time. One-line fix added (mirrors the `core-version` entry exactly, placed
   before the bare `@jarv1s/module-sdk` alias). **Confirmed fix**: `ai-module-service-bindings.test.ts`
   now passes; re-run of full `tests/unit` was in flight when this doc was written — check
   `/tmp/.../scratchpad/test-unit-post-fix.log` in the *new* session's own scratchpad (old path is
   gone) or just rerun `pnpm exec vitest run tests/unit` fresh.
2. **NOT FIXED, flagged only** — `tests/unit/module-web-browser-safety.test.ts` fails: its
   `walkImportGraph` regex-based specifier extractor (`IMPORT_RE`) does not distinguish
   `export type {...} from "x"` from a runtime `export {...} from "x"`. `packages/shared/src/index.ts:5`
   has `export type { JarvisError, JarvisErrorClass } from "@jarv1s/module-sdk"` (bare specifier,
   type-only) — the walker treats it as pulling in the whole barrel and flags
   `module-sdk/src/{logger,rate-limit-key,route-errors}.ts` (fastify/node:crypto) as reachable from
   news's web bundle. **Almost certainly a false positive** — `export type` is fully erased by
   tsc/esbuild at build time, so the real Vite browser bundle should not actually contain these
   files (unlike the real `AI_MODEL_CAPABILITIES` leak that 34457186 legitimately fixed, which was
   a runtime const, not type-only). Real fix needs either (a) walker enhancement to skip
   `type`-only specifiers, or (b) moving `JarvisError`/`JarvisErrorClass` to their own node-clean
   subpath file mirroring the `ai-capabilities.ts` pattern. **Out of scope for Task 8** — do not
   attempt without a spec; file a new GitHub issue (pattern: #1120/#1121 already exist for adjacent
   relay-14 cleanup items) and reference it in the PR body.

## Also confirmed clean this session

`lint`, `format:check`, `check:file-size`, `check:design-tokens`, `check:no-ambient-dates`,
`check:package-deps`, `typecheck` (root + web + external-modules) — **all pass**. Fixed 4 files with
pre-existing prettier drift unrelated to Task 8 (via targeted `prettier --write` on exact paths,
diffs verified cosmetic-only against last commit): `packages/module-sdk/src/index.ts`,
`packages/news/src/manifest.ts`, `tests/unit/app-map-contract.test.ts`,
`tests/unit/chat-runtime-persona.test.ts`.

`db:migrate` passes (161 migrations current, no-op). `test:uat-seed` **also cascade-failed** on the
same missing-alias root cause (10/12 suites) — rerun it after confirming the vitest.config.ts fix
holds; expect it to now pass. `test:integration` has not run yet (blocked behind `&&` on
`test:uat-seed`'s prior failure) — run it after seed passes.

## Remaining steps (in order)

1. **DONE, CONFIRMED** — `pnpm exec vitest run tests/unit` after the alias fix: **418/419 files,
   3460/3463 tests pass. Exactly ONE failure remains: `module-web-browser-safety.test.ts`** (the
   flagged pre-existing false positive above). The other previously-noted failures
   (`jobs-pg-boss.test.ts`, `module-registry-mcp-url.test.ts`, `settings-connector-sync.test.tsx`,
   `settings-personal-panes.test.tsx`) were all part of the missing-alias cascade and are now fixed
   — no further triage needed on those.
2. **DONE** — `pnpm test:uat-seed`: 11/12 files, 21/23 tests pass now (alias fix confirmed). The
   remaining 2 failures are in `tests/uat/seed/guard.test.ts` (`assertTargetIsEphemeral`), NOT
   touched by Task 8's diff — it's rejecting the run because the shared dev DB already has
   real/bootstrap users, matching the documented pre-existing shared-DB issue (#1087, memory:
   `uat-seed-shared-db-no-reset`). Confirmed no other Herdr session was concurrently writing to
   this DB (`herdr pane list` — all others idle) when this ran, so the residue predates this
   session. Not fixed — out of scope, pre-existing infra gap, flag in PR body, do not attempt a DB
   reset yourself (shared resource). `pnpm test:integration` run standalone separately since it
   was blocked behind `&&` on this failure — check its own log for the real result.
3. Decide final commit file list: the original 12 (11 + `ai.ts`) **plus** `vitest.config.ts` (real
   fix, required) **plus** the 4 prettier-only files (optional — cosmetic, pre-existing drift; fine
   to leave uncommitted if you'd rather not touch unrelated files, but format:check requires them
   fixed on disk to pass locally). Recommend: commit all of it as ONE Task-8 commit with a clear
   message explaining the vitest.config.ts fix is a required unblock, not scope creep. **Do NOT
   `git add -A`** — explicit path list only.
4. Pre-push trio: `pnpm format:check && pnpm lint && pnpm typecheck`, then
   `git fetch origin main && git rebase origin/main`.
5. `coordinated-wrap-up`: open PR, report URL to coordinator (label `Coord-1109-1110-g3`). PR body
   must call out: (a) the vitest.config.ts alias fix and why it was necessary/in-scope, (b) the
   flagged `module-web-browser-safety.test.ts` false positive + recommendation to file a new issue,
   (c) the UI-gate-vs-backend-gate discovery in News add-source flow (tests 1+2 rewrite), (d) the
   deferred real-LLM e2e + UAT harness gap + issue #1121 pointer, (e) the test4/#1121 vacuous-until
   caveat from relay-14. **Never merge, touch the board, or close anything** — coordinator's job.

## Explicit file list so far (13, pending step 3 decision on the 4 formatting files)

`tests/uat/seed/chunks/ai.ts`, `tests/uat/specs/app-map-grounding.uat.spec.ts` (new),
`tests/uat/provisioner.ts`, `tests/uat/run-uat.ts`, `tests/uat/seed/cli.ts`,
`tests/uat/seed/levels.ts`, `tests/uat/seed/types.ts`, `packages/module-registry/src/index.ts`,
`packages/news/src/personalization-routes.ts`, `packages/news/src/routes.ts`,
`docs/WHATS_NEW.md`, `vitest.config.ts` (new this session).

**Resolved**: `.claude/context-meter.log` is pure session-telemetry noise (every session's
context-usage line auto-appended by a hook) — confirmed via diff review, unrelated to Task 8.
**Excluded from the commit.**

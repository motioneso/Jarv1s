# mod-dist-964 relay 8 — Task 7 Step 9 typecheck, 4 errors to fix

Branch: `mod-dist-964`, worktree `/home/ben/Jarv1s/.claude/worktrees/mod-dist-964`.
Plan: `docs/superpowers/plans/2026-07-12-module-distribution-install.md` (read BY SECTION only).
Coordinator label: `Coordinator` — re-resolve via `herdr pane list` fresh, never a baked pane number.
Security tier: Coordinator owns merge/board. Never merge/close/move board yourself.

## Done

- Task 6 fully committed: `72cb5e27` (admin module-registry surface). Gates were green at commit time.
- Task 7 Steps 1-8 (boot-time module reconcile) are code-complete and unit-tested but **uncommitted**:
  - `scripts/module-reconcile.ts` (new, ~330 lines) — `reconcileModules`, `assertQualifiedModuleTable`,
    `decideStagedAcceptance`, `purgeModule`, CLI entrypoint. Deviated from plan text in 2 places
    (verified against branch, not assumed): plan's `sweepStaging` (async) → actual export is
    `sweepStagingDirs` (sync, from `@jarv1s/module-registry/node`), called in try/catch not
    `.catch()`; dropped an unused `hashExternalPackage` import the plan listed but never used.
  - `scripts/module-install.ts` (modified) — loosened `ModuleInstallOptions.manifest` from the
    branded `JarvisModuleManifest` to a structural type (see plan Step 3) so it accepts both the
    dev-CLI's and boot-loader's manifest shapes.
  - `tests/unit/module-reconcile-plan.test.ts` (new, 47 lines) — 6/6 pass at runtime
    (`pnpm vitest run`).

## Not done — Task 7 Step 9 (typecheck) has 4 errors from `pnpm --filter @jarv1s/module-registry typecheck`

1. **`scripts/module-reconcile.ts:21`** — `TS2307: Cannot find module '@jarv1s/module-sdk/core-version'`.
   Diagnosed but unsolved mystery: this exact import string resolves fine from
   `packages/module-registry/src/external/validate.ts:18` and
   `packages/settings/src/module-registry-rows.ts:8` (both typecheck clean). Root tsconfig.json
   `paths` has only `"@jarv1s/module-sdk": [...]`, no `/core-version` subpath — yet it works
   elsewhere. `packages/module-sdk/package.json` has an `exports` entry
   `"./core-version": "./src/core-version.ts"`. Working theory (untested): node16/nodenext
   subpath resolution falls through to the package's own `node_modules` symlink + its `exports`
   map, and that only works when the *importing file's nearest package.json* has
   `@jarv1s/module-sdk` as a declared dependency with a workspace symlink in its local
   `node_modules`. `scripts/` has no own `package.json` (it's under the root package), so it may
   not get that symlink chain the way `packages/module-registry` and `packages/settings` do
   (each has `@jarv1s/module-sdk` in their own `package.json` deps). **Next step: check root
   `package.json` deps + `node_modules/@jarv1s/module-sdk` symlink presence; if confirmed, likely
   fix is adding `@jarv1s/module-sdk` to root deps, OR importing `CORE_VERSION` via the plain
   `@jarv1s/module-sdk` bare specifier (re-exported from its `index.ts`) instead of the subpath,
   OR adding an explicit `paths` entry for the subpath.** Verify whichever fix against
   `pnpm check:package-deps` too (it enforces declared-not-unused workspace deps).
2. **`scripts/module-reconcile.ts:31`** — `TS2835`, missing extension. Fix:
   `import { installModule } from "./module-install";` → `"./module-install.js"`.
3. **`tests/unit/module-reconcile-plan.test.ts:6`** — `TS2835`, missing extension. Fix:
   `from "../../scripts/module-reconcile";` → `"../../scripts/module-reconcile.js"`.
4. **`tests/integration/module-install.test.ts:65`** — `TS2353`, `'migrations' does not exist in
   type '{ readonly ownedTables?: readonly string[] | undefined; }'`. Caused by this session's
   Step 3 loosening of `ModuleInstallOptions.manifest` to a narrower structural type — an existing
   integration test constructs a manifest object literal with an extra `migrations` property,
   which now fails TS's excess-property check on object literals. **Not yet investigated** — read
   `tests/integration/module-install.test.ts` around line 65 to see what `migrations` is for and
   whether the fixture should drop it (likely, since `installModule` never reads it — grep confirms
   only `database.ownedTables` is read) or whether the structural type needs a wider (but still
   structural, not branded) shape. Verify, don't assume.

## Next steps

1. Fix all 4 errors above.
2. Re-run: `pnpm --filter @jarv1s/module-registry typecheck` clean, then full repo
   `pnpm typecheck` clean, then `pnpm vitest run tests/unit/module-reconcile-plan.test.ts` (6/6),
   then `pnpm check:package-deps` clean.
3. Task 7 Step 10 commit — explicit paths only:
   `git add scripts/module-reconcile.ts scripts/module-install.ts tests/unit/module-reconcile-plan.test.ts`
   Message: `feat(modules): boot-time reconcile — purge, compose-ensure, accept-staged, install, drift (#964)`
   + `Co-Authored-By: Claude <noreply@anthropic.com>` trailer.
4. Continue via `coordinated-build` into Task 8 (Boot & compose wiring, plan lines ~4207-4442),
   Task 9 (Admin web UI, ~4442-4884), Task 10 (integration suite/docs/full gates, ~4884+). Read
   each task by section fresh; verify premises against the branch before coding (per
   `coordinated-build` step ½) — Task 7 already found one real drift (`sweepStagingDirs`), don't
   assume the rest of the plan is current either.
5. Pre-push trio before every push: `pnpm format:check && pnpm lint && pnpm typecheck`, then
   `git fetch origin main && git rebase origin/main`.
6. On final green gates: `coordinated-wrap-up` (PR titled "Part of #964"). Never merge/close/move
   board — report to Coordinator only.
7. Relay again at the next 70% context-meter warning or immediately on any compaction summary.

# News Slice 4 — relay continuation #6 (issue #975, epic #954)

You are the sixth relay successor building News Slice 4. Same worktree
(`~/Jarv1s/.claude/worktrees/coord-2026-06-30-rfa-fleet/.claude/worktrees/news-s4`), branch
`news-s4`. Relay, not parallel work — predecessors are gone once reaped.

## Primary state (read these, in this order)

1. This doc (short).
2. `docs/superpowers/plans/2026-07-12-news-s4-chat-actions-revalidation-notifications.md` —
   THE plan. Read it BY TASK, only the task you are on. Never front-to-back.
3. Do NOT read the spec in full. If a task needs detail, the plan cites the section
   (`docs/superpowers/specs/2026-07-11-personalized-news-sources-topics.md`).
4. `docs/coordination/handoff-news-s4.md` — coordinator-owned, READ-ONLY, never `git add` it.

## Coordinator + protocol

- Label `Coordinator` (resolve pane fresh via `herdr pane list`; session id authority
  `58a78927-385c-4b1d-8fa0-94db20255d6f`). Caveman-terse messages via `herdr-pane-message`.
- Build per `coordinated-build`; TDD per `superpowers:test-driven-development`; commit green per
  task with `Co-Authored-By: Claude <noreply@anthropic.com>`; explicit-path `git add` only.
- Relay at the 70% context-meter warning via the `relay` skill. **Successor MUST be Fable.**
- Pre-push: `pnpm format:check && pnpm lint && pnpm typecheck` + rebase on origin/main.
- Closeout = `coordinated-wrap-up` (PR only). Merge gate = named-unanimous Opus+Codex+Gemini.
  PR body MUST state the migration RLS approach (column-scoped grants, owner-scoped policy)
  and the notification privacy test.
- Plan approval ALREADY GRANTED — do not re-ask. Directives: (1) migration 0161 done;
  (2) ONE summary notification, spec wording; (3) Task 9 = wire EXISTING Settings scaffolds +
  Retry button only; (4) 6 chat tools, edit+unexclude stay REST/Settings-only.

## Done so far (commits on news-s4)

- Tasks 1–4 ✅ — see relay5 doc §"Done so far" if needed (`bd8c8241`, `decd1725`+`d3348a10`,
  `4c7cbb97`, `616c34fe`). API facts from Task 4 live in relay5 doc — read only if a task
  touches jobs/notifications.
- Task 5 RED ✅ `8e2dd24a` — 3 schedule tests appended to
  `tests/integration/news-revalidation.test.ts` (third describe). 2 fail (no schedule row),
  best-effort test passes trivially now and guards GREEN.

## Resume here — Task 5 GREEN (design already settled; just build it)

Plan "### Task 5" (line ~207). Implementation worked out this leg:

1. **Create `packages/news/src/schedule.ts`** (pattern `packages/briefings/src/schedule.ts`):
   - `export const NEWS_REVALIDATE_CRON = "43 4 * * *";`
   - `reconcileNewsRevalidationSchedule(boss, scopedDb, repository: Pick<NewsPersonalizationRepository,
     "countCustomSources" | "countCustomTopics">, actorUserId, logger: Pick<FastifyBaseLogger,"error"> = NOOP)`.
     Plan says `ModuleLogger` — **that type does not exist**; use the briefings
     `Pick<FastifyBaseLogger,"error">` + NOOP_LOGGER pattern.
   - Body: count sources+topics (Promise.all); key = `` `news-revalidate:${actorUserId}` ``;
     `>0` → payload `{ actorUserId, kind: "revalidate", idempotencyKey: key }`,
     `assertMetadataOnlyPayload(payload)` (boss.schedule bypasses sendJob guard — Hard
     Invariant), `boss.schedule(NEWS_REVALIDATE_QUEUE, NEWS_REVALIDATE_CRON, payload,
     { tz: "UTC", key })`; `===0` → `boss.unschedule(NEWS_REVALIDATE_QUEUE, key)`.
   - Wrap WHOLE body in try/catch → `logger.error({event:"news_revalidation_schedule_failed",
     error: e.name, message: e.message.slice(0,200)}, "…")`. Internal catch = best-effort at
     every call site (RED test asserts route still 201 + refresh job still enqueued).
   - Import `NEWS_REVALIDATE_QUEUE` from `./jobs.js` (no cycle: jobs.ts doesn't import schedule).
2. **`packages/news/src/personalization-routes.ts`:**
   - Add `countCustomSources` / `countCustomTopics` to the `NewsPersonalizationStore` interface
     (real repo already implements them: personalization-repository.ts:143,356).
   - `triggerNewsRefresh`: widen repository Pick to include both counts; add optional 6th param
     `logger?: Pick<FastifyBaseLogger,"error">`; body: bump → afterBump → `if (!boss) return false;
     await reconcileNewsRevalidationSchedule(boss, scopedDb, repository, actorUserId, logger);
     return enqueueNewsRefresh(boss, actorUserId);`.
   - GET `/api/news/personalization` self-heal: in the `isNewsSnapshotFresh` check, `else if
     (dependencies.boss)` → reconcile with `request.log` (stale branch already reconciles via
     triggerNewsRefresh). Optionally pass `request.log` at write-route call sites.
3. **`tests/unit/news-routes.test.ts`** `makePersonalization` (~line 143): add
   `countCustomSources: async () => 1, countCustomTopics: async () => 0` stubs — interface gains
   members; unit tests use `boss: null` so reconcile never runs there.
   `packages/news/src/routes.ts` callers pass the real repo — structurally fine, no edit expected.
4. Verify: focused integration (below, ~21s all 3 new tests green + 13 old), unit
   `pnpm exec vitest run tests/unit/news-routes.test.ts`, `pnpm typecheck`.
5. Commit `feat(news): per-owner daily revalidation schedule (#975)` (+ user-facing summary line).

Then Tasks 6–10 in order (task list also mirrors this): retry route → chat tools
(preview/confirm, then remove/topic/exclusion) → Settings wiring → full gate + wrap-up.
Re-read the plan's task section before each.

## Facts verified this leg

- `pgboss.schedule` columns (pg-boss 12.18.2): `name, key, cron, timezone, data, options`;
  PK (name,key). App-role boss (schedule:false) CAN write/delete rows (grant 0002, briefings
  test precedent) — the new tests' `scheduleRows()` helper reads it via bootstrap client.
- Preview-by-URL flow in the new describe uses the dated-item feed + prompt-sniffing AI stub
  copied from `news-personalization-routes.test.ts` (proven combo; `makeAi` enum[0] stub was
  NOT trusted for the preview path).

## Traps (carried)

- **Focused integration test:** `pnpm exec tsx scripts/test-integration.ts
  tests/integration/news-revalidation.test.ts` (~21s). `pnpm test:integration -- <name>` does
  NOT filter — runs all 146 files, ~11 min.
- `tests/integration/ai-tools.test.ts` (~line 481) asserts the FULL queue-name list — any new
  queue must be appended there (none needed for Tasks 5–6).
- `tests/unit/news-jobs.test.ts` boss stub captures handlers PER QUEUE — two news workers now.
- Repo/test files occasionally touched by an outside watcher — if Read reports stale, re-read.
- `has_table_privilege` ignores column grants — worker-posture `[true,false,false,false]`
  expectation stays. Don't "fix" it.
- fast-json-stringify strips undeclared response fields — declare every new field in
  `packages/shared/src/news-api.ts`; test routes via `app.inject`.
- Add NO new keys to `ALLOWED_PAYLOAD_KEYS` (`packages/jobs/src/pg-boss.ts:76`).
- Chat write tools: omit `actionFamilyId` → gateway confirm-gates (`packages/email/src/manifest.ts:218`).
- Prettier every doc AND test file BEFORE committing (`pnpm exec prettier --write <file>` —
  this leg's test commit needed it).
- agentmemory `memory_smart_search` returns empty in this env — don't block on recalls.

## Boot sequence for you

1. `[ -d node_modules ] || pnpm install` (no-op expected).
2. Build Task 5 GREEN per the settled design above. BUILD — reading is not progress.
3. Message Coordinator: "relay #6 driving, Task 5 GREEN in progress" and ask it to reap
   predecessor pane (label `News S4 relay5`, session `fb625ebf-1b55-4f21-bdaf-df96057c5850`).

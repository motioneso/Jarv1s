# Build Relay 1 — #918 Open module system Slice 2 (Build phase)

**Relayed:** 2026-07-10, context-meter 70% warning, fired before any code was written (Task 1 not
yet started — this is a very early relay, don't be alarmed at zero commits).
**Worktree:** `/home/ben/Jarv1s/.claude/worktrees/918-implementation-plan` — stay here, do NOT
create a new worktree.
**Branch:** `plan/918-open-module-system-slice2` — continue on this exact branch.
**`node_modules`:** already installed. Skip `pnpm install`.
**Build-phase handoff (read first, unchanged, read-only):**
`docs/coordination/handoffs/handoff-918-build-phase.md` — has Coordinator label/session id,
risk tier (`security`), collision notes vs. parallel #914 build, run-specific bans. Read it in
full before doing anything.
**Approved plan (the work to execute):**
`docs/superpowers/plans/2026-07-10-open-module-system-slice2.md` (27 tasks, TDD). Plan is
committed and Ben-approved — do not re-plan, do not message coordinator for plan approval again.
Enter directly at `coordinated-build`'s **Build** step.

## What's done

Nothing coded yet. This session only completed the pre-build verification (`coordinated-build`
step ½ — "verify the spec against the actual branch"):

- Confirmed migration head is `0152` (`packages/settings/sql/`, sorted) — `0153`/`0154` still free,
  matches plan's provisional numbers.
- Confirmed **Task 1** target (`packages/module-sdk/src/index.ts` lines ~495-564) is unimplemented:
  current code has OLD `ModuleAuthDeclaration {id, kind:"api-key"|"oauth2", label}` (line ~514) and
  OLD `ModuleStorageDeclaration {namespace, kind:"kv"}` (line ~525); `JsonJarvisModuleManifest`
  (lines ~538-557) has `auth?`/`storage?` but no `web?` field. Task 1 replaces the declarations per
  spec §5/§6 (`{id, displayName, kind:"api-key", scope:"instance"|"user"}` /
  `{namespace, scopes:("instance"|"user")[]}`, drop oauth2, rename label→displayName) and adds
  `web?`.
- Confirmed **Task 2** target (`packages/module-registry/src/external/validate.ts`) is
  unimplemented: `FORBIDDEN_FIELDS` array (currently 20 entries, grepped lines 27-48) still ends
  with `"auth"`, `"storage"` — these need removing + positive validation added + fields included in
  the re-shaped manifest literal (loop at ~109-115, re-shape comment ~117-121 confirms manifest is
  rebuilt fresh, never spreads `raw`).
- Tasks 3-27 premises NOT yet spot-checked — do the same step-½ verify-against-branch check per
  task as you reach it (this repo's discipline, motivated by a real past incident on #456 where a
  stale spec caused rework).

## ⚠️ CAUTION — plan file has masked spans, must re-Read before implementing

When this predecessor session read
`docs/superpowers/plans/2026-07-10-open-module-system-slice2.md`, several spans rendered as opaque
`<<ccr:HEXID,type,sizeB>>` placeholder tokens instead of literal text — **not readable content**,
just a size/type marker. This affected parts of **Tasks 13, 14, 17 (partial), 18, 19, 23, 24, 25,
26, 27**. The predecessor never saw the real text of those spans and could not summarize them
faithfully. **Before implementing any of those tasks, re-Read the exact line range fresh from the
file on disk** — do not trust any second-hand description of that content (including this doc).
If the masking recurs on your Read, try reading a narrower line range around it, or `grep -n`
around the task heading to pull the literal text via a different tool.

## What's left

Execute all 27 tasks via `superpowers:test-driven-development`, each committing green with
`Co-Authored-By: Claude`, `git add` by explicit path only (never `-A`). Local task tracker already
seeded (`TaskList` shows 27 pending tasks, #1-#27, one per plan task + a final wrap-up task) —
reuse it, don't recreate.

Order: start at Task 1 (SDK types) exactly as scoped above, then Task 2, then proceed sequentially
per the plan document's own task order (it's dependency-ordered — types before consumers before
routes before UI before integration tests).

After all 27: pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck`, then
`git fetch origin main && git rebase origin/main`) before every push; full gate
(`pnpm verify:foundation` + `pnpm test:integration`) green before wrap-up; then
**`coordinated-wrap-up`** (PR + report to coordinator, never merge/board/close yourself).

## Escalation / relay

- Coordinator label: `Coordinator`, session id `4d68fcc5-bc2c-44cd-ae0d-a2e305f94069` (verify fresh
  via `herdr pane list` before messaging — this predecessor already confirmed exactly one pane with
  this label and matching session id at relay time).
- This predecessor already messaged the coordinator that it was relaying (no blockers, no forks) —
  no need to re-send that specific message, but do announce yourself once driving per the
  `coordinated-build` skill's normal cadence.
- Relay again yourself on the next context-meter 70% warning or compaction summary — same protocol
  (message coordinator, write a new relay doc, spawn successor, request reap of yourself).

## Collision notes (unchanged from build-phase handoff — do not re-derive)

Parallel build #914 (`build/914-module-data-plane`) — Opus-verified no serialization needed.
Shared mechanical-conflict-only files at merge time (coordinator resolves, not you):
`tests/integration/foundation.test.ts`, `packages/db/src/types.ts`, migration numbers (you hold
0153/0154 provisionally, expected to land first), possibly `scripts/audit-release-hardening.ts`.

## Run-specific bans (unchanged, non-negotiable)

- Work only in this worktree/branch; `git add` by explicit path, never `-A`/repo-wide `pnpm format`.
- Never touch `docs/coordination/` (coordinator-only), the project board, milestones, or merge.
- No secrets in any doc/payload/log/prompt.

# News Slice 4 — relay continuation #9 (issue #975, epic #954)

Ninth relay successor, same worktree/branch `news-s4`. Plan:
`docs/superpowers/plans/2026-07-12-news-s4-chat-actions-revalidation-notifications.md` — read BY
TASK only (T7=~:255, T8=~:301, T9=~:318, T10=~:335). Never the spec in full. Coordinator label
`Coordinator` (session authority `58a78927-385c-4b1d-8fa0-94db20255d6f`), caveman-terse via
`herdr-pane-message`. Plan approval GRANTED — do not re-ask. Directives: migration 0161 done; ONE
summary notification (spec wording); T9 = wire EXISTING Settings scaffolds + Retry button only;
6 chat tools, edit+unexclude stay REST-only. Build per `coordinated-build` + TDD; commit green per
task; explicit-path `git add`; relay at 70% meter warning, successor MUST be Fable.

## State: Tasks 1–6 ✅ (see relay8 doc for commits). Task 7 = RED done, GREEN not started

`tests/integration/news-chat-tools.test.ts` is WRITTEN and VERIFIED RED (fails: cannot find
`packages/news/src/chat-tools.js`). It encodes all decided assertions — read it first; do NOT
weaken it to pass. Run:
`pnpm exec tsx scripts/test-integration.ts tests/integration/news-chat-tools.test.ts`

## Verified facts (do NOT re-read these files)

- `renderAndCap` (`packages/ai/src/gateway/output-validation.ts`): undefined outputSchema →
  result passes through untouched; output is ALWAYS `{text}`; `externalContent` → text sits
  HTML-escaped in `<tool_result source="…">` envelope; `renderToolResult` on non-`items` data →
  `JSON.stringify(data, null, 2)`. The test's `parseToolText` helper already strips/unescapes.
- module-sdk: `ToolExecute=(scopedDb, input, ctx, services?)=>Promise<ToolResult>`;
  `ToolContext={actorUserId, requestId, chatSessionId, localTimezone?}`;
  `ToolSummarize=(input, ctx)=>string`; AssistantTool optional fields incl. `actionFamilyId`,
  `outputSchema`, `summarize`, `externalContent` (~:445-479). Write tool with NO actionFamilyId →
  always confirm-gated.
- js08 gateway harness facts all verified (action_request record has `kind`/`actionRequestId`;
  audit cols owner_user_id/approval_mode/outcome/tool_name).
- `personalization-routes.ts`: confirm route logic :287-325; `triggerNewsRefresh` exported :148
  (signature `(scopedDb, repository, boss, actorUserId, afterBump?, logger?)`);
  `PersonalizationRouteDependencies` :107 is NOT yet exported and has no `previews`; route
  creates its own store at :173 (`const previews = createPreviewStore()`).
- Preview store: `put(preview)->id`, `take(owner,id)` owner-checked, deletes ONLY on owner match,
  TTL 10min, max 10/owner (`discovery/preview-store.ts`).
- Preview route logic :235-285: availability gates → `resolveSourceInput(db, {...discovery,
  repo: repository}, {raw, hasWebSearch})` → keep `ok|ambiguous` else fail → `previews.put` →
  duplicate check vs `listCustomSources`.

## GREEN design (decided — build exactly this)

1. `personalization-routes.ts`: export deps interface; add optional `previews?` (default
   `createPreviewStore()` at :173). Extract confirm logic into exported
   `confirmSourceFromPreview(scopedDb, deps: {previews, repository, boss}, actorUserId,
   {confirmationId, candidateId?, expected?: {label, domain}})` →
   `{ok:true, source} | {ok:false, reason:"expired"|"ambiguous"|"duplicate"|"limit", message}`.
   Order inside: take → pick candidate (>1 && !candidateId → ambiguous "Choose a publisher
   candidate"; bad candidateId → ambiguous "Publisher candidate is invalid") → if `expected` and
   stored candidate label/canonicalDomain ≠ expected → **THROW** (security; gateway sanitizes to
   "Tool news.confirmSource failed" + audit failed) → write (replace-target-missing → expired
   "Source to replace was not found"; LimitError → limit; DuplicateSourceError → duplicate, both
   with error.message) → `triggerNewsRefresh` → ok. REST route maps reason→HttpError(expired 409,
   ambiguous 400, duplicate 409, limit 400, helper's message) to stay byte-identical.
2. `packages/news/src/chat-tools.ts`: deferred-deps per `briefing-tool.ts` (module-level `let
   deps`, `configureNewsChatTools({previews, discovery: {fetch, search, ai}, availability, boss:
   PgBoss|null, repository: NewsPersonalizationStore})`, throw "(composition-root bug)" if unset,
   `assertDataContextDb(scopedDb)`). Exports `newsPreviewSourceExecute` (mirror preview route:
   gates + resolveSourceInput + put with `ownerUserId: ctx.actorUserId, replaceSourceId: null` +
   duplicate check; benign failures → `{data: {error: "...string"}}`; success →
   `{data: {confirmationId, candidates: [{candidateId, label, domain}], duplicateOfSourceId?}}` —
   NEVER include validationFingerprint/feedUrl) and `newsConfirmSourceExecute` (calls
   confirmSourceFromPreview with `expected: {label, domain}` from input; benign `ok:false` →
   `{data: {error: message}}` (test greps "expired"); success → `{data: {source: {label,
   domain}}}`-ish, no fingerprint) + `summarizeNewsConfirmSource(input)` from INPUT label/domain
   only.
3. `manifest.ts` assistantTools: `news.previewSource` (risk read, permissionId news.prefs,
   `externalContent: true`, inputSchema `{source: string}` required) and `news.confirmSource`
   (risk write, NO actionFamilyId, permissionId news.prefs, inputSchema confirmationId/label/
   domain required + candidateId optional, `summarize`). Precedent: email.draftReply
   `packages/email/src/manifest.ts:218` (but that one HAS actionFamilyId — omit ours).
4. `routes.ts` (NOT yet read — read it): create ONE `createPreviewStore()`, pass as `previews` to
   `registerNewsPersonalizationRoutes` AND `configureNewsChatTools` (routes.ts already holds
   discovery/availability/boss/repository). `index.ts`: export `configureNewsChatTools`.
5. Import direction (no cycle): chat-tools ← personalization-routes exports; manifest ← chat-tools.

Possible GREEN surprises: `app.news_custom_sources` column names in the test's raw SQL
(`owner_user_id`, `canonical_domain`, `created_at`) — if mismatch, fix TEST SQL, not the schema;
`resolveSourceInput` status strings (grep `discovery/source-resolution.ts` only if needed).
Commit: `feat(news): assistant preview/confirm source tools (#975)` + user-facing summary line.
Then T8→T10 per plan (re-read each task section fresh).

## Traps (carried)

- Focused test: `pnpm exec tsx scripts/test-integration.ts tests/integration/<file>.test.ts`
  (`pnpm test:integration -- <name>` does NOT filter). `ai-tools.test.ts` ~:458 asserts FULL
  queue list — no new queues. fast-json-stringify strips undeclared REST response fields
  (declare in `packages/shared/src/news-api.ts`; chat-tool output NOT route-serialized). NO new
  `ALLOWED_PAYLOAD_KEYS` (`packages/jobs/src/pg-boss.ts:76`). `herdr pane read` MUST use
  `--source recent --lines 12`. Prettier every doc AND test file before committing. agentmemory
  smart_search returns empty here — don't block. Never `sed -i` a file you'll Edit. Pre-push:
  `pnpm format:check && pnpm lint && pnpm typecheck` + rebase origin/main. T10/PR body must state
  migration RLS approach (column-scoped grants, owner-scoped policy) + notification privacy test.
- `docs/coordination/handoff-news-s4.md` is coordinator-owned — never `git add` it.

## Boot

1. `[ -d node_modules ] || pnpm install` (no-op).
2. Message Coordinator: "relay #9 driving, Task 7 GREEN starting"; ask reap of relay8 pane (label
   `News S4 relay8`, session `6ad8d89c-22a7-4de7-b5a7-c26d117ed3cb`). Note: relay7 pane reap was
   already requested (session `d49fe0bd-7a4b-43e3-802b-9de5f22f1bd3`) — mention if still alive.
3. Build GREEN per design above. Reading is not progress — the RED test + this doc are the spec.

# #1393 Tasks + Notifications UI consolidation — relay handoff

Relaying at context-meter 70% warning. **Zero code written yet** — this session did orientation
only (confirmed plan/sign-off against the live tree). Do not repeat that reading; start building.

## Where

- Worktree: `/home/ben/Jarv1s/.claude/worktrees/ui-1393-tasks`, branch `feat/1393-ui-tasks`,
  based on `origin/main` @ `5ff2423d`. Clean tree except the harness's own
  `.claude/context-meter.log` (ignore it, don't stage it).
- Task issue: **#1393**. Epic: **#1387** (section 6 of 9). Coordinator pane label:
  **"DESIGN ELEMENTS"** (`w1:p1` at last check — re-resolve by label, panes reflow).
- The plan is **already approved** — issue #1393 comment `5187960649` (the build spec, Task 0–6)
  + sign-off comment `5188097889` (approves 4 amendments, corrects 2 imprecisions). **Do not
  re-plan.** Full text of both was fetched into
  `/tmp/claude-1000/-home-ben-Jarv1s--claude-worktrees-ui-1393-tasks/969661e2-a546-4133-a2f4-9d24ce791952/scratchpad/plan-comment.md`
  and `.../scratchpad/signoff-comment.md` in the prior session — that scratchpad may not survive
  into your session (it's session-scoped). If it's gone, re-fetch with:
  `gh api repos/motioneso/Jarv1s/issues/comments/5187960649 -q '.body'` and
  `gh api repos/motioneso/Jarv1s/issues/comments/5188097889 -q '.body'`.
  **Read the plan by section, for the task you're on — not front to back.** A full read is what
  bloated this session to 70% with nothing built.

## Three build-time corrections (from sign-off 5188097889 — override the plan text)

1. `tk-native` date inputs are at `task-details-dialog.tsx:355, 364, 412` (not 353/362/410).
2. `tk-statusctl` (`task-details-sections.tsx:231`) is a `<div>` wrapper around two `<button>`s
   (`__main`, `__more`) — convert the two inner buttons to `<Button>`, keep the wrapper
   layout-only.
3. `FormLabel` renders a real `<label>` (today's `tk-flabel` is an inert `<span>`) — wire
   `htmlFor` to the matching input id, don't ship inert labels.

Also: two items in D6 comment `5186900563` (item 5, and item 3's button inventory) are **wrong**
— the plan supersedes them, don't follow them.

## Task list (mirror the TaskCreate list already in this session — recreate if lost)

- **Task 0 (START HERE, not started):** Add two new capture scenarios to
  `tests/e2e/capture-screens.spec.ts`: `06b-tasks-empty` and `10c-notifications-empty`, using
  empty mock lists (`tasks: []`, `notifications: []`) via the existing `baseState()`/`mockApi`
  pattern already used by the current `capture: tasks` (~line 192) and `capture: notifications`
  (~line 224) tests in that file. Capture them on the **current, pre-migration** look — this is
  the first commit, before any restyle. Run `pnpm capture:screens`, verify the two new PNGs
  exist and look sane, commit with explicit paths (`git commit tests/e2e/capture-screens.spec.ts
  <new-png-paths> -m "..."` — never `git add -A`, never bare `git commit`).
- **Task 1:** New `packages/ui/src/empty-state.tsx` (`EmptyState`, `div.jds-empty`,
  props `icon?`/`title`/`description?`/`children?`) and `packages/ui/src/field.tsx` (`Field` →
  `div.jds-field`, `FormLabel` → `label.jds-label`, both in one file per #1406 cross-file-type
  safety). Move `.jds-field`/`.jds-label` CSS verbatim from `apps/web/src/styles/components-forms.css:8,13`
  into new `packages/ui/src/styles/components-forms.css`. Author new
  `packages/ui/src/styles/components-empty.css` (`.jds-empty` + `__mark`/`__title`/`__sub`).
  Import both new CSS files from `packages/ui/src/styles.css`. Export from
  `packages/ui/src/index.ts`. Register `"jds-empty": "EmptyState"`, `"jds-field": "Field"`,
  `"jds-label": "FormLabel"` in `scripts/check-migrated-sections.ts`'s `CLASS_FAMILY_TO_COMPONENT`
  (18→21). Verify zero blast radius UNPIPED before proceeding:
  `pnpm check:migrated-sections > /tmp/g5.log 2>&1; echo "EXIT=$?"` must print `EXIT=0`. Then
  `pnpm build:ui-catalogue`.
- **Task 2:** Migrate `apps/web/src/notifications/notifications-page.tsx` — raw `jds-task` family
  adoption (+ new `.jds-task--unread` modifier authored in
  `packages/ui/src/styles/components-jarvis.css`, using `--accent-soft-border-weak` border /
  `--surface-assistant` fill), toolbar → `<Segmented>`/`<IconButton>`/`<Button variant="secondary">`,
  local `EmptyState` → `@jarv1s/ui` `EmptyState`. Delete `.notification-row.unread` from root
  `styles.css` ONLY after the `10-notifications` capture proves `.jds-task--unread` renders
  correctly — if not reproducible, STOP and escalate to the coordinator, don't self-approve. Post
  the required note on issue #1405 (notifications adopted raw `jds-task`, family gained an
  authored `--unread` modifier).
- **Task 3:** Convert `tk-*` families across the 6 `apps/web/src/tasks/*.tsx` files per the plan's
  detailed table (Button/Chip/Menu/Dialog/Card/Avatar/Select/Field/FormLabel/EmptyState mappings,
  `.tk-native`→`.jds-input` CSS-only, `tk-act` family visual props → CSS-only `jds-*` in new
  `packages/ui/src/styles/components-tasks.css`). Strip the 3 section CSS files
  (`kit-tasks.css`, `kit-tasks-modal.css`, `tasks/tasks.css`) to layout-only.
  **Open question this session did not resolve:** what happens to the section-owned `tk-task`
  row family (in `task-list-view.tsx`'s exported `TaskRow`, 11 selectors, distinct from Today's
  `jds-task`). `jds-task*` is reserved for Today (confirmed in memory `ui-1393-tasks-scoping.md`)
  — do NOT rename `tk-task` to `jds-task`. The plan's Task 3 general rule says a visual-property
  class either (a) maps to a component or (b) re-authors as a CSS-only `jds-*` class in
  `components-tasks.css`. Read `apps/web/src/styles/kit-tasks.css` (not yet read this build) to
  see `tk-task`'s actual declarations — that will make the correct handling obvious. Check
  `packages/ui/src/styles/components-wellness-1.css`/`components-wellness-2.css` or
  `components-chat.css` for precedent: did #1392/#1396 keep a section's own prefix when
  CSS-only-authoring into `packages/ui`, or rename everything to `jds-*`? Resolve this when you
  actually reach Task 3, not before — Task 0/1/2 don't depend on it.
- **Task 4:** Dead-selector burn-down in root `apps/web/src/styles.css`, diffed against **BASE
  commit `5ff2423d`** (never a plain `git diff` — a selector that moved into `packages/ui` reads
  as a deletion under plain diff). D6's 13 selectors + 2 sign-off-approved additions
  (`.task-create-form`/`.task-detail-form`, already dead) + duplicate `.task-title-link`
  block (~line 342) + `@media` dupes (~947-956/969-970). `.notification-row.unread` deleted last,
  gated on Task 2's capture proof.
- **Task 5:** Register both guard lists in the same PR — `MIGRATED_SECTION_CSS_FILES` in
  `scripts/check-design-tokens.ts` 11→14 (+`kit-tasks.css`/`kit-tasks-modal.css`/`tasks/tasks.css`;
  do NOT add root `styles.css`), `MIGRATED_SECTION_PATHS` in `scripts/check-migrated-sections.ts`
  49→56 (+ the 7 section TSX paths).
- **Task 6:** Verification, all UNPIPED with echoed exit codes (`cmd > /tmp/x.log 2>&1; echo
  "EXIT=$?"`): `check:design-tokens`, `check:migrated-sections`, `check:ui-classes`,
  `check:ui-catalogue` (after `build:ui-catalogue`), `verify:foundation` (fresh exported gate DB
  only — never live dev DB). Plus property-by-property split check (0 shared CSS properties) for
  each of the 3 section CSS files vs. its `packages/ui` destination. Plus `pnpm capture:screens`
  diff confined to the 4 approved amendments (empty/loading states adopt `jds-empty`; notifications
  toolbar picks up component metrics; `.tk-native`→`.jds-input`; the 2 extra dead selectors) —
  **kill gate**: any other visible change, or unread-vs-read lost → STOP, report to coordinator,
  don't self-approve. Plus live-path proof on a dev instance (tasks list+matrix, details dialog,
  edit date field, tag menu, change status; notifications filter/mark/both empty states) recorded
  on the PR.

## Finish line

Merge `origin/main`, run the pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck`),
push, open PR against `main` with a release-note-style user-facing summary, post the live-path
proof + the #1405 note, report the PR number + measured guard numbers (should land at 14 and 56)
to the coordinator pane ("DESIGN ELEMENTS" — re-resolve by label). **Do not merge, close the
issue, or move the board yourself** — that's the coordinator's job per `coordinated-build`.

## Reminders

- Shared checkout: never `git add -A`/`git add .`, never bare `git commit` — explicit paths only.
- This worktree is yours; don't touch the main tree.
- Relay again on the next 70% warning or compaction summary — don't wait for a felt degradation.

# News Slice 4 — relay continuation #11 (issue #975, epic #954)

Eleventh relay successor, same worktree/branch `news-s4`. Plan:
`docs/superpowers/plans/2026-07-12-news-s4-chat-actions-revalidation-notifications.md` — read BY
TASK only (T9=:318, T10=:335). Never the spec in full. Coordinator label `Coordinator` (session
authority `58a78927-385c-4b1d-8fa0-94db20255d6f`), caveman-terse via `herdr-pane-message`. Plan
approval GRANTED — do not re-ask. Directives: ONE summary notification (done, T4); T9 = wire
EXISTING Settings scaffolds + Retry button only; 6 chat tools total, source edit + exclusion
removal stay REST-only. Build per `coordinated-build` + TDD; commit green per task; explicit-path
`git add`; relay ONLY at 70% meter warning, successor MUST be Fable.

## State: Tasks 1–8 ✅. Resume at Task 9 (plan :318)

Task 8 committed `8527f3b9` (`feat(news): assistant topic/exclusion/removal tools (#975)`):
9/9 chat-tool tests + 7/7 REST regression green, lint/typecheck/format 0.

## Verified facts from Task 8 (do NOT re-derive)

- All six chat tools live in `packages/news/src/chat-tools.ts`; manifest has all six
  `assistantTools` entries (4 new: write risk, NO actionFamilyId, `news.prefs`, `summarize` from
  INPUT only). Executes mirror REST route write paths exactly; benign failures (limit/duplicate/
  policy/not-found/invalid domain) return `{data:{error}}`; `stringField` + `describeBenignWriteError`
  helpers there. Cross-owner/garbage ids: pre-check via `listCustomSources`/`listCustomTopics`
  find → friendly not-found (never hits a uuid cast).
- `cleanTopic` now EXPORTED from `personalization-routes.ts` (chat pre-checks empty label so its
  HttpError never fires in chat). `validateTopic` ← `discovery/policy-validation.js`,
  `normalizePublisherDomain` ← `personalization-domain.js`, error classes ←
  `personalization-repository.js` — all leaves, no cycle.
- Exclusion DB column is `canonical_domain` (DTO field `canonicalDomain`); tool output maps it to
  `domain`. Topic/source caps = 10 (`NEWS_MAX_CUSTOM_TOPICS/SOURCES`), exclusions 100.
- Test file `tests/integration/news-chat-tools.test.ts`: nested describe "topic/exclusion/removal
  write tools (#975 Task 8)"; `waitForAudit` now takes optional `ownerUserId`; gateway
  action-request records carry `summary` (asserted for addTopic). Focused run:
  `pnpm exec tsx scripts/test-integration.ts tests/integration/news-chat-tools.test.ts`.

## Task 9 design (plan :318 — re-read the section, then build)

Files: `packages/news/src/web/news-client.ts`, `packages/news/src/settings/index.tsx` (510 lines —
split to `settings/add-source.tsx` if near the 1000-line gate), `query-keys.ts` only if a new key
is needed (personalization key exists). Wire EXISTING scaffolds only + Retry button — no new
surfaces.

- Client helpers (mirror existing fetch style exactly): `previewNewsSource({source})`,
  `confirmNewsSource({confirmationId, candidateId?})`, `deleteNewsCustomSource(id)`,
  `createNewsTopic({label, guidance?})`, `deleteNewsTopic(id)`, `triggerNewsRevalidation()`.
- UI (authored jds-\* only, no new raw colors): Add source input → preview → candidate radio list
  (>1) → confirm → invalidate personalization query; 409/400/503 → existing authored error states;
  candidate labels/domains as plain text. Add topic form (422 → policy message). Remove buttons per
  custom source/topic row. Retry button when any `validationStatus !== "approved"` or
  `healthStatus === "unavailable"` → `triggerNewsRevalidation` → transient "queued".
- NOTE: REST confirm requires label+domain in body? CHECK `confirmNewsSourceSchema` in
  `packages/shared/src/news-api.ts` before writing the helper — plan's helper signature omits them
  but the REST route may expect the tamper-check fields; mirror the actual schema.
- Gates: `pnpm --filter @jarv1s/news typecheck && pnpm lint && pnpm check:file-size`. e2e: extend
  an existing apps/web news settings spec if one exists; if none, note in PR body (don't invent a
  harness). Commit `feat(news): settings add/remove/retry flows (#975)` + user-facing summary.
- Then T10 (:335) — PR body must state migration 0161 RLS approach (column-scoped worker grants,
  owner-scoped policy) + notification privacy test + e2e note. Pre-push gate:
  `pnpm format:check && pnpm lint && pnpm typecheck` + rebase origin/main.

## Traps (carried)

- `ai-tools.test.ts` ~:458 asserts FULL queue list — no new queues. NO new `ALLOWED_PAYLOAD_KEYS`
  (`packages/jobs/src/pg-boss.ts:76`). fast-json-stringify strips undeclared REST response fields —
  new/changed REST fields must also go in `packages/shared/src/*-api.ts` schema.
- CYCLE TRAP: never close manifest→chat-tools→personalization-routes→jobs→manifest; under tsx a
  cycle leaves manifest `execute` bindings undefined → gateway silently drops tools
  ("Tool not available" ⇒ check import cycles first).
- Frontend: Vite dev server needs `--host` (Ben tests over LAN). Recall
  `"jarv1s frontend workspace querykey"` via memory_smart_search before React Query changes (may
  return empty — don't block). Preserve authored design system; raw colors only in
  `apps/web/src/styles/tokens.css`.
- `herdr pane read` MUST use `--source recent --lines 12`. Prettier every doc AND test file before
  committing. Never `sed -i` a file you'll Edit. Read a file before editing (stale-read errors).
- `docs/coordination/handoff-news-s4.md` is coordinator-owned — never `git add` it. Explicit-path
  staging only; shared tree. Don't stage `.claude/context-meter.log`.

## Boot

1. `[ -d node_modules ] || pnpm install` (no-op expected).
2. Message Coordinator via `herdr-pane-message`: "relay #11 driving, Task 9 starting"; ask reap of
   relay10 pane (label `News S4 relay10`, session `e3370515-a254-49dc-bf9f-94dd1dd2e13b`).
3. Read plan Task 9 section (:318) + `packages/news/src/settings/index.tsx` +
   `packages/news/src/web/news-client.ts`, then BUILD. Reading is not progress.

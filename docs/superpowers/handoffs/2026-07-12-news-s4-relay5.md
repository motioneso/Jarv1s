# News Slice 4 — relay continuation #5 (issue #975, epic #954)

You are the fifth relay successor building News Slice 4. Same worktree
(`~/Jarv1s/.claude/worktrees/coord-2026-06-30-rfa-fleet/.claude/worktrees/news-s4`), branch
`news-s4`. Relay, not parallel work — predecessors are gone once reaped.

## Primary state (read these, in this order)

1. This doc (short).
2. `docs/superpowers/plans/2026-07-12-news-s4-chat-actions-revalidation-notifications.md` —
   THE plan. Read it BY TASK, only the task you are on. Never front-to-back.
3. Do NOT read the spec in full. If a task needs spec detail, the plan cites the section —
   read that section only (`docs/superpowers/specs/2026-07-11-personalized-news-sources-topics.md`).
4. `docs/coordination/handoff-news-s4.md` — coordinator-owned, READ-ONLY, never `git add` it.

## Coordinator + protocol

- Label `Coordinator` (resolve pane fresh via `herdr pane list`; session id authority
  `58a78927-385c-4b1d-8fa0-94db20255d6f`). Caveman-terse messages via `herdr-pane-message`.
- Build per `coordinated-build`; TDD per `superpowers:test-driven-development`; commit green per
  task with `Co-Authored-By: Claude <noreply@anthropic.com>`; explicit-path `git add` only.
- Relay at the 70% context-meter warning via the `relay` skill. **Successor MUST be Fable.**
- Pre-push: `pnpm format:check && pnpm lint && pnpm typecheck` + rebase on origin/main.
- Closeout = `coordinated-wrap-up` (PR only; never merge/board/close). Merge gate is
  named-unanimous Opus+Codex+Gemini, no fallback. PR body MUST state the migration RLS
  approach (column-scoped grants, owner-scoped policy) and the notification privacy test.

## Plan approval — ALREADY GRANTED (do not re-ask)

Coordinator directives: (1) migration 0161 column-scoped worker UPDATE grants, owner-scoped
policy, no BYPASSRLS — done; (2) ONE summary notification only, spec wording, no "new matching
items"; (3) Task 9 = wire EXISTING Settings scaffolds + Retry button only, no new UI;
(4) 6 chat tools confirmed, edit+unexclude stay REST/Settings-only.

## Done so far (commits on news-s4)

- Task 1 ✅ `bd8c8241` — migration 0161 + manifest + catalog row.
- Task 2 ✅ `decd1725` (RED) + `d3348a10` (GREEN) — repo validation-state methods. 15/15.
- Task 3 ✅ `4c7cbb97` — `packages/news/src/revalidation.ts` core + 6 stub-port tests.
- Task 4 ✅ `616c34fe` — `news.revalidate` queue + `enqueueNewsRevalidation` + worker
  (revalidateOwnerNews → ONE counts-only notification iff `transitionedToAttention`, create
  failure caught + logged `news_notification_failed`), refresh-worker drift hook (fingerprint
  compare → enqueue), module-registry wiring (NotificationsRepository w/ quiet-hours +
  preference ports, revalidationLogger). 12/12 revalidation, 2/2 refresh-jobs, 13/13 ai-tools,
  full `pnpm typecheck` green.

## Resume here — Task 5 (per-owner revalidation schedule)

Read plan "### Task 5" section (plan line ~207), TDD it, commit. Then Tasks 6–10 in order:
retry route → chat tools (preview/confirm, then remove/topic/exclusion) → Settings wiring →
full gate + wrap-up. Re-read the plan's task section before each.

## API facts from Task 4 (verified this leg)

- `enqueueNewsRevalidation(boss, actorUserId): Promise<void>` (void, unlike refresh's boolean);
  payload `{ actorUserId, kind: "revalidate", idempotencyKey: "news-revalidate:<id>" }`,
  `{ singletonKey: actorUserId }`.
- `registerNewsJobWorkers` deps added: `notificationsRepository?: Pick<NotificationsRepository,
  "create">`, `revalidationLogger?: NewsRevalidationLogger`. Returns TWO work ids now.
- `NewsRevalidationLogFields` gained `news_notification_failed` variant (error name +
  message ≤200).
- `@jarv1s/notifications` added to `packages/news/package.json` deps (lockfile committed).
- Notifications contract has NO link/href field — summary body text is the pointer:
  "Open News settings to retry or remove them." Title: "News sources need attention".
  Metadata exactly `{ kind: "news_revalidation", sourceCount, topicCount }` — counts only.
- Test harness for jobs: second describe in `tests/integration/news-revalidation.test.ts`
  (real DB + two pg-boss clients, seed via bootstrap SQL, `waitFor` poll helper,
  `makeWorkerAi` dual-mode stub serves compile rankings AND policy category calls).

## Traps (carried + new this leg)

- **Focused integration test:** `pnpm exec tsx scripts/test-integration.ts tests/integration/<file>.test.ts`
  (~25s). `pnpm test:integration -- <name>` does NOT filter — runs all 146 files, ~11 min.
- **NEW:** `tests/integration/ai-tools.test.ts` (~line 481) asserts the FULL queue-name list —
  every new queue must be appended there or it fails.
- **NEW:** `tests/unit/news-jobs.test.ts` boss stub captures handlers PER QUEUE
  (`if (queue === NEWS_REFRESH_QUEUE)`) — two news workers register now.
- **NEW:** repo/test files in this tree get touched by an outside watcher occasionally —
  if Read reports stale, just re-read; content is usually what you expect.
- `has_table_privilege` ignores column-level grants — worker-posture test's
  `[true,false,false,false]` expectation stays green after 0161. Don't "fix" it.
- fast-json-stringify strips undeclared response fields — declare every new field in
  `packages/shared/src/news-api.ts`; test routes via `app.inject`.
- `assertMetadataOnlyPayload` must be called manually for `boss.schedule` (bypasses the
  sendJob guard). Add NO new keys to `ALLOWED_PAYLOAD_KEYS` (`packages/jobs/src/pg-boss.ts:76`).
- Chat write tools: omit `actionFamilyId` so the gateway always confirm-gates (precedent
  `packages/email/src/manifest.ts:218`).
- Worker DB context in tests: `createDatabase({connectionString: connectionStrings.worker})` +
  `DataContextRunner` (see the new describe block in `news-revalidation.test.ts`).
- Prettier this handoff and any doc BEFORE committing (`pnpm exec prettier --write <file>`).
- `[ -d node_modules ] || pnpm install` — node_modules exists; skip install.
- agentmemory `memory_smart_search` returns empty in this env — don't block on recalls; this
  doc + plan carry the state.

## Boot sequence for you

1. `[ -d node_modules ] || pnpm install` (no-op expected).
2. Read plan Task 5, TDD it. BUILD — reading is not progress.
3. Message Coordinator: "relay #5 driving, starting Task 5" and ask it to reap predecessor
   pane (label `News S4 relay4`, session `93d5f3e7-225f-4958-8352-96d56144620e`).

# #1121 UAT real-chat relay 3

Branch: `fix/1121-uat-scriptable-chat`  
Worktree: `~/Jarv1s/.claude/worktrees/fix-1121-uat-chat`  
Coordinator: `Coordinator`, immutable session `019f7da3-2d14-7ee2-a42d-c0618a7d821e`

## State

- Ben replaced the fake-CLI proposal with a dedicated test/service Anthropic account using the existing real Claude login machinery.
- Commit `a160216b` contains the red-capable check proving `seedAiProviderChunk` has no chat-capable model through `AiRepository.selectChatModelForUser`.
- No real-access implementation or plan document exists yet.
- `.claude/context-meter.log` is modified session telemetry; preserve it and do not commit it.

## Approved mechanism

- One-time `claude setup-token` for a dedicated test/service Anthropic account, never a personal account.
- Token stays outside Git/chat/logs in a host-side GPG-encrypted file.
- Opt-in ephemeral UAT decrypts only to a temporary mode-`0600` env file, injects via the existing compose `--env-file` convention, and persists through the existing Claude-scoped cli-auth volume/token store.
- Default CI remains credential-free and fails closed when real-chat credentials are absent.
- Existing login ready transition and `autoRegister.ensureDefaultChatModel` must create the usable assistant route.
- Non-read tools must retain the real model `action_request` -> browser Approve -> gateway execute flow. Direct cookie-auth invocation remains `403 confirmation_required`; no audit-only resolve bypass.

## R3 trace findings

- `provisionForUat` and `composeSeedHook` in `tests/uat/provisioner.ts` are the orchestration seams.
- `packages/cli-runner/src/login-service.ts` has the ready-state short circuit around line 176.
- `packages/module-registry/src/onboarding-login.ts` contains the existing login mechanism.
- The seed compose service appears to lack the Claude cli-auth volume mount.
- `persistProviderToken` appears not to be exported from the cli-runner package index.
- Whether the root package already depends on `@jarv1s/cli-runner` was not verified.

## Next steps

1. Verify the root-package dependency first and confirm the three apparent gaps against current code.
2. Produce the smallest concrete implementation plan reusing existing exports/mounts; avoid a new credential store, wrapper, or secret utility.
3. Send the plan and exact red command to the Coordinator for approval. Do not implement beyond the already-committed red check until approved.
4. Never request, display, or store the real token during plan work.

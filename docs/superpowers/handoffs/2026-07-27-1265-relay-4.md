# #1265 relay 4 — T4 done, T5+ next

Worktree/branch: `1265-module-content-self-operation` (this worktree, don't re-`pnpm install` —
`node_modules` present). Coordinator label: `Coordinator` in Herdr — **re-resolve pane fresh via
`herdr pane list` before messaging, never reuse a pane id from this doc.**

Plan: `docs/superpowers/plans/2026-07-27-module-content-self-operation.md`. **Read by SECTION for
your current task only** — 1128 lines, full-read has already burned multiple sibling contexts.
Task→line map:

- T1 47-135 DONE (`f9347444`)
- T2 136-384 DONE (`62d8b375`)
- T3 385-476 DONE (`3e78b741`)
- T4 477-873 DONE (`eb924e7c`) — sports.followTeam/unfollowTeam assistant tools
- **T5 874-960 ← start here**
- new task #8 (install-grant routing test) — not in plan doc; see "SETTLED" below
- T6 961-1042
- T7 1043-1107

Fuller predecessor context (if line map insufficient): read `~/Jarv1s/.claude/worktrees/coord-1262/docs/coordination/handoff-1265-module-content-self-operation.md` by path (on Coordinator's branch, not yours).

## SETTLED — do not re-derive

- `SportsFollowsWriter` lives in `packages/sports/src/sports-service.ts` (not `routes.ts` — moved
  in T2). `index.ts` re-exports it from there.
- `sports.followTeam`/`sports.unfollowTeam`: `risk: "write"`, `executionPolicy: "auto"`,
  `selfOperationGrant: "granted_at_install"`, `actionFamilyId: "sports_follows"`. Family
  `defaultTier: "ask_each_time"`, `allowedTiers` includes `trusted_auto` — this asymmetry (grant
  promotes availability, family default stays conservative) is intentional, already covered by a
  task-#8 install-routing regression test still to write, not a bug to fix now.
- Binding rule (coordinator, task #9): any test asserting a tool's risk/tier combo MUST also assert
  `risk !== "destructive"` explicitly — nothing else catches a `granted_at_install` +
  `risk:"destructive"` combo (policy.ts: destructive never auto-runs regardless of tier). Already
  done for T4's two tools in `tests/unit/sports-manifest.test.ts`. Apply the same pattern to any
  further sports/self-operation tool test you touch.
- Test DataContextDb fakes need the brand: `{ db: {} as never, [dataContextBrand]: true } satisfies
  DataContextDb` (import `dataContextBrand` from `@jarv1s/db`) — plan's inline test code sometimes
  omits it; add it or `assertDataContextDb` throws at runtime.
- `ToolContext` requires `chatSessionId` (not just `actorUserId`/`requestId`) — plan's inline test
  code sometimes omits it too; add `chatSessionId: "<anything>"`.
- Prettier: run `pnpm format:check` (part of the pre-push trio) before pushing, not after.

## What's done this session

Commits `62d8b375` (T2), `3e78b741` (T3), `eb924e7c` (T4). All target tests + typecheck green after
each commit. No divergence from plan approved by coordinator; two minor spec-drift items absorbed
without escalation (documented in commit context, not repeated here — read `git log -p` on those
commits if you need the reasoning).

## Next steps in order

1. T5 (plan lines 874-960): update `tests/unit/self-operation-manifests.test.ts` inventory, bump
   tool counts. **Before landing:** verify the People-module tool count independently (don't trust
   the plan's number without checking `packages/people` or equivalent manifest), and check for
   rebase conflicts against concurrent #1264 (settings self-operation) — that lane touches the same
   inventory test file.
2. New task #8: install-grant routing test for sports (not in plan doc — coordinator-added). Grep
   the news module's equivalent test (`#1263`/`#1268` territory) to mirror its shape, then write the
   sports analog asserting `granted_at_install` actually routes to auto-execution at install time.
3. T6 (961-1042): cross-actor RLS isolation test,
   `tests/integration/sports-follows-tool-rls.test.ts`. Read
   `tests/integration/sports-follows-repository.test.ts` first for harness conventions.
4. T7 (1043-1107): news preview/confirm regression check + denylist check in
   `self-operation-manifests.test.ts`.
5. Pre-push trio: `pnpm format:check && pnpm lint && pnpm typecheck`, then `git fetch origin main &&
   git rebase origin/main`.
6. Full gate: `pnpm verify:foundation`.
7. `coordinated-wrap-up` — PR + report to Coordinator. **Never merge/board/close yourself.**

## Relay discipline reminder

Two 70%-context-meter warnings fired during this session's T4 build; this relay is already later
than ideal. Successor: relay the MOMENT you see the meter warning or a compaction summary — don't
push for "one more clean commit" past it. Reading the plan section-by-section (not whole) is what
buys you the headroom to actually build before your own relay trigger fires.

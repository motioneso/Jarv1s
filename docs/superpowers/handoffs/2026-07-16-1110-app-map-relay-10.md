# #1110 app-map — relay checkpoint 10

Spec/plan: read Task 8 section only of `docs/superpowers/plans/2026-07-16-1110-app-map-plan.md`
(lines 1775-2162, snippets stale — follow this doc + relay-9 instead).
Branch/worktree: `build/1110-app-map` at `/home/ben/Jarv1s/.claude/worktrees/build-1110-app-map`
(shared — check `herdr pane list` before tree-wide git actions).
Coordinator label: **`Coord-1109-1110-g3`** — confirm exactly one pane holds it before messaging.

## Full detail for every remaining step lives in relay-9

Read `docs/superpowers/handoffs/2026-07-16-1110-app-map-relay-9.md` IN FULL — it has exact
line numbers, exact code snippets, and the exact commit command. This doc only tracks what
changed since then. **No further exploration needed — go straight to editing.**

## State right now (uncommitted, on disk in this worktree — no commit yet, work isn't green)

```
 M tests/uat/seed/types.ts
?? tests/uat/specs/app-map-grounding.uat.spec.ts
```

- **Step 1 DONE**: `tests/uat/specs/app-map-grounding.uat.spec.ts` written in full (4 tests: declared
  prerequisite deep-link, unknown-app honest-unknown, transient-error no-invented-fix, non-admin
  privacy). Typechecked clean (`npx tsc --noEmit`).
- **Step 2 DONE**: confirmed RED via `pnpm test:uat -- app-map-grounding` — failed with the expected
  `must export uatLevel per harness spec §5` error (regex in `run-uat.ts` doesn't parse
  `withoutNewsJsonBinding` yet — that's the next piece of plumbing).
- **Step 3A IN PROGRESS**: `tests/uat/seed/types.ts` — `SeedOptions` now has
  `readonly withoutNewsJsonBinding?: boolean;` added (done). **Nothing else in table A done.**

## Remaining work — do NOT re-derive, table is exact in relay-9

**Step 3A, still to do** (relay-9's table, section "A. withoutNewsJsonBinding flag"):
1. `tests/uat/run-uat.ts` — extend `readUatLevel()` regex (line 44) + `runSpec()` (65-67).
2. `tests/uat/provisioner.ts` — `SeedHook` ctx (159-163), `composeSeedHook` (178-195),
   `buildSeedHookInput` (382-388), `provisionForUat` opts type (408-410), `writeUatEnvFile`
   (92-133, 2 new lines before trailing `""`).
3. `tests/uat/seed/cli.ts` — read `JARVIS_UAT_WITHOUT_NEWS_JSON_BINDING` env, pass through.
4. `tests/uat/seed/levels.ts` — line 79, pass 3rd arg `{ bindNews: options.withoutNewsJsonBinding !== true }`.
5. `tests/uat/seed/chunks/ai.ts` — add 3rd param `options: { readonly bindNews: boolean }`, gate
   `setServiceBinding` call.

**Step 3B, not started** (relay-9's table, section "B. previewOverride mechanism"):
- `packages/news/src/routes.ts`, `packages/news/src/personalization-routes.ts`,
  `packages/module-registry/src/index.ts` — exact interfaces/insertion points/function body all
  given verbatim in relay-9.

**Step 4**: heads-up via `herdr-pane-message` (re-check `herdr pane list` fresh for concurrent UAT
docker runs — no subnet-overlap guard, see agentmemory `uat-docker-subnet-map`), then:
```
pnpm test:uat -- app-map-grounding && pnpm verify:foundation
```

**Step 5**: single commit once fully GREEN, exact file list + message in relay-9 (includes
`package.json` and `docs/WHATS_NEW.md` — relay-9 confirmed the WHATS_NEW.md prepend location is
safe but **the prepend itself was never written this session or the prior one** — check whether
`package.json` needs a script addition too; neither was investigated, re-check relay-9's "Step 3B"
table plus the plan's Task 8 Files line before assuming they're no-ops).

**Post-Task-8**: `pnpm verify:foundation` → pre-push trio (`format:check && lint && typecheck`) →
`git fetch origin main && git rebase origin/main` → `coordinated-wrap-up` skill, report to
`Coord-1109-1110-g3` (resolve label fresh). Never merge/board/close.

## Process reminders

- Strict TDD: RED → confirm failure → implement → GREEN → commit with exact `git add <files>`
  (never `-A`).
- Relay on the context-meter 70% warning or immediately on seeing a compaction summary.
- `node_modules` already exists in this worktree — never `pnpm install` on relay resume.
- Read relay-9 by section for the step at hand — it's short, but don't re-read plan lines you
  don't need.

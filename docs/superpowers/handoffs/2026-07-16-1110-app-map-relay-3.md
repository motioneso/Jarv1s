# #1110 app-map — relay checkpoint 3

Successor to `2026-07-16-1110-app-map-relay-2.md`. That doc's Task 2 plan-gap note is now
**resolved** — do not redo it (see below).

**Branch/worktree:** `build/1110-app-map` at `/home/ben/Jarv1s/.claude/worktrees/build-1110-app-map`
(reuse in place, `[ -d node_modules ] || pnpm install`).
**Plan:** `docs/superpowers/plans/2026-07-16-1110-app-map-plan.md` (8 tasks, headers at lines
51/244/428/540/1083/1538/1698/1775).
**Spec:** `docs/superpowers/specs/2026-07-16-1110-app-map-design.md`.
**Coordinator label:** `Coord-1109-1110` — confirm exactly one pane holds it via `herdr pane list`
before messaging (per `herdr-pane-message` skill).

## Done

- **Task 1 committed: `2ffadbf1`** (see relay-2 doc for detail).
- **Task 2 committed: `432fe68e`** "feat(app-map): require truthful surface descriptions" — files:
  `packages/module-registry/src/index.ts` (adds `assertAppMapDescription`/`assertAppMapDeclarations`,
  called first-statement in `assertModuleRegistryConsistency`'s registration loop — **note:** these
  had to sit ABOVE the module-level `assertModuleRegistryConsistency(BUILT_IN_MODULES);` call
  (~line 1651), not below it, else `const MAX_APP_MAP_DESCRIPTION_LENGTH` hits a TDZ
  ReferenceError at import time — same trap the file already documents for
  `LIFECYCLE_MIGRATION_PENDING`); the 11 manifest files get their exact `description:` literals
  per plan lines 326-366; `tests/unit/app-map-contract.test.ts` gets the 3 new registration tests.
  **Plan-gap resolved, not just folded in:** of the 7 undisclosed fixtures from relay-2, only
  `tests/unit/module-settings-ui-contract.test.ts` actually needed a manual fix (added inline) —
  the other 6 (`calendar-delete`, `email-reply-tools`, `focus-time`, `news-chat-tools`,
  `tasks-agency-tools`, `source-behaviors`) construct manifests by spreading the real built-in
  manifest objects, so fixing the 11 `manifest.ts` files fixed them for free. One extra mechanical
  fix not in the plan: `packages/connectors/src/manifest.ts` has **two** settings surfaces
  (`connectors.user-settings` + `connectors.admin-settings`) but the plan only gave one description
  string — added a second one for admin-settings. Also fixed a pre-existing
  `noUncheckedIndexedAccess` TS2532 in Task 1's own test (`app-map-contract.test.ts` line ~70,
  unrelated to Task 2 but blocked green typecheck).
  **Verified:** `pnpm vitest run tests/unit/app-map-contract.test.ts && pnpm typecheck` exit 0.
  Integration test files (calendar-delete/focus-time/news-chat-tools/tasks-agency-tools) fail when
  run via plain `pnpm vitest run` — expected, they refuse the shared DB and require
  `pnpm test:integration`; not a regression, don't re-chase this.

## Next: Task 3 (plan lines 428-540)

Read that section fresh — not summarized here, per this skill's "read by section" rule.

## Tasks 4-5, 7 — already verified clean, do not re-verify

Per original relay doc `2026-07-16-1110-app-map-relay.md`. Proceed straight to build when reached.

## Tasks 6, 8 — verify premises just-in-time, right before starting each

Same just-in-time verification list as relay-2 doc (unchanged):
- **Task 6** (plan lines 1538-1697): re-check `packages/shared/src/news-api.ts`,
  `packages/news/src/personalization-routes.ts`, `packages/news/src/settings/add-source.tsx`
  (plan claims plain `alert()`, no `ClosedWriteGate` — confirm still true).
- **Task 8** (plan lines 1775-end): re-check `tests/uat/seed/{types.ts,chunks/ai.ts,levels.ts,cli.ts}`,
  `tests/uat/provisioner.ts`, `tests/uat/run-uat.ts`, `packages/news/src/routes.ts` +
  `personalization-routes.ts` (`previewOverride` insertion points), `docs/WHATS_NEW.md` top content.

## After all 8 tasks green

`pnpm verify:foundation` → pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck`) →
`git fetch origin main && git rebase origin/main` → `coordinated-wrap-up` (PR + report to
`Coord-1109-1110`). Never merge/board/close — coordinator's job.

## Process notes

- TDD strict: red → green → commit per task, `git add` exact file list only, never `-A`.
- Relay again on next context-meter 70% warning or compaction summary — commit first, write doc,
  spawn successor, message coordinator, request reap.
- Read plan/spec by section for the task at hand — never front-to-back.
- Coordinator pane confirmed single-match at last check: label `Coord-1109-1110`, session
  `ed4b90d8-5e7f-40b1-b395-5e2beb2bd006` (agent_status was "done" at that check — re-verify
  liveness before messaging).

# Handoff — build lane: structured briefing payload and suppression (#1371, parent #1327)

**Coordinator:** label `Coordinator`, Claude session `43e5f5e2-0deb-4ab5-9237-436e8795b611`.
Re-resolve the pane fresh via `herdr pane list` before messaging — never trust a written pane number.

**Worktree:** `~/Jarv1s/.claude/worktrees/build-1327-core` (you are in it), branch
`build/1327-core`, based on `origin/main` at `d8bf5e3b`.
**Issue:** #1371. **Spec:** `docs/superpowers/specs/2026-07-29-1327-briefing-action-rows.md`.
**Risk tier:** `security` — owner-only suppression state under FORCE RLS, plus cross-module contract
and shared-table changes. That means Opus adversarial QA, a posted PR verdict, and Ben's explicit
merge sign-off before anything lands. Build accordingly.

## Step 1

`pnpm install`. Then read **§9 Task 1** of the spec and build it. Read the spec **by section, for
your current task only** — §3 and §4 for Task 1, §5 for Task 2, §6 for Task 3, §7 for Task 4. Never
read it in full; full-reads bloat a fresh context and trigger premature relays.

## Your scope — §9 Tasks 1 through 4, in order

1. **Task 1** — typed contracts and storage.
2. **Task 2** — resolve reply targets and provider links. **This is the real unknown.**
   `TaskDto.sourceRef` is *not* a `cacheMessageId`; reply cannot resolve its target today. It needs
   composite account + external-ID cache resolution, and the Gmail deep link must be verified
   against a real dev account, not assumed.
3. **Task 3** — extraction, monitor, suppression, and resurfacing.
4. **Task 4** — compose and API structured channel.

**Not yours:** Task 5 (the Today-page prose surface) is being built in parallel on
`build/1327-prose` and touches `apps/web/src/today/*`. Stay out of those files. Tasks 6–7 (the
unified row UI and integrated proof) come after both lanes land.

## Open ruling you must not guess

§13 says **"Accept does not clear prior subject dismissals in v1."** Ben has questioned it and the
coordinator is getting his answer. If you reach that behaviour in Task 3 and no ruling has arrived,
**stop and ask** — do not pick one.

## Constraints that are not negotiable

- **RLS applies to every actor, including admins.** Classify every new table/column: owner-only /
  owner-or-share / recipient-only. FORCE RLS on the new suppression table.
- **`DataContextDb` only** for repositories — never a root Kysely instance. `VaultContext` for all
  vault I/O.
- **`AccessContext` carries only `actorUserId` and `requestId`.** Do not add fields.
- **Secrets never escape** to responses, logs, job payloads, exports, or AI prompts.
- **Metadata-only job payloads** — actor/resource IDs, job kind, idempotency key, small params.
- **Module isolation** — declared public APIs/events only; never another module's tables.
- **Never edit an applied migration.** Resolve the next free number by scanning module `sql/`
  directories immediately before the migration commit — the spec deliberately does not reserve one.
- **Every new stored model-written field needs the body-echo guard** from
  `packages/connectors/src/email-extract.ts`. Per field, say which guard applies.
- **Provider-agnostic AI** — request a capability, never name a model or provider.
- **Never `git add -A` / `git add .`** — stage explicit paths. **Never** run repo-wide
  `pnpm format`; use `npx prettier --write <single file>`.
- **Do not touch `docs/coordination/`** — coordinator-only.
- Commit per task. Reading is not progress — build.

## Definition of done

1. `pnpm verify:foundation` green locally. **Export an isolated `JARVIS_PGDATABASE`** (freshly
   DROP/CREATEd) before any test or DB command — never inline it, never share a gate DB. Use
   `docker exec jarv1s-postgres psql -U postgres`, never `-U jarv1s`, and pass SQL on **stdin**,
   never as an argv `-c` string.
2. Every test named in §9 Tasks 1–4 exists and passes.
3. PR opened against `main` (`Closes #1371`), with a short user-facing summary in release-note
   language. Say plainly if the change is not user-visible on its own.
4. Follow the `coordinated-wrap-up` skill, then report to the `Coordinator` pane. **Do not merge.**

## Escalation

Tag `[SECURITY]` / `[AUTH]` / `[RLS]` / `[DESIGN-FORK]` / `[CRIT]` and stop rather than guessing.
Message via `herdr pane run <coordinator-pane> "<msg>"` after resolving the pane by label.

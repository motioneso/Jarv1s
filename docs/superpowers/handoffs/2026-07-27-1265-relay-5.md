# #1265 relay 5 — T5 done, task#8 next (research done, no code yet)

Worktree/branch: `1265-module-content-self-operation` (this worktree, don't re-`pnpm install` —
`node_modules` present). Coordinator: agent name `coord-1262` (label `Coordinator`, worktree
`coord-1262`) — **re-resolve pane fresh via `herdr pane list` before messaging, never reuse a pane
id from this doc.**

Plan: `docs/superpowers/plans/2026-07-27-module-content-self-operation.md`. **Read by SECTION for
your current task only** — 1128 lines, full-read has already burned multiple sibling contexts.
Task→line map:

- T1-T4 DONE (see relay-4 doc for commits)
- T5 874-960 DONE (`3408c1ee`)
- **new task #8 (install-grant routing test) ← start here** — not in plan doc
- T6 961-1042
- T7 1043-1107 (now includes an SSRF regression restore, see below — not in plan doc either)

## SETTLED — do not re-derive

- T5 verified correct by Coordinator: exact `toBe(31)`/confirmAlways `5`/userPromotable `4`/total
  `toBe(40)`. Do not touch these numbers again unless a rebase against origin/main forces it.
- Sports chat-tools use a **composition-root singleton**, not per-call DI:
  `packages/sports/src/chat-tools.ts` has module-scoped `let service`, set once via
  `configureSportsChatTools(datasetClient, writer)`. Any gateway test exercising
  `sports.followTeam`/`unfollowTeam` MUST call `configureSportsChatTools(fakeDatasetClient,
  fakeWriter)` **before** constructing `AssistantToolGateway` — unlike calendar's per-call
  `toolServices: {calendarWrite: fake}` option. `followTeam`/`unfollowTeam` never touch
  `datasetClient` (only `catalogEntry()` + `repository`), so a stub/unused `DatasetClient` is fine.
  `fakeWriter` must implement `SportsFollowsWriter` (`list`/`create`/`remove`). Real catalog key:
  `"nfl"`.
- **Task #8 template confirmed**: `tests/integration/mcp-gateway-self-operation.test.ts`, the
  `"install grants for the calendar module still leave calendar.deleteEvent asking..."` test (real
  manifest + `grantSelfOperationForModule` + `dbBackedActionPolicy` reading real tiers via
  `AiRepository`/`DataContextRunner`) is the shape to mirror — NOT the fixture-remap test earlier
  in that file (sports tools are already declared `granted_at_install` in the real manifest, no
  remap needed). Likely append the new test to this same file. Assert `res.ok === true` and
  `emitted` contains only `["action_result"]` (no `action_request` card) for `sports.followTeam`
  called with a real catalog key.
- Full memory detail (sports singleton pattern + template) also saved to agentmemory
  (`mem_ms33q7vb_0be761fcb837`) if this doc is insufficient.
- Binding rule (coordinator, task #9 inherited): any test asserting a tool's risk/tier combo MUST
  also assert `risk !== "destructive"` explicitly. Already done for T4's two tools in
  `tests/unit/sports-manifest.test.ts`. Apply the same pattern to any further sports/self-operation
  tool test you touch.
- Test `DataContextDb` fakes need the brand: `{ db: {} as never, [dataContextBrand]: true }
  satisfies DataContextDb` (import `dataContextBrand` from `@jarv1s/db`).
- `ToolContext` requires `chatSessionId` too, not just `actorUserId`/`requestId`.
- Prettier: run `pnpm format:check` (pre-push trio) before pushing, not after.

## NEW — T7 scope restoration (Coordinator, this turn)

The plan doc **dropped an approved spec item**: zero "SSRF"/"previewSource" occurrences in the
plan's 1128 lines, but the approved spec (lines 47-49) requires confirming SSRF/host containment on
`news.previewSource` (risk `"read"`, `externalContent: true`, fetches an arbitrary user-named URL —
this is WHY the lane is security-tier at all). This is a **confirmation/containment check, not new
hardening** — do NOT add a confirm prompt, do NOT change the risk tier.

Coordinator already traced the code path:
- `news.previewSource` → `resolveSourceInput` in
  `packages/news/src/discovery/source-resolution.ts` (calls the domain guard at lines 83, 102, 132,
  160).
- Reject-by-default control: `packages/news/src/personalization-domain.ts` — refuses
  `non_https_scheme`, `credentials`, `explicit_port`, `ip_literal`, `single_label`, plus IDN
  hardening (#953). Looks adequate on inspection.
- **Load-bearing property to regression-test**: line 83 normalizes the **final URL after
  redirects**, not just the user-typed one — this is what stops "publisher.example redirects to
  169.254.169.254". A test that only proves a typed `ip_literal` is rejected is the WEAK version and
  is NOT what's being asked for.

**Deliverable for T7 (in addition to the plan's original T7 content, lines 1043-1107)**: confirm
the path in code, then add a regression through the `previewSource` tool path proving a public
domain that **redirects** to a private/internal address is refused. If post-redirect coverage
turns out inadequate, **STOP, do not fix it yourself** — message Coordinator tagged `[SECURITY]`.
State the conclusion either way in the PR body (Opus security QA will check for this explicitly).
Full detail also in agentmemory (`mem_ms33qcfq_0999e3f626f2`).

## Next steps in order

1. **Task #8** (start here): write the sports install-grant routing test per the confirmed
   template above. TDD: red first (no grant → action_request), then the grant path
   (`res.ok===true`, only `action_result` emitted). Commit.
2. T6 (961-1042): cross-actor RLS isolation test,
   `tests/integration/sports-follows-tool-rls.test.ts`. Read
   `tests/integration/sports-follows-repository.test.ts` first (already read this session — same
   `signUp`/`disableApproval`/`DataContextRunner` harness pattern, RLS via
   `SportsFollowsRepository` directly under each actor's DataContext GUC).
3. T7 (1043-1107 + SSRF restoration above): news preview/confirm regression + denylist check, PLUS
   the post-redirect SSRF regression.
4. Pre-push trio: `pnpm format:check && pnpm lint && pnpm typecheck`, then `git fetch origin main &&
   git rebase origin/main`.
5. Full gate: `pnpm verify:foundation`.
6. `coordinated-wrap-up` — PR + report to Coordinator, include the SSRF confirmation conclusion in
   the PR body. **Never merge/board/close yourself.**

## Relay discipline reminder

This is relay 5. Prior relays (3, 4) already warned about full-plan-reads burning context before
real building starts. This relay's context hit 74% mid-research for task #8 (no code written yet
this session beyond T5) — reading the three candidate template test files in full to find the right
one cost real budget. Successor: you now have the template confirmed (see SETTLED above), so you
should be able to go straight to writing task #8's test without re-reading
`action-policy-install-grants.test.ts` or `module-enablement.test.ts` again — they were ruled out,
not useful. Relay the MOMENT the meter warns or you see a compaction summary.

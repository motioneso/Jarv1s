# #1121 UAT real-chat relay 4 — APPROVED, implementation IN PROGRESS (TDD)

Branch: `fix/1121-uat-scriptable-chat`
Worktree: `~/Jarv1s/.claude/worktrees/fix-1121-uat-chat`
Coordinator: `Coordinator`, immutable session `019f7da3-2d14-7ee2-a42d-c0618a7d821e`

All research is done. This doc is self-contained — no need to re-derive anything below.

## Progress so far (uncommitted, working tree — `git status --short`)

```
 M package.json                    # task 1 DONE: added "@jarv1s/cli-runner": "workspace:*"
 M packages/cli-runner/src/index.ts # task 2 DONE: barrel-exports the 4 provider-token-store fns
 M pnpm-lock.yaml                   # task 1 DONE: pnpm install already run, lockfile regenerated
?? tests/unit/cli-runner-provider-token-store-export.test.ts  # task 2's red->green test, PASSING
```

Both new/modified checks pass:
`pnpm vitest run tests/unit/cli-runner-provider-token-store-export.test.ts` → green.

Tasks 1 and 2 are COMPLETE. Do not redo them — verify with `git status --short` /
`git diff package.json packages/cli-runner/src/index.ts` first if anything looks off.

**Test file location convention (important, learned this session):** this repo's root
vitest config (`vitest.config.ts`) only globs `tests/**/*.test.ts`, `spikes/**/*.test.ts`,
and one `packages/people/src/__tests__/**` special case — NOT `packages/*/src/**/*.test.ts`.
All cli-runner tests live under `tests/unit/cli-runner-*.test.ts` using relative imports
(`../../packages/cli-runner/src/...js`), not package-name imports (`@jarv1s/cli-runner` is
not in the vitest alias list). Follow this convention for every remaining task's tests.

## Next: task 4 (compose regression test), THEN task 3 (compose edit)

Was mid-way checking whether a YAML parser (`yaml` or `js-yaml`) is already a repo
dependency, to parse `infra/docker-compose.prod.yml` in a new static test
(`tests/unit/` or `tests/uat/`, following the convention above) without touching
containers. Full exact target compose-file content (204 lines, `seed:` at 78-113,
`jarv1s:` at 115-174, top-level `volumes:` at 190-198) is already captured verbatim
in task 3's section below — re-read that before writing the test, not the live file
again (it's unchanged from what's quoted here).

If no YAML parser dependency exists, either add one (check for an existing similar
precedent added by another package first) or write a minimal test that does NOT need a
real parser — e.g. line/regex-based assertions on the exact block text (the file is
small and stable), which avoids a new dependency entirely and is likely the better
choice given "reusing existing seams" was the explicit design instruction throughout
this plan.

## Approval (verbatim, binding)

> REVISED PLAN APPROVED. Implement the six functional diffs plus pnpm lockfile
> only, reusing existing seams.
>
> 1. Decrypted credential material lives under a mode-0700 temp dir in a
>    mode-0600 file; validate the plaintext env file contains exactly one
>    allowed nonempty key, `CLAUDE_CODE_OAUTH_TOKEN`, and reject extra keys or
>    malformed content. Never log token, decrypted content, or place token
>    value in argv. Cleanup/unlink in the existing `finally`; do not claim
>    guaranteed secure shredding.
> 2. Add a compose/config regression proving seed and jarv1s mount the same
>    project-scoped cli-auth volume while only seed receives the opt-in env
>    file; jarv1s must not receive `CLAUDE_CODE_OAUTH_TOKEN`.
> 3. In the authenticated real-chat UAT, call the existing admin begin-login
>    route using the established session/request helper, then poll an existing
>    status/model endpoint until ready/chat model registered; no fixed sleeps
>    or race assumptions.
> 4. Default CI remains credential-free and unchanged. Opt-in real-chat mode
>    fails closed if configured but invalid.
> 5. One actual dedicated-account real-token UAT run is mandatory before
>    merge, with no secret output.
> 6. Preserve action_request -> browser Approve -> gateway execute for
>    non-read tools.
> 7. Implement TDD now, focused checks plus format/lint/typecheck, push/open
>    PR, no merge/board/coord-doc actions.

Never request/display/store the real token. Preserve `.claude/context-meter.log`, don't commit it.

## The 7 tasks (implement in this order, TDD — red test before each diff)

**1. Root `package.json`** — add `"@jarv1s/cli-runner": "workspace:*"` to
`dependencies` (copy line from `packages/module-registry/package.json`). Run
`pnpm install` to regen lockfile. No test needed (dependency wiring).

**2. `packages/cli-runner/src/index.ts`** — barrel-export from
`./provider-token-store.js`: `persistProviderToken`, `readProviderToken`,
`providerTokenPath`, `isTokenProvider`. Currently NOT exported (confirmed). No
existing `.test.ts` for `provider-token-store.ts` — write one first
(`packages/cli-runner/src/provider-token-store.test.ts`) asserting the barrel
re-export resolves, then export.

**3. `infra/docker-compose.prod.yml`** (204 lines total):
- `x-app-env-file: &app-env-file` anchor at lines 21-24 (`env_file: [{path: ${JARVIS_ENV_FILE:-./env.production.local}, required: false}]`).
- `seed:` service lines 78-113, currently `<<: *app-env-file` (line 83), `volumes:` only has `jarv1s-vault-data:/data/vaults` (line 107) — **missing** `jarv1s-cli-auth`.
- `jarv1s:` service lines 115-174, `<<: *app-env-file` (line 120), `volumes:` (159-169) already has `jarv1s-cli-auth:/data/cli-auth` (line 167) — the only service with it today.
- Top-level `volumes:` block lines 190-198, `jarv1s-cli-auth:` declared once (195).
- **Change**: give `seed:` its own explicit `env_file:` list (not the shared anchor) with two entries: the existing anchor path, plus a new `${JARVIS_UAT_REAL_CHAT_ENV_FILE:-/dev/null}` (`required: false`). Add `jarv1s-cli-auth:/data/cli-auth` to `seed:`'s `volumes:`. Leave `jarv1s:` untouched.

**4. New compose/config regression test** (static YAML parse, no containers —
put under `tests/uat/` e.g. `tests/uat/compose-cli-auth.test.ts`). Proves:
(a) `seed` and `jarv1s` resolve to the identical project-scoped
`jarv1s-cli-auth` volume; (b) only `seed`'s env_file list references
`JARVIS_UAT_REAL_CHAT_ENV_FILE`; (c) `jarv1s`'s env_file/environment can never
include `CLAUDE_CODE_OAUTH_TOKEN`. Write this test FIRST (red against
unmodified compose file), then make task 3's edit turn it green.

**5. `tests/uat/provisioner.ts`** (543 lines, not yet edited). Existing
pattern to mirror: `writeUatEnvFile({webPort})` (lines 92-138) —
`mkdtempSync(join(tmpdir(), "jarv1s-uat-"))` → `writeFileSync(path, ..., {mode: 0o600})`
→ `{path, cleanup}`. New opt-in `writeUatRealChatEnvFile()`:
- Triggered by an operator env var, e.g. `JARVIS_UAT_REAL_CHAT_TOKEN_FILE` (path to a GPG-encrypted file). Absent → no-op, returns `null`, default/CI path unchanged.
- `mkdtempSync` then `chmodSync(dir, 0o700)`.
- Decrypt via `execFile("gpg", ["--decrypt", "--quiet", "--output", tmpPath, encryptedPath])` — argv carries only paths, never token content.
- `chmodSync(tmpPath, 0o600)` (or ensure gpg output honors mode; verify then chmod explicitly).
- Read plaintext, parse as `KEY=value` lines, validate: **exactly one** nonempty key, and it must be `CLAUDE_CODE_OAUTH_TOKEN` — throw (fail closed) on extra keys, missing key, or malformed lines. Never log the value.
- Set `process.env.JARVIS_UAT_REAL_CHAT_ENV_FILE = tmpPath` for compose interpolation (mirrors `uatComposeInterpolationEnv` pattern at provisioner.ts:151-160).
- Return `{path, cleanup}`; wire `cleanup()` into the SAME existing `finally`/teardown paths as `envFile.cleanup()` in `provisionForUat` (~line 501 success teardown, ~507 catch block). Best-effort unlink only — do not claim guaranteed shredding.
- Write the validation-rejection tests FIRST (red): extra key → throws; missing key → throws; empty → throws; malformed line → throws; happy path → returns path with mode 0600 in a 0700 dir.

**6. `tests/uat/seed/cli.ts`** (55 lines, full content already known — do not
reorder the existing `assertTargetIsEphemeral(db)` guard, lines 28-33). After
that guard (never before), add: if `process.env.CLAUDE_CODE_OAUTH_TOKEN` is
set (populated only via task 3/5's seed-only env file), call
`persistProviderToken(homeBase, "anthropic", token)` (now exported per task 2)
using the cli-auth mount now available in the `seed` container (task 3).
Never log the token. Absent env var → skip, behavior unchanged. Write a test
first if feasible (may need to be an integration-style test under
`tests/uat/seed/` gated on the env var being unset by default — assert skip
path; the set path likely can't run without `gpg`/a real token in CI, so keep
that branch covered by code review + the one mandatory real-token run, not by
an automated CI test).

**7. Real-chat UAT Playwright spec** (new file, e.g.
`tests/uat/specs/real-chat-onboarding.uat.spec.ts`):
- Reuse `signIn(page)` pattern from `tests/uat/specs/runtime-context.uat.spec.ts:31-46` (fills `UAT_ADMIN_EMAIL`/`UAT_ADMIN_PASSWORD`, handles "Skip setup" → "Skip anyway" onboarding branch, asserts `.jds-usermenu__trigger` visible).
- `page.request.post` (shares the signed-in browser context's session cookie) to `POST /api/onboarding/provider-login/begin` with `{providerKind: "anthropic"}` — exact route confirmed at `packages/settings/src/onboarding-routes.ts:697-728`, admin-gated via `assertBootstrapOwnerAdminUser`, manifest path confirmed at `packages/settings/src/manifest.ts:389`.
- Poll `GET /api/ai/models` (confirmed: `packages/ai/src/routes.ts`, manifest `packages/ai/src/manifest.ts:197`, schema `listAiConfiguredModelsResponseSchema` in `packages/shared/src/ai-api.ts:457`, admin-gated `permissionId: "ai.view"`) with backoff (e.g. `expect.poll` or a manual retry loop with increasing delay, NOT a fixed sleep) until the response contains a row with `capabilities.includes("chat")` and an active `status`/`provider_status` (row shape: `AiConfiguredModelSafeRow`, `packages/ai/src/repository.ts:102-121`).
- Then drive an actual chat turn and assert a real model reply (this is the actual point of #1121 — replaces the `test.fixme` stubs in `runtime-context.uat.spec.ts:110/121`, which cite #1121 and should be un-`fixme`'d or a new spec should supersede them once this exists).
- Gate the whole spec: skip/`test.fixme` when no real-chat token is configured for this UAT run (detect via an env var the harness sets only in opt-in mode, e.g. presence of `JARVIS_UAT_REAL_CHAT_TOKEN_FILE` at provision time) — default CI must stay credential-free and green.

## Order of operations

1. TDD per task above, 1→2→3(+4)→5→6→7.
2. Focused checks: the new/modified test files only, plus `pnpm format`, `pnpm lint`, `pnpm typecheck`.
3. The mandatory one real-token dedicated-account UAT run happens outside this session (I must never touch/view the real token) — likely Ben or Coordinator provisions the GPG file and runs `pnpm test:uat` with `JARVIS_UAT_REAL_CHAT_TOKEN_FILE` set; note this as an outstanding pre-merge step in the PR description.
4. Push branch, open PR. Do NOT merge, touch the board, or edit coordination docs (this file is the one exception already in flight).

## Key facts already confirmed (don't re-derive)

- Red check: `pnpm vitest run tests/uat/seed/chunks/ai.test.ts` — commit `a160216b`'s two tests (news binding + "#1121: default seed has no chat-capable model") must both keep passing unmodified; the null-chat-model assertion stays true under default/CI seed (only the opt-in path changes).
- `packages/cli-runner/src/provider-token-store.ts` — full file already known: `persistProviderToken(homeBase, provider, token)` (mkdir 0700 + write tmp 0600 + atomic rename), `readProviderToken`, `providerTokenPath`, `isTokenProvider`; `TOKEN_ENV_VAR = {anthropic: "CLAUDE_CODE_OAUTH_TOKEN"}`; `TOKEN_DIR = ".jarvis/cli-tokens"`.
- No prior `gpg` usage anywhere in repo — the decrypt helper in task 5 is genuinely new, minimal.
- `docker compose run --help` has no `run`-scoped `--env-file` flag — this is why `seed:` gets its own `env_file:` list instead.
- `tests/uat/seed/guard.ts`: `assertTargetIsEphemeral(db)` — full content known, unchanged by this plan.
- `LoginService.start()` ready-short-circuit (`packages/cli-runner/src/login-service.ts` ~176) → `persistLoginTerminal` → `ensureDefaultChatModel` (`packages/ai/src/auto-register.ts`, sole call site `packages/module-registry/src/onboarding-login.ts:118`) is awaited synchronously inside the `begin` route handler before the HTTP response returns — so polling `/api/ai/models` is defense against any future async variant / replication lag, not because `begin` itself is async today. Keep the poll anyway per the Coordinator's explicit "no fixed sleeps or race assumptions" instruction.

## Next action for whoever picks this up

Start at task 1 (simplest). Use `TaskCreate`/`TaskList` if resuming fresh — 7 tasks already exist in this session's task list (all `pending`); recreate if starting a genuinely new session without access to them.

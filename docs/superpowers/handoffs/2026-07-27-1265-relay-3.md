# Relay 3: issue #1265 module-content-self-operation

Branch/worktree: `1265-module-content-self-operation` (this worktree, unchanged). node_modules
present, skip install.

Plan (coordinator-approved, do not re-litigate): `docs/superpowers/plans/2026-07-27-module-content-self-operation.md`

Coordinator label: `Coordinator` in Herdr — re-resolve pane fresh via `herdr pane list` before
every message (label + `agent_session.value`, never a bare pane number).

## State

- T1 DONE + committed: `e71f4f78` (schema drop), `f9347444` (execute-fn fix + regression test —
  coordinator required this after catching that the schema-only drop didn't close the hole; see
  git log for detail, don't re-derive), `e027c5db` (prettier fix on plan doc — coordinator also
  corrected handling of this; standing rule below).
- T2 NOT started (one exploratory edit was made and reverted — file `packages/sports/src/sports-service.ts`
  is back to its original, pre-plan state; no partial work survives).
- T3–T9 NOT started. No sports code touched yet.

## Standing rules from Coordinator (binding, do not re-derive)

1. Any doc committed into this worktree gets `pnpm exec prettier --write <file>` BEFORE the
   commit, not after CI complains. `format:check` is an early CI gate link — red there means
   `test:unit`/`test:integration` never run at all (cost a build-and-verify cycle on #1263 already).
2. **DELETE /api/sports/follows/:id stays exactly as-is** (calls `repository.remove(id)` directly,
   NOT routed through the service). Add a short why-comment at that route explaining REST already
   has the row id, so the service's catalogKey→id resolution is only needed by the tool. This
   changes plan's T3: no service routing for DELETE, comment-only.
3. **New task (#8):** sports write tools declare `granted_at_install` while `sports_follows`
   family keeps `defaultTier: "ask_each_time"` (correct — grant promotes, default stays
   conservative). But real behavior depends on module install writing the
   `assistant.action_policy.v1.sports.sports_follows` row. #1263's QA caught this exact failure
   mode: a test driving the compat helper directly can stay green even if real install-routing is
   deleted. Must add a test that installs the sports module and asserts the policy row exists at
   the routing level (not just manifest classification). Grep #1263/#1268 for the news equivalent
   (`assistant.action_policy` / `action_policy.v1` / module install + policy row assertions,
   likely `tests/integration/`) and mirror for sports.
4. **New task (#9):** explicitly assert `risk !== "destructive"` for `sports.followTeam` /
   `sports.unfollowTeam` in the same commit as their declaration — `policy.ts:36-37` makes
   `risk: "destructive"` never auto-run regardless of tier, so nothing else catches a
   `granted_at_install` + `risk: "destructive"` combo. Fold into T4's test file.
5. **T5 counts (29→31, 38→40)** correct as of current main, but #1264 (settings self-operation,
   different worktree, active concurrently) is moving the same numbers. Whoever lands second
   rebases and recomputes — never loosen to a range. Counting gotcha: **People module declares
   grants in `packages/people/src/tools.ts`, NOT a `manifest.ts`** — verify
   `tests/unit/self-operation-manifests.test.ts`'s inventory logic still picks up People's tools
   correctly before touching counts (existing behavior to verify, not something this plan changes).

## Next steps in order

1. **T2** — `packages/sports/src/sports-service.ts` (889 lines, original/unmodified). Plan's Task 2
   has full code already written — copy from there, don't re-derive. Verified this session: line
   numbers match plan exactly (`SportsFollowsReader` L70, `SportsServiceDependencies` L74,
   `readonly repository` L83, `private readonly repository` L144, `constructor` L147). Steps: add
   `type CreateSportsFollowRequest,` to the `@jarv1s/shared` import block; add `SportsFollowsWriter`
   interface (extends `SportsFollowsReader`, adds `create`/`remove`); widen
   `SportsServiceDependencies.repository` and the private field to `SportsFollowsWriter`; add
   `followTeam`/`unfollowTeam` methods (catalog-key-validated via `catalogEntry()`, unfollow is
   idempotent no-op `{removed:false}` if never followed). Update `packages/sports/src/routes.ts`:
   delete its locally-declared `SportsFollowsWriter` interface, import from `sports-service.js`
   instead. Update `tests/unit/sports-service.test.ts`'s `makeDeps()` fake repo to satisfy widened
   type. New `tests/unit/sports-service-follows.test.ts` (full code in plan, 3 cases: rejects
   unknown competitionKey, follow/unfollow idempotent round-trip, unfollow-never-followed no-op).
2. **T3** — `packages/sports/src/routes.ts:153-188`: route `POST /api/sports/follows` through
   `service.followTeam` (throw `HttpError(400, result.error)` on failure). DELETE stays as-is +
   why-comment (binding rule 2 above — comment text not yet authored, write it fresh). One new
   test case in `tests/unit/sports-routes.test.ts` (400 on unknown competitionKey).
3. **T4** — new `packages/sports/src/chat-tools.ts` (plan Task 4 full code): exports
   `configureSportsChatTools`, `sportsFollowTeamExecute`/`summarizeSportsFollowTeam`,
   `sportsUnfollowTeamExecute`/`summarizeSportsUnfollowTeam`. Modify `packages/sports/src/manifest.ts`:
   add `assistantActionFamilies: [{id:"sports_follows", label:"Sports follows",
   defaultTier:"ask_each_time", allowedTiers:["ask_each_time","trusted_auto","always_confirm"]}]`
   and two `assistantTools` entries (both `risk:"write"`, `executionPolicy:"auto"`,
   `selfOperationGrant:"granted_at_install"`, `actionFamilyId:"sports_follows"`,
   `permissionId:"sports.follow"`). Modify `packages/module-registry/src/index.ts` (~L1515). New
   `tests/unit/sports-chat-tools.test.ts` + update `tests/unit/sports-manifest.test.ts`. **Add the
   risk!=="destructive" assertion (binding rule 4 / task #9) in this task's test, not deferred.**
4. **T5** — `tests/unit/self-operation-manifests.test.ts`: import `sportsModuleManifest`, add
   `GRANTED_AT_INSTALL_SPORTS_TOOLS`, new describe block asserting risk/actionFamilyId/
   executionPolicy/selfOperationGrant for both tools. Bump counts per binding rule 5 above —
   verify People-module counting first, rebase-check against #1264 before landing.
5. **New task (#8)** — install-grant routing test for sports (binding rule 3). Grep #1263/#1268
   first, mirror for sports.
6. **T6** — new `tests/integration/sports-follows-tool-rls.test.ts`. Read
   `tests/integration/sports-follows-repository.test.ts` FIRST for exact harness (DB connection
   helper, AccessContext construction, cleanup) — do not invent new harness calls. Cross-actor RLS:
   user A's follow invisible/unremovable via user B's tool calls. No production code change
   expected (RLS already exists at migration level, `sql/0133_sports_follows.sql`).
7. **T7** — grep `tests/integration/news-revalidation.test.ts` / `tests/unit/news-settings-pane.test.tsx`
   for an existing confirmSource domain-mismatch regression test before adding one. Add denylist
   assertion to `self-operation-manifests.test.ts` (`isSelfOperationExcluded` across news+sports
   write tools) — plan states this should PASS immediately (positive-confirmation, not red/green).
8. Pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck`, `git fetch origin main && git
   rebase origin/main`) + full gate (`pnpm verify:foundation`), then `coordinated-wrap-up`: PR,
   report to Coordinator pane (re-resolved fresh). Never merge/board/close — Coordinator owns that.

## Rules still in force

- Commit per task, `git add` explicit paths only (never `-A`).
- Relay again at next 70% context-meter warning or compaction summary seen — act immediately.
- Any doc committed here gets `prettier --write` before the commit (binding rule 1).
- No further plan re-litigation — plan + coordinator's binding conditions above are final; build
  from them. Escalate genuine forks; don't invent new ones.

# News Slice 4 — relay continuation #7 (issue #975, epic #954)

You are the seventh relay successor building News Slice 4. Same worktree
(`~/Jarv1s/.claude/worktrees/coord-2026-06-30-rfa-fleet/.claude/worktrees/news-s4`), branch
`news-s4`. Relay, not parallel work — predecessors are gone once reaped.

## Primary state (read these, in this order)

1. This doc (short).
2. `docs/superpowers/plans/2026-07-12-news-s4-chat-actions-revalidation-notifications.md` —
   THE plan. Read it BY TASK, only the task you are on. Never front-to-back.
3. Do NOT read the spec in full. If a task needs detail, the plan cites the section
   (`docs/superpowers/specs/2026-07-11-personalized-news-sources-topics.md`).
4. `docs/coordination/handoff-news-s4.md` — coordinator-owned, READ-ONLY, never `git add` it.

## Coordinator + protocol

- Label `Coordinator` (resolve pane fresh via `herdr pane list`; session id authority
  `58a78927-385c-4b1d-8fa0-94db20255d6f`). Caveman-terse messages via `herdr-pane-message`.
- Build per `coordinated-build`; TDD per `superpowers:test-driven-development`; commit green per
  task with `Co-Authored-By: Claude <noreply@anthropic.com>`; explicit-path `git add` only.
- Relay at the 70% context-meter warning via the `relay` skill. **Successor MUST be Fable.**
- Pre-push: `pnpm format:check && pnpm lint && pnpm typecheck` + rebase on origin/main.
- Closeout = `coordinated-wrap-up` (PR only). Merge gate = named-unanimous Opus+Codex+Gemini.
  PR body MUST state the migration RLS approach (column-scoped grants, owner-scoped policy)
  and the notification privacy test.
- Plan approval ALREADY GRANTED — do not re-ask. Directives: (1) migration 0161 done;
  (2) ONE summary notification, spec wording; (3) Task 9 = wire EXISTING Settings scaffolds +
  Retry button only; (4) 6 chat tools, edit+unexclude stay REST/Settings-only.

## Done so far (commits on news-s4)

- Tasks 1–4 ✅ (`bd8c8241`, `decd1725`+`d3348a10`, `4c7cbb97`, `616c34fe`) — details in relay5/6
  docs, read only if needed.
- Task 5 ✅ `8e2dd24a` (RED) + `eed0142d` (GREEN) — per-owner daily schedule,
  `packages/news/src/schedule.ts`, reconcile wired into triggerNewsRefresh + GET self-heal.
  **Deviation (reported to Coordinator): pg-boss schedule keys reject colons** — schedule key =
  bare `actorUserId`; payload `idempotencyKey` keeps `news-revalidate:<id>` form.
- Task 6 ✅ `b7f76737` — POST `/api/news/revalidation` (202 `{queued}`, singleton-deduped),
  schema in `packages/shared/src/news-api.ts`, manifest route entry.

## Resume here — Task 7 RED (chat tools: preview/confirm source)

Plan "### Task 7". Precedent study already done this leg — facts you need:

- **Gateway harness:** copy `tests/integration/ai-tools.test.ts` — full
  `createApiServer({ appDb, logger: false })`, `resetFoundationDatabase()` + seed, `invokeTool`
  helper; read that file ~lines 22–340 for invoke/pending/confirm patterns (pending-confirmation
  test ~:235, confirm-resolution ~:305). NEW test file: `tests/integration/news-chat-tools.test.ts`.
- **Tool types** (`packages/module-sdk/src/index.ts:40-139`): `ToolExecute(scopedDb: unknown,
  input, ctx, services?) => Promise<{ data: Record<string, unknown> }>`;
  `ToolSummarize(input, ctx) => string`. Manifest tool fields (:445-479): name, description,
  permissionId, risk, inputSchema, execute, summarize, externalContent. **Omit `actionFamilyId`**
  → gateway always confirm-gates writes (email precedent `packages/email/src/manifest.ts:218`).
- **Deferred-deps pattern:** `packages/news/src/briefing-tool.ts` — module-level `let` deps,
  `configureNewsChatTools(deps)`, execute throws "(composition-root bug)" if unconfigured,
  `assertDataContextDb(scopedDb)` to narrow.
- **Preview store** (`packages/news/src/discovery/preview-store.ts`):
  `createPreviewStore({ttlMs?, maxPerOwner?, now?}) => { put(preview) => confirmationId,
  take(ownerUserId, confirmationId) }` — owner-checked, TTL 10min, single-use.
  `VerifiedSourceCandidate {candidateId, label, canonicalDomain, homepageUrl, feedUrl,
  retrievalMethod, sampleCount, validationFingerprint}`.
- **Build order (plan):** `packages/news/src/chat-tools.ts` with
  `configureNewsChatTools({previews, discovery, availability, boss, repository})`;
  PersonalizationRouteDependencies gains `previews?: PreviewStore` (default createPreviewStore());
  extract confirm-route logic (personalization-routes.ts ~:286) into exported
  `confirmSourceFromPreview(scopedDb, deps, actorUserId, {confirmationId, candidateId?})` →
  `{ok:true, source} | {ok:false, reason:"expired"|"ambiguous"|"duplicate"|"limit"}`; routes.ts
  creates ONE store shared by routes + chat tools; manifest assistantTools entries
  (both `permissionId:"news.prefs"`, no actionFamilyId): `news.previewSource` (risk:"read",
  externalContent:true, input `{source}`; availability gates → tool error strings; previews.put;
  returns `{confirmationId, candidates:[{candidateId,label,domain}], duplicateOfSourceId?}`) and
  `news.confirmSource` (risk:"write", input `{confirmationId, candidateId?, label, domain}`;
  summarize renders card from INPUT label/domain only; execute cross-checks stored candidate
  label/canonicalDomain vs resubmitted fields — mismatch → error; stored candidate is the only
  thing written; on success also triggerNewsRefresh).
- **RED tests:** previewSource runs unconfirmed + returns candidates; confirmSource without
  confirmation → pending action request, NOT executed; confirming pending → source row + audit
  `approval_mode="confirmed"` `outcome="success"`; tampered domain → error, no row; cross-owner
  confirmationId replay → expired, no row; no provider/model string in tool output.
- Commit `feat(news): assistant preview/confirm source tools (#975)` (+ user-facing summary line).

Then Tasks 8–10 in order: Task 8 = remaining 4 tools (removeSource/addTopic/removeTopic/
addExclusion — read plan section fresh), Task 9 = Settings wiring (directive #3), Task 10 =
full gate + `coordinated-wrap-up`. Re-read the plan's task section before each.

## Traps (carried)

- **Focused integration test:** `pnpm exec tsx scripts/test-integration.ts
  tests/integration/<file>.test.ts`. `pnpm test:integration -- <name>` does NOT filter (~11 min).
- `tests/integration/ai-tools.test.ts` (~:481) asserts the FULL queue-name list — no new queues
  needed for Tasks 7–9, so leave it.
- fast-json-stringify strips undeclared response fields — declare every new field in
  `packages/shared/src/news-api.ts`; test routes via `app.inject`/server inject.
- Add NO new keys to `ALLOWED_PAYLOAD_KEYS` (`packages/jobs/src/pg-boss.ts:76`).
- Repo/test files occasionally touched by an outside watcher — if Read reports stale, re-read.
- `has_table_privilege` ignores column grants — worker-posture `[true,false,false,false]`
  expectation stays. Don't "fix" it.
- Prettier every doc AND test file BEFORE committing (`pnpm exec prettier --write <file>`).
- agentmemory `memory_smart_search` returns empty in this env — don't block on recalls.
- Never `sed -i` a file you plan to Edit — it breaks read-tracking.

## Boot sequence for you

1. `[ -d node_modules ] || pnpm install` (no-op expected).
2. Read plan "### Task 7" section, then write the RED tests
   (`tests/integration/news-chat-tools.test.ts`) and build GREEN. BUILD — reading is not progress.
3. Message Coordinator: "relay #7 driving, Tasks 5+6 done (`eed0142d`, `b7f76737`), Task 7 in
   progress" and ask it to reap predecessor pane (label `News S4 relay6`, session
   `24fd78ea-967e-4401-a31e-abc3706f8af1`).

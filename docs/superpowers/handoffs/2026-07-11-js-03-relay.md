# JS-03 relay continuation — onboarding + resume truth guard

**Date:** 2026-07-11. R8→R9 relay at the 70% context-meter warning: Tasks 1–10 committed green
(latest `c5109256`); **Task 0 Steps 0.1–0.5 IMPLEMENTED ON DISK, UNCOMMITTED** — see "R8 state"
below. Steps 0.6–0.7 (API wiring, green verification, commit) remain, then Tasks 11–13.
(R1→R2 at plan approval; R2→R3 after Tasks 1–3; R3→R4 after Task 5; R4→R5 after Task 7 + Task 8
prep; R5→R6 after Task 8; R6→R7 mid-Task-10; R7→R8 before Task 0 code.)

## Where you are

- **Worktree/branch:** `~/Jarv1s/.claude/worktrees/coord-2026-06-30-rfa-fleet/.claude/worktrees/js-03-onboarding-truth`,
  branch `feat/js-03-onboarding-truth`, rooted at `origin/main` = `a8d638e4` (JS-02 merged).
- **Task issue:** #932 (Part of epic #913). PR must say `Closes #932`.
- **Spec:** `docs/superpowers/specs/2026-07-10-job-search-js-03-onboarding-truth-guard.md` — read
  BY SECTION for the current task only, never in full.
- **Plan (APPROVED, see scope below):** `docs/superpowers/plans/2026-07-11-js-03-onboarding-truth.md`
  — this is your primary driver; it is complete and self-contained (exact files, code, tests).
- **Build handoff (read-only, NEVER commit):** `docs/coordination/2026-07-11-js-03-build-handoff.md`
  — guardrails, coordinator routing, run bans. Read it in full (it is short).
- **Skill:** resume via `coordinated-build`, build with `superpowers:test-driven-development`
  (`executing-plans` / `subagent-driven-development` are disabled — drive the plan yourself).

## Coordinator verdict (2026-07-11, binding — SUPERSEDES any earlier "Task 0 held" note)

- **Tasks 1–10 APPROVED** on the **graceful-degrade path**: worker `ai` port NULLABLE; resume
  critique returns an "AI critique unavailable" question when no worker `ctx.ai` exists. That
  nullable seam STAYS (it is what makes Task 0 severable) — Task 0 then fills it.
- **Task 0 APPROVED — Option A (build the severable ctx.ai worker bridge).** Opus [DESIGN-FORK]
  adjudication: Task 0 COMPLETES the already-approved D6 section of
  `docs/superpowers/specs/2026-07-09-external-worker-capabilities-design.md` (lines 269–337)
  that #919/#945 under-delivered — spec-before-build is satisfied; this is completion, not
  net-new. **Binding guards from the verdict:**
  1. Module isolation via the injected-callback pattern: mirror the existing optional
     `createFetch?` closure on `createExternalModuleRpcHandler`; `@jarv1s/module-registry`
     MUST NOT import `@jarv1s/ai`.
  2. Wire the `ai` closure ONLY into synchronous tool dispatch
     (`apps/api/src/external-module-tools.ts` stdin/stdout path), NEVER into
     `external-module-jobs.ts` or any pg-boss payload — resume prose stays out of queued
     payloads. Keep the `forbidden_ai_call` read-risk fail-closed gate.
  3. Envelope rebuild is load-bearing: DROP usage/model/provider ids crossing back to the
     module; keep an integration test asserting no host-extras leak.
  4. Provider-agnostic error union (`needs_config|validation_failed|provider_error|aborted`)
     + plan Task 10's leak-sweep cover naming.
  5. **D6 COMPLETENESS — Task 0 must include BOTH spec'd controls:** the composition guard
     (reject a ctx.ai input resolvable via ctx.auth — D6 L334) AND the per-invocation call
     cap (D6 L326). Read D6 lines 269–337 before implementing Task 0. If either control is
     non-trivial to build, escalate `[DESIGN-FORK]` rather than silently dropping it —
     partial-D6 must not become permanent.
  6. PR body must note Task 0 completes the #919/#915 worker-capabilities charter.
  7. Task 0 stays IN-SLICE — JS-03 council QA covers it.
- Plan Tasks 11–13 (manifest schema tightening, provider-leak sweep, integration cross-owner
  test) remain in scope.
- **Plan delta to apply:** plan Task 0 as written predates guards 2 and 5 — fold in the
  composition guard, the per-invocation call cap, and the jobs-path exclusion (plus tests for
  each) when you execute it. Sensible order: Tasks 1–10 first (approved path, no host edits),
  then Task 0 + Tasks 11–13, unless the plan's own ordering says otherwise.

## What's done / not done

- DONE (earlier): spec-vs-branch verification (agentmemory id `mem_mrg59jy4_436ea63903b2`);
  plan written; Coordinator approval obtained (Tasks 1–10 + Task 0 verdict above).
- DONE (R2, all committed green, TDD):
  - **Task 1** `737d95e4` — `domain/diff.ts` (`diffLines`, LCS with 10k wholesale fallback,
    removed-before-added ordering) + `tests/unit/external-module-job-search-diff.test.ts`.
  - **Task 2** `de765a7c` — `domain/confirmations.ts` (`confirmationIdFor` =
    `contentHash("confirm\0kind\0text")`, `saveConfirmation` first-write-wins + id
    re-derivation check, `listConfirmationIds`), `keys.resumeConfirmation`, structured
    `ResumeEvidence` on `ResumeRevision.evidence`, barrel exports.
  - **Task 3** `2c0b5cc1` — `domain/truth-guard.ts` (`CRITIQUE_SCHEMA`, `parseCritique`
    strict re-check, `verifyClaims` quote-first-match / confirmation / fail-closed;
    fabricated quote FAILS the claim, never falls through to confirmation) +
    `tests/unit/external-module-job-search-truth-guard.test.ts` (27 tests).
- DONE (R3, all committed green, TDD):
  - **Fix** `b851ca58` — R2 committed on vitest green only; `pnpm typecheck` + `pnpm lint`
    were RED on `domain/diff.ts` (noUncheckedIndexedAccess) and the truth-guard test
    (`no-explicit-any`). diff LCS table is now a flat `Uint32Array` + `cell(i,j)` helper.
  - **Task 4** `604b5b3b` — `worker/ai-port.ts` (structural `JobSearchAi`, **`WorkerPorts`
    lives here**, `aiFromWorkerContext` catches rejections → `provider_error`),
    `worker/validate.ts` (`InputError`, `readString/readBool/readPlainObject/readEnum`
    with required-overloads; errors name key+constraint, never values),
    `worker/handlers/flow.ts` (`STEP_ORDER`, `deriveStep`, `updateOnboarding` — spreads
    loaded state so poisoned records fail closed on the JS-02 write guard),
    `worker/index.ts` (`ports(ctx)` with structural `ctx.ai` read → nullable, `wrap`
    converts `JobSearchKvError`/`InputError` to `{status:"error",code,message}`, `tool()`
    wires factory handlers; all 14 keys unchanged, still `notImplemented` except below).
  - **Task 5** `438f88a9` — `worker/handlers/onboarding.ts` `getStateHandler` (gates
    DERIVED from records, not stored flags; leak-sweep test) wired in `index.ts`. Test
    file has a `portsFor(kv)` helper (ai:null, fixed clock).
- DONE (R4, all committed green, TDD):
  - **Task 6** `e07b9278` — `worker/handlers/profile.ts` (get/save-draft/approve;
    inferred-approve → question naming field NAMES only; deterministic id
    `contentHash("profile\0" + provenance + "\0" + canonicalJson(fields))`, retry skips
    rewrite so a moved clock can't trip the immutability conflict; get returns active in
    full + `draftRevisionIds` ids-only) + **`worker/wrap.ts`** (error envelope EXTRACTED
    from `index.ts` so tests pin the handler+envelope contract without importing the
    side-effecting dispatch module — tests use `wrap(handler(ports))` for error cases).
  - **Task 7** `2172c182` — `worker/handlers/resume.ts` `getResumeHandler` (read order:
    explicit revisionId > active pointer > original `"0"`; no resume → question inviting
    paste; `includeDiff` → parent→child `diffLines` hunks, omitted on parentless `"0"`;
    `evidence` passed through verbatim).
  - **Task 8 prep** `edd5b34e` — `MATERIAL_CLAIM_KINDS` exported from truth-guard +
    domain barrel (input validation must name exactly the guard's seven kinds).
- DONE (R5, committed green, TDD):
  - **Task 8** `f8d2661a` — `resume.save-draft` in `worker/handlers/resume.ts`, wired in
    `index.ts` (23 tests green in `…-handlers-resume.test.ts`). Manual: early handler-level
    48 KB gate BEFORE confirmations → confirmations → original-at-`"0"` or markdown revision
    (deterministic id `contentHash("rev\0parent\0content")` via `draftRevisionId`, read-first
    skip in `persistDraftRevision`) → `updateOnboarding({complete:["resume_intake"]})`;
    requires ≥1 of content/confirmedClaims; explicit `parentRevisionId` is
    existence-checked. Critique: ai-null question (provider-name-free) → require rev `"0"`
    → base = explicit `baseRevisionId` ?? active ?? `"0"` → sources = unique
    [original, base, active] → fixed `CRITIQUE_INSTRUCTIONS` + base content + optional
    `instructions` (≤2000 bytes) → `generateStructured({schema: CRITIQUE_SCHEMA,
    maxOutputTokens: 16_384})` → `parseCritique` → `verifyClaims`; unsupported →
    `{status:"question", unsupportedClaims}` NOTHING persisted (dump-equality proven);
    ok → revision with evidence + `resume_critique` complete. Adversarial tests: fabricated
    claim never persists/approves; confirmed-after flow passes with `confirmed` evidence.
- DONE (R6, committed green, TDD):
  - **Task 9** `82d794be` — `resume.approve` (`approveResumeHandler` in
    `handlers/resume.ts`, wired in `index.ts`; thin: readString+assertId → domain
    `approveResume` → `updateOnboarding({complete:[all three resume flags],
    approvedResumeRevisionId})`). 26 tests green; typecheck+lint green. NOTE:
    `listConfirmationIds` returns a **Set**, and onboarding state uses
    `completed: Record<string,boolean>` (not a `complete` array) — both bit R6's tests.
- DONE (R7, committed green, TDD):
  - **Task 10** `c5109256` — `worker/handlers/monitor.ts` (save w/ active-pointer enable
    gate + question naming only what's missing; list metadata-only; get w/ cursor
    timestamps only + `missing_record`), **`worker/registry.ts`** (`ToolFactory`,
    `notImplemented`, `HANDLERS` with all 14 keys — extracted because `defineModuleWorker`
    attaches a stdin readline at import time), `worker/index.ts` shrunk to
    `ports()`/`tool()`/`defineModuleWorker` over the registry. Both Task-10 test files
    committed; 30/30 vitest + typecheck + lint + format:check green. Test-draft bug fixed
    during implementation: onboarding test's fake failing ai returned
    `{ok:false, code:…}` but `JobSearchAiResult` failure shape is `{ok:false, error:
    string}` — corrected to `error: "provider_error"`. Verified
    `@typescript-eslint/no-non-null-assertion` is NOT enabled (eslint --print-config),
    so the test's `HANDLERS["monitor.run"]!` is fine.
- NEXT: finish **Task 0** from the R8 state below (Step 0.6 onward). Then Tasks 11–13, then
  `coordinated-wrap-up`. Commit green per task, `git add` explicit paths only.

## R8 state — Task 0 Steps 0.1–0.5 DONE ON DISK (all four files UNCOMMITTED)

Do NOT rewrite these files — verify with `git status`/`git diff`, then continue at Step 0.6.

- **`tests/integration/module-worker-rpc.test.ts`** — appended `describe("ai.generateStructured")`,
  8 tests: sanitized callback result; host-extras strip (exact `toEqual` on a callback returning
  usage/model/provider — asserts drop); no-`ai`-dep fail-closed → `invalid_rpc` (jobs-path
  semantics); read-risk → `forbidden_ai_call`; malformed-params loop (incl. `maxOutputTokens: 1.5`)
  with callback-call counter = 0; unexpected error label → `provider_error`; composition guard
  (describe-level `beforeAll` RE-SEEDS `acme-a.shared` — an earlier test revokes it — with secret
  `"ai-bridge-runtime-secret"`; `auth.getCredential` then secret-in-prompt AND secret-in-schema →
  `forbidden_secret_in_ai_input`, clean call on same handler passes); call cap (loop to
  `AI_CALLS_PER_INVOCATION_CAP` ok, next call resolves `{ok:false,error:"usage_limited"}`).
  Imports `AI_CALLS_PER_INVOCATION_CAP` from `@jarv1s/module-registry/node` (barrel is
  `export *` at `packages/module-registry/src/node.ts:21` — flows through). Handler config is
  built by a `base()` FUNCTION (`workerDb` only exists after the file-level `beforeAll`).
- **RED verified** against pre-implementation host: 6 of the new tests failed `invalid_rpc` as
  expected; rest of suite 137 files / 1500 tests green. ⚠️ `pnpm test:integration -- <file>` did
  NOT filter — the FULL integration suite ran (~10 min). Budget for that on the green re-run.
- **`packages/module-registry/src/external/worker-rpc-host.ts`** — host IMPLEMENTED: error union
  gains `forbidden_ai_call` + `forbidden_secret_in_ai_input`; exports
  `ExternalModuleAiRequest`/`ExternalModuleAiError`/`ExternalModuleAiResult` (error union is
  FIVE members incl. `usage_limited` per D6) and `AI_CALLS_PER_INVOCATION_CAP = 8`; consts
  `AI_ERRORS` (all five), `AI_TIERS`, `AI_MAX_OUTPUT_TOKENS_CAP = 32_768`; optional `ai?` dep
  typed `(scopedDb: DataContextDb, request) => Promise<ExternalModuleAiResult>`; per-invocation
  closures `resolvedSecrets: Set<string>` + `aiCalls` counter; `resolvedSecrets.add(value)` sits
  beside the existing `rememberSecret(value)` in `auth.getCredential`; ai branch is inside
  `withDataContext` right after `set_config`, ordered: no-dep `invalid_rpc` → read-risk
  `forbidden_ai_call` → `aiRequest()` validation → composition guard (prompt +
  `JSON.stringify(schema)` substring scan) → cap check → callback → envelope REBUILT from
  scratch with unknown-error coercion → `provider_error`.
- **`packages/module-sdk/src/worker.ts`** — SDK IMPLEMENTED: `ModuleWorkerContext.ai
  .generateStructured` (five-member error union), `ai` const bridges via
  `callParent("ai.generateStructured", aiInput)`, passed into the handler ctx next to `kv`.
- **`tests/unit/module-sdk-worker.test.ts`** — spawn boilerplate extracted to a `spawnWorker()`
  helper (original test's behavior preserved verbatim — diffed against `git show HEAD:`), new
  ctx.ai bridge test added. **`pnpm vitest run tests/unit/module-sdk-worker.test.ts` → 2/2 GREEN.**
- ⚠️ **typecheck/lint/format NOT yet run on any of this** (R3 got bitten by exactly that —
  vitest-green ≠ gate-green). Run `pnpm typecheck && pnpm lint` and
  `pnpm prettier --write <the four files>` before committing.

## R9 next steps (in order)

1. **Step 0.6 wiring** (plan L424–451): `apps/api/src/external-module-tools.ts` accepts optional
   `ai?: (scopedDb, moduleId, request) => Promise<ExternalModuleAiResult>` and passes
   `...(input.ai ? { ai: (db, req) => input.ai!(db, module.id, req) } : {})` into
   `createExternalModuleRpcHandler`; `apps/api/src/server.ts` (tools wiring ~L361, `aiRepository`
   ~L211) builds the callback over `generateStructured` from `@jarv1s/ai` with service
   `` `module.${moduleId}` ``, drops `usage`, `catch → {ok:false,error:"provider_error"}` —
   grep server.ts for an existing AI secret cipher instance before creating one.
2. Green verification: full integration suite (see filter warning above) + the SDK unit file +
   `pnpm typecheck && pnpm lint` + prettier on touched files.
3. **Step 0.7 commit** — the six Task-0 paths from plan L454, message
   `feat(external-modules): ctx.ai structured-generation bridge for module workers (#932)`.
4. Tasks 11–13 (read each plan section only when starting it), then `coordinated-wrap-up`.
   PR body: `Closes #932` + note Task 0 completes the #919/#915 worker-capabilities charter +
   flag the D6 L334 ctx.fetch-body composition parenthetical as deliberately NOT built (open).
5. Jobs-path exclusion (verdict guard 2) is satisfied by design — `apps/worker/src/worker.ts:263`
   builds its handler without `ai` and stays untouched; the no-dep fail-closed test covers it.

## Task 0 implementation decisions (R7, settled — NOW MOSTLY EMBODIED in the R8 on-disk code above; kept for rationale. Resume at Step 0.6, not 0.1)

Drive plan Steps 0.1–0.7 (plan L250–456; core interfaces L103–202). Spec D6 =
`docs/superpowers/specs/2026-07-09-external-worker-capabilities-design.md` L260–340. R7 read
every target file in full; the deltas below fold the Coordinator's 7-guard verdict in. Go
straight to Step 0.1 — do not re-read the target files front-to-back.

- **Per-invocation state is free:** `createExternalModuleRpcHandler`
  (`packages/module-registry/src/external/worker-rpc-host.ts`) is constructed PER TOOL
  INVOCATION inside `apps/api/src/external-module-tools.ts` — so a closure-scoped
  `resolvedSecrets: Set<string>` + AI call counter IS per-invocation state. Add the optional
  `ai?` dep mirroring the existing `createFetch?` pattern. `@jarv1s/module-registry` must NOT
  import `@jarv1s/ai` — the callback is injected from `apps/api/src/server.ts` (tool wiring
  ~L361, `aiRepository` ~L211) per plan Step 0.6, threaded through `external-module-tools.ts`
  as `ai?: (scopedDb, moduleId, request) => Promise<ExternalModuleAiResult>`.
- **Composition guard (verdict guard 5a):** where `auth.getCredential` already calls
  `rememberSecret(value)` (worker-rpc-host.ts ~L92), ALSO add the value to `resolvedSecrets`.
  On `ai.generateStructured`, reject if the prompt or `JSON.stringify(schema)` contains any
  resolved secret → typed fail-closed error. The transport layer ALREADY rejects secrets in
  outbound child→parent RPC params (`worker-runtime.ts:188` `containsSecret` → rpc_failed),
  so the host-side guard is defense-in-depth with a TYPED error — test it by calling the rpc
  handler directly (the integration test already invokes the handler, not a live child).
- **Call cap (verdict guard 5b):** closure counter + `AI_CALLS_PER_INVOCATION_CAP` const;
  over cap → `{ok: false, error: "usage_limited"}` per D6 L326 (repair attempts included in
  the count). The plan's `AI_ERRORS` union predates D6's `usage_limited` — ADD it to the
  union and the SDK types (D6 wins; this is D6 completeness, not a fork).
- **Jobs-path exclusion (verdict guard 2):** `apps/worker/src/worker.ts:263` builds its own
  rpc handler (toolRisk "write", no `ai` dep) — leave it untouched; the optional dep means ai
  calls there fail closed. Add an explicit test: handler built WITHOUT `ai` →
  `ai.generateStructured` rejects fail-closed, and keep the plan Step 0.1 `forbidden_ai_call`
  read-risk gate test.
- **No host extras cross the boundary (verdict guard 3):** API wiring drops `usage` and never
  forwards model/provider ids; the module sees `{ok: true, object}` or the typed error union
  only. Integration test asserts no `model`/`provider`/`usage` keys cross to the module.
- **Deliberately NOT built:** the D6 L334 parenthetical ctx.fetch-body composition check
  (legit OAuth flows POST credentials in request bodies). Do NOT silently drop it — flag it
  to the Coordinator in the wrap-up report as an open D6 parenthetical.
- Tests: extend `tests/integration/module-worker-rpc.test.ts` (setup understood: moduleA
  "acme-a", `workerDb`, seeded "runtime-secret" credential; run via
  `pnpm test:integration -- tests/integration/module-worker-rpc.test.ts`) + SDK unit
  `tests/unit/module-sdk-worker.test.ts` (`ai.generateStructured` via
  `callParent("ai.generateStructured", …)`). Commit with plan Step 0.7 paths/message; the PR
  body later notes Task 0 completes the #919/#915 worker-capabilities charter.

## R2 build notes (save yourself the rediscovery)

- Single test file: `pnpm vitest run <file>` — NOT `pnpm test:unit -- <file>` (the script
  is `vitest run tests/unit`, so an extra path becomes a second root and runs the whole
  suite, ~109 s).
- Run `pnpm prettier --write <your files>` before every commit — format:check bit once on
  a multi-line import.
- Domain layer import discipline: consumers import from `domain/index.js` barrel only.
  truth-guard.ts → confirmations.js is a runtime import; confirmations.ts → truth-guard.js
  is `import type` only — keep it that way (acyclic at runtime).
- `.claude/context-meter.log` shows modified and
  `docs/coordination/2026-07-11-js-03-build-handoff.md` untracked — NEVER commit either.

## Guardrails (still binding, SECURITY tier)

ZERO migrations / no SQL / `ctx.kv` only; owner-only isolation proven by adversarial
cross-owner test (extend `tests/integration/external-module-job-search-kv-isolation.test.ts`
pattern); secrets never escape; provider-agnostic (no provider/model names anywhere in module
code or outputs); metadata-only payloads; unsupported claims are questions, never persisted.
`git add` by explicit path — never `-A`/`.`; never repo-wide `pnpm format`.

## Coordinator routing

Label `Coordinator` (session id authority `58a78927-385c-4b1d-8fa0-94db20255d6f`), via
`herdr-pane-message`; verify EXACTLY ONE pane with that label, resolved fresh. Tag escalations
`[SECURITY]` / `[RLS]` / `[DESIGN-FORK]` / `[CRIT]`.

## Context discipline

You have a fresh full budget. BUILD — reading is not progress. Relay only on the 70% meter
warning or a compaction summary, after real committed work. Pre-push trio before every push:
`pnpm format:check && pnpm lint && pnpm typecheck` + `git fetch origin main && git rebase origin/main`.
Close out with `coordinated-wrap-up` (PR + report; Coordinator owns QA/merge/board).

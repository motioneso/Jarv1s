# JS-03 relay continuation — onboarding + resume truth guard

**Date:** 2026-07-11. R10→R11 relay at the 70% context-meter warning: **Tasks 0–12 ALL COMMITTED
GREEN** (latest `a7a09f5d`). Remaining: **Task 13** (final verification — one full
`verify:foundation` re-run) then `coordinated-wrap-up`. ZERO migrations on this branch.

## Where you are

- **Worktree/branch:** `~/Jarv1s/.claude/worktrees/coord-2026-06-30-rfa-fleet/.claude/worktrees/js-03-onboarding-truth`,
  branch `feat/js-03-onboarding-truth`, rooted at `origin/main` = `a8d638e4` (checked R10: branch
  is 0 behind origin/main — no rebase was needed; re-check before push).
- **Task issue:** #932 (Part of epic #913). PR must say `Closes #932`.
- **Plan (APPROVED):** `docs/superpowers/plans/2026-07-11-js-03-onboarding-truth.md` — Task 13 ≈
  L736–754 only. Do NOT read other sections.
- **Skill:** resume via `coordinated-build` (`executing-plans`/`subagent-driven-development`
  disabled — drive it yourself).
- **Build handoff (read-only, NEVER commit):** `docs/coordination/2026-07-11-js-03-build-handoff.md`.

## What R10 did (all committed)

- **Task 12** `20d0af3a` — new describe in
  `tests/integration/external-module-job-search-kv-isolation.test.ts`: userA re-seeded (the JS-02
  lifecycle test DELETES userA incl. the `app.users` row — the describe's `beforeAll` re-inserts
  it), resume revision "0" + confirmation record + approved profile written via A's rpc-backed kv;
  userB get/list null/empty on resume+profile namespaces; B's `resume.get` handler returns the
  onboarding question (and A's body text asserted absent); admin (isActorAdmin true) reads nothing
  via kv AND via worker-role SQL. **Full integration suite after the change: 138 files,
  1510 passed, 2 skipped, exit 0** (+4 = the new tests).
- **Gate catch + fix** `a7a09f5d` — first `verify:foundation` run FAILED at `check:file-size`:
  `apps/api/src/server.ts` hit 1019 lines (main already sat at the 1000 cap; Task 0's ctx.ai
  wiring pushed it over — R9's "integration green" never ran the full gate). Fix: extracted the
  ctx.ai bridge to NEW `apps/api/src/external-module-ai-bridge.ts`
  (`createModuleAiBridge({aiRepository, logger})`, same drop-usage / catch→`provider_error`
  semantics, comment-documented) and the per-actor active-modules resolver to
  `createActiveExternalModulesResolverForApi(...)` in `apps/api/src/external-module-tools.ts`.
  server.ts now 996. Behavior unchanged. After the fix: `check:file-size` + `typecheck` + `lint`
  + prettier all green; targeted job-search unit suites green (21 files, 209 tests).

## R11 next steps (in order)

1. **Task 13 — final verification:** run `pnpm verify:foundation` IN FULL and record the exit
   code (~15 min; run in background, verify the process with `ps` before waiting). The only
   commits since the last full-suite-green are the Task 12 test (`20d0af3a`, proven green in the
   full 1510-test run) and the pure-move refactor (`a7a09f5d`, fast gates green) — expect green,
   but the recorded full-gate exit code is the wrap-up evidence.
2. **Pre-push trio:** `pnpm format:check && pnpm lint && pnpm typecheck`, then
   `git fetch origin main && git rebase origin/main` (was 0 behind at R10).
3. **`coordinated-wrap-up`.** PR body: release-note summary + `Closes #932`. MUST mention:
   - Task 0 completes the #919/#915 worker-capabilities charter.
   - Spec D6 L334 ctx.fetch-body composition parenthetical deliberately NOT built (open).
   - **Plan-drift (profile.get):** plan said optional `revisionId`, but the Task-6 handler
     ignores input entirely — schema made empty-strict per the plan's own "schemas mirror
     handler inputs" rule (noted in a test comment in
     `tests/unit/external-module-job-search-manifest.test.ts`).
   - **Plan-drift (Task 13 refactor):** the server.ts file-size extraction (`a7a09f5d`) was
     unplanned but forced by the gate; flag for QA attention.
4. Report PR + evidence to the Coordinator; it owns QA/merge/board.

## Build notes (save the rediscovery)

- Single test file: `pnpm vitest run <file>` — NOT `pnpm test:unit -- <file>`.
- ⚠️ `pnpm test:integration -- <file>` does NOT filter — FULL suite runs (~10 min).
- `pnpm prettier --write <your files>` before every commit (this doc too, if you edit it).
- `.claude/context-meter.log` (modified) and `docs/coordination/2026-07-11-js-03-build-handoff.md`
  (untracked) — NEVER commit either. `git add` explicit paths only — never `-A`/`.`.

## Guardrails (still binding, SECURITY tier)

ZERO migrations / no SQL in module code / `ctx.kv` only; owner-only isolation proven by the
adversarial cross-owner test (`20d0af3a`); secrets never escape; provider-agnostic (no
provider/model names in module code or outputs); metadata-only payloads; unsupported claims are
questions, never persisted. Never repo-wide `pnpm format`.

## Coordinator routing

Label `Coordinator` (session id authority `58a78927-385c-4b1d-8fa0-94db20255d6f`), via
`herdr-pane-message`; verify EXACTLY ONE pane with that label, resolved fresh. Tag escalations
`[SECURITY]` / `[RLS]` / `[DESIGN-FORK]` / `[CRIT]`.

## Context discipline

Fresh full budget. BUILD — reading is not progress. Relay only on the 70% meter warning or a
compaction summary, with `--model claude-fable-5` in this same worktree/tab. Close out with
`coordinated-wrap-up` (PR + report; Coordinator owns QA/merge/board).

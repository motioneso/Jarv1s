# JS-03 relay continuation — onboarding + resume truth guard

**Date:** 2026-07-11. R9→R10 relay at the 70% context-meter warning: **Tasks 1–10 + Task 0 +
Task 11 ALL COMMITTED GREEN** (latest `0ae3643b`). Remaining: **Task 12** (cross-owner isolation
integration test), **Task 13** (final verification), then `coordinated-wrap-up`.
(R1→R2 plan approval; R2→R3 Tasks 1–3; R3→R4 Task 5; R4→R5 Task 7+8-prep; R5→R6 Task 8;
R6→R7 mid-Task-10; R7→R8 pre-Task-0; R8→R9 Task 0 on disk; R9→R10 after Task 0 + 11 commits.)

## Where you are

- **Worktree/branch:** `~/Jarv1s/.claude/worktrees/coord-2026-06-30-rfa-fleet/.claude/worktrees/js-03-onboarding-truth`,
  branch `feat/js-03-onboarding-truth`, rooted at `origin/main` = `a8d638e4` (JS-02 merged).
- **Task issue:** #932 (Part of epic #913). PR must say `Closes #932`.
- **Plan (APPROVED):** `docs/superpowers/plans/2026-07-11-js-03-onboarding-truth.md` — read BY
  SECTION for the current task only. Task 12 ≈ L701–735, Task 13 ≈ L736–754.
- **Spec:** `docs/superpowers/specs/2026-07-10-job-search-js-03-onboarding-truth-guard.md`
  (by section only).
- **Build handoff (read-only, NEVER commit):** `docs/coordination/2026-07-11-js-03-build-handoff.md`.
- **Skill:** resume via `coordinated-build` + `superpowers:test-driven-development`
  (`executing-plans` / `subagent-driven-development` disabled — drive it yourself).

## What's done (all committed green)

- **Tasks 1–10** — domain (diff/confirmations/truth-guard), worker ports/validate/wrap/registry,
  all 10 handlers (onboarding.get-state, profile ×3, resume ×3, monitor ×3). Latest `c5109256`.
  Details live in git history + plan; do not re-read old relay sections.
- **Task 0** `1edbd370` — ctx.ai worker bridge COMPLETE per the Coordinator's 7-guard verdict:
  host (`packages/module-registry/src/external/worker-rpc-host.ts`: five-member error union
  `needs_config|validation_failed|provider_error|usage_limited|aborted` + `forbidden_ai_call` +
  `forbidden_secret_in_ai_input` rpc errors, composition guard over `resolvedSecrets`, call cap
  `AI_CALLS_PER_INVOCATION_CAP = 8`, envelope rebuilt dropping usage/model/provider), SDK
  (`packages/module-sdk/src/worker.ts` ctx.ai.generateStructured), API wiring
  (`apps/api/src/external-module-tools.ts` optional injected `ai?` callback;
  `apps/api/src/server.ts` builds it over `generateStructured` from `@jarv1s/ai`, service
  `` `module.${moduleId}` ``, drops usage, catch → `provider_error`). Jobs path
  (`apps/worker/src/worker.ts:263`) untouched — no `ai` dep → fails closed (tested).
  **Full integration suite GREEN: 138 files, 1506 passed, 2 skipped, exit 0.**
- **Task 11** `0ae3643b` — strict input schemas (`additionalProperties:false`) on the 10
  implemented tools in `external-modules/job-search/jarvis.module.json`; 3 JS-05/06 stubs stay
  `{"type":"object"}`. New describe in `tests/unit/external-module-job-search-manifest.test.ts`
  (12/12). Bundle test's not-implemented probe retargeted `profile.get` → `opportunities.list`
  (profile.get now issues kv RPCs the bare harness never answers — was a latent 15s timeout).
  **Plan-drift decision to report at wrap-up:** plan said profile.get gets optional `revisionId`,
  but the implemented Task-6 handler ignores input entirely — schema made empty-strict per the
  plan's own "schemas mirror handler inputs" rule (noted in a test comment).
- Task 12 was RENUMBERED by the plan as the integration test; Task "12" in plan = provider-leak
  sweep was folded into Task 10/0 tests. Trust plan section headings, not this doc's memory —
  read the actual Task 12/13 sections before building.

## R10 next steps (in order)

1. **Task 12 — cross-owner isolation integration test.** Extend
   `tests/integration/external-module-job-search-kv-isolation.test.ts` per plan: userA writes
   resume revision `"0"` + a confirmation record + profile revision + active pointer through
   A's rpc-backed kv; userB sees NONE via get/list on `job-search.resume` / `job-search.profile`;
   B's `resume.get` handler returns the no-resume question; admin actor (isActorAdmin true)
   still cannot read A's records. Prep facts already gathered (R9):
   - Existing pattern: `kvForActor(actorUserId, {toolRisk?, admin?})` builds `JobSearchKv` over
     a real `createExternalModuleRpcHandler` with the real parsed manifest; `ids.userA/userB/
     adminUser` from `./test-database.js`; `workerQuery(actorUserId, moduleId, query)` for
     worker-role RLS SQL; `bootstrap` pg client.
   - ⚠️ The file's LAST existing test DELETES userA's data (cascade test) — a new describe
     appended after it must RE-SEED.
   - `WorkerPorts = { kv: JobSearchKv; ai: JobSearchAi | null; now(): Date }`
     (`external-modules/job-search/src/worker/ai-port.ts`).
   - `getResumeHandler` no-resume question: "I don't have a resume for you yet. Paste your
     current resume text and I'll store it as the original to work from."
   - `ConfirmationRecord = {schemaVersion:1, confirmationId, claimKind, claimText, confirmedAt}`;
     `confirmationIdFor(kind,text) = contentHash(` `confirm\0${kind}\0${text}` `)`.
   - Domain exports: saveOriginalResume, approveResume, saveProfileRevision, approveProfile,
     saveConfirmation, NS, keys.
   - ⚠️ `pnpm test:integration -- <file>` does NOT filter — FULL suite runs (~10 min). Budget it.
   - Commit: `test(job-search): cross-owner isolation proof for onboarding/resume/profile
     records (#932)` — explicit path only.
2. **Task 13 — final verification:** `pnpm verify:foundation` (record exit code), targeted
   suites, pre-push trio `pnpm format:check && pnpm lint && pnpm typecheck` +
   `git fetch origin main && git rebase origin/main`.
3. **`coordinated-wrap-up`.** PR body: `Closes #932`; note Task 0 completes the #919/#915
   worker-capabilities charter; flag D6 L334 ctx.fetch-body composition parenthetical as
   deliberately NOT built (open); mention the profile.get plan-drift decision (above).

## Build notes (save the rediscovery)

- Single test file: `pnpm vitest run <file>` — NOT `pnpm test:unit -- <file>`.
- `pnpm prettier --write <your files>` before every commit; vitest-green ≠ gate-green — run
  `pnpm typecheck && pnpm lint` too (noUncheckedIndexedAccess bites test indexed accesses;
  `@typescript-eslint/no-non-null-assertion` is NOT enabled; optional chaining in expects works).
- `.claude/context-meter.log` (modified) and `docs/coordination/2026-07-11-js-03-build-handoff.md`
  (untracked) — NEVER commit either.

## Guardrails (still binding, SECURITY tier)

ZERO migrations / no SQL in module code / `ctx.kv` only; owner-only isolation proven by
adversarial cross-owner test; secrets never escape; provider-agnostic (no provider/model names
anywhere in module code or outputs); metadata-only payloads; unsupported claims are questions,
never persisted. `git add` by explicit path — never `-A`/`.`; never repo-wide `pnpm format`.

## Coordinator routing

Label `Coordinator` (session id authority `58a78927-385c-4b1d-8fa0-94db20255d6f`), via
`herdr-pane-message`; verify EXACTLY ONE pane with that label, resolved fresh. Tag escalations
`[SECURITY]` / `[RLS]` / `[DESIGN-FORK]` / `[CRIT]`.

## Context discipline

You have a fresh full budget. BUILD — reading is not progress. Relay only on the 70% meter
warning or a compaction summary, after real committed work. Close out with `coordinated-wrap-up`
(PR + report; Coordinator owns QA/merge/board).

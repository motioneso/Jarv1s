# Relay 8 — #1265 module content self-operation (security QA RED remediation)

**Read this doc, then read `2026-07-27-1265-relay-7.md` §BLOCKING-2 / §ALSO / §ESCALATE only when you
start that task.** Do not re-read relay-7 front to back — it is long and its BLOCKING-1 section is
now history. Everything still open is restated below.

- Worktree/branch: `1265-module-content-self-operation` (this dir). `node_modules` present — do NOT
  `pnpm install`.
- PR **#1273** is OPEN on this branch. Do not open a new PR, do not close/reopen.
- Coordinator label: `Coordinator` (agent `coord-1262`). **Resolve its pane fresh via
  `herdr pane list` and confirm exactly one pane holds that label before messaging.** Talk caveman.
- Issue #1265, epic #1262. Risk tier: **security** (cross-model QA + Ben merge sign-off).

## Binding constraints from the Coordinator (still in force — do not loosen)

- **HARD STOP:** if a fix cannot be done as specified, STOP and message the Coordinator. Never
  resolve by reclassifying a grant, renaming, or downgrading risk.
- **Do not touch / re-litigate** (QA confirmed clean): the actionPolicy injected-seam caveat (#1263
  pattern), the install-grant routing test, the inventory counts (31/5/4 = 40), and the
  previewSource/news transport SSRF containment (DISCHARGED).
- **ESCALATE, do not decide:** the cross-actor RLS gap (see Task 5 below).
- **One re-QA cycle only** after all fixes land. Commit **per fix**, not one giant commit.
- Never pipe a gate/test run through `tail`/`head` as the last stage — capture the real exit code.
- `pgrep -f <pattern>` self-matches your own shell; confirm a pid via `/proc/<pid>/cwd` +
  `ps aux` before trusting a `/proc/<pid>/environ` read.

## DONE — commit `bc506b6a` (green, do not redo)

`fix(sports): close assistant-supplied team keys against the catalog (#1265)` — BLOCKING-1 (a)(b)(c),
all three belts:

- **(a)** `packages/sports/src/sports-service.ts` `followTeam` now closes `teamKey` against the live
  roster (`getLeagueTeams`) as well as `competitionKey` against the catalog. `getLeagueTeams` fails
  soft (empty list + `degraded`) so an ESPN outage rejects every teamKey — deliberate **fail-CLOSED**
  for an auto-run write tool. A comment says "do not add a degraded-bypass"; leave it.
  `unfollowTeam` deliberately unchanged (no outbound-URL sink).
- **(b)** `packages/sports/src/manifest.ts` — both follow tools bound `competitionKey`/`teamKey` with
  `maxLength: 100` + `pattern: "^[a-z0-9.]{1,100}$"` (const `CATALOG_KEY_PATTERN`).
- **(b, non-obvious)** `packages/ai/src/gateway/input-validation.ts` — **`validateToolInput` parsed
  none of `minLength`/`maxLength`/`pattern`**, so (b) as literally specified would have been
  decorative. Extended the gateway validator with exactly those three string keywords (anchored
  pattern, compiled+cached, unparseable pattern ignored). Header comment documents this as the one
  deliberate exception to the "swap in ajv rather than extend by hand (#133)" rule. **This is the
  finding the Coordinator has been told about; it touches shared `packages/ai` — flag it at wrap-up
  as a possible merge-order collision with the #1264 settings lane.**
- **(c)** `packages/sports/src/source/espn-source.ts` `getSchedule` — `encodeURIComponent` on the
  path key (matches what `getHeadlines` already did for its query-string use).

Tests added/updated in that commit: `tests/unit/sports-service-follows.test.ts` (off-roster reject,
competition-wide follow still allowed, fail-closed on degraded), `tests/unit/sports-chat-tools.test.ts`
(added `makeFakeDatasetClient`, wired into all 4 `configureSportsChatTools` calls, off-roster reject),
`tests/unit/sports-manifest.test.ts` (bounds assertions), `tests/unit/espn-source.test.ts`
(percent-encoding), `tests/unit/mcp-gateway-units.test.ts` (6 string-bounds cases incl. whole-string
anchoring), `tests/integration/sports-follows-tool-rls.test.ts` (static roster stub so the real "dal"
follow still reaches the real DB), `tests/integration/mcp-gateway-self-operation.test.ts` (comment only).

Verified green immediately before commit: 117 sports unit tests, 95 validator+sports tests,
`format:check` = 0, `lint` = 0, `typecheck` = 0.

## DONE — commit `4e1eca69` (Coordinator follow-ups to the validator change)

The Coordinator reviewed `bc506b6a` in the tree, kept the (b) validator extension, and raised four
follow-ups because it is a **shared platform validator changed on a security PR**. Three are done:

- **Blast radius, verified independently (the Coordinator's own grep was WRONG).** It said no other
  in-tree module declares string bounds. There is one: **`packages/settings/src/app-map-tool.ts`**
  declares `maxLength` on `screenId`/`settingId` (120), `errorCode` (160), `query` (240). Those are
  now **newly enforced** — an over-long `app.getMapSlice` query is now rejected with a validation
  error instead of passing through. That is the bound its author wrote, so it is intent, not
  breakage, but it is a real behavior change. No `pattern` is declared anywhere outside sports.
  (`packages/shared/src/*-api.ts` hits are Fastify **route** schemas — a different validator, not
  blast radius. Numeric `minimum`/`maximum`, `additionalProperties`, and `anyOf` are still NOT
  enforced, so `app-map-tool`'s `limit` bounds and `anyOf` are unchanged.)
- **Stale docstring fixed.** `packages/calendar/src/manifest.ts` told authors the validator honours
  `required` but NOT `pattern`/`minLength`/`maxLength` — that had become a lie and is how the next
  author ships a decorative bound on purpose.
- **Anchoring + external reach documented** in the exported `validateToolInput` docstring, where
  manifest authors will actually see it.

**Still owed (Task 6 / PR body, NOT yet written):** the PR body must explicitly state (1) the
blast-radius result above including the `app-map-tool.ts` hit, (2) that `pattern` is anchored to the
whole string even when authored unanchored — a behavior change, and (3) that the change reaches
**external installed modules**, whose previously-decorative bounds now become real rejections. The
Coordinator's instruction is that a reviewer must be *told* this, not discover it.

## OPEN — pick up here

### Task 1 — BLOCKING-2: UAT coverage

1. New `#1000`-harness Playwright spec under `tests/uat/specs/` covering: add a news topic, follow a
   team ("Follow the Yankees"), unfollow — **each asserting NO confirmation card appears** (that is
   the point of `selfOperationGrant: granted_at_install` + `executionPolicy: auto`). Grep the existing
   UAT specs first for a sibling self-operation / no-confirmation-card pattern and match it.
2. Run `tests/uat/specs/module-install.uat.spec.ts` for real and report the **real exit code**.

### Task 2 — ALSO-1: mutation-tight SSRF case

Add ONE case to `tests/unit/news-source-resolution.test.ts` that **only `normalizePublisherDomain`
can catch** — i.e. same-host scheme downgrade to `http://`, an explicit non-443 port, or embedded
credentials. Grep for where `normalizePublisherDomain` actually lives before writing it.

### Task 3 — ALSO-2: vacuous denylist test

`tests/unit/self-operation-manifests.test.ts` currently asserts nothing that can fail. Preferred fix
(a): add a positive assertion using a known-excluded moduleId from `SELF_OPERATION_EXCLUSIONS`,
asserting `isSelfOperationExcluded` returns `true`.

### Task 4 — ALSO-3: restore the mutated singleton

`tests/integration/mcp-gateway-self-operation.test.ts` mutates the process-wide
`configureSportsChatTools` singleton and never restores it (order-dependent pollution). Add an
`afterEach` that restores it.

### Task 5 — ESCALATE (do not decide)

Attempt a **gateway → real DB → RLS** cross-actor span test for sports.
`tests/integration/sports-follows-tool-rls.test.ts` today enters at `SportsService.followTeam`
directly, so it does not prove the gateway path carries the actor through to RLS. If a real
gateway-entry span test is **structurally infeasible**, message the Coordinator with the **specific
structural reason** and wait. Do not conclude "good enough" yourself.

### Task 6 — gate + push + report

- **Fresh isolated gate DB — do NOT reuse `jarv1s_gate_1265b`.** Drop+create, or use a new suffix
  (`jarv1s_gate_1265c`). Full-gate runs leave durable uat-seed rows that break the next run.
- `pnpm verify:foundation` and `pnpm audit:release-hardening`, capturing **real exit codes** (no
  trailing `tail`/`head`).
- Pre-push trio `pnpm format:check && pnpm lint && pnpm typecheck` + `git fetch origin main &&
  git rebase origin/main`, then push to **this same branch** (PR #1273 stays open).
- Report to the Coordinator: every fix, both exit codes, the `packages/ai/src/gateway/input-validation.ts`
  scope note above, and the Task 5 outcome. Then `coordinated-wrap-up`. Do not merge, do not move the
  board.

## Model

Your predecessor booted on **Opus 5** — herdr's default leaked through; build agents run **Sonnet**.
You are spawned with `--model sonnet` explicitly. Confirm your status bar says Sonnet; if it does
not, switch yourself and tell the Coordinator.

## Relay note

Relayed at the context-meter 70% warning right after `bc506b6a` landed. Nothing is in-flight or
uncommitted — the tree is clean at the start of your session. You have a **fresh full budget:
build, commit per task, relay only after real work past ~80%.** Reading is not progress.

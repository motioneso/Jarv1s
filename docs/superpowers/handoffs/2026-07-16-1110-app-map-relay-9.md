# #1110 app-map — relay checkpoint 9

Spec: `docs/superpowers/specs/` (app-map). Plan: `docs/superpowers/plans/2026-07-16-1110-app-map-plan.md`
(read Task 8 section only, lines 1775-2162 — snippets are stale, see redesign below).
Branch/worktree: `build/1110-app-map` at `/home/ben/Jarv1s/.claude/worktrees/build-1110-app-map` (shared —
check `herdr pane list` before tree-wide git actions).
Coordinator label: **`Coord-1109-1110-g3`** — confirm exactly one pane holds it before messaging.

## Done and committed

Tasks 1-7 done (see relay-8 for detail; Task 7 at commit `08e6a716`). **Tree is clean except
`.claude/context-meter.log`** (never add).

## This session: pure verification, zero code written

Relay-8 flagged 2 unverified items (news/module-registry `previewOverride` insertion points,
`docs/WHATS_NEW.md` conflict check). Both now fully confirmed against real files. **No code
changes made — go straight to implementation, no more exploration needed.**

## Task 8 implementation — fully specified, ready to code

### Step 1 (do first): write failing spec

`tests/uat/specs/app-map-grounding.uat.spec.ts` — adapt plan lines 1782-1847's 4 tests
(declared-prerequisite deep-link, unknown-app honest-unknown, transient-error no-invented-fix,
non-admin-map-query privacy) to the **real** harness pattern:

```ts
export const uatLevel = { level: "multi-user", without: [], withoutNewsJsonBinding: true } as const;
```

For the non-admin test, use the **existing** `UAT_SECOND_OWNER_EMAIL`/`UAT_SECOND_OWNER_PASSWORD`
from `tests/uat/seed/admin.ts` (`isInstanceAdmin: false`, already seeded by `multi-user` level's
`seedSecondOwner`) — do **not** create `tests/uat/seed/member.ts` (plan's version collides:
hardcoded `userId` duplicates `UAT_SECOND_OWNER_ID`).

### Step 2: confirm RED

`pnpm test:uat -- app-map-grounding` — must fail before any implementation (new spec file, no
`withoutNewsJsonBinding` plumbing yet, no `previewOverride` yet).

### Step 3: implement — two independent pieces of plumbing

**A. `withoutNewsJsonBinding` flag**, threaded end-to-end (all line numbers confirmed this
session against current file contents):

| File | Change |
|---|---|
| `tests/uat/run-uat.ts` | `readUatLevel()` regex (line 44) → extend to optionally capture `withoutNewsJsonBinding: true\|false`. `runSpec()` (lines 65-67) → pass into `provisionForUat`. **Do not touch `main()`/`resolveSpecPaths` (18-37, 99-107).** |
| `tests/uat/provisioner.ts` | `SeedHook` type ctx (159-163): add `withoutNewsJsonBinding?: boolean`. `composeSeedHook` (178-195): destructure it, add docker `-e JARVIS_UAT_WITHOUT_NEWS_JSON_BINDING=0\|1`. `buildSeedHookInput` (382-388): widen `opts` param, pass field through. `provisionForUat` (408-410): widen opts type to new `UatProvisionOptions` interface. Call at line 459 (`composeSeedHook(buildSeedHookInput(...))`) stays as-is — already correct once types widen. `writeUatEnvFile` (92-133): add 2 new unconditional lines to the env-var array, **before the trailing `""`**: `"JARVIS_UAT_SEED_CONFIRM=1"` and `"JARVIS_UAT_NEWS_TRANSIENT_INPUT=uat-transient.invalid"` (these feed the running app container via `env_file:` — NOT the seed container's docker `-e` args — because `buildUatNewsPreviewOverride()` in module-registry reads `process.env` at app runtime). |
| `tests/uat/seed/cli.ts` | Read `JARVIS_UAT_WITHOUT_NEWS_JSON_BINDING` env var, pass `withoutNewsJsonBinding` into `seedLevel(...)`. |
| `tests/uat/seed/types.ts` | `SeedOptions` (33-37: currently `{level, excludeChunks?}`) → add `readonly withoutNewsJsonBinding?: boolean;`. |
| `tests/uat/seed/levels.ts` | `seedLevel()` line 79 (`await seedAiProviderChunk(runner, adminUserId);`) → pass 3rd arg `{ bindNews: options.withoutNewsJsonBinding !== true }`. |
| `tests/uat/seed/chunks/ai.ts` | `seedAiProviderChunk(runner, actorUserId)` → add 3rd param `options: { readonly bindNews: boolean }`; gate the `repo.setServiceBinding(scopedDb, "module.news", ...)` call on `options.bindNews`. |

**B. `previewOverride` mechanism** (lets News preview route deterministically return a transient
error for one sentinel input, no live upstream needed):

| File | Change |
|---|---|
| `packages/news/src/routes.ts` | `NewsRoutesDependencies` interface (56-77): add `readonly previewOverride?: (input: string) => NewsSourcePreviewResponse \| undefined;`. Add `import type { NewsSourcePreviewResponse } from "@jarv1s/shared";` (not currently imported here). Call site (219-227, `registerNewsPersonalizationRoutes(server, {...})`): add `previewOverride: dependencies.previewOverride,`. |
| `packages/news/src/personalization-routes.ts` | `PersonalizationRouteDependencies` interface (110-130): same `previewOverride?` field. In `/api/news/sources/preview` POST handler (starts 334): **immediately after** `const input = request.body as NewsSourcePreviewRequest;` (line 340) and **before** `return await dependencies.dataContext.withDataContext(...)` (341) — outside the data-context callback, no DB needed:<br>`const override = dependencies.previewOverride?.(input.input); if (override) return override;`<br>**Must run BEFORE the `hasJsonModel` gate (342-355)** — both the "declared prerequisite" and "transient error" tests hit this route under the SAME seeded level (`withoutNewsJsonBinding: true`), so if the override ran after the gate, the transient-input test would incorrectly get `no_json_model`/`class: "prerequisite"` instead of `discovery_unavailable`/`class: "transient"`. |
| `packages/module-registry/src/index.ts` | Add `type NewsRoutesDependencies` to the existing `@jarv1s/news` import block (240-256) — already exported from `packages/news/src/index.ts` line 5. New function beside `buildNewsDiscoveryPorts` (defined 556-620):<br>`function buildUatNewsPreviewOverride(): NewsRoutesDependencies["previewOverride"] \| undefined { const transientInput = process.env.JARVIS_UAT_NEWS_TRANSIENT_INPUT?.trim(); if (process.env.JARVIS_UAT_SEED_CONFIRM !== "1" \|\| !transientInput) return undefined; return (input) => input === transientInput ? { status: "unavailable", error: { code: "news.add_source.discovery_unavailable", class: "transient" } } : undefined; }`<br>Wire into the news `registerRoutes: (server, deps) => {...}` closure (1470-1537): add `const previewOverride = buildUatNewsPreviewOverride();` and add `previewOverride,` to the `registerNewsRoutes(server, {...})` call's object literal (1493-1515), alongside `dataContext`/`resolveAccessContext`/`datasetClient`/`discovery`/`boss`/`availability`. **Do not touch `registerWorkers` (1517-1536)** — unrelated, has its own separate `buildNewsDiscoveryPorts` call at 1518. |

`NewsSourcePreviewResponse` shape confirmed (`packages/shared/src/news-api.ts` 186-194) — the
override's `{status: "unavailable", error: {code, class}}` literal is structurally valid.

`docs/WHATS_NEW.md` — no conflicting entry at top; plan's proposed prepend (between H1 line 1 and
`## Second run — 2026-06-29` line 3) is safe as authored.

### Step 4: run hard UAT + full gate

Send a `herdr-pane-message` heads-up before running (check `herdr pane list` fresh for any other
UAT-running pane — subnet collision risk, no overlap guard; see agentmemory
`uat-docker-subnet-map`). Then:
```
pnpm test:uat -- app-map-grounding && pnpm verify:foundation
```

### Step 5: commit

**Exact file list** (plan's list minus `tests/uat/seed/member.ts`, which is not created):
```bash
git add tests/uat/specs/app-map-grounding.uat.spec.ts tests/uat/run-uat.ts tests/uat/provisioner.ts \
  tests/uat/seed/types.ts tests/uat/seed/chunks/ai.ts tests/uat/seed/levels.ts tests/uat/seed/cli.ts \
  packages/news/src/routes.ts packages/news/src/personalization-routes.ts \
  packages/module-registry/src/index.ts package.json docs/WHATS_NEW.md
git commit -m "test(app-map): prove grounded help in real UAT"
```

## After Task 8 green

1. `pnpm verify:foundation`
2. Pre-push trio: `pnpm format:check && pnpm lint && pnpm typecheck`
3. `git fetch origin main && git rebase origin/main`
4. `coordinated-wrap-up` skill: PR + report to `Coord-1109-1110-g3` (resolve label fresh).
   **Never merge/board/close.**

## Process reminders (don't relitigate)

- Read plan **by section** for the task at hand only — never front-to-back.
- Strict TDD: RED → confirm failure → implement → GREEN → commit with **exact** `git add <files>`
  (never `-A`).
- Relay on the **context-meter 70% warning** or immediately on seeing a compaction summary.
- `node_modules` already exists in this worktree — **never** `pnpm install` on relay resume.

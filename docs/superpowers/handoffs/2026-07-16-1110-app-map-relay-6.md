# #1110 app-map — relay checkpoint 6

Spec: `docs/superpowers/specs/` (app-map). Plan: `docs/superpowers/plans/2026-07-16-1110-app-map-plan.md`.
Branch/worktree: `build/1110-app-map` at `/home/ben/Jarv1s/.claude/worktrees/build-1110-app-map` (shared —
another pane may be present; check `herdr pane list` before tree-wide git actions).
Coordinator label: **`Coord-1109-1110-g3`** — confirm exactly one pane holds it before messaging
(`herdr pane list`).

## Done and committed

- Tasks 1-4: done (see relay checkpoint 5 / commit `568d6a55` etc.)
- **Task 5 (this session): DONE, green, committed** at `5fc87016` — "feat(app-map): expose
  actor-visible map slices". `app.getMapSlice` read tool: `packages/settings/src/app-map.ts`
  (+`app-map-tool.ts`), manifest wiring, chat/module-registry threading through
  `readToolServices`/`registerChatRoutes`. Full vitest gate green:
  `pnpm vitest run tests/unit/app-map-tool.test.ts tests/unit/gateway-read-tool.test.ts tests/unit/mcp-gateway-units.test.ts`
  → 3 files / 57 tests passed. `pnpm --filter @jarv1s/module-registry typecheck`,
  `--filter @jarv1s/settings typecheck`, `--filter @jarv1s/chat typecheck` all clean.
  Two incidental TS fixes were needed and are already committed as part of this same commit:
  `app-map.ts`'s `selector` tuple typed `readonly [string, string]` (noUncheckedIndexedAccess), and
  `gateway-read-tool.test.ts`'s `makeReadTool` tool literal cast `as ModuleAssistantToolManifest`
  plus a `!` on `module.assistantTools[0]` spread (same root cause).

**Nothing else is pending or uncommitted right now** — tree is clean except
`.claude/context-meter.log` (never add that; it's noise, not code).

## Next: Task 6 (plan lines 1538-1697)

Read the plan **by section** (lines 1538-1697 only) fresh — do not re-verify Task 7 (see below).
Handoff/plan claims to just-in-time verify before writing code:
- `packages/shared/src/news-api.ts`
- `packages/news/src/personalization-routes.ts`
- `packages/news/src/settings/add-source.tsx` — plan claims plain `alert()`, no `ClosedWriteGate`;
  confirm this is still true on current branch state before trusting the plan's diff.

## Task 7 (plan lines 1698-1775)

Already verified clean by an earlier session (per checkpoint 5) — **do not re-verify**, just build
it when you get there.

## Task 8 (plan lines 1775-end)

Needs just-in-time verify before coding:
- `tests/uat/seed/{types.ts,chunks/ai.ts,levels.ts,cli.ts}`
- `tests/uat/provisioner.ts`, `tests/uat/run-uat.ts`
- `packages/news/src/routes.ts` + `personalization-routes.ts` `previewOverride` insertion points
- `docs/WHATS_NEW.md` top (release-note entry)

## After all 8 tasks green

1. `pnpm verify:foundation`
2. Pre-push trio: `pnpm format:check && pnpm lint && pnpm typecheck`
3. `git fetch origin main && git rebase origin/main`
4. `coordinated-wrap-up` skill: PR + report to `Coord-1109-1110-g3` (resolve the label fresh).
   **Never merge/board/close** — coordinator's job only.

## Process reminders (already burned a relay cycle learning these — don't relitigate)

- Read plan **by section** for the task at hand only — never front-to-back. A full read alone
  burns ~30 pts of context budget before any code is written.
- Strict TDD: RED → confirm failure → implement → GREEN → commit with **exact** `git add <files>`
  (never `-A`; `.claude/context-meter.log` will otherwise sneak into every commit).
- Relay on the **context-meter 70% warning** or immediately on seeing a compaction summary — do
  not wait for it to feel urgent.
- `node_modules` already exists in this worktree — **never** `pnpm install` on relay resume.

# Relay — #1263 self-operation chassis (builder)

**Spec:** `docs/superpowers/specs/2026-07-26-module-self-operation-settings-commands.md` (chassis half only).
**Plan (authoritative task order):** `docs/superpowers/plans/1263-chassis-plan.md` — 17 tasks, top to bottom. Grep the task heading, read ONLY that section — do not read the plan front-to-back (burned two prior builders).
**Handoff (binding rulings, read once, don't revisit):** `docs/coordination/handoff-1263-self-operation-chassis.md`
**Worktree/branch:** this worktree, branch `1263-self-operation-chassis` off `origin/main`.
**Coordinator:** Herdr label `Coordinator` — resolve pane + session id fresh via `herdr pane list` every time, never reuse a cached pane id or session id from this doc.
**Risk tier:** `security` — adversarial Opus QA + Ben merge sign-off required. Never merge, never touch board — wrap-up is PR + report only.

## Done

- **Task 1** `b2840f7b`: `feat(module-sdk): declare self-operation grants` — `selfOperationGrant` type + field on `ModuleAssistantToolManifest`.
- **Task 2** `d11c4481`: `feat(ai): enforce self-operation classifications` — `packages/ai/src/gateway/self-operation.ts` (`SELF_OPERATION_EXCLUSIONS`, `isSelfOperationExcluded`, `assertBuiltInSelfOperationManifests`, `BUILT_IN_SELF_OPERATION_SCOPE_NOTE`), exported from `gateway/index.ts`. `tests/unit/self-operation-chassis.test.ts` — all 10 named tests green, `pnpm --filter @jarv1s/ai typecheck` clean. Exclusion matchers scoped to `settings`/`ai` module ids only (zero collision with real inventory).

## Coordinator rulings already applied — do not re-litigate

- **Task 7 (notes.delete) is UNBLOCKED.** Coordinator ruling: keep `notes.delete` risk `destructive`, declare `selfOperationGrant: "confirm_always"`. create/edit classify normally. This is status quo — `policy.ts:37` already confirms every destructive tool regardless of tier, so the declaration changes no runtime behavior.
- **Tasks 16–17 exact-count assertions use FOUR `confirm_always` tools**: `memory.forget`, `people.merge`, `people.splitIdentity`, `notes.delete`. Do not stub or soften this count — already baked into Task 2's `PLANNED_CONFIRM_ALWAYS_TOOLS` allowlist in `self-operation.ts`.
- **PR body requirement:** state plainly that `notes.delete` preserves today's confirmation behavior, and that flipping it to `granted_at_install` would mean Jarvis deleting notes without asking.

## Context discipline (coordinator directive, in force)

Three builders in a row burned context without shipping code by reading too broadly. Rule: grep the task heading in the plan, read ONLY that section (files/symbols/tests/verify/commit are all listed per task). Do not re-read the handoff doc, do not read the spec unless a task instructs it, do not pattern-hunt through `gateway.ts`/`policy.ts`/manifest files beyond the exact symbol a task names. If you need a third file for one task, stop and ask the coordinator instead.

## Next: Task 3, then Tasks 4–17 top to bottom per the plan

- **Task 3**: hoist ONLY the exclusion/unavailability check above the YOLO branch in `packages/ai/src/gateway/gateway.ts`. `resolvePolicy` must NOT move; YOLO must keep bypassing `confirm_always`/destructive/`requiresConfirmation`. `policy.ts` stays untouched. Grep plan for `## Task 3` and read only that section.
- **Tasks 4–13**: classify write tools package by package (tasks, commitments, goals, memory[done], news, people, notes[apply the unblock ruling above], email, calendar, web-research, ai). Real inventory = 38 tools across 10 packages — re-measure on this branch, don't trust stale counts. `people.merge`/`people.splitIdentity` both `confirm_always` (settled, don't re-verify).
- **Tasks 14–15**: action-policy store insert-if-absent (never reuse clobbering `setActionPolicy`, never add a migration); install-time grant precedence (must never clobber a user-set tier — write the regression test).
- **Tasks 16–17**: final assertions/wiring + inventory-count tests (four `confirm_always`, per ruling above — no longer blocked).

## Bans (still in force)

Work only in this worktree/branch. `git add` explicit paths only, never `-A`. Never repo-wide `pnpm format`. Never touch `docs/coordination/` or `docs/superpowers/plans/`. Never merge, never touch board. Never pipe the gate through `tail`.

## Exit

`pnpm verify:foundation` green with real exit code (never `| tail`), then `coordinated-wrap-up` (PR + report to coordinator, never merge).

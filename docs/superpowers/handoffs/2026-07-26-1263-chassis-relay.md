# Relay — #1263 self-operation chassis (builder)

**Spec:** `docs/superpowers/specs/2026-07-26-module-self-operation-settings-commands.md` (chassis half only).
**Plan (authoritative task order):** `docs/superpowers/plans/1263-chassis-plan.md` — 17 tasks, top to bottom.
**Handoff (binding rulings, read once, don't revisit):** `docs/coordination/handoff-1263-self-operation-chassis.md`
**Worktree/branch:** this worktree, branch `1263-self-operation-chassis` off `origin/main`.
**Coordinator:** Herdr label `Coordinator`, session id `43e5f5e2-0deb-4ab5-9237-436e8795b611` — resolve pane fresh via `herdr pane list` every time, never reuse a cached pane id.
**Risk tier:** `security` — adversarial Opus QA + Ben merge sign-off required. Never merge, never touch board — wrap-up is PR + report only.

## Done

- **Task 1** committed `b2840f7b`: `feat(module-sdk): declare self-operation grants`
  - `packages/module-sdk/src/index.ts`: added `ModuleAssistantToolSelfOperationGrant = "granted_at_install" | "confirm_always"` type export, and optional `selfOperationGrant?: ModuleAssistantToolSelfOperationGrant` field on `ModuleAssistantToolManifest` (after `executionPolicy`). Did NOT touch `JarvisActionPermissionTier` or `defaultTier` typing.
  - `tests/unit/mcp-gateway-units.test.ts`: added test `"accepts the selfOperationGrant vocabulary without widening action tiers"` inside existing `describe("module-sdk tool contract", ...)`. Verified: `pnpm vitest run tests/unit/mcp-gateway-units.test.ts` → 52 passed.
- Relayed to coordinator already (message sent, this doc is the follow-up).

## Next: Task 2 (not started, no code written yet)

Per plan: create `packages/ai/src/gateway/self-operation.ts` exporting:
- `SELF_OPERATION_EXCLUSIONS` — 7 immutable rule categories: `self_authority`, `prompt_shaping`, `secrets`, `identity_auth_registration`, `data_scope_consent`, `assistant_brain`, `external_effect`.
- `isSelfOperationExcluded(moduleId, tool)` — matches ONLY on manifest-owned identity (module id, tool name, permission id, action-family id, input-schema property names). Never on runtime tool inputs.
- `assertBuiltInSelfOperationManifests(manifests)` — build-time assertion (wire into boot only after Tasks 4–13 classify the real inventory).

Also add exports to `packages/ai/src/gateway/index.ts` (currently re-exports from `./policy.js`, `./session-tokens.js`, `./confirmation-registry.js`, `./input-validation.js`, `./output-validation.js`, `./types.js`, `./gateway.js` — add `./self-operation.js`).

Then `tests/unit/self-operation-chassis.test.ts` with the plan's exact named test list (read plan Task 2 section for exact names — do not re-derive).

**Design resolution already reached (use it, don't re-derive):** the 7 exclusion categories describe tools that don't exist yet in this codebase (#1264's future settings/AI-admin tools — this issue ships no new assistant tools). Per plan: "Use synthetic manifests in this task; do not wire the assertion into boot until Tasks 4–13 have made the built-in inventory valid." Scope exclusion-rule matchers by module id values that currently own **zero** built-in write assistantTools (e.g. `"settings"` = `SETTINGS_MODULE_ID` in `packages/settings/src/manifest.ts:14`, `"ai"` = `AI_MODULE_ID` in `packages/ai/src/manifest.ts:48`) so there is zero collision risk with the real tools Tasks 4–13 classify. Confirmed via grep: `secret: true` only appears in `packages/settings/src/runtime-config-keys.ts` and `instance-settings-keys.ts` — an external registry the matcher cannot and must not reach into; encode "secrets" exclusion structurally (module/tool/permission-id patterns), not by cross-referencing that registry at match time.

TDD order: write the test file first (per `coordinated-build`/TDD), then implement `self-operation.ts` to pass it.

## Then: Tasks 3–17, top to bottom, per the plan

- **Task 3**: hoist ONLY the exclusion/unavailability check above the YOLO branch in `packages/ai/src/gateway/gateway.ts` (`callTool` ~line 127-197, `executableTools` ~line 580-620). `resolvePolicy` itself must NOT move; YOLO must keep bypassing `confirm_always`/destructive/`requiresConfirmation`. `policy.ts` stays untouched.
- **Tasks 4–6, 8–13**: classify write tools package by package (tasks, commitments, goals, memory[done as reference example above], news, people, notes[**see gate below**], email, calendar, web-research, ai). Real inventory per plan = **38 tools across 10 packages** (re-measure on this branch per plan instructions, don't trust the handoff's stale 39/11 table). `people.merge`/`people.splitIdentity` are BOTH `confirm_always` (round-trip already verified NOT true — this is settled, don't re-verify).
- **Task 7 (Notes) — HARD GATE, DO NOT DECIDE:** `notes.delete` (`packages/notes/src/write-tools.ts:232`, calls `unlink` directly) is BLOCKED pending Ben's confirmation that it's a fourth `confirm_always` tool (only `memory.forget`, `people.merge`, `people.splitIdentity` are confirmed so far). **Skip Task 7 when you reach it, keep building everything else, escalate to Coordinator, do not decide yourself.** Already flagged to coordinator: Task 7's outcome transitively blocks Task 16/17's exact-count assertions (build-time assertion of "N confirm_always tools" can't be written/tested until notes.delete is classified one way or the other).
- **Tasks 14–15**: action-policy store insert-if-absent method (never reuse clobbering `setActionPolicy`, never add a migration); install-time grant precedence (must never clobber a user-set tier — write the regression test).
- **Tasks 16–17**: final assertions/wiring + inventory-count tests — blocked on Task 7 resolution, see above.

## Bans (still in force)

Work only in this worktree/branch. `git add` explicit paths only, never `-A`. Never repo-wide `pnpm format`. Never touch `docs/coordination/` or `docs/superpowers/plans/`. Never merge, never touch board. Never pipe the gate through `tail`.

## Exit

`pnpm verify:foundation` green with real exit code (never `| tail`), then `coordinated-wrap-up` (PR + report to coordinator, never merge).

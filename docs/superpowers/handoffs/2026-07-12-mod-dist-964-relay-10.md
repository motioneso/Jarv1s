# mod-dist-964 relay 10 handoff

Branch: `mod-dist-964`, worktree: `/home/ben/Jarv1s/.claude/worktrees/mod-dist-964`
Plan: `docs/superpowers/plans/2026-07-12-module-distribution-install.md` (5464 lines — read BY
SECTION only, never front-to-back).
Coordinator: label `Coordinator` — always re-resolve via fresh `herdr pane list`, never a baked
pane number. Security tier: never merge/close/move board yourself.

## Done (committed)

- Task 7 Step 10, Task 8: `c08949a1`, `0ca51722` (prior relays).
- **Task 9 (admin web UI, module registry section): `da376b53`** — query-keys.ts +
  client.ts fns + settings-feedback.tsx `requireText` type-to-confirm + new
  `settings-module-registry-section.tsx` + wired into `InstanceModulesPane`
  (`settings-admin-panes.tsx`, now 989/1000 lines). `pnpm --filter @jarv1s/web typecheck`
  and `lint` both green.

## Current task: Task 9.5 (NEW, Coordinator-approved) — split server.ts under file-size cap

`pnpm check:file-size` is RED: `apps/api/src/server.ts` is 1082/1000 lines. **Confirmed not
caused by my Task 9 diff** (`git diff --stat` on server.ts is empty for Task 9's commit) — it's
been flat at 1082 since commit `72cb5e27` (Task 6, "admin module-registry surface"), predating
this relay chain. Earlier relays' gate runs only covered typecheck/vitest/compose-validate, so
this slipped through uncaught until now.

**Coordinator's ruling (verbatim, approved 2026-07-12):** "APPROVED — fix now, you own it. No
other lane touches server.ts; your Task6 routes pushed it over the 1000-line cap (main is green
so main was ≤1000). This blocks verify:foundation at Task10 regardless. CONSTRAIN the split:
extract ONLY your new module-distribution route handlers into a dedicated route module (e.g.
apps/api/src/routes/*.ts or the module-owned surface), matching the module-isolation invariant —
do NOT reorg unrelated server.ts sections or move other modules routes (that widens the diff and
regression surface). After: run check:file-size (server.ts + any new file all <1000), typecheck,
lint, and the route tests. Add a one-line note in the eventual PR body that Task6 crossed the cap
and Task9.5 extracted the module-dist routes to restore it. Then continue to Task10."

**Exact extraction target** (already diffed, see `git show 72cb5e27 -- apps/api/src/server.ts`):
- New imports added: `downloadAndStageModule`, `fetchRegistryIndex`, `ModuleDownloadError`,
  `parseModulesEnsure` from `@jarv1s/module-registry/node`; `ModuleRegistryEntryLike` type from
  `@jarv1s/settings`; `readdir`, `rm` from `node:fs/promises`; `join` from `node:path`.
- New block (~78 lines, in `createApiServer`, after the `externalModuleSnapshot`-adjacent code
  around what is now line ~480-560): a `moduleDistribution` object built only when
  `externalRuntimeEnabled && apiServerConfig.externalModulesDir !== null`, exposing
  `fetchRegistryEntries` (10-min in-process cache, `REGISTRY_CACHE_TTL_MS`), `download`,
  `removeModuleFiles` (path-safety regex + traversal guard — keep exactly as-is, it's a security
  boundary), `listOnDiskModuleIds`, `ensureIds`. Then wired into the route-deps object as
  `moduleDistribution,` (one line, ~current line 613ish — re-grep, Task 9's commit didn't touch
  server.ts so line numbers are exactly what `72cb5e27` left).
- **Plan:** move the whole cache+port-construction block into a new file, e.g.
  `apps/api/src/module-distribution-port.ts`, exporting a factory
  `createModuleDistributionPort(options: { server, apiServerConfig, externalRuntimeEnabled,
  fetchFn })` (or whatever params it actually closes over — re-read the closure before
  extracting, don't guess signatures) returning the same shaped object (or `undefined`).
  `server.ts` then imports the factory and replaces the ~78-line block with a single call. This
  should land server.ts comfortably under 1000 (removes ~78 lines, only adds back ~3-5 for the
  import + call).
  - **Module-isolation invariant** (CLAUDE.md hard invariant): this is app-level wiring (not a
    module's own internals), so a new file under `apps/api/src/` is fine — don't try to relocate
    it into `packages/module-registry` or `packages/settings` (those already own the lower-level
    `downloadAndStageModule`/`fetchRegistryIndex` primitives this file composes).
  - Keep the `removeModuleFiles` path-traversal guard byte-for-byte identical — it's a security
    boundary (moduleId comes from a URL param), don't refactor its regex while moving it.
- After the move: `pnpm check:file-size` (server.ts + new file both <1000),
  `pnpm --filter @jarv1s/api typecheck`, `pnpm --filter @jarv1s/api lint`, and whatever route
  tests cover module-registry download/remove (grep `tests/` for `module-registry` or
  `moduleDistribution` — Task 6's plan section listed `tests/unit/module-registry-derive-rows.test.ts`
  and similar; confirm current names before running).
- Commit separately from Task 9 (already committed as `da376b53`) — this is its own logical
  change. Suggested message: `fix(api): extract module-distribution port to restore server.ts
  file-size gate (#964)` with a body noting Task 6 crossed the 1000-line cap and this restores it
  (Coordinator asked for this note to also appear in the eventual PR body).

## After Task 9.5

**Task 10** (plan lines ~4884+): integration suite, docs, spec-example fix, full gates. Not yet
read this session — read by section when you get there, don't front-load it.

Then pre-push trio before any push: `pnpm format:check && pnpm lint && pnpm typecheck`, then
`git fetch origin main && git rebase origin/main`. No push has occurred yet this branch.

Finally: `coordinated-wrap-up` (PR titled "Part of #964") once Task 10's Exit Criteria are green.
Include in the PR body: Task 6 crossed the file-size cap on `server.ts`; Task 9.5 extracted the
module-distribution port to restore it (per Coordinator's note above). Never merge/close/move
board — report PR + evidence to Coordinator only.

## Notes

- `node_modules` already exists in this worktree — skip `pnpm install`.
- Read the plan doc by section only; a full front-to-back read burns context with zero code
  written (this has bitten prior relays in this chain).
- Relay trigger: context-meter 70% warning (fired this relay) or any compaction summary seen.

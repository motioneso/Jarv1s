# Relay: #964 module distribution & install (relay 3)

## Where things are

- Worktree: `/home/ben/Jarv1s/.claude/worktrees/mod-dist-964`, branch `mod-dist-964`.
- Spec: `docs/superpowers/specs/2026-07-12-module-distribution-install.md` (approved, council waived by Ben).
- Plan: `docs/superpowers/plans/2026-07-12-module-distribution-install.md`. **Read ONE task section at a time.**
- Handoff contract: `docs/coordination/handoff-mod-dist-964.md` — read in full, never edit/stage it.
- Prior relays: `docs/superpowers/handoffs/2026-07-12-mod-dist-964-relay.md` (Tasks 1-2), `...-relay-2.md` (Task 3 + Task 4 premise notes).
- Coordinator: Herdr label `Coordinator` — re-resolve pane fresh by label before messaging, confirm exactly one match.
- Risk tier: **security**. Build to green; coordinator runs the adversarial council and owns merge/board. Never merge yourself.
- Migration: **0162**, not 0161 (0161 reserved for News S4, #977). Already applied — don't touch.

## Done (commits on this branch, newest last)

- Task 1 — registry index schema + ensure-list parsing: `b4af4976`.
- Task 2 — manifest `database.ownedTables` + `sql/**` in package hash: `02dc9f5c`.
- Task 3 — migration 0162 + staged/purge repository state: `a56a644b`.
- **Fix — `ownsTables` boolean→`readonly string[]`: `c0c55a8d`.** Task 1's committed schema
  (`packages/module-registry/src/distribution/index-schema.ts`) typed `capabilities.ownsTables`
  as `boolean`, matching the plan's own Task 1 prose (L67) but contradicting every downstream
  consumer written later in the same plan doc: Task 4 (L1545 `ownsTables: manifest.database?.ownedTables ?? []`),
  Task 6's DTO/JSON-schema (L2562/2608-2613), Task 9's confirm-dialog copy (L4633-4634
  `Owns database tables: ${caps.ownsTables.join(", ")}`). Escalated to coordinator, approved,
  fixed to `readonly string[]` in the type + validator (`distribution/index-schema.ts`) + Task 1's
  test fixture (`tests/unit/module-registry-index-schema.test.ts`). Suite green, `@jarv1s/module-registry`
  typecheck clean. **If you read the plan's Task 1 section (L1-1284) literally, ignore its
  `ownsTables: boolean` text — the committed code and every later task now agree on `string[]`.**
- Task 4 steps 1-4 — publish script core: `8a784b60`. `scripts/publish-module-registry.ts`
  (`mergePreviousVersions`, `packModuleArtifact`, `buildRegistryArtifacts`), TDD red→green
  (`tests/unit/publish-module-registry.test.ts`, 6/6 green), `publish:module-registry` script
  added to root `package.json`. `tar@^7.5.16` + `@types/tar@^6.1.13` added as root devDeps
  (`pnpm add -D -w`, lockfile updated and committed).

## Next: finish Task 4, then Task 5 onward

Read plan lines 1611-1691 (Task 4 Steps 5-6 only — workflow file + local validation):

1. **Step 5**: create `.github/workflows/modules-registry.yml` exactly as specified (rolling
   release tagged `modules`, mirrors `ci.yml` setup: checkout@v5, pnpm/action-setup@v4 v10.6.2,
   setup-node@v5 node 24). Full YAML is in the plan verbatim — copy it.
2. **Step 6**: `pnpm vitest run tests/unit/publish-module-registry.test.ts` (should already be
   green — re-verify), then `pnpm publish:module-registry` (local end-to-end: builds `job-search`,
   packs, writes `index.json` under `dist/registry` — `dist/` is gitignored, don't stage it).
   Commit: `scripts/publish-module-registry.ts` already committed; this commit is just
   `.github/workflows/modules-registry.yml` (+ anything Step 6 touches, check `git status`).
3. Then continue Task 5 (Download → verify → extract → stage pipeline, plan starts ~line 1693) —
   read that section fresh, verify its premises against the branch same as always (grep cited
   exports before trusting line numbers).
4. Tasks 6-10 as in relay-2's doc (contracts/admin routes, boot reconcile, boot/compose wiring,
   admin web UI, integration suite + full gates).

After Task 10 green: `coordinated-wrap-up` → open PR (Part of #964) → report to Coordinator.
Do not merge, move the board, or close issues.

## Standing constraints (don't re-litigate)

- Stage explicit paths only, never `git add -A`/`.`/repo-wide `pnpm format` (shared tree).
- Never touch `docs/coordination/`, board, milestones, or merge.
- No secrets in any doc/payload/log/prompt.
- Never edit an applied migration. Module SQL only in the owning module's `sql/`.
- **Confirm with coordinator before any new migration number** or before editing another task's
  already-committed file (as done this relay for the `ownsTables` fix) — cheap to ask, expensive
  to guess wrong on a security-tier build.
- `@jarv1s/settings` must NOT import `@jarv1s/module-registry`.
- File-size gate: all source (incl. CSS) ≤1000 lines.
- Every new Fastify response field declared in `packages/shared/src/*-api.ts` (schema strips
  undeclared fields) — test via `app.inject`.
- Prove security invariants via tests, not prose.
- 3 blessed spec deviations (relay-2 doc has details) — don't re-litigate: (1) `last_install_error`
  mirror column; (2) dev-boot parity via root `db:reconcile`, no `scripts/dev.ts`; (3) Task 10
  fixes spec's `jarv1s.job-search`-style example ids to bare kebab.
- Relay again on the 70% context-meter warning or immediately on a compaction summary — message
  Coordinator first (re-resolve pane by label + session id, confirm exactly one), then `relay` skill.

## Bootstrap for the next session

`[ -d node_modules ] || pnpm install` (should already exist — skip). Read this doc in full, then
read plan lines 1611-1691 (Task 4 Steps 5-6), finish Task 4, commit, then proceed to Task 5 via
`coordinated-build`. Remember: 0162 not 0161; `ownsTables` is `string[]` not `boolean` (see Done
section above) — don't re-derive that discrepancy from the plan's Task 1 prose again.

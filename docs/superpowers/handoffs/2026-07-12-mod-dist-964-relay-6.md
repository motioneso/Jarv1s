# mod-dist-964 relay 6

Issue: #964 (module distribution & install, epic #860). Security risk tier.
Coordinator label: **Coordinator** — re-resolve pane fresh via `herdr pane list`
before messaging (never trust a baked-in pane number).

Plan: `docs/superpowers/plans/2026-07-12-module-distribution-install.md`.
Worktree/branch: this worktree, branch `mod-dist-964`.

## Standing constraints (still in force)

- Migration 0162 already applied — **do not touch**.
- Explicit-path staging only (`git add <paths>`, never `-A`/`.`).
- Never touch `docs/coordination/` or the GitHub board/issues — Coordinator-owns those.
- No secrets ever logged/returned. Never edit an applied migration.
- `@jarv1s/settings` must not import `@jarv1s/module-registry` (structural mirror types only).
- 1000-line file-size gate on every source file (incl. CSS).
- Fastify response schemas need every emitted field declared explicitly (silent drop otherwise).
- Relay again at the 70% context-meter warning or immediately on seeing a compaction summary.
- Read the plan **by section** for your current task only — never front-to-back.

## Task 6 status: Steps 1-3 done (uncommitted by design), Steps 4-10 remain

Task 6 boundary: plan lines 2488-3609 (`grep -n "^### Task" docs/superpowers/plans/2026-07-12-module-distribution-install.md`
to reconfirm — Task7=3609 if unmoved).

**Deliberately uncommitted** (Step 10 bundles ALL Task 6 files into one commit — see plan
line 3600-3605 for the exact `git add`/message). Current tree has only these 2 diffs, both
verified correct, do NOT redo:

- `packages/shared/src/platform-api-modules.ts` — Step 1 code appended verbatim (plan lines
  2534-2726). File is now 575 lines (was 384). No further action needed on this file.
- `tests/unit/module-registry-rows.test.ts` — NEW, Step 2 code verbatim (plan lines 2732-2940).
  Confirmed FAILING as expected: `pnpm vitest run tests/unit/module-registry-rows.test.ts` →
  `Cannot find module '.../module-registry-rows.js'` (module doesn't exist yet — correct
  pre-Step-4 state, Step 3 satisfied).

## All Task 6 premises pre-verified against this branch (do NOT re-verify, just build)

- `packages/settings/src/repository-external-modules.ts` (348 lines): Task 3's
  `ExternalModuleAdminState`/`listExternalModuleAdminStates`/`updateExternalModuleStaging`/
  `setExternalModulePurgeRequested` all present, matching plan signatures exactly.
  `markExternalModuleRemoved` absent — Step 6(a) plan code (lines 3088-3132) is correct
  verbatim, `updated_at: new Date()` idiom matches file convention.
- `packages/settings/src/routes.ts` (892 lines): `ExternalModulesDependencies` interface at
  L132-150 (plan assumed ~L131-150, exact match). `SettingsRoutesDependencies` at L152.
  `registerModuleRoutes(server, { dependencies, repository, assertAdminUser, requireRequestId })`
  call site at **L806** (exact match). Import block for `registerModuleRoutes` at **L84**
  (exact match, `from "./routes-modules.js"`). `assertAdminUser` exported at L821, signature
  `(repository, scopedDb, userId) => Promise<User>` matches. `requireRequestId` at L865,
  `(accessContext: AccessContext) => string`. Step 6(b) plan code (lines 3142-3194) is
  accurate as written — paste verbatim at the cited locations.
- `packages/settings/src/routes-modules.ts` (320 lines): `ModuleRoutesContext` exported at
  L33, shape exactly `{ dependencies, repository, assertAdminUser, requireRequestId }` —
  matches plan assumption, no cast/adjustment needed.
- `@jarv1s/module-sdk/core-version` (`packages/module-sdk/src/core-version.ts`):
  `compareJarvisVersions(a, b)` (L66) and `satisfiesCoreVersion(range, version = CORE_VERSION)`
  (L81, single-arg call works via default) both match Step 4's usage verbatim.
- **CORRECTION vs. plan text for Step 7** (`packages/settings/src/route-error.ts`, 24 lines):
  this file exports ONLY `handleSettingsRouteError` — it does NOT export `HttpError` or
  `handleRouteError`. The plan's Step 7 import line
  `import { HttpError, handleRouteError } from "./route-error.js";` is WRONG and will fail
  typecheck. Use the same pattern `routes-modules.ts` already uses (verified at its L25, L30):
  ```ts
  import { HttpError } from "@jarv1s/module-sdk";
  import { handleRouteError } from "./routes-serializers.js";
  ```
  Every other line of the plan's Step 7 code block (lines 3198-3469) is correct verbatim —
  only this one import statement needs the substitution above.
- `apps/api/src/server.ts` (996 lines): module-registry/node import block at **L53-56**
  (`ExternalModuleWorkerRuntime`, `getExternalModuleRegistrations`) — extend in place per
  Step 8(a). `externalRuntimeEnabled` at **L199-200**. `apiServerConfig.externalModulesDir`
  confirmed (`ApiServerConfig.externalModulesDir: string | null`, L123). No existing
  `node:fs/promises` or `node:path` import anywhere in the file — add both fresh as the plan
  says. **Correction vs. plan text:** there is no function literally named
  `registerSettingsRoutes` called in server.ts — the actual call is
  `registerBuiltInApiRoutes(server, {...})` at **L478**, and the `externalModules:` key the
  plan points at is at **L525-530** inside that same call's dependencies object (exact line
  match) — add `moduleDistribution,` there, same object. `options.fetchFn` is already
  threaded through this call (L542) for the download/fetchRegistryEntries closures.
  `fetchRegistryIndex`, `downloadAndStageModule`, `ModuleDownloadError`, `parseModulesEnsure`
  all confirmed exported (transitively via `packages/module-registry/src/node.ts`'s
  `export * from "./distribution/{pipeline,ensure-list,registry-source}.js"`).
- `packages/settings/src/index.ts` (35 lines): re-export pattern confirmed — mix of
  `export * from "./x.js"` and named `export { ... } from "./x.js"` blocks. Add
  `module-registry-rows.js`'s `ModuleRegistryEntryLike` (and `deriveModuleRegistryRows` /
  `ModuleRegistryDeriveInput` if needed elsewhere) following the existing named-export style
  (see the `notes-source-routes.js` block at L24-30 for the pattern).

## Next steps (resume at Step 4)

1. Step 4: implement `packages/settings/src/module-registry-rows.ts` (plan lines 2952-3072,
   verbatim — no corrections needed).
2. Step 5: `pnpm vitest run tests/unit/module-registry-rows.test.ts` → expect 14/14 PASS.
3. Step 6(a): append `markExternalModuleRemoved` to `repository-external-modules.ts` (plan
   lines 3088-3132, verbatim).
4. Step 6(b): add `ModuleDistributionDependencies` port + `SettingsRoutesDependencies` field
   + registration call to `routes.ts` (plan lines 3142-3194, verbatim; line numbers
   pre-verified above — L132-150 interface block, L806 call site, L84 import).
5. Step 7: implement `packages/settings/src/routes-module-registry.ts` (plan lines
   3198-3469) — **apply the HttpError/handleRouteError import correction above**, everything
   else verbatim.
6. Step 8: wire the port in `apps/api/src/server.ts` (plan lines 3476-3585) — line numbers
   pre-verified above (import block L53-56, `externalRuntimeEnabled` L199-200,
   dependencies object L525-530); add `node:fs/promises` (`readdir`, `rm`) and `node:path`
   (`join`) imports fresh, plus the path-safety guard in `removeModuleFiles` (plan line
   3572-3575). Export `ModuleRegistryEntryLike` from `packages/settings/src/index.ts` first
   (see index.ts note above) so server.ts's `import type { ModuleRegistryEntryLike } from
   "@jarv1s/settings";` resolves.
7. Step 9: gates —
   ```bash
   pnpm --filter @jarv1s/shared typecheck
   pnpm --filter @jarv1s/settings typecheck
   pnpm --filter @jarv1s/api typecheck
   pnpm vitest run tests/unit/module-registry-rows.test.ts
   pnpm check:package-deps
   ```
   All must pass. `check:package-deps` proves settings still has zero `module-registry` dep.
8. Step 10: commit exactly the plan's line-3603 file list (includes the 2 files already
   sitting uncommitted from this relay) with the given message
   (`feat(modules): admin module-registry surface — list/download/remove/cancel-purge routes (#964)`).
9. Continue to **Task 7** onward (plan lines 3609+): boot reconcile, boot/compose wiring,
   admin web UI, integration suite, full gates. Read each task's section fresh, verify
   premises before coding — don't assume anything carries over from Task 6's verification.
10. After the final task's gates are green: invoke `coordinated-wrap-up` → open PR ("Part of
    #964") → report to Coordinator with verified evidence. Never merge, move the board, or
    close issues — Coordinator-only.

## Commits so far (Tasks 1-5 done; Task 6 in progress, uncommitted WIP described above)

`b4af4976`, `02dc9f5c`, `a56a644b`, `c0c55a8d`, `8a784b60`, `b21bdda2`, `1394d3fe`, `c13aeb4d`.

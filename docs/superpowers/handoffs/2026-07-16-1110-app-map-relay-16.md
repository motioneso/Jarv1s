# #1110 app-map relay — checkpoint 16

Worktree: `~/Jarv1s/.claude/worktrees/build-1110-app-map`, branch `build/1110-app-map`.
Coordinator label: `Coord-1109-1110-g3`, session `cb9ca6a3-11c9-40b6-944f-6bf3713e8536`.
**Never merge/board/close** — report PR URL only.

## State: code is DONE, committed, rebased. Only `coordinated-wrap-up`'s final gate + PR + report remain.

- HEAD is `30284c28` (`fix(uat): correct app-map grounding seed so hasJsonModel() is genuinely
  false`), on top of `1d825066` (`docs(handoff): ...checkpoint 15`), on top of freshly-rebased
  `origin/main` (`65b8a7f8`, PR #1117) — rebase was clean, no conflicts. **29 commits ahead of
  origin/main.** Not yet pushed.
- Pre-push trio (`format:check && lint && typecheck`) verified green **twice**: once before
  rebase, once after. Full `test:unit` (418/419, 1 pre-existing flagged failure),
  `test:uat-seed` (11/12, 2 pre-existing flagged failures), `test:integration` (156/156, all
  green) all independently verified this relay hop (checkpoint 15's doc has full detail — read it
  for the vitest.config.ts root-cause story, don't re-derive).
- Working tree has exactly one harmless dirty file: `.claude/context-meter.log` (session-telemetry
  noise, intentionally excluded from every commit this relay chain — leave it alone, don't stage it).

## What's left (mechanical — just execute `coordinated-wrap-up`'s remaining steps)

I invoked the `coordinated-wrap-up` skill and got through step 1 (clean tree confirmed). Was
mid-step-2 (`pnpm verify:foundation` as ONE full command, real exit code) when context hit 70%
again. **Background task in flight**: `pnpm verify:foundation` was launched writing to
`/tmp/claude-1000/.../scratchpad/cb-vf.log` (task id `b2p2gyes4` — that tmp path is
session-specific and will be GONE in a fresh session; just relaunch the command fresh, don't try
to find the old log).

1. Run `pnpm verify:foundation > <scratchpad>/cb-vf.log 2>&1; echo "VF_EXIT=$?" >>
   <scratchpad>/cb-vf.log` (own scratchpad, real exit code, don't pipe to tail/grep as final
   stage). Given everything already passed piecewise this hop, expect `VF_EXIT=0` — if not, stop
   and diagnose (`superpowers:systematic-debugging`) before proceeding, don't just re-report.
2. Run `pnpm audit:release-hardening > <scratchpad>/cb-audit.log 2>&1; echo "AUDIT_EXIT=$?" >>
   ...` — **not yet run this relay chain at all**, first time. Check its result for real.
3. If both exit 0: `git push -u origin build/1110-app-map`, then `gh pr create --base main --head
   build/1110-app-map` with title like `fix(uat): #1110 app-map grounding seed correctness` and a
   body covering (see checkpoint-15 doc for full detail, don't re-derive):
   - the seed fix + why (bindNews:false must seed zero providers, not just unbind one — the
     implicit-default-provider fallback in `resolveModelForService`/`resolveDefaultProviderId`
     defeats a merely-unbound provider)
   - the UI-gate-vs-backend-gate test rewrite (tests 1-2 now hit `page.request.post()` directly
     since the corrected seed legitimately hides the add-source-by-URL form)
   - the vitest.config.ts alias fix (required unblock, not scope creep) — cite 34457186
   - two flagged pre-existing gaps needing their own follow-up issues: (a)
     `module-web-browser-safety.test.ts` false-positive on type-only re-exports (see
     `packages/shared/src/index.ts:5`), (b) `guard.test.ts` shared-dev-DB non-ephemeral-state
     failure (#1087)
   - VF_EXIT/AUDIT_EXIT evidence
4. Report to coordinator via `herdr-pane-message` (caveman mode, terse) — label
   `Coord-1109-1110-g3`: PR link, VF_EXIT/AUDIT_EXIT, branch+sha, deferred items (the two flagged
   gaps, recommend new issues). **Then stop.** Do not merge/board/close.

## Durable memory already saved this relay chain (don't re-save)

Two `memory_save` calls done (project `jarv1s`): the vitest.config.ts subpath-alias-pairing
invariant, and the module-web-browser-safety.test.ts type-only-import blind spot. Both searchable
via `memory_smart_search` if needed — don't re-derive from scratch.

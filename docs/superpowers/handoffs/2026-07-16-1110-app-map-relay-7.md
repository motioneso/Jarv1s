# #1110 app-map — relay checkpoint 7

Spec: `docs/superpowers/specs/` (app-map). Plan: `docs/superpowers/plans/2026-07-16-1110-app-map-plan.md`.
Branch/worktree: `build/1110-app-map` at `/home/ben/Jarv1s/.claude/worktrees/build-1110-app-map` (shared —
check `herdr pane list` before tree-wide git actions; predecessor pane from checkpoint 6 already
closed cleanly, no collision seen this cycle).
Coordinator label: **`Coord-1109-1110-g3`** — confirm exactly one pane holds it before messaging
(`herdr pane list`).

## Done and committed

- Tasks 1-5: done (see relay checkpoint 6, commit `5fc87016` etc.)
- **Task 6 (this session): DONE, green, committed** at `a274d98d` — "feat(news): expose structured
  add-source errors". `NewsSourcePreviewResponse.error?: JarvisError` in
  `packages/shared/src/news-api.ts` (+ JSON schema `error` object, `additionalProperties:false`);
  `personalization-routes.ts` preview route returns `error` envelopes for `no_json_model`
  (prerequisite, with remediationRef) and `discovery_unavailable` (transient, no remediationRef);
  `add-source.tsx` exports `NewsAddSourceError` (structured DOM attrs
  `data-jarvis-error-{code,class,remediation-ref}`) and `previewOutcomeMessage` no longer guesses
  settings copy for `status:"unavailable"` (returns null; caller renders `NewsAddSourceError` from
  `preview.error` instead). Full vitest green:
  `pnpm vitest run tests/unit/news-routes.test.ts tests/unit/news-settings-pane.test.tsx` → 2
  files / 58 tests passed. `pnpm --filter @jarv1s/shared typecheck` and
  `--filter @jarv1s/news typecheck` both clean.

**Nothing else is pending or uncommitted right now** — tree is clean except
`.claude/context-meter.log` (never add that; it's noise, not code).

## Next: Task 7 (plan lines 1698-1775)

Read the plan **by section** (lines 1698-1775 only) fresh. Handoff checkpoint 5 already verified
this task's premises clean — **do not re-verify from scratch**, but note one wrinkle discovered
this session:

- `packages/settings/src/app-map.ts`'s `createAppMapReadService` **already has a wired `logGap`
  dep** (`deps.logGap({ kind, value: raw.slice(0, 120) })` at ~line 91, from Task 5). The plan's
  Step 1 test snippet ("logs an undeclared query as a coverage gap") may **already pass** —
  confirm with a RED run first; if it's already green, that's not a bug, just don't re-derive work
  Task 5 already did. The real remaining work in Task 7 is the **persona** half: current
  `DEFAULT_JARVIS_PERSONA` in `packages/chat/src/live/runtime.ts:61-70` still has the OLD ambient
  `notes.search` / `connectors.startGoogleGuidance` copy — that part is genuinely still TODO, per
  plan Step 3's replacement text (closed-world grounding rules, `app.getMapSlice` /
  `chat.getCurrentView`, "I don't know from the current app map", non-prerequisite honesty rule).
  Byte-stable — don't add dates/versions/module lists per plan Step 3 note.
- After implementing, run `pnpm check:no-ambient-dates` per plan Step 4 (not just vitest).

## Task 8 (plan lines 1775-end)

Needs just-in-time verify before coding (per checkpoint 6, still unverified):
- `tests/uat/seed/{types.ts,chunks/ai.ts,levels.ts,cli.ts}`
- `tests/uat/provisioner.ts`, `tests/uat/run-uat.ts`
- `packages/news/src/routes.ts` + `personalization-routes.ts` `previewOverride` insertion points
- `docs/WHATS_NEW.md` top (release-note entry)

## After all 8 tasks green

1. `pnpm verify:foundation`
2. Pre-push trio: `pnpm format:check && pnpm lint && pnpm typecheck`
3. `git fetch origin main && git rebase origin/main`
4. `coordinated-wrap-up` skill: PR + report to `Coord-1109-1110-g3` (resolve the label fresh).
   **Never merge/board/close** — coordinator's job only.

## Process reminders (don't relitigate)

- Read plan **by section** for the task at hand only — never front-to-back.
- Strict TDD: RED → confirm failure → implement → GREEN → commit with **exact** `git add <files>`
  (never `-A`; `.claude/context-meter.log` will otherwise sneak into every commit).
- Relay on the **context-meter 70% warning** or immediately on seeing a compaction summary.
- `node_modules` already exists in this worktree — **never** `pnpm install` on relay resume.

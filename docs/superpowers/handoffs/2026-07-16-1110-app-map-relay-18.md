# #1110 app-map relay — checkpoint 18

Worktree: `~/Jarv1s/.claude/worktrees/build-1110-app-map`, branch `build/1110-app-map`.
Coordinator: label `Coord-1109-1110-g5`, pane `w1:pSX` as of this checkpoint (re-resolve fresh —
labels/panes drift constantly; session `435f7c1a-1c09-493b-b091-af1cf10919f0`, cwd
`coord-2026-06-30-rfa-fleet`). **Never merge/board/close** — report only.

## DONE this relay hop — compose-smoke fix, pushed, confirmed working

Root-caused + fixed PR #1122's "Compose deployment smoke" CI failure. Root cause:
`apps/api/package.json`'s `"start"` script never ran `build:app-map` (unlike `"dev"`), so on a
fresh CI checkout `packages/module-registry/src/index.ts`'s `registerBuiltInApiRoutes` threw
`ENOENT` on `dist/app-map.json` at boot via `packages/settings/src/app-map.ts`'s uncaught
`loadAppMap()`. Fix: 1-line change, `"start": "pnpm --dir ../.. build:app-map && tsx
src/server.ts"` (mirrors `"dev"`'s existing pattern exactly). **Verified empirically**: moved
`dist/app-map.json` aside, repro'd the exact `ENOENT` crash matching CI, applied fix, repro'd
again — `GET /health` now returns `200`. Committed `8e30e1da` (fix) + `8e35e67f` (this doc's
predecessor, checkpoint-17), pushed to `build/1110-app-map`. Coordinator kicked a new CI run
(29559158348) on `8e35e67f` and is monitoring it separately — **not my job to watch that one**.

## IN PROGRESS — second, separate VF regression (do NOT re-derive the above)

Coordinator (message just received, verbatim preserved below since it has exact IDs/log
excerpts worth keeping) reports the **other** red check, "Verify foundation and app", is a
**genuine NEW regression**, not the `packages/shared/src/index.ts:5` type-only false positive
flagged as "pre-existing" in checkpoint-15. That checkpoint-15 conclusion was **wrong** — it
only compared against this branch's own bare HEAD before Task 8 (commit `9db4ec93`, which
already included earlier relay commits like `34457186`), not against actual `origin/main`.
Coordinator did the correct comparison and proved it:

> Job 87815542433 log: module-web-browser-safety.test.ts fails — module "news": reaches
> packages/module-sdk/src/route-errors.ts which imports backend-only package "fastify" (plus 3
> more offenders, only first shown in the assertion diff — full set per checkpoint-15/VF log:
> `logger.ts` "fastify", `rate-limit-key.ts` "node:crypto"+"fastify", `route-errors.ts`
> "fastify").
>
> Proof it's new: main's latest green run (29554454835, job 87803601966) passes this test 3/3
> clean, no news/fastify/route-errors mention.
>
> No `modules/news/` files changed in this PR's diff at all — indirect. The two `apps/web/src/`
> files importing `@jarv1s/module-sdk` are `apps/web/src/chat/use-chat-stream.ts` and
> `apps/web/src/settings/settings-admin-panes.tsx`. Coordinator looked at the
> `AI_MODEL_CAPABILITIES`-move commit (`34457186`) — new exports there are type-only
> interfaces, didn't look like the direct cause on inspection, but didn't chase further.
> **Explicit instruction: verify with an actual import-graph trace, don't assume either way.**

## Investigation state — where I was, mid Phase 1

Read `tests/unit/module-web-browser-safety.test.ts` in full (136 lines). Key mechanics of its
`walkImportGraph`/`resolveSpecifierToFile`:
- For a **bare** `@jarv1s/*` specifier (no subpath, e.g. `import type {...} from
  "@jarv1s/module-sdk"`), it resolves ONLY via that package's `package.json`
  `exports["."]` entry — i.e. always the barrel `packages/module-sdk/src/index.ts` — then
  walks EVERY specifier found in that barrel file, **including `export type {...} from
  "./x.js"` re-exports**, with no distinction between type-only and runtime imports (the regex
  `IMPORT_RE` matches both `import` and `export` keywords identically). This is the known
  blind spot from checkpoint-15 (`packages/shared/src/index.ts:5`:
  `export type { JarvisError, JarvisErrorClass } from "@jarv1s/module-sdk"`) — but per the
  coordinator's proof, SOMETHING changed to make this reachable from news's `./web` graph
  specifically, when it wasn't reachable on `origin/main`.
- The three-message-scoped test (`it("never reaches ... from any discovered ./web
  contribution")`) iterates every module discovered by `scanModuleWeb`, resolves each
  module's `package.json` `exports["./web"]` entry, and walks from there.

**Last action before this checkpoint**: confirmed via `git show origin/main:packages/shared/src/index.ts`
that this file EXISTS on `origin/main` with a long list of `export * from "./*-api.js"` lines
(schema-fragments, ai-api, ai-service-binding-api, ai-voice-api, ai-audit-api, ai-summary-api,
briefings-api, calendar-*, chat-*, connectors-api, email-*, notifications-api, ... — truncated
at 20 lines read). **Had NOT yet confirmed**: (a) whether line 5's specific `export type {
JarvisError, JarvisErrorClass } from "@jarv1s/module-sdk"` line exists verbatim on
`origin/main` too (need `git show origin/main:packages/shared/src/index.ts | grep -n
"module-sdk"` — not yet run), (b) if it does exist on main, why the walker doesn't currently
flag it there (maybe `@jarv1s/module-sdk`'s barrel on `origin/main` doesn't yet re-export
`route-errors.js`/`logger.js`/`rate-limit-key.js` at all — i.e. **this PR's diff to
`packages/module-sdk/src/index.ts` is what newly re-exports those files from the barrel**,
which would make THIS PR's `module-sdk` diff the actual root cause, not `packages/shared`).

## Most likely hypothesis (NOT YET CONFIRMED — verify before fixing)

`packages/module-sdk/src/index.ts`'s diff (part of `34457186`, "stop leaking node:crypto into
the web bundle via AI_MODEL_CAPABILITIES") likely **reorganized the barrel's exports** — e.g.
adding fresh `export type {...} from "./route-errors.js"` / `./logger.js` /
`./rate-limit-key.js"` lines (for the new `JarvisErrorClass`/`JarvisError`/
`ModuleErrorManifest` etc. manifest interfaces mentioned in the original summary) that did NOT
exist on `origin/main`'s version of that barrel. If so: the walker's blind spot
(type-only-vs-runtime) is unchanged, but the barrel now has NEW type-only re-export lines
pointing at previously-unreachable backend files, making them newly "reachable" per the
walker's (flawed but pre-existing) logic. **This would mean the actual regression is in
`packages/module-sdk/src/index.ts`'s diff, not `packages/shared`** — need to `git diff
origin/main -- packages/module-sdk/src/index.ts` and check exactly what re-export lines are
new vs. present on main already.

## Next steps (in order)

1. `git show origin/main:packages/shared/src/index.ts | grep -n module-sdk` — confirm whether
   line 5 exists on main verbatim.
2. `git diff origin/main -- packages/module-sdk/src/index.ts` — full diff, look specifically
   for new `export type {...} from "./route-errors.js"` / `"./logger.js"` /
   `"./rate-limit-key.js"` lines that aren't on main.
3. If confirmed: that's the root cause. Decide fix — likely candidates: (a) move the
   newly-exported type-only interfaces to their own leaf subpath file (mirroring the
   `ai-capabilities.ts` pattern from `34457186`) so the barrel doesn't need to re-export
   `route-errors.ts`/`logger.ts`/`rate-limit-key.ts` at all, OR (b) if those files are
   ALREADY re-exported from the barrel on `main` (i.e. hypothesis is wrong), keep digging —
   check `apps/web/src/chat/use-chat-stream.ts` and
   `apps/web/src/settings/settings-admin-panes.tsx` (the two web files importing
   `@jarv1s/module-sdk`) for exactly what they import, and whether a **subpath** vs **bare**
   import distinction matters here (bare `@jarv1s/module-sdk` pulls the whole barrel per the
   walker's `resolveSpecifierToFile`; a subpath import like `@jarv1s/module-sdk/ai-capabilities`
   would NOT resolve via `exports["."]` and wouldn't even be walked by this test's logic —
   confirm which form these two files actually use).
4. Once root cause is nailed down (Phase 3: state hypothesis explicitly), fix, verify via
   `pnpm exec vitest run tests/unit/module-web-browser-safety.test.ts`, commit (explicit path
   list, not `git add -A`), push to `build/1110-app-map` (same PR #1122).
5. Report back to coordinator (re-resolve pane fresh) — terse. Do NOT touch the compose-smoke
   CI run the coordinator is separately watching; do NOT merge/board/close.

## Do NOT re-derive

- compose-smoke fix (done, see above) — don't re-investigate.
- Original "known pre-existing" framing of this exact test failure from checkpoints 15/16 —
  **that framing is now proven wrong** by the coordinator's origin/main comparison. Don't cite
  it as pre-existing in the eventual PR body/report; this is a real regression this PR
  introduced and must be fixed, not just flagged.

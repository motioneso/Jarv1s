# PR #1058 QA remediation handoff

## Assignment

- PR: #1058, branch `ux/987-notes-people-implementation`
- Worktree: `~/Jarv1s/.claude/worktrees/ux-987-notes-people-implementation`
- Risk tier: sensitive, user-facing UI
- Coordinator: exact label `UX Coordinator`, session
  `019f6226-78b2-7c31-9a84-f01d3c85eb0c`
- Ben explicitly reopened the stopped lane on 2026-07-14 and requested a Sol-high agent.

## Scope

Fix every blocking finding in the durable QA cycle 2 verdict:
`https://github.com/motioneso/Jarv1s/pull/1058#issuecomment-4973456840`.
Tracking issue: `https://github.com/motioneso/Jarv1s/issues/1060`.

Ground the fixes against these approved documents by relevant section only:

- `docs/superpowers/specs/2026-07-12-notes-people-source-picker-hardening.md`
- `docs/superpowers/plans/2026-07-13-notes-people-source-picker-hardening.md`

Keep the diff limited to the five blockers and the smallest tests that prove them. Preserve
DataContextDb/VaultContext, metadata-only payload, module-isolation, and non-leaking error
invariants. Do not begin #995 or touch parked PR #1050.

## Guardrails

- Invoke `coordinated-build`; use `coordinated-wrap-up` when fixes are ready.
- Do not touch `docs/coordination/`, merge, close issues, or move project items.
- Do not run repo-wide formatting or use broad `git add`; stage only owned files.
- Do not change product behavior beyond what the QA findings and approved spec require.
- A short remediation plan is required before code. Send its pointer to exact label
  `UX Coordinator` for approval, then wait.
- Final handoff must include pushed exact head and check evidence. Coordinator owns fresh QA and
  live Webwright UAT; the previous green CI and blocked UAT do not transfer to the new head.

## Start

1. Confirm the branch and clean worktree; install dependencies only if missing.
2. Read this handoff in full, then read the QA verdict and only relevant spec/plan sections.
3. Verify each finding against current code and write the short remediation plan.
4. Message the exact coordinator label with the plan pointer and wait for approval.

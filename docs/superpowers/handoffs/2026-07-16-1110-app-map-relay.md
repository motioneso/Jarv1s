# Relay — #1110 app-map build, mid step-½ verification

**Coordinator label:** `Coord-1109-1110`
**Worktree/branch:** this worktree, `build/1110-app-map` (unchanged, no `pnpm install` needed)
**Spec:** `docs/superpowers/specs/2026-07-16-1110-app-map-design.md`
**Plan (approved, do NOT re-plan):** `docs/superpowers/plans/2026-07-16-1110-app-map-plan.md`
**Original handoff:** `docs/superpowers/handoffs/2026-07-16-1110-app-map-build.md`

## State

No code written yet. Still in step-½ (verify plan premises against branch) per `coordinated-build`.
**No drift found — every task checked so far is confirmed buildable exactly as the plan says.**
No escalation sent.

### Verified clean (Tasks 1–3, fully; Task 4/5/7, mostly)
- Task 1: module-sdk lacks all new types/fields; `shared/ai-types.ts` defines tier/capability
  locally (needs re-export); `shared/package.json` lacks `@jarv1s/module-sdk` dep; `shared/src/index.ts`
  has no `JarvisError` export.
- Task 2: `assertModuleRegistryConsistency` exists in `packages/module-registry/src/index.ts`
  (now ~line 1666, drifted from plan's ~1576-1660 — expected, not a break); lacks description/error
  validation, confirming plan's "add near" instruction.
- Task 3: `hasJsonModel` closure at module-registry `~1474-1481` hardcodes `"module.news"` /
  `"json"` / `"economy"` exactly as plan describes; `packages/news/src/manifest.ts` has no `features`
  array, no `description` fields on nav/settings entries; `packages/news/src/index.ts` doesn't
  export `newsAddSourceRequirement` yet.
- Task 4: none of `packages/shared/src/app-map-core.ts`, `scripts/build-app-map.ts` exist (create).
  `apps/web/src/app-route-metadata.ts`: `todayNavEntry` exists (line 33), also consumed by
  `shell/command-palette-model.ts` — plan's derivation must account for that second usage.
  `apps/web/src/settings/settings-page.tsx`: `SettingsSection<Id>` interface (line 45),
  `PERSONAL_SECTIONS`/`ADMIN_SECTIONS` built via `flattenSettingsGroups` (lines 152/181) — matches
  plan. Root `package.json` scripts block confirmed (build:api = `tsx scripts/build-app.ts api`,
  line 39). `apps/api/package.json` dev = `tsx watch src/server.ts`. `scripts/build-app.ts` has
  `buildTarget()` at line 76 — insertion point plan describes should still apply (not yet checked
  in full detail, just existence/shape).
- Task 5: `packages/settings/src/app-map.ts` and `app-map-tool.ts` don't exist (create).
  `packages/settings/src/index.ts` has no app-map export yet. **Finding: `packages/chat/package.json`
  does NOT yet depend on `@jarv1s/settings`** — Task 5 must add that workspace dep (plan may already
  say this; re-check plan text when building). `packages/chat/src/routes.ts`: `ChatRoutesDependencies`
  interface at line 99, `collaborators` object built ~line 691, `toolServices`/`readToolServices`
  built ~lines 723-731 — no existing `appMapService` reference (confirmed absent, safe to add
  top-level per DI-seam contract). `packages/ai/src/routes.ts`: `readToolServices?: ToolServices` at
  line 120, `dependencies.readToolServices ?? {}` at line 698.
- Task 7: `DEFAULT_JARVIS_PERSONA` in `packages/chat/src/live/runtime.ts` lines 61-66 currently
  contains `notes.search` guidance and Google-connector guidance — confirms plan's failing-test
  premise that ambient app facts need stripping.

### NOT yet verified — check when build reaches these tasks (don't front-load)
- **Task 6**: `packages/shared/src/news-api.ts` `NewsSourcePreviewResponse` shape +
  `previewNewsSourceSchema`; `packages/news/src/personalization-routes.ts` no-model/discovery-
  unavailable branches; `packages/news/src/settings/add-source.tsx` (plan claims plain `alert()`,
  no `ClosedWriteGate` — re-verify before touching).
- **Task 8**: `tests/uat/seed/{types.ts,chunks/ai.ts,levels.ts,cli.ts}`, `tests/uat/provisioner.ts`,
  `tests/uat/run-uat.ts`, `packages/news/src/routes.ts` + `personalization-routes.ts`
  `previewOverride` insertion points, `docs/WHATS_NEW.md` top content.

## Next step for successor

1. `[ -d node_modules ] || pnpm install` (should already exist — skip).
2. Do NOT re-read the plan front-to-back. Read plan **Task 1 section only**, confirm against
   findings above (already fresh), then **start the TDD build at Task 1** immediately —
   verification for Tasks 1–5/7 is done; don't re-verify.
3. Build task-by-task per `coordinated-build` step 2 (TDD, green commit per task, exact file
   `git add`). Verify Task 6 / Task 8 premises just-in-time, right before starting those tasks.
4. Self-monitor context; relay again on the next 70% warning or compaction summary.
5. Escalate to `Coord-1109-1110` (confirm exactly one pane holds that label via `herdr pane list`
   first) only on real blockers/drift — none found yet.
6. Closeout is `coordinated-wrap-up` (PR + report) once all 8 tasks are green and
   `pnpm verify:foundation` passes — not merge/board/close.

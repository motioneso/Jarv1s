# Handoff — JS-00 Surface-scoped chat sessions (task #1231)

**You are the JS-00 build worker.** Model: gpt-5.6-luna @ xhigh. Build **only Phase JS-00**.

## Authoritative refs (read in full first)
- Plan: `docs/superpowers/plans/2026-07-22-job-search.md` → **Phase JS-00** (scope, tasks,
  invariants, tests, and the `⛔ UAT GATE JS-00 (Ben)` checklist). This is your contract.
- Spec: `docs/superpowers/specs/2026-07-22-job-search.md` (context only).
- GitHub task: **#1231** (Part of epic #1230).

## What JS-00 is
Live chat sessions are keyed by bare `actorUserId` at
`packages/chat/src/live/chat-session-manager.ts:247` (~7 actor-keyed maps) → the drawer and any
module surface share one conversation and bleed. Fix: key sessions by **`actor:surface`** and
thread a `surface` param (default `"drawer"`) through the turn / stream / seed routes in
`packages/chat/src/live-routes.ts`. Enumerate and convert every actor-keyed map identified in the
plan — miss one and the bleed persists.

## Guardrails (blockers if violated)
- **Never reference the prior job-search implementation.** It failed; it is not a reference.
- Backward-compatible: absent `surface` = `"drawer"`; existing drawer behavior unchanged.
- Host-platform change only in this phase — no module code yet.
- Respect all CLAUDE.md hard invariants.

## Exit criteria — then STOP
1. The **anti-bleed integration test** from the plan passes (drawer and a second surface are
   independent sessions; no transcript bleed either direction).
2. `pnpm verify:foundation` green on a **fresh gate DB** (DROP/CREATE first — see memory
   [[verify-foundation-fresh-gate-db]]).
3. **STOP at the JS-00 UAT gate. Do NOT start JS-01.** Post a comment on #1231 summarizing what
   changed + how Ben verifies it over LAN (per the plan's gate checklist). Ben UATs and records
   sign-off on #1231; the next phase does not begin until he clears it.

## Start
1. `pnpm install` (fresh worktree).
2. Read the plan's Phase JS-00 in full; write your task checklist.
3. Build with TDD — anti-bleed test first. Commit in small steps with issue-referencing messages.

# #1110 app-map — Task 8 relay checkpoint 11

Branch/worktree: `build/1110-app-map` at `/home/ben/Jarv1s/.claude/worktrees/build-1110-app-map`
(current, do NOT re-clone/re-worktree). `node_modules` present — do NOT `pnpm install` (a
lockfile-only regen already ran, see below; that's different and already done).
Coordinator label: `Coord-1109-1110-g3` — re-resolve fresh by label via `herdr pane list` (pane
numbers reflow); do not reuse any `w1:pSN` written here or in older relay docs.

Read **relay-9** (`2026-07-16-1110-app-map-relay-9.md`) for the exact Step 3A/3B code table and
the exact Step 5 commit command — still valid, no further exploration needed there.

## Done this session (uncommitted, working tree)

Task 8 Steps 3A + 3B + WHATS_NEW.md fully implemented per relay-9's table, verified against live
branch, `tsc --noEmit` clean, `eslint` clean, `prettier --write` applied. Files (all modified, not
yet committed — Step 5 commit is still blocked, see below):
`tests/uat/specs/app-map-grounding.uat.spec.ts` (new), `tests/uat/run-uat.ts`,
`tests/uat/provisioner.ts`, `tests/uat/seed/types.ts`, `tests/uat/seed/chunks/ai.ts`,
`tests/uat/seed/levels.ts`, `tests/uat/seed/cli.ts`, `packages/news/src/routes.ts`,
`packages/news/src/personalization-routes.ts`, `packages/module-registry/src/index.ts`,
`docs/WHATS_NEW.md`. `package.json` needed no change (confirmed).

## Two infra fixes already committed (pre-existing #1110 drift, unrelated to Task 8's own file list)

- `bcc74686` — `pnpm install --lockfile-only` (node_modules untouched) to fix
  `packages/shared`/`packages/chat` workspace deps (`@jarv1s/module-sdk`, `@jarv1s/settings`) added
  in earlier #1110 commit `2ffadbf1` without a matching lockfile regen — was failing
  `pnpm install --frozen-lockfile` inside the UAT Dockerfile build.
- `6c4ae260` — `.dockerignore`: `docs` → `docs/*` + `!docs/WHATS_NEW.md`. `scripts/build-app-map.ts`
  (from `568d6a55`) reads `docs/WHATS_NEW.md` at image-build time; blanket `docs` exclusion ENOENT'd
  every UAT docker build since that commit landed.

Both are real, already-landed-on-branch bugs unrelated to my Step 3A/3B edits — full root-cause in
agentmemory (`memory_smart_search "jarv1s uat docker build lockfile dockerignore"`).

## ACTIVE BLOCKER — not fixed, escalated, awaiting coordinator reply

Third infra issue, found after the two fixes above: `apps/web` vite build fails —
`"createHash" is not exported by "__vite-browser-external"` from `packages/module-sdk/src/rate-limit-key.ts`.

Root cause: `2ffadbf1` made `packages/shared/src/ai-types.ts` + `index.ts` re-export
`AI_MODEL_CAPABILITIES`/`AiModelCapability`/`AiModelTier`/`JarvisError`/`JarvisErrorClass` from
`@jarv1s/module-sdk`'s single barrel `index.ts`. That barrel also re-exports server-only code
(`route-errors.ts` uses fastify, `rate-limit-key.ts` imports `node:crypto`), and module-sdk's
`package.json` has no `"sideEffects": false`, so importing anything from `@jarv1s/module-sdk`
pulls the whole graph into the browser bundle. Violates the "shared is Vite-bundled, never node:*"
invariant, transitively. Full detail in agentmemory (`memory_smart_search "jarv1s module-sdk
browser bundle vite"`).

**I escalated to `Coord-1109-1110-g3`** with a recommended fix (split module-sdk's browser-safe
declarations — all self-contained inline in `index.ts`, zero node deps — into a new export subpath,
e.g. `@jarv1s/module-sdk/types`, and repoint `packages/shared`'s two files at that subpath instead
of the bare specifier). Coordinator was actively reading `ai-types.ts` (investigating) when I
relayed — **no reply captured yet**. First action: re-resolve the coordinator pane fresh by label
and check for a reply/decision before doing anything else. If no reply, it's fine to proceed with
the recommended fix (scoped, ~15 min, doesn't touch any other module-sdk consumer's import path)
and report back rather than block indefinitely.

## Next steps (in order)

1. Check coordinator reply on the module-sdk blocker (label `Coord-1109-1110-g3`, fresh-resolved).
2. Implement the module-sdk export split (or the coordinator's redirect), commit separately from
   Task 8's Step 5 commit (own message, e.g. `fix(module-sdk): split browser-safe type exports`).
3. Re-run `pnpm test:uat -- app-map-grounding` — must pass all 4 UAT behaviors (declared-prerequisite
   deep-link, unknown-app honest-unknown, transient-error no-invented-fix, non-admin privacy).
4. `pnpm verify:foundation`.
5. Step 5: single commit, exact file list from relay-9 (does NOT include `pnpm-lock.yaml` or
   `.dockerignore` — those are already committed separately above).
6. Pre-push trio (`format:check && lint && typecheck`) + `git fetch origin main && git rebase origin/main`.
7. `coordinated-wrap-up` skill → report to coordinator. Never merge/board/close directly.

## Side note (handled, no action needed)

Mid-session, `UX Coordinator` (unrelated codex session, PR #1117) asked to be notified when the
#1110 UAT docker stack tears down off shared subnet `10.254.0.0/24` — confirmed clean and
already messaged. No further action needed on that thread.

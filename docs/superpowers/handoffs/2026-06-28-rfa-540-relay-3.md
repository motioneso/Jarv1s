# Relay 3 — rfa-540-safe-automation-audit-log

**Relay reason:** context at 70%+ — relaying before compaction.
**Branch / worktree:** `rfa-540-safe-automation-audit-log` / `~/Jarv1s/.claude/worktrees/rfa-540-safe-automation-audit-log`
**Spec:** `docs/superpowers/specs/2026-06-28-safe-automation-audit-log.md`
**Plan:** `docs/superpowers/plans/2026-06-28-rfa-540-safe-automation-audit-log.md` ← READ IN FULL
**Coordinator label:** `Coordinator` (session id: `5e1a6b62-a480-4b5c-9706-e476cfe77044`, pane `w1:p59`)
**Migration slot:** placeholder `XXXX` in filename; coordinator assigns **0128** at merge.

## What's done (all committed to branch)

| Commit     | What                                                                                                                                                                                    |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `a82a816c` | Task 1: SQL migration `packages/ai/sql/XXXX_jarvis_action_audit_log.sql` — table, indexes, RLS, purge function                                                                          |
| `461dd177` | Task 2: DB types — `JarvisActionAuditLogTable`, `JarvisDatabase` entry, `JarvisActionAuditLog` Selectable                                                                               |
| `257e8500` | Task 3: Repository — `InsertAuditLogInput`, `ListAuditLogOptions`, `insertActionAuditLog`, `listActionAuditLog`, `purgeActionAuditLog`                                                  |
| `7aa98437` | Task 4: `packages/ai/src/jobs.ts` — `AI_PURGE_AUDIT_LOG_QUEUE`, `AI_QUEUE_DEFINITIONS`, `registerAiMaintenanceWorkers`; exported from `packages/ai/src/index.ts`                        |
| `0553751f` | Task 5: AI manifest (`XXXX` migration + `app.jarvis_action_audit_log` ownedTable) + module-registry (`AI_QUEUE_DEFINITIONS`, `registerWorkers`)                                         |
| `b8bab790` | Task 6: Gateway — `recordAudit` private method + 3 write sites (callTool auto-run, confirmAndRun denied/cancelled/timeout, confirmAndRun confirmed+ran)                                 |
| `2cde9413` | Task 7: Shared DTOs (`ActionAuditLogEntryDto`, `ListActionAuditLogResponse`, schemas), manifest route entry, `GET /api/ai/action-audit` handler + `serializeAuditLogEntry` in routes.ts |

**Migration applied to local `jarv1s` DB** — `pnpm db:migrate` ran clean.

## What's next (START HERE)

**STEP 1 — Node modules present, skip install.**

**STEP 2 — Resume via `coordinated-build` skill. Plan approved. Build Tasks 8–12.**

Task order:

### Task 8: Data export (`packages/settings/src/data-export.ts`)

Add `jarvisActionAuditLog` to `UserDataExportTables`, add `jarvisActionAuditLogQuery(userId)` function, add entry in `readExportTables`. Full plan detail at Plan Task 8.

Commit: `git add packages/settings/src/data-export.ts`

### Task 9: Frontend API client + query keys

- `apps/web/src/api/client.ts` — add `listActionAuditLog()` function
- `apps/web/src/api/query-keys.ts` — add `ai.actionAuditLog` key

Commit: `git add apps/web/src/api/client.ts apps/web/src/api/query-keys.ts`

### Task 10: Settings Activity pane

Create `apps/web/src/settings/settings-activity-pane.tsx` with `ActivityPane` component. Date range filter (today/7d/30d/90d), family filter, badges, empty state. No curved/rounded accent left-border. See full plan for the component code.

Commit: `git add apps/web/src/settings/settings-activity-pane.tsx`

### Task 11: Wire pane into settings-page

- `apps/web/src/settings/settings-page.tsx` — lazy-import `ActivityPane`, add `"activity"` to `PersonalSectionId`, add entry to `PERSONAL_SECTIONS` with `Activity` icon from lucide-react.

Commit: `git add apps/web/src/settings/settings-page.tsx`

### Task 12: Tests

- `tests/integration/foundation.test.ts` — add `{ version: "XXXX", name: "XXXX_jarvis_action_audit_log.sql" }` to migration list assertion
- `tests/integration/action-audit-log.test.ts` — **Create** (6 test cases: insert+read-back, RLS isolation, WITH CHECK violation, purge, no UPDATE/DELETE grant, CASCADE delete)
- Also: `tests/integration/data-export.test.ts` — add assertion for `jarvisActionAuditLog` key in export

**IMPORTANT for foundation.test.ts:** Check what version string the migration runner extracts from `XXXX_jarvis_action_audit_log.sql`. It likely extracts the prefix before `_`. Since the filename starts with `XXXX`, the version will be `XXXX`.

Gate command:

```bash
pnpm format:check && pnpm lint && pnpm typecheck
JARVIS_PGDATABASE=jarvis_build_540 pnpm test:integration
```

Create lane DB if not yet exists:

```bash
docker exec jarv1s-postgres psql -U postgres -c 'CREATE DATABASE jarvis_build_540;' 2>/dev/null || true
```

**STEP 3 — After all tasks green: `coordinated-wrap-up` → PR → report to Coordinator.**

## Key invariants (don't forget)

- Audit failure NEVER changes action result — try/catch + swallow in `recordAudit` (already wired)
- No UPDATE/DELETE granted to `jarvis_app_runtime` — SELECT+INSERT only
- Never `git add -A` — explicit file paths only
- Migration slot XXXX — coordinator assigns 0128 at merge; never hardcode 0128
- Do not touch: packages/chat, packages/shared/src/chat-api.ts, packages/people, packages/briefings

## Coordinator notes

- Coordinator approved plan at session start with 2 notes: (1) `purgeActionAuditLog` uses raw Kysely root DB — fine for SECURITY DEFINER; (2) keep XXXX placeholder until merge.
- WAITING: no new escalation needed — proceed directly to Tasks 8–12.

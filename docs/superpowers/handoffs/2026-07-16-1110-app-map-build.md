# Handoff — #1110 app-map build agent

**Date:** 2026-07-16
**Coordinator label (escalate here):** `Coord-1109-1110`
**Model:** Claude Sonnet 5
**Worktree:** `~/Jarv1s/.claude/worktrees/build-1110-app-map`
**Branch:** `build/1110-app-map` (off `origin/main` @ `1ca37063`; specs+plans cherry-picked)

## What you're building

"Jarvis knows every SCREEN + the SETTINGS behind it" — the app map. A build step harvests
enriched feature/screen/settings declarations into a queryable artifact, exposed to chat as a
`risk:"read"` gateway tool that returns sliced, capped, visibility-filtered map slices. This is
the **first half** of the "Jarvis knows Jarvis" pair; #1109 (runtime context) is coupled and
builds **after** you.

- **Spec:** `docs/superpowers/specs/2026-07-16-1110-app-map-design.md`
- **Approved plan (READ THIS, 8 tasks):** `docs/superpowers/plans/2026-07-16-1110-app-map-plan.md`

## Plan is ALREADY coordinator-approved — do NOT re-plan

The plan was written by a Codex planner, then reviewed and **hardened by the coordinator** (CRITICAL
DI-seam + feature-flag-filter fixes applied) and committed. **Treat it as the approved plan.** Skip
the stock `writing-plans` + plan-approval gate. Instead:

1. Do the coordinated-build **step ½ (verify spec against the actual branch)**: for each task's cited
   files/line numbers/"X doesn't exist" claims, grep/read on THIS branch and confirm still current.
   Your branch is fresh `origin/main` — plan line anchors were taken against a similar base, but
   verify. If any premise drifted, **escalate to `Coord-1109-1110` before building** with the re-scope.
2. Then build task-by-task with **TDD**, green + committed per task, `git add` only that task's files.

## Coupling with #1109 — keep the public surface STABLE

#1109 (runtime context) consumes YOUR output. Do not change these once landed without telling the
coordinator:

- **Canonical DI seam:** `ChatRoutesDependencies.appMapService` (top-level, optional
  `appMapService?: AppMapReadService`). **Never** move it under Chat `collaborators`, into
  `toolServices`, or into persona-based filtering. #1109 reads `dependencies.appMapService`.
- **`AppMapReadService` interface** including **`getBuildInfo(): AppBuildInfo`** — #1109's
  CurrentView tool calls `getBuildInfo()` for the version/buildId stamp.
- `AppMapArtifact` / `AppMapItem` concrete types.

You land the seam FIRST; the #1109 agent spawns once your seam is on a pushed PR branch.

## Guardrails (CLAUDE.md Hard Invariants — non-negotiable)

- **Visibility filter must fail CLOSED.** The plan's `resolveFeatureFlagState` resolves each surface
  item's `featureFlagId` against live flag state and **excludes** OFF items. Keep the flagged-OFF
  exclusion test. A no-op filter (the original `defaultEnabled !== false`) is a blocker.
- **No secrets in the artifact.** The app-map is derived from declarations/settings structure —
  never connector/AI credentials, tokens, or private content. Provider-agnostic AI.
- **RLS via `scopedDb`.** Read path takes a branded `DataContextDb`; no raw `fs` (use the workspace
  read). Repo-relative reads break in bundled prod api → use the **`import.meta.url` workspace-marker
  walk** (already in the plan; memory `bundled-path-resolution-trap`).
- **No migration expected** for #1110 (build artifact + read tool). If you find you need one, do
  **NOT** assume a number — escalate to the coordinator for landing-order assignment.

## Exit criteria

Spec **§8 UAT exit criteria** (#1000 Playwright harness, hard gate on a real dev instance) — a
seeded chat session asks an app-map question and Jarvis answers from the map (per memory
`e2e-dev-uat-for-ui-features`). Full local gate green (`pnpm verify:foundation`).

## Escalation + closeout

- **Escalate** blockers / spec drift / forks to `Coord-1109-1110` via the `herdr-pane-message`
  skill. Before messaging, run `herdr pane list` and confirm **exactly one** pane holds that label;
  if 0 or >1, halt and wait — do not guess.
- **Relay** on the context-meter 70% warning (use the `relay` skill; successor in THIS worktree).
- **Closeout = `coordinated-wrap-up`** (clean tree → your gate → pre-push trio
  `format:check && lint && typecheck` + rebase on origin/main → push → open PR → report PR + evidence
  to coordinator). You do **NOT** merge, move the board, or close issues — that's the coordinator's.

## Start

1. `[ -d node_modules ] || pnpm install` (fresh worktree; shares pnpm store).
2. Run the CLAUDE.md agentmemory recalls for state + frontend + integration-test rows.
3. Read the plan (`…-1110-app-map-plan.md`) by task. Do step ½ (branch verification). Then build.

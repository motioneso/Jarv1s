# ext-nav-1019 relay 6 — 2026-07-13

Bug #1019, sensitive tier. Worktree: this one (`.claude/worktrees/ext-nav-1019`, branch
`ext-nav-1019`, off `origin/main`). Coordinator: label `Coordinator`, session
`58a78927-385c-4b1d-8fa0-94db20255d6f`.

**Bug fixed + verified green.** HEAD `55bca625`. Do NOT re-investigate — the finding is closed
and Coordinator-approved (approved refined branch-2: real fix is running
`scripts/module-reconcile.ts` in the harness, mirroring real boot order migrate->reconcile->server
per `scripts/start-jarv1s.ts`; not a routes-modules.ts bug, not a Switch-click). `nav-1019.spec.ts
resume` produces `RESUME OK` end-to-end (real API, real nav click, job-search page renders).

Read `docs/superpowers/handoffs/2026-07-13-ext-nav-1019-relay-5.md` for the original bug
investigation writeup if you want the full trace — not required to proceed.

## Remaining tasks (in order) — nothing else left to build

1. **Cleanup UAT scratch infra:**
   - `rm scripts/uat/_scratch-provision-db.ts scripts/uat/_scratch-mock-registry.ts` (untracked,
     never `git add`ed — safe to delete, not a git operation).
   - `DROP DATABASE jarvis_uat_1019;` against the `jarv1s-postgres` container.
   - Kill background processes: `ps aux | grep -E "tsx src/server|vite|_scratch-mock-registry"` →
     kill API (port 47101), web (port 47102), mock registry (port 37743).
2. **Rebase onto `origin/main`:** `git fetch origin main && git rebase origin/main`. Known 1-line
   conflict in `tests/e2e/app-shell.spec.ts` (main advanced past `7d852092` via #986
   settings-shell heading rename, pure-frontend, didn't touch app-shell.tsx/app-route-metadata.ts).
   Keep BOTH #986's heading text AND this branch's Modules-nav assertions.
3. **Pre-push trio:** `pnpm format:check && pnpm lint && pnpm typecheck`. Then full
   `pnpm verify:foundation` — record exit code.
4. **`coordinated-wrap-up`:** push, open PR (base `main`, `Closes #1019`, user-facing "What's
   new": "Downloaded modules now appear in the navigation menu after you install them —
   previously an installed module could only be reached by typing its URL"). Report PR number to
   the `Coordinator` pane (resolve fresh by label + session id — never a baked pane number). Do
   NOT merge — sensitive tier, expects cross-model review + Coordinator sign-off.

## Process reminders

- `coordinated-build` skill for gates/escalation. `[ -d node_modules ] || pnpm install` — don't
  reinstall, worktree already has it.
- Stage only this task's own files. No `git add -A`. No `docs/coordination/` edits.
- Relay again at the next 70% context-meter warning or on seeing a compaction summary.
- Coordinator is spawning/verifying you and will reap the relay-6 session once you confirm driving
  — you don't need to message about the handoff itself, just proceed straight into task 1.

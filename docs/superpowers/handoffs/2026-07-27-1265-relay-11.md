# Relay 11 — #1265 module content self-operation

**Read this doc only.** Do not read relay-7/8/9/10 front-to-back. Everything open is below.

- Worktree/branch: `1265-module-content-self-operation` (this dir), `node_modules` present, skip install.
- PR **#1273** OPEN on this branch. Do not open new, do not merge, do not close/reopen.
- Coordinator label `Coordinator`, agent name `coord-1262`. Resolve pane fresh via `herdr pane list`
  before messaging — confirm exactly one match. Talk caveman.
- Issue #1265, epic #1262. Risk tier: **security**. Tree clean, all work below committed.

## Coordinator ruling this relay (binding — Task 5 was NOT escalated, it was built)

Coordinator pre-empted the escalation: checked the harness itself, ruled Task 5 **feasible**, gave
the exact shape (userA follows via gateway tool path, userB's own token attempts
list/unfollow through the same path, must not see/remove it — because the trust boundary
self-operation introduces is whether the gateway threads the *caller's* actor into the data
context, not whether the SQL policy is attached). Built exactly that. Do not re-open Task 5 or
re-litigate feasibility.

## DONE this relay (commit, green, do not redo)

- `dba071e2` — Task 5: added
  `sports.followTeam/unfollowTeam through the gateway are RLS-isolated across actors (#1265 Task 5)`
  to `tests/integration/mcp-gateway-self-operation.test.ts`. Grants self-operation for both
  `ids.userA`/`ids.userB` (sports_follows family), swaps in the REAL `SportsFollowsRepository`
  (not the fake in-memory writer the install-grant test above it uses), builds a real
  `AssistantToolGateway` against the real DB, mints two tokens, and asserts: userA's
  `sports.followTeam` succeeds; userB's `sports.unfollowTeam` on the same competitionKey returns
  `removed: false` (own token, own gateway call, real RLS); userA's own subsequent unfollow
  returns `removed: true` (positive control — proves the row was real, not vacuous). Ran via
  `pnpm exec tsx scripts/test-integration.ts tests/integration/mcp-gateway-self-operation.test.ts`
  — 7/7 pass.
  - **Verified mutation-tight** (per coordinator ruling — required, not optional): temporarily
    hardcoded `access.actorUserId` in `AssistantToolGateway.runHandler`
    (`packages/ai/src/gateway/gateway.ts`) to a fixed userA literal, re-ran only the new test — it
    FAILED as predicted (`removed: true` instead of `false`), proving the test actually exercises
    actor-threading, not the repository-level RLS wiring one layer down. Reverted immediately
    (`cp` from a scratch backup, confirmed `git diff --stat` on the file is empty) — **not
    committed**, gateway.ts has zero diff on this branch.
  - Pre-push trio + typecheck run clean on this file specifically: `prettier --check`, `eslint`,
    full `pnpm typecheck` all exit 0 (recorded, not re-run needed unless Task 6's full gate says
    otherwise).
  - Told coordinator both messages (Task 5 done, then relay notice) via `herdr agent prompt
    coord-1262`.

## OPEN — pick up here, in order

### Task 6 — gate + push + report (last task, start here)

- Fresh isolated gate DB — do NOT reuse any prior `jarv1s_gate_1265*`/`jarvis_gate_1265*` name.
  Use `jarvis_gate_1265d` (drop+create first — check what actually exists first, don't assume).
- `pnpm verify:foundation` and `pnpm audit:release-hardening` — capture **real exit codes**, never
  piped through `tail`/`head`. (`pnpm typecheck` alone already confirmed green this relay on a
  clean tree — full gate still required, don't skip it as "already known good".)
- Pre-push trio `pnpm format:check && pnpm lint && pnpm typecheck`, then
  `git fetch origin main && git rebase origin/main`, then push to **this same branch** (PR #1273
  stays open).
- **Update PR #1273 body** — FOUR callouts required (binding from relay-10's coordinator ruling,
  still applies, not renegotiable):
  1. exit-criterion-unmet language (state plainly what part of the spec's exit criteria this PR
     does/doesn't fully close)
  2. pattern-anchoring note (the manifest input validator now anchors patterns to the whole
     string, not a substring match — a real behavior change)
  3. external-module blast-radius note (this reaches installed external modules too, not just
     built-in ones)
  4. a fourth line **naming `packages/settings/src/app-map-tool.ts`** explicitly as the one
     in-tree file whose bounds go from decorative to enforced — a live behavior change outside
     this PR's nominal scope a reviewer must be told about, not left to find. Do not drop this
     line — it is new as of relay-10 and easy to omit if you're working from an older mental model
     of the three-item list.
- **Shared-surface note (read before writing the PR body):** the inventory assertion is a shared
  surface with #1264. Whoever lands SECOND rebases and recomputes from its OWN tree with exact
  `toBe` — do not hard-code an assumption either way into the PR body about which lands first.
  For reference only (may be stale by the time you read this — recompute, don't trust): #1264's
  branch was last reported computing 37/5/4.
- Report to Coordinator: every fix this + all prior relays (Task 1.2, 3, 4, 5 — see relay-10 for
  the earlier three), both gate exit codes, confirmation PR body updated with all four callouts.
  Then invoke `coordinated-wrap-up`. **Do not merge. Do not move the board.**
- Ben's manual sign-off remains a merge blocker regardless of gate color — PR will NOT merge even
  fully green tonight. #1264 and #1265 both park on Ben's hands-on chat-turn LAN UAT pass. Build to
  done, get CI green, get the QA verdict (qa-1265, worktree, held alive per relay-10 for the delta
  re-review — your push is the one it reviews), then stop and report.

## Model

Boot on **Sonnet** explicitly (`--model sonnet`). Confirm status bar.

## Relay note

Relayed at context-meter 71% warning, immediately after landing `dba071e2` (Task 5) and
confirming/reverting the mutation check. Tree clean, nothing in-flight, nothing uncommitted except
this doc. Fresh full budget: BUILD, commit per task, relay only after real work past ~80%. Only
Task 6 remains — it is entirely mechanical (gate, push, PR body) with no open design questions.

# #1110 app-map — relay checkpoint 8

Spec: `docs/superpowers/specs/` (app-map). Plan: `docs/superpowers/plans/2026-07-16-1110-app-map-plan.md`.
Branch/worktree: `build/1110-app-map` at `/home/ben/Jarv1s/.claude/worktrees/build-1110-app-map` (shared —
check `herdr pane list` before tree-wide git actions).
Coordinator label: **`Coord-1109-1110-g3`** — confirm exactly one pane holds it before messaging
(`herdr pane list`; last seen `agent_status: "done"` — may need a fresh coordinator).

## Done and committed

- Tasks 1-6: done (see relay checkpoint 7, commits `5fc87016`, `a274d98d`, etc.)
- **Task 7 (this session): DONE, green, committed** at `08e6a716` — "fix(chat): ground app answers
  in declared tools". `DEFAULT_JARVIS_PERSONA` in `packages/chat/src/live/runtime.ts` replaced
  (byte-stable, no dates/versions/module lists) with closed-world grounding rules referencing
  `app.getMapSlice`/`chat.getCurrentView`, "I don't know from the current app map", non-prerequisite
  honesty. New assertions in `tests/unit/chat-runtime-persona.test.ts`. The
  `app-map-tool.test.ts` "logs an undeclared query as a coverage gap" case was added too — it was
  **already green** (Task 5 pre-wired `logGap`; the plan's literal test snippet omits
  `resolveFeatureFlagState`, which is a **required** dep on `createAppMapReadService` — added a
  `vi.fn().mockReturnValue(true)` stub or the call throws before reaching the assertion). Full run:
  `pnpm vitest run tests/unit/chat-runtime-persona.test.ts tests/unit/app-map-tool.test.ts` → 2
  files / 5 tests green. `pnpm check:no-ambient-dates` clean. `pnpm --filter @jarv1s/chat typecheck`
  clean.

**Tree is clean except `.claude/context-meter.log`** (never add; noise not code).

## Task 8: NOT started — plan text is materially stale, redesign needed before coding

I did the "just-in-time verify" the checkpoint-6/7 handoffs flagged as outstanding, and the plan's
Task 8 snippets (plan lines 1775-2162) do **not** match the current UAT harness. Do not paste them
verbatim. Concretely:

1. **`run-uat.ts`/`provisioner.ts` architecture has moved on.** The plan assumes a `specFilters` /
   `runsJobSearchInstall` selector design in `run-uat.ts`'s `main()`. The **real** current design
   (already shipped, unrelated to #1110) is per-spec: every `tests/uat/specs/*.uat.spec.ts` exports
   `export const uatLevel = { level: "...", without: [...] } as const;`, which
   `run-uat.ts`'s `readUatLevel()` regex-parses and feeds straight into `provisionForUat()`. See
   `tests/uat/specs/job-search-install.uat.spec.ts` for the working pattern. **Do not modify
   `run-uat.ts`'s `main()`/`resolveSpecPaths` selector logic** — write the new spec's `uatLevel`
   export instead.
2. **`tests/uat/seed/member.ts` must NOT be created as the plan specifies.** Its hardcoded
   `userId: "00000000-0000-4000-8000-000000000002"` **collides** with the already-existing
   `UAT_SECOND_OWNER_ID` in `tests/uat/seed/admin.ts` (same id, already seeded). `multi-user` level
   (`tests/uat/seed/levels.ts`) **already** seeds a second, non-admin user via `seedSecondOwner` —
   `UAT_SECOND_OWNER_EMAIL`/`UAT_SECOND_OWNER_PASSWORD`, `isInstanceAdmin: false`. Use those
   directly for the plan's "non-admin map query never reveals admin settings" test — skip
   `member.ts` entirely.
3. **No existing mechanism carries a `withoutNewsJsonBinding`-style flag.** `seedAiProviderChunk`
   (`tests/uat/seed/chunks/ai.ts`) unconditionally binds the fake model to `module.news` and is
   called unconditionally in `levels.ts:79`, separate from the chunk-exclude loop
   (`excludeChunks`/`without` only covers `UatSeedChunk` = news/sports/tasks/calendar/notes/job-search,
   which doesn't touch the AI/news binding). To get the News "no_json_model" prerequisite path for
   this spec, a new optional flag needs threading through the **real** current chain, end to end:
   - `tests/uat/specs/app-map-grounding.uat.spec.ts`'s `uatLevel` export (extend the exported shape
     with e.g. `withoutNewsJsonBinding: true` alongside `level`/`without`)
   - `run-uat.ts`'s `readUatLevel()` regex (extend to capture the new field) and `runSpec()`'s call
     into `provisionForUat`
   - `provisioner.ts`: `UatProvisionOptions`/`SeedHook` ctx, `composeSeedHook`'s docker `-e` args
     (new `JARVIS_UAT_WITHOUT_NEWS_JSON_BINDING=0|1`), `buildSeedHookInput`, `provisionForUat`'s opts
   - `tests/uat/seed/cli.ts`: read the new env var, pass into `seedLevel`
   - `tests/uat/seed/types.ts`: add the field to `SeedOptions`
   - `tests/uat/seed/levels.ts`: pass `{ bindNews: !options.withoutNewsJsonBinding }` into
     `seedAiProviderChunk`
   - `tests/uat/seed/chunks/ai.ts`: accept `{ bindNews: boolean }`, skip `setServiceBinding` when false
   This is additive (new optional field, existing calls default it away) — should not disturb the
   other three existing specs, but re-run `pnpm test:uat` for at least one of them
   (`cli-terminal.uat.spec.ts` is cheapest) after the change as a regression check.
4. **`package.json`'s `test:uat` script is already correct** (`"tsx tests/uat/run-uat.ts"`, line 44)
   — the plan's Step 3 snippet for it is a no-op, don't re-touch.
5. **Not yet verified:** current line ranges / exact shape for the `previewOverride` insertion in
   `packages/news/src/routes.ts` (`NewsRoutesDependencies`, `registerNewsPersonalizationRoutes`
   deps), `packages/news/src/personalization-routes.ts` (`PersonalizationRouteDependencies`, the
   post-body-read override check), and `packages/module-registry/src/index.ts`'s
   `buildNewsDiscoveryPorts`-adjacent composition (plan cites lines ~475-545,1380-1435 — read fresh,
   don't trust those numbers). These looked architecturally plausible from a first read (file heads
   only) but line numbers need reconfirming before editing.
6. **`docs/WHATS_NEW.md` prepend text** — not yet checked against the current file top; likely still
   fine as authored in the plan, confirm no conflicting recent entry first.

## Operational note before running any UAT/docker command

Another pane is live: label `UX 1117 UAT 4d2d17ba-3`, worktree
`/home/ben/Jarv1s/.claude/worktrees/uat-1117-final-4d2d17ba`, status `working`. UAT's default
Docker subnet (`UAT_DOCKER_SUBNET`, 10.254.0.0/24) has **no overlap guard** across concurrent runs
(see agentmemory `uat-docker-subnet-map`). Send a heads-up via `herdr-pane-message` before running
`pnpm test:uat` / `pnpm verify:foundation` here, to avoid a false-negative from a subnet/resource
collision rather than a real bug.

## After Task 8 green

1. `pnpm verify:foundation`
2. Pre-push trio: `pnpm format:check && pnpm lint && pnpm typecheck`
3. `git fetch origin main && git rebase origin/main`
4. `coordinated-wrap-up` skill: PR + report to `Coord-1109-1110-g3` (resolve the label fresh).
   **Never merge/board/close** — coordinator's job only.

## Process reminders (don't relitigate)

- Read plan **by section** for the task at hand only — never front-to-back.
- Strict TDD: RED → confirm failure → implement → GREEN → commit with **exact** `git add <files>`
  (never `-A`).
- Relay on the **context-meter 70% warning** or immediately on seeing a compaction summary.
- `node_modules` already exists in this worktree — **never** `pnpm install` on relay resume.

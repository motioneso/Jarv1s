# Relay — rfa-540-safe-automation-audit-log

**Relay reason:** context at 70% before plan was written — relaying clean before auto-compaction.
**Branch / worktree:** `rfa-540-safe-automation-audit-log` / `~/Jarv1s/.claude/worktrees/rfa-540-safe-automation-audit-log`
**Spec:** `docs/superpowers/specs/2026-06-28-safe-automation-audit-log.md` (now present in branch after coordinator copied it)
**Handoff:** `docs/coordination/handoff-rfa-540-safe-automation-audit-log.md`
**Coordinator label:** `Coordinator` (session id authority: `5e1a6b62-a480-4b5c-9706-e476cfe77044`)
**Migration slot:** placeholder `XXXX` during dev; coordinator assigns **0128** at merge.

## What's done

- `pnpm install` complete (node_modules present — skip on next session)
- Spec read fully (`docs/superpowers/specs/2026-06-28-safe-automation-audit-log.md`)
- Spec premises verified against branch:
  - `packages/ai/src/gateway/gateway.ts` — no `recordAudit` / no audit writes exist ✅
  - `packages/ai/src/gateway/policy.ts` — no audit writes ✅
  - `packages/ai/sql/0016_ai_assistant_actions.sql` — no `jarvis_action_audit_log` table ✅
  - Repo-wide grep for `audit_log|jarvis_action_audit|action_audit` → **zero matches** ✅
  - Gap is real; all spec premises are current.
- Read all relevant source: gateway, policy, repository, routes, SQL patterns, auto-register,
  data-export, settings-page, settings-audit-pane (admin-only — not ours), personal panes.
- Coordinator escalated about missing spec (coordinator copied it). No plan approval yet.

## What's next (START HERE)

**STEP 1 — Write plan.**
Use `superpowers:writing-plans` → save to `docs/superpowers/plans/2026-06-28-rfa-540-safe-automation-audit-log.md`.
Then message coordinator via `herdr-pane-message` (label `Coordinator`): "plan ready for rfa-540: docs/superpowers/plans/2026-06-28-rfa-540-safe-automation-audit-log.md — approve or flag".
STOP until coordinator approves.

**STEP 2 — Build per plan (only after coordinator approval).**
Use `superpowers:test-driven-development`. Commit green per task. Explicit `git add` paths only.

**STEP 3 — Wrap up.**
`coordinated-wrap-up` → PR → report to coordinator.

## Architecture context (for plan writing)

### Files to create / modify

**SQL migration (new):**

- `packages/ai/sql/XXXX_jarvis_action_audit_log.sql`
  - Table: `app.jarvis_action_audit_log` with fields: id, owner_user_id (FK→users CASCADE), tool_module_id, tool_name, action_family_id (nullable), action_kind (write|destructive), approval_mode (auto|confirmed|rejected|cancelled|timeout), outcome (success|failed|denied|cancelled), error_class (text ≤64, nullable), request_id (nullable), chat_session_id (nullable), source_surface (chat|proactive|scheduled|unknown, default chat), occurred_at (timestamptz, default now)
  - Indexes: (owner_user_id, occurred_at DESC) + (owner_user_id, action_family_id, occurred_at DESC)
  - ENABLE RLS + FORCE RLS; SELECT + INSERT policies for jarvis_app_runtime owner-scoped
  - GRANT SELECT, INSERT (NO UPDATE, NO DELETE) to jarvis_app_runtime
  - SECURITY DEFINER purge function `app.purge_jarvis_action_audit_log(older_than timestamptz)` → deletes + returns count; REVOKE ALL FROM PUBLIC; GRANT EXECUTE TO jarvis_app_runtime
  - Pattern: copied from `0016_ai_assistant_actions.sql` (RLS/grants) + `0098_ai_cancel_stale_assistant_actions.sql` (SECURITY DEFINER purge pattern)

**DB types (modify):**

- `packages/db/src/database.ts` — add `JarvisActionAuditLog` row type + add to `JarvisDatabase`
- NOTE: Check how other tables are typed. Pattern: `JarvisActionAuditLog` interface with snake_case fields.

**AI Repository (modify `packages/ai/src/repository.ts`):**

- Add `insertActionAuditLog(scopedDb, row)` method (INSERT into jarvis_action_audit_log)
- Add `listActionAuditLog(scopedDb, { since, familyFilter, limit })` method (SELECT owner-scoped, newest-first, clamped)
- Add `purgeActionAuditLog(scopedDb, olderThan)` method calling `app.purge_jarvis_action_audit_log`

**Gateway write sites (modify `packages/ai/src/gateway/gateway.ts`):**

- Add private `recordAudit(ctx, found, opts: { approvalMode, outcome, errorClass? })` helper
  - Inserts via `this.deps.runner.withDataContext(access, (scopedDb) => repo.insertActionAuditLog(…))`
  - Only writes if `found.tool.risk !== 'read'`
  - Audit failure: catch + log metadata-only, swallow — NEVER change action result
- Wire in `runHandler()`: if `found.tool.risk !== 'read'`, record audit after handler returns (success or catch)
  - auto path: `approval_mode: 'auto'`, `outcome: result.ok ? 'success' : 'failed'`, `errorClass` from bounded category if failed
- Wire in `confirmAndRun()`:
  - denied/cancelled/timeout branch: record before return (no handler ran)
    - outcome=timeout→`{approvalMode:'timeout', outcome:'denied'}`
    - outcome=rejected→`{approvalMode:'rejected', outcome:'denied'}`
    - outcome=cancelled→`{approvalMode:'cancelled', outcome:'cancelled'}`
  - approved branch: record after `runHandler()` returns
    - `{approvalMode:'confirmed', outcome: result.ok?'success':'failed', errorClass if failed}`
- IMPORTANT: `runHandler` is called by BOTH `run` path AND `confirmAndRun`; need to know caller context to set `approvalMode`. Pass an optional `approvalMode` param or record directly in each call site rather than inside `runHandler`.

**Maintenance job (modify `packages/ai/src/manifest.ts` or wherever pg-boss jobs are registered):**

- Register daily pg-boss job for retention purge: `scheduleJob('purge-action-audit-log', '0 3 * * *', {})` or similar
- Job handler calls `purgeActionAuditLog(scopedDb, olderThan90Days)`
- Payload metadata-only (just cutoff timestamp, no content)
- Find existing stale-action-cancel job registration and follow its pattern

**Shared types (modify `packages/shared/src/ai-api.ts`):**

- Add `ActionAuditLogEntryDto`: id, ownerUserId, toolModuleId, toolName, actionFamilyId (nullable), actionKind, approvalMode, outcome, errorClass (nullable), requestId (nullable), chatSessionId (nullable), sourceSurface, occurredAt
- Add `ListActionAuditLogResponse: { entries: ActionAuditLogEntryDto[] }`
- Add route schema for `GET /api/ai/action-audit` (since, family, limit query params)

**API route (modify `packages/ai/src/routes.ts`):**

- Add `GET /api/ai/action-audit` route with schema
- Query params: since (ISO string, default 90d ago, clamp to ≥90d ago), family (moduleId/familyId optional filter), limit (default 200, max 500)
- Owner-scoped via DataContextDb; RLS enforces isolation (no hand-rolled WHERE)
- No create/update/delete routes — gateway is only writer

**Data export (modify `packages/settings/src/data-export.ts`):**

- Add `jarvisActionAuditLog` to `UserDataExportTables` interface
- Add `jarvisActionAuditLogQuery()` function (all metadata fields, no content)
- Add to `readExportTables()` call

**Frontend API client (modify `apps/web/src/api/client.ts`):**

- Add `listActionAuditLog(params?: { since?, family?, limit? })` fetching `GET /api/ai/action-audit`

**Frontend query keys (modify `apps/web/src/api/query-keys.ts`):**

- Add `ai.actionAuditLog: (params?) => [...]`

**New Settings Activity pane (new `apps/web/src/settings/settings-activity-pane.tsx`):**

- `ActivityPane` component (export default or named)
- Fetches owner's audit log via React Query
- Date filter: Today / 7 days / 30 days / 90 days (maps to `since` param)
- Family filter: dropdown sourced from unique families in current window
- Each row: tool/family label (from toolName + actionFamilyId), approval badge (Auto-run / Confirmed / Declined / Cancelled / Timed out), outcome chip (Done / Failed / Declined / Cancelled), relative + absolute timestamp, source surface hint (if not 'chat')
- No pagination, no row detail drawer in V1
- Empty state: existing authored empty pattern ("No Jarvis actions yet")
- Failed/declined rows visually distinct but not alarming
- Use jds-\* primitives + existing list/empty/loading patterns; NO curved accent left-border

**Wire Activity pane into settings-page (modify `apps/web/src/settings/settings-page.tsx`):**

- Add lazy import for `ActivityPane`
- Add `{ id: "activity", icon: ..., label: "Activity", Pane: ActivityPane }` to personal sections
  - Suggested icon: `Activity` from lucide-react (already imported for connector oversight)
  - Or: `History`, `ClipboardList` — whichever fits best; check existing imports

### Tests to write

Location: check existing test files, likely `packages/ai/src/__tests__/` or similar.

Key test cases (from spec §15 Verification):

1. trusted_auto write succeeds → approval_mode=auto, outcome=success, one row
2. trusted_auto write throws → approval_mode=auto, outcome=failed, bounded error_class, no message
3. confirmed write → approval_mode=confirmed, outcome=success
4. denied proposal → approval_mode=rejected, outcome=denied, no handler run
5. cancelled → approval_mode=cancelled, outcome=cancelled
6. timeout → approval_mode=timeout, outcome=denied
7. read tool → no audit row
8. destructive confirmed → action_kind=destructive
9. audit insert failure → action result unchanged
10. audit row no input_summary/content (schema + value assertions)
11. RLS isolation: user A cannot select user B's rows; mismatched owner_user_id INSERT rejected
12. runtime role has no UPDATE/DELETE grant
13. GET /api/ai/action-audit → owner-scoped, honors since/family/limit, clamps since
14. retention function deletes older rows, leaves newer rows, returns count
15. account deletion cascade removes all audit rows
16. user data export includes jarvisActionAuditLog metadata-only
17. foundation.test.ts migration list includes XXXX_jarvis_action_audit_log.sql

### Key invariants to honor

- Audit insert failure NEVER changes action result (swallow after logging)
- ONLY gateway writes audit rows — no module/route/job/UI bypass
- Rows are insert-only for app_runtime (no UPDATE/DELETE grant)
- FORCE RLS + ENABLE RLS + owner-scoped SELECT/INSERT policies
- No admin cross-user view
- No content/input/prompt ever stored in audit rows
- Migration slot: placeholder XXXX in SQL file, coordinator assigns 0128 at merge
- Do NOT touch: packages/chat, packages/shared/src/chat-api.ts, packages/people, packages/briefings
- Stage only your own files; never git add -A

### Collision notes (from handoff)

- Migration 0128 is the reserved slot; use XXXX placeholder during dev
- Parallel in-flight: #538 (packages/people), #539 (packages/notes?), #541 (data freshness)
- You own `packages/ai/src/gateway/` exclusively

## Escalation history

- Escalated to coordinator: spec file missing → coordinator copied it → unblocked
- Plan NOT yet sent to coordinator for approval — successor must write plan and send it

## Gate command

```bash
pnpm format:check && pnpm lint && pnpm typecheck
pnpm test:ai && pnpm test:settings && pnpm test:api && pnpm test:web && pnpm test:integration
```

# mod-dist-964 relay 9 handoff

Branch: `mod-dist-964`, worktree: `/home/ben/Jarv1s/.claude/worktrees/mod-dist-964`
Plan: `docs/superpowers/plans/2026-07-12-module-distribution-install.md` (5464 lines — read BY
SECTION only, never front-to-back).
Coordinator: label `Coordinator` — always re-resolve via fresh `herdr pane list`, never a baked
pane number. Security tier: never merge/close/move board yourself.

## Done (committed)

- Task 7 Step 10 (typecheck fixes): `c08949a1`
- Task 8 (boot reconcile + compose modules volume): `0ca51722`
- Both gates green when committed (typecheck, vitest, compose config validation).

## Current task: Task 9 — Admin web UI, module registry section

Plan section: lines ~4442-4884 of the plan doc.

Premises verified so far (all match plan, only trivial line-number drift):
- `apps/web/src/api/query-keys.ts` lines 10-25 — `adminExternalModules` key present at expected spot.
- `apps/web/src/api/client.ts` — `setExternalModuleEnabled` at line 384 (plan said ~392),
  `requestJson<T>` at line 1182 (exact). Plan adds `getModuleRegistry`, `downloadRegistryModule`,
  `removeRegistryModule`, `cancelModulePurge` here.
- `packages/shared/src/platform-api-modules.ts` lines 385-440 — Task 6's DTOs confirmed present
  exactly as plan expects: `MODULE_REGISTRY_LIFECYCLE_STATES`, `ModuleRegistryToolDto`,
  `ModuleRegistryCapabilitiesDto`, `ModuleRegistryRowDto` (note `capabilities:
  ModuleRegistryCapabilitiesDto | null` — **can be null**), `GetModuleRegistryResponse`.

**Plan-text bug found (must fix, don't copy literally):** the plan's `describeCapabilities(row)`
snippet does `const caps = row.capabilities;` then immediately dereferences `caps.permissions.length`
etc. with no null check. Since the DTO type allows `capabilities: null`, this will NOT typecheck as
written. Add a null guard / fallback before dereferencing when implementing this function.

**Not yet read this session** (do first, before writing code):
- `apps/web/src/settings/settings-feedback.tsx` — verify current `ConfirmOptions` shape (~lines
  24-31) and dialog markup (~lines 117-146) against what plan Step expects (`requireText` for
  type-to-confirm on the destructive purge action).
- `apps/web/src/settings/settings-admin-panes.tsx` — verify `InstanceModulesPane` location (~line
  553) and current total line count. File was at 987/1000 per plan notes — **file-size gate is
  tight**, keep the wiring edit here minimal (~3 lines), put the bulk of new code in a new file
  `settings-module-registry-section.tsx`.

### Remaining Task 9 steps (plan lines ~4442-4884)
1. `query-keys.ts` — add `adminModuleRegistry` sibling key.
2. `client.ts` — add `getModuleRegistry`, `downloadRegistryModule`, `removeRegistryModule`,
   `cancelModulePurge`.
3. `settings-feedback.tsx` — extend `ConfirmOptions` with `requireText`, wire into dialog.
4. New file `settings-module-registry-section.tsx` — module registry list/download/remove/cancel
   UI using jds-* primitives (`jds-btn`, `jds-btn--danger`, `jds-input`, etc.), React Query
   patterns (`useQuery`/`useMutation`), **with the null-guard fix above**.
5. Wire `<ModuleRegistrySection />` into `InstanceModulesPane` in `settings-admin-panes.tsx`
   (minimal edit, watch file-size gate).
6. Gates: `pnpm --filter @jarv1s/web typecheck`, `pnpm check:file-size`,
   `pnpm --filter @jarv1s/web lint`, manual LAN smoke check (dev servers bind 0.0.0.0 per
   CLAUDE.md — user tests over LAN).
7. Commit.

## After Task 9

Task 10 (plan lines ~4884+): integration suite, docs, spec-example fix, full gates. Not yet read —
read by section when you get there.

Then pre-push trio before any push: `pnpm format:check && pnpm lint && pnpm typecheck`, then
`git fetch origin main && git rebase origin/main`. No push has occurred yet this branch.

Finally: `coordinated-wrap-up` (PR titled "Part of #964") once Task 10's Exit Criteria are green.
Never merge/close/move board — report PR + evidence to Coordinator only.

## Notes
- `node_modules` already exists in this worktree — skip `pnpm install`.
- Read the plan doc by section only; a full front-to-back read burns context with zero code
  written (this has bitten prior relays in this chain).

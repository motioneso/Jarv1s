# Relay 9 — #1265 module content self-operation

**Read this doc only.** Do not read relay-7/relay-8 front-to-back — everything still open is
restated below. If you need exact Coordinator wording, it's in relay-8's history, not needed to act.

- Worktree/branch: `1265-module-content-self-operation` (this dir). `node_modules` present — do NOT
  `pnpm install`.
- PR **#1273** OPEN on this branch. Do not open a new PR, do not close/reopen, do not merge.
- Coordinator label: `Coordinator` (agent `coord-1262`). Resolve its pane fresh via
  `herdr pane list` before messaging — confirm exactly one pane holds that label. Talk caveman.
- Issue #1265, epic #1262. Risk tier: **security**.
- Tree is clean, all work below committed. Nothing in-flight.

## Binding constraint from Coordinator (do not loosen)

Coordinator approved BLOCKING-2 remediation (UAT fixmes + mocked e2e) with **four conditions**,
all satisfied by the commits below EXCEPT running module-install for real (still open):

1. Every `test.fixme` states its structural reason inline + names where real proof lives — DONE.
2. Mocked e2e must be mutation-tight (fails if the card-suppression control is removed) — DONE,
   verified by hand.
3. **PR body must NOT claim the exit criterion met.** State plainly: automated exit criterion is
   UNMET for a structural harness reason; backend + frontend proven separately; real-chat leg
   needs a hands-on LAN pass. No skimmable "satisfied" language. **Not yet drafted into PR body —
   your job at Task 6.**
4. **Ben's manual UAT sign-off is a merge blocker on this PR — not the Coordinator's to waive.**
   Build to done, get CI green, get re-QA verdict, then it waits for Ben. Do not expect to merge.

Also binding, cite `#1121` as **reopened-and-open** (Coordinator reopened it with evidence after
finding it wrongly closed) — already done correctly in the commits below; keep that phrasing in
anything new you write.

## DONE this relay (commits, all green, do not redo)

- `addc0492` — Task 2/ALSO-1: mutation-tight same-host redirect SSRF case in
  `tests/unit/news-source-resolution.test.ts` (scheme downgrade / port / credentials). 8 tests pass.
- `b55c6287` — new `tests/e2e/self-operation-no-confirmation-card.spec.ts`. Mocked SSE transcript
  with BOTH an `action_result` (self-op, no card) and an `action_request` (needs confirm, gets
  card) — differential design so a regression either way fails. Verified mutation-tight by hand:
  temporarily removed `"action_result"` from `ACTIVITY_KINDS` in
  `apps/web/src/chat/message-row.tsx` → test failed; reverted via `git checkout --` → test passed.
  Playwright run: 1 passed.
- `fd26a9db` — new `tests/uat/specs/self-operation-content-commands.uat.spec.ts`, three
  `test.fixme` blocks (news add-topic, follow Yankees, unfollow), each with inline structural
  reason + pointer to the backend proof (`tests/integration/mcp-gateway-self-operation.test.ts`)
  and frontend proof (the e2e spec above). Prettier-reformatted, `eslint` clean.

## OPEN — pick up here, in order

### Task 1.2 — run module-install UAT for real (STILL OPEN, do this first)

`tests/uat/specs/module-install.uat.spec.ts` must run for real against the actual harness and you
must report the **true unpiped exit code**. A bare `pnpm exec playwright test` does NOT discover
this file (confirmed: returns "No tests found") — it needs `tests/uat/playwright.uat.config.ts`
and env vars (`JARVIS_UAT_PROJECT_NAME`, `JARVIS_UAT_BASE_URL`) that `tests/uat/run-uat.ts` sets up.
**Read `tests/uat/run-uat.ts` and `package.json`'s `test:uat` script first**, then invoke the real
way (likely `pnpm test:uat` with some filter/arg, or directly `tsx tests/uat/run-uat.ts <args>`).
Do not pipe through `tail`/`head`. This spins up a real Docker Compose dev instance — expect it to
take real wall-clock time.

### Task 3 — ALSO-2: vacuous denylist test

`tests/unit/self-operation-manifests.test.ts` currently asserts nothing that can fail. Read
`packages/ai/src/gateway/self-operation.ts` (`SELF_OPERATION_EXCLUSIONS`, `isSelfOperationExcluded`
— array starts with `self_authority.settings` moduleId `"settings"` prefixes like
`settings.yolo.`, and `self_authority.ai` moduleId `"ai"`, read the rest for other entries). Add a
positive assertion: pick a known-excluded moduleId + tool-name prefix pair, assert
`isSelfOperationExcluded(moduleId, toolName)` returns `true` for a name under that prefix. Commit.

### Task 4 — ALSO-3: restore mutated singleton

`tests/integration/mcp-gateway-self-operation.test.ts` mutates the process-wide
`configureSportsChatTools` singleton (mutation site ~line 270: `configureSportsChatTools({} as
never, fakeWriter);`) and never restores it — order-dependent test pollution. Add an `afterEach`
that restores the original. Commit.

### Task 5 — ESCALATE, do not decide

Attempt a **gateway → real DB → RLS** cross-actor span test for sports `followTeam`, entering via
the tool/gateway path (not `SportsService.followTeam` directly — `tests/integration/sports-follows-tool-rls.test.ts`
already does that direct-entry version). If a real gateway-entry span test is **structurally
infeasible**, message the Coordinator with the **specific structural reason** and wait for a
ruling. Do not conclude "good enough" yourself — that call is the Coordinator's per prior ruling.

### Task 6 — gate + push + report (last)

- Fresh isolated gate DB — do NOT reuse any prior `jarv1s_gate_1265*` name. Use `jarvis_gate_1265c`
  (drop+create first).
- `pnpm verify:foundation` and `pnpm audit:release-hardening` — capture **real exit codes**, never
  piped through `tail`/`head`.
- Pre-push trio `pnpm format:check && pnpm lint && pnpm typecheck`, then
  `git fetch origin main && git rebase origin/main`, then push to **this same branch** (PR #1273
  stays open, do not touch it besides pushing + editing its body).
- **Update the PR #1273 body** to include, explicitly and unskimmably:
  1. Condition 3 language: automated exit criterion for the chat-command scenarios is **UNMET**
     for a structural harness reason (no chat-capable seeded AI provider); backend proven in
     `tests/integration/mcp-gateway-self-operation.test.ts`; frontend proven in
     `tests/e2e/self-operation-no-confirmation-card.spec.ts`; real-chat leg needs a hands-on LAN
     pass. Do not word this so a skimming reader thinks it's satisfied.
  2. The `packages/ai/src/gateway/input-validation.ts` scope note from relay-8: blast radius includes
     `packages/settings/src/app-map-tool.ts` (newly-enforced `maxLength` bounds), pattern is anchored
     to the whole string even if authored unanchored, and the change reaches external installed
     modules whose previously-decorative bounds now become real rejections.
- Report to the Coordinator: every fix this relay + prior relays, both gate exit codes, the Task
  1.2 real exit code, the Task 5 outcome (built or escalated+reason), confirmation the PR body was
  updated per condition 3 and the input-validation scope note. Then invoke `coordinated-wrap-up`.
  **Do not merge. Do not move the board.** Ben's sign-off is parked as a merge blocker per condition 4.

## Model

Boot on **Sonnet** explicitly (`--model sonnet`). Confirm status bar; if wrong, switch and tell
the Coordinator.

## Relay note

Relayed at the context-meter 70% warning after landing `addc0492`/`b55c6287`/`fd26a9db`. Tree clean,
nothing in-flight. Fresh full budget: BUILD, commit per task, relay only after real work past ~80%.
Reading is not progress — Task 1.2 needs you to actually run the harness, not just read about it.

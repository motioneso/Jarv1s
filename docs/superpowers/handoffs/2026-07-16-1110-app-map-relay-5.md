# #1110 app-map relay checkpoint 5

Branch/worktree: `build/1110-app-map` at `/home/ben/Jarv1s/.claude/worktrees/build-1110-app-map` (already checked out, `node_modules` exists — skip install).
Coordinator label: **`Coord-1109-1110-g3`** — re-resolve via `herdr pane list` fresh before every message, confirm exactly one pane holds it.

## State
Tasks 1-4 done and committed (Task 4: `568d6a55`, `432fe68e`). Task 5 ("actor-visible bounded map read tool") is **fully premise-verified, zero code written**. Resume straight into TDD Step 1 — do not re-read the files below, their exact content/line numbers are already confirmed.

## Task 5 — confirmed ground truth (do not re-verify)

Plan: `docs/superpowers/plans/2026-07-16-1110-app-map-plan.md` lines 1083-1537 (Step 1 tests, Step 3 impl file contents, Step 5 commit list — all fully specified there, read that range only).

- **Gateway sanitization machinery already exists** — Task 5 does NOT build it, only declares `outputSchema` on the new tool manifest entry:
  - `packages/ai/src/gateway/gateway.ts` `runReadToolForActor` (lines 301-344) already calls `renderAndCap(found.tool.outputSchema, result, ...)`.
  - `packages/ai/src/gateway/output-validation.ts` `sanitizeAssistantToolResult` (confirmed, full file read) does recursive allow-list projection against JsonSchema — strips undeclared keys automatically.
  - Both re-exported from `packages/ai/src/gateway/index.ts` → `@jarv1s/ai`.
- **`packages/chat/src/routes.ts`** (NOT yet edited) — exact edits needed:
  - `ChatRoutesDependencies` interface, lines 99-158: add `readonly appMapService?: AppMapReadService;` alongside existing optional collaborator fields (e.g. `featureGrantService?`, `sourceContextService?`).
  - `registerChatRoutes` wiring block, lines 191-221: the `buildChatGatewayDependencies` call has a `collaborators: {...}` object literal at lines 206-213. Add `appMapService: dependencies.appMapService` as a **sibling** arg to `collaborators` (not inside it).
  - `buildChatGatewayDependencies`, lines 682-746: its `readToolServices` field (lines 724-734) is built via a ternary — `args.collaborators.featureGrantService || args.collaborators.sourceContextService ? {...spread...} : undefined`. Extend the condition with `|| args.appMapService` and the spread with `...(args.appMapService ? { appMap: args.appMapService } : {})`. `appMapService` is a new **top-level** param on the args object, sibling to `collaborators`.
- **`packages/module-registry/src/index.ts`** (NOT yet edited) — exact edits needed, all confirmed by direct read this session:
  - `BuiltInRouteDependencies` interface (starts line 340, ends ~529): add `readonly appMapService?: AppMapReadService;` — zero existing references to `appMapService` anywhere in file (grep-confirmed), clean insertion.
  - `aiModuleManifest` registration entry (line 1161-1193): its `registerAiRoutes` call has `readToolServices: deps.connectorsRepository ? {featureGrants:..., sourceContext:...} : undefined` at lines 1174-1182. Add `appMap: deps.appMapService` into that object (pattern TBD — confirm whether it should be gated on `deps.connectorsRepository` too, or independent, when writing this).
  - `chatModuleManifest` registration entry (line 1194-1246): its `registerChatRoutes` call (lines 1198-1234) passes `googleConnectionService`, `googleApiClient`, `connectorsRepository`, `featureGrantService`, `sourceContextService` etc. as top-level args (NOT inside a nested object — module-registry constructs the flat arg list that `registerChatRoutes` itself buckets into `collaborators` internally). Add `appMapService: deps.appMapService` as a new top-level line here.
  - `registerBuiltInApiRoutes` function (starts line 1932). The `const deps: BuiltInRouteDependencies = { ...dependencies, chatEngineFactory, ... }` object construction is at **lines 2040-2081** (confirmed full read). This is where to instantiate the actual `AppMapReadService` via `createAppMapReadService(...)` per the plan's Step 3 snippet — add a new field to this object literal (e.g. `appMapService: createAppMapReadService({...})` or similar per plan's exact snippet — reread plan lines 1083-1537 Step 3 for the exact factory call signature before writing).
  - `for (const module of BUILT_IN_MODULES) { module.registerRoutes?.(server, deps); }` runs right after (line 2083-2085) — confirms `deps` (with `appMapService` added) flows to both `aiModuleManifest` and `chatModuleManifest` registration closures above via the shared `deps` param name they each destructure.
- **Still unverified before writing code** (do this first, cheap, ~1 read):
  - `packages/settings/package.json` — confirm no dependency back on `@jarv1s/chat` (circular-dep guard) before adding `@jarv1s/settings` to `packages/chat/package.json`.
- **Carried forward, already confirmed in an earlier session** (not re-verified this session, trust unless contradicted): `settingsModuleManifest` (`packages/settings/src/manifest.ts`) has no `assistantTools` field yet — Task 5 adds it fresh. `packages/ai/src/routes.ts`'s `AiRoutesDependencies` already has `readonly readToolServices?: ToolServices;` — likely no change needed there. `tests/unit/gateway-read-tool.test.ts`'s `makeReadTool` helper has no `outputSchema` field yet — the plan's new test case adds it inline.

## Next steps (in order)
1. Quick read `packages/settings/package.json` (circular-dep check).
2. Reread plan lines 1083-1537 (Step 1 test content, Step 3 impl file full contents for `packages/settings/src/app-map.ts` + `app-map-tool.ts`, Step 5 commit file list) — do this fresh, don't rely on memory of it.
3. TDD: write `tests/unit/app-map-tool.test.ts` (new) + append test case to `tests/unit/gateway-read-tool.test.ts`. Run `pnpm vitest run tests/unit/app-map-tool.test.ts tests/unit/gateway-read-tool.test.ts` — confirm RED.
4. Implement: `packages/settings/src/app-map.ts`, `packages/settings/src/app-map-tool.ts`, + edits to `packages/settings/src/index.ts`, `packages/settings/src/manifest.ts`, `packages/chat/package.json`, `packages/chat/src/routes.ts`, `packages/module-registry/src/index.ts` (all per confirmed insertion points above). Confirm GREEN incl. `tests/unit/mcp-gateway-units.test.ts`.
5. Commit exact file list (plan Step 5) — never `git add -A`. Message: `feat(app-map): expose actor-visible map slices` + `Co-Authored-By: Claude <noreply@anthropic.com>` trailer.
6. Continue toward 2-3 tasks/session target: Task 6 next (plan lines 1538-1697) — needs just-in-time verify of `packages/shared/src/news-api.ts`, `packages/news/src/personalization-routes.ts`, `packages/news/src/settings/add-source.tsx` (plan claims plain `alert()`, no `ClosedWriteGate` — confirm still true).
7. Task 7 (plan lines 1698-1775) already verified clean by an earlier session — do not re-verify.
8. Task 8 (plan lines 1775-end) needs just-in-time verify of `tests/uat/seed/{types.ts,chunks/ai.ts,levels.ts,cli.ts}`, `tests/uat/provisioner.ts`, `tests/uat/run-uat.ts`, `packages/news/src/routes.ts` + `personalization-routes.ts` `previewOverride` insertion points, `docs/WHATS_NEW.md` top.
9. After all 8 green: `pnpm verify:foundation` → pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck`) → `git fetch origin main && git rebase origin/main` → `coordinated-wrap-up` skill (PR + report to `Coord-1109-1110-g3`, re-resolved fresh). Never merge/board/close.

## Reminders
- Never `git add -A` — exact file lists only.
- Strict TDD, red before green, commit per task.
- Read plan by section only, never front-to-back.
- Escalate blockers to `Coord-1109-1110-g3` via `herdr-pane-message`, re-resolving the pane fresh each time.

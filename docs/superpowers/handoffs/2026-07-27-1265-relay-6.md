# #1265 relay 6 — all build tasks done, gate must run against a VERIFIED isolated DB

Worktree/branch: `1265-module-content-self-operation` (don't `pnpm install`, `node_modules`
present). Coordinator: agent name `coord-1262` (label `Coordinator`) — **re-resolve pane fresh via
`herdr pane list` before messaging, never reuse a pane id from this doc.**

## CORRECTED FACTS (Coordinator verified directly against the DB server — read this before anything else)

My prior belief that I created and reused an isolated DB called `jarvis_gate_1265` across 3 runs
was **wrong**. The Coordinator checked the Postgres server directly and found:

1. **No DB named `jarvis_gate_1265` exists on `jarv1s-postgres`.** I never actually created it (or
   it never persisted). The correct naming convention for isolated gate DBs is
   `jarv1s_gate_NNNN` — **digit-1 spelling** (`jarv1s`, not `jarvis`) — matching the project's own
   name. Any command I ran using `jarvis_gate_1265` (missing-digit spelling) silently targeted
   nothing, or `JARVIS_PGDATABASE` was simply unset.
2. **The live gate process's actual env had `JARVIS_PGDATABASE` UNSET** (Coordinator read
   `/proc/<pid>/environ` on the running vitest workers directly — not inferred). Unset resolves to
   the default DB name, which is **Ben's shared dev DB (`jarv1s`)**. So every gate attempt this
   session — including the ones I believed were isolated — ran against the shared dev DB.
3. **Ben's shared `jarv1s` DB has rows written today at 11:05:16Z**: 3 rows in
   `app.ai_provider_configs` and 3 rows in `app.ai_configured_models`. This is `seedAiProviderChunk`
   writing into his dev DB before `test:uat-seed` failed.
4. **Correction to my earlier report**: I told the Coordinator attempt 1 created "≥2 synthetic
   solo-admin users." **That was wrong — there are no new users at all.** `seedSoloAdmin` was
   correctly *refused* by the ephemeral-DB guard (no user rows were written); only the AI
   provider/model chunk rows above were written, because that seed step ran before the guard
   caught it. Do not over- or under-report this number — the Coordinator has now logged the exact
   6 rows itself.

**Do NOT touch or clean Ben's `jarv1s` database.** Those 6 stray rows are Ben's to decide on; the
Coordinator has already logged them. Leave them alone.

## Build is DONE. Only the gate is outstanding.

All 5 tasks committed, in order:
- `3408c1ee` — T5 (sports manifest classification, prior relay)
- `5d66e3a9` — task#8, install-grant routing test for sports.followTeam
- `7919df2f` — T6, cross-actor RLS isolation (`tests/integration/sports-follows-tool-rls.test.ts`)
- `74a4b886` — T7 denylist check (`tests/unit/self-operation-manifests.test.ts`)
- `1c961a4b` — T7 SSRF post-redirect regression (`tests/unit/news-source-resolution.test.ts`)
- `935cd955` — fix: missing `truncated` field on the SSRF test's mock (typecheck fix)

Pre-push trio already run clean this session: `format:check`, `lint`, `typecheck` all pass.
`git fetch origin main && git rebase origin/main` already run — branch is already ahead of
`origin/main` (73e50847), nothing to rebase.

`tests/unit/news-source-resolution.test.ts` run standalone (no DB involved — pure mock
`db = {} as DataContextDb`): unpiped, real exit code 0, 7/7 pass including the new SSRF redirect
case. This is fully verified and settled, independent of the gate-DB confusion above.

**Coordinator already verified and closed T7's SSRF conclusion and the install-grant test** — do
not re-litigate:
- SSRF: existing containment CONFIRMED adequate via `acceptedFinalDomain` normalizing
  `fetched.finalUrl` (post-redirect) + `samePublisherIdentity` — refuses ANY cross-domain redirect,
  not just ones landing on a private IP. No prompt/tier change made. This requirement was absent
  from the plan doc and restored by the Coordinator from approved-spec lines 47-49 — say so in the
  PR body.
- Install-grant test asserts the exact emitted sequence (`toEqual(['action_result'])`), contrast
  with the #1263 spy-based test a reviewer called theater — mention this in the PR body.

**PR body must state these 4 things** (Coordinator's explicit ask, or Opus security review costs a
cycle):
1. SSRF conclusion as above (confirmed adequate, restored-from-spec framing, cross-domain-redirect
   property, no tier/prompt change).
2. Install-grant test asserts exact emitted sequence, contrast with #1263 spy.
3. Sports DELETE-route asymmetry and why it stays on the raw repository (from earlier plan
   context — re-derive from plan doc T6/T8 section if unclear, don't re-ask Coordinator).
4. Inventory counts 31/5/4=40 (exact `toBe`), and that #1264 rebases onto this branch rather than
   this branch loosening anything.

## THE ONLY REMAINING WORK: get one clean, VERIFIED-isolated full-gate run

**Coordinator's required procedure — follow exactly, in order. Do not skip step C.**

### A. Set the env var FIRST, verify it before running anything

```bash
export JARVIS_PGDATABASE=jarv1s_gate_1265b
echo "JARVIS_PGDATABASE=${JARVIS_PGDATABASE:?EMPTY}"   # must print a non-empty value, must not error
```
Run this as the literal first command of the session, before any `pnpm` command. Do not proceed if
the echo is empty or errors.

### B. Create the DB explicitly, DROP+CREATE before every single gate run — never reuse

```bash
docker exec jarv1s-postgres psql -U postgres -c "DROP DATABASE IF EXISTS jarv1s_gate_1265b;"
docker exec jarv1s-postgres psql -U postgres -c "CREATE DATABASE jarv1s_gate_1265b;"
```
If you need to re-run the gate for any reason (retry after a real fix, etc.), repeat this
drop+create immediately before — never invoke `test:uat-seed` or `verify:foundation` twice against
the same DB state.

### C. Verify isolation is REAL before trusting the run — do not just trust that you typed the export

Once the gate is running (specifically once `test:unit` or `test:uat-seed` has spawned its vitest
worker process), find the running node/vitest worker PID and read its actual environment directly:

```bash
pgrep -af vitest   # find the worker PID(s)
tr '\0' '\n' < /proc/<pid>/environ | grep JARVIS_PGDATABASE
```
Confirm it prints `JARVIS_PGDATABASE=jarv1s_gate_1265b` — the actual env of the actual running
process, not what you believe you exported in your own shell. This is the exact check the
Coordinator used to catch this session's error; do not skip it.

### D. Run the gate, capture the real exit code to a dedicated file, report the LITERAL command

```bash
export JARVIS_PGDATABASE=jarv1s_gate_1265b
( pnpm verify:foundation; echo "GATE_EXIT:$?" > /tmp/gate-1265b.rc ) > /tmp/gate-1265b.log 2>&1
cat /tmp/gate-1265b.rc   # <-- read THIS, not the log, for the exit code
```
This will take ~5-8 min (`test:unit` alone is ~160s, `test:integration` ~159 files, ~13 min in a
prior run — use a background Bash call + poll, or `Monitor`, not a blocking foreground call, which
hits the 10-min single-command cap).

When reporting to the Coordinator, **quote the exact literal command string you ran** (copy-paste,
not a paraphrase) so it can be checked directly, per Coordinator's explicit requirement.

### Outcomes

- **If `GATE_EXIT:0`**: proceed straight to `coordinated-wrap-up` (PR + report to Coordinator, with
  the 4 PR-body points above). Never merge/board/close yourself.
- **If `GATE_EXIT` non-zero**: do NOT start wrap-up. `grep -n "FAIL "` the log for failing file
  names ONLY (never paste the log body) and report those file names to Coordinator (re-resolve its
  pane fresh via `herdr pane list` first — label `Coordinator`), along with the literal command
  string used and the `/proc/<pid>/environ` verification output from step C. If a failure appears
  outside `tests/uat/seed/` (especially in sports/news/self-operation files), that would NOT be the
  known DB-state trap — escalate immediately with the specific file/test name.

## Everything else already settled — do not re-derive

See relay-5 doc (`docs/superpowers/handoffs/2026-07-27-1265-relay-5.md`) for the sports
composition-root-singleton pattern, the `DataContextDb` fake brand shape, `ToolContext` fields, and
the plan's task→line map — all still accurate, no need to re-read the plan doc again for anything
above; the only open item is the gate.

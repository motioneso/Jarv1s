# #1110 app-map — relay checkpoint 2

Successor to `2026-07-16-1110-app-map-relay.md`. That doc's step-½ verification (Tasks 1-5, 7
confirmed clean) still stands — do not redo it.

**Branch/worktree:** `build/1110-app-map` at `/home/ben/Jarv1s/.claude/worktrees/build-1110-app-map`
(reuse in place, `[ -d node_modules ] || pnpm install`).
**Plan:** `docs/superpowers/plans/2026-07-16-1110-app-map-plan.md` (8 tasks, headers at lines
51/244/428/540/1083/1538/1698/1775).
**Spec:** `docs/superpowers/specs/2026-07-16-1110-app-map-design.md`.
**Coordinator label:** `Coord-1109-1110` — confirm exactly one pane holds it via `herdr pane list`
before messaging (per `herdr-pane-message` skill).

## Done

- **Task 1 committed: `2ffadbf1`** "feat(app-map): define manifest grounding contracts" — files:
  `packages/module-sdk/src/index.ts`, `packages/shared/src/ai-types.ts`,
  `packages/shared/src/index.ts`, `packages/shared/package.json`,
  `tests/unit/app-map-contract.test.ts`. Adds `JarvisError`/`JarvisErrorClass`,
  `ModuleAiRequirementManifest`, `ModuleRemediationManifest`, `ModuleErrorManifest`,
  `ModuleFeatureManifest` to `@jarv1s/module-sdk`; moves `AiModelTier`/`AiModelCapability` canonical
  there, re-exported from `@jarv1s/shared`; makes `description` **required** on
  `ModuleNavigationEntryManifest`/`ModuleSettingsSurfaceManifest`; adds `features?` to
  `JarvisModuleManifest`. `tests/unit/app-map-contract.test.ts` has 2 passing tests (type-equality +
  fixture-satisfies). Verified: that test file green in isolation; **full-repo `pnpm typecheck` is
  RED** — expected, see below, Task 2 fixes it.

## Next: Task 2 (plan lines 244-427)

"Required descriptions and registration-time enforcement." Steps:

1. Re-add to `tests/unit/app-map-contract.test.ts` (currently only has Task 1's 2 tests): import
   `assertModuleRegistryConsistency`, `BuiltInModuleRegistration` from `@jarv1s/module-registry`;
   add `registration()`/`baseManifest()` helpers + test cases per plan lines ~244-320 (a prior pass
   drafted these then reverted them to keep Task 1 committable — redo from the plan text, don't
   assume the old draft was right).
2. Add exact `description:` string literals to nav/settings entries in: `packages/ai/src/manifest.ts`
   (~80-115), `packages/briefings/src/manifest.ts` (~35-60), `packages/calendar/src/manifest.ts`
   (~45-80), `packages/chat/src/manifest.ts` (~45-70), `packages/connectors/src/manifest.ts`
   (~60-90), `packages/email/src/manifest.ts` (~45-70), `packages/news/src/manifest.ts` (~85-115),
   `packages/settings/src/manifest.ts` (~36,44), `packages/sports/src/manifest.ts` (~53,63),
   `packages/tasks/src/manifest.ts` (~268,278), `packages/wellness/src/manifest.ts` (~64,74).
   Exact strings are given in plan lines 326-366 — use those, don't invent new ones.
3. Add `assertAppMapDescription`/`assertAppMapDeclarations` to `packages/module-registry/src/index.ts`;
   call `assertAppMapDeclarations(registration.manifest);` as the first statement inside
   `assertModuleRegistryConsistency`'s registration loop (function is present; grep it, don't assume
   line numbers — Task 1 shifted line numbers in module-sdk but not this file).

## Plan gap found — fold into Task 2, not a fork

After Task 1's required `description` field landed, `pnpm typecheck` also fails (independent of the
plan's Task 2 file list) on **7 test fixtures never mentioned anywhere in the plan** — grepped the
whole plan for these filenames, zero hits:

- `tests/integration/calendar-delete.test.ts` (~243, ~368, ~390)
- `tests/integration/email-reply-tools.test.ts` (~184)
- `tests/integration/focus-time.test.ts` (~137, ~649, ~757, ~777, ~862)
- `tests/integration/news-chat-tools.test.ts` (~120)
- `tests/integration/tasks-agency-tools.test.ts` (~47)
- `tests/unit/source-behaviors.test.ts` (~136, twice)
- `tests/unit/module-settings-ui-contract.test.ts` (~7)

Each constructs a raw manifest object literal missing `description` on a navigation/settings entry.
This is mechanical (add a short description string, no design decision) — already disclosed to the
coordinator as a non-blocking scope note, not awaiting approval. Add descriptions to these 7 files
and fold them into Task 2's commit alongside the plan's listed files; note the addition explicitly
in the commit message so it's traceable.

4. Run `pnpm vitest run tests/unit/app-map-contract.test.ts && pnpm typecheck`, confirm exit 0
   before committing.
5. Commit per plan's Step 5 file list **plus** the 7 fixture files above.

## Tasks 3-5, 7 — already verified clean, do not re-verify

Per original relay doc `2026-07-16-1110-app-map-relay.md`. Proceed straight to build when reached.

## Tasks 6, 8 — verify premises just-in-time, right before starting each

- **Task 6** (plan lines 1538-1697, "Emit structured News errors at origin surface"): before
  starting, re-check `packages/shared/src/news-api.ts` (`NewsSourcePreviewResponse`,
  `previewNewsSourceSchema`), `packages/news/src/personalization-routes.ts` (no-model /
  discovery-unavailable branches), `packages/news/src/settings/add-source.tsx` (plan claims plain
  `alert()`, no `ClosedWriteGate` — confirm still true, don't assume).
- **Task 8** (plan lines 1775-end, "Real #1000 UAT + narrative capability entry"): before starting,
  re-check `tests/uat/seed/{types.ts,chunks/ai.ts,levels.ts,cli.ts}`, `tests/uat/provisioner.ts`,
  `tests/uat/run-uat.ts`, `packages/news/src/routes.ts` + `personalization-routes.ts`
  (`previewOverride` insertion points), `docs/WHATS_NEW.md` top content.

## After all 8 tasks green

`pnpm verify:foundation` → pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck`) →
`git fetch origin main && git rebase origin/main` → `coordinated-wrap-up` (PR + report to
`Coord-1109-1110`). Never merge/board/close — coordinator's job.

## Process notes

- TDD strict: red → green → commit per task, `git add` exact file list only, never `-A`.
- Relay again on next context-meter 70% warning or compaction summary — commit first, write doc,
  spawn successor, message coordinator, request reap.
- Read plan/spec by section for the task at hand — never front-to-back.

# Relay 2: issue #1265 module-content-self-operation

Branch/worktree: `1265-module-content-self-operation` (this worktree, unchanged). node_modules
present, skip install.

Plan (coordinator-approved, do not re-litigate): `docs/superpowers/plans/2026-07-27-module-content-self-operation.md`

## State

- T1 DONE + committed (`e71f4f78` schema fix, `4a5794f5` plan doc). `tests/unit/news-manifest.test.ts`
  passes. Ran `tests/integration/news-chat-tools.test.ts` via plain vitest — it correctly refused
  (needs `pnpm test:integration`, not raw vitest, for its isolated DB). **Not yet verified via the
  real integration runner** — do that first thing on resume: `pnpm test:integration -- news-chat-tools`
  (check package.json for exact script name/filter syntax) and confirm the existing guidance-passing
  case still passes unmodified.
- T2-T9 NOT started. No sports code touched yet.

## Coordinator's approval message — binding, do not re-derive

1. **DELETE /api/sports/follows/:id stays exactly as-is** (calls `repository.remove(id)` directly,
   NOT routed through the service). Add a short why-comment at that route explaining REST already
   has the row id, so the service's catalogKey→id resolution is only needed by the tool — a future
   reader must not "tidy" this into breaking that path. This changes the plan's T3: no service
   routing for DELETE, comment-only.
2. **Condition 1 (binding, new task, #8 in task list):** the sports write tools declare
   `granted_at_install` while `sports_follows` family keeps `defaultTier: "ask_each_time"` — correct
   per the rule (grant promotes; default stays conservative). But this means the actual behavior
   depends on module install writing the `assistant.action_policy.v1.sports.sports_follows` row.
   #1263's security QA caught exactly this failure mode once: a test that drives the compatibility
   helper directly can stay green even if the real install-routing branch is deleted. **Must add a
   test that installs the sports module and asserts the policy row exists at the routing level**,
   not just a manifest-classification unit test. Find the equivalent test from #1263/#1268 for news
   (grep `assistant.action_policy` / `action_policy.v1` / module install + policy row assertions,
   likely in `tests/integration/` for settings or self-operation) and mirror it for sports.
3. **Condition 2 (binding, new task, #9):** explicitly assert `risk !== "destructive"` for
   `sports.followTeam`/`sports.unfollowTeam` in the same commit as their declaration —
   `policy.ts:36-37` makes `risk: "destructive"` never auto-run regardless of tier, so a
   `granted_at_install` + `risk: "destructive"` tool would silently prompt forever with nothing
   catching it. Fold this into T4's tests (or T5's self-operation-manifests block) rather than a
   separate file.
4. **T5 counts (29→31, 38→40) are correct as of current `main` — but #1264 is moving the same
   numbers.** Whoever lands second must rebase and recompute; never loosen to a range/toBeGreaterThan
   to dodge the conflict. Counting gotcha from coordinator: **People module declares its grants in
   `packages/people/src/tools.ts`, NOT a `manifest.ts`** — if the inventory-counting logic in
   `tests/unit/self-operation-manifests.test.ts` iterates `getBuiltInModuleManifests()` assuming
   every module's tools live in one place, double check People's tools are still being picked up
   correctly (this is existing behavior to verify, not something this plan changes).
5. T1 (guidance drop) confirmed "highest-value item, land first" — done.

## Next steps in order

1. Verify T1's integration test passes via the real runner (see above) — quick sanity check, not
   a new task.
2. T2: `packages/sports/src/sports-service.ts` — add `SportsFollowsWriter` interface, widen
   `SportsServiceDependencies.repository`, add `followTeam`/`unfollowTeam` methods. Full code
   already written in the plan doc's Task 2 — copy from there, don't re-derive. Update
   `tests/unit/sports-service.test.ts`'s `makeDeps()` fake repository per the plan. New test file
   `tests/unit/sports-service-follows.test.ts` (full code in plan).
3. T3: `packages/sports/src/routes.ts` — route `POST /api/sports/follows` through
   `service.followTeam` (plan's Task 3 code, adjusted: **do NOT touch DELETE, add why-comment
   instead** per binding condition 1 above).
4. T4: new `packages/sports/src/chat-tools.ts` (plan's Task 4 code), manifest.ts tool + family
   entries, module-registry wiring, update `sports-manifest.test.ts`. **Add the risk!=="destructive"
   assertion (condition 2) in this task's test, not deferred.**
5. T5: `tests/unit/self-operation-manifests.test.ts` sports describe block + count bump (plan's
   Task 5 code) — verify People-module counting still correct per the gotcha above before touching
   counts.
6. New task (condition 1): install-grant routing test for sports — find #1263's equivalent test
   first (grep before writing), mirror it.
7. T6: cross-actor RLS integration test (plan's Task 6, needs real harness read from
   `tests/integration/sports-follows-repository.test.ts` first — plan flags this explicitly).
8. T7: denylist assertion + news confirmSource regression (plan's Task 7 — check if the regression
   test already exists before adding one).
9. Pre-push trio + full gate, `coordinated-wrap-up`: PR, report to Coordinator pane (find it fresh
   via `herdr pane list`, label `Coordinator` — don't reuse a stale pane_id). Never merge/board/close.

## Rules still in force
- Commit per task, `git add` explicit paths only (not `-A`).
- Relay again at next 70% warning or compaction summary — act on it immediately, don't keep
  working past it (this relay itself is the correction for a prior session's delay).
- No further plan re-litigation — the plan + coordinator's binding conditions above are final;
  build from them.

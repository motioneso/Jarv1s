# News S4 relay13 — council rework: two test gaps on PR #977

You are the relay13 successor (**Fable**, per Coordinator instruction) on branch `news-s4`,
worktree `~/Jarv1s/.claude/worktrees/coord-2026-06-30-rfa-fleet/.claude/worktrees/news-s4`.
Resume via `coordinated-build` → finish with `coordinated-wrap-up`. Coordinator label
`Coordinator`, session `58a78927-385c-4b1d-8fa0-94db20255d6f` (resolve pane fresh by label,
verify session id; never trust a remembered pane number).

## State

- Tasks 1–10 DONE. **PR #977 open** (https://github.com/motioneso/Jarv1s/pull/977), branch pushed,
  rebased on `origin/main` @ `9af57f81`. Top commits: `089020a9` (0161 negative controls),
  `4d63b18f` (scannable manifest + sanctioned news→notifications edge), `01461bd9` (prettier).
- **Council verdict: NOT merged.** 2 of 3 lenses (Gemini + Codex) REJECT on TEST-COVERAGE only;
  Opus GREEN, all Hard Invariants confirmed. Code is correct; tests don't prove it.
- Mandate: fix exactly the TWO gaps below, **test-only** (no product/schema/migration change),
  keep every existing assertion, re-run full gates, update PR body with the sentinel approach,
  wrap-up, re-notify Coordinator for re-council. **You do NOT merge.**

## GAP 1 — Req C sentinel privacy scan (`tests/integration/news-revalidation.test.ts`)

Seed ONE distinctive sentinel string (e.g. `SENTINEL-975-PRIVATE-…`) into EACH of: the fixture
article body (FEED_BODY), the seeded source label, and the seeded topic guidance/query. Assert its
ABSENCE across THREE surfaces: (1) pg-boss payload, (2) **worker/process LOGS — the genuinely
missing surface** (capture log output and scan it), (3) notification body/title/metadata.
Non-vacuous: a deliberately-leaked sentinel MUST fail the scan (positive control test).
KEEP the existing `assertMetadataOnlyPayload` + exact `toEqual`/`toMatchObject` assertions.

Investigated facts (don't re-derive):

- Fixtures: `makeSource` label `"The Example Times"` ~:54; `makeTopic` label `"AI Safety"`,
  guidance `"focus on policy"` ~:69; `FEED_BODY` (RSS xml with headline + link) ~:114; `fetchOk`
  returns `body: FEED_BODY` ~:120. Another literal `payload: { label: "AI Safety", guidance:
  "focus on policy" }` ~:632.
- `seedValidationRows(fingerprint)` ~:339: raw-SQL INSERTs into `app.news_custom_sources`
  (label `'The Example Times'`, …) and `app.news_custom_topics` (label `'AI Safety'`, guidance
  `'focus on policy'`). Put the sentinel in these seeded strings + FEED_BODY so it flows through
  the real pipeline.
- Log-capture seams: `registerWorkers(overrides)` calls `registerNewsJobWorkers(workerBoss,
  workerContext, { fetch, search, ai, logger: {...}, notificationsRepository,
  revalidationLogger: overrides.revalidationLogger ?? {...} })`. The existing test "still succeeds
  and logs when the notification write fails" already captures `events: NewsRevalidationLogFields[]`
  via a `revalidationLogger` override — reuse that pattern; also capture the `logger` seam.
  Scan `JSON.stringify` of every captured log call for the sentinel.
- Target tests: ~:422 "enqueues a metadata-only, singleton payload" (queries `pgboss.job WHERE
  name = $1 AND state = 'created'`); ~:440 "revalidates drifted items and writes exactly one
  counts-only notification" (waitFor notification row; metadata keys ["kind","sourceCount",
  "topicCount"]).
- Worker log fields (packages/news/src/revalidation.ts): `news_revalidation_run` emits counts
  only; jobs.ts `news_notification_failed` logs error name + message.slice(0,200).

## GAP 2 — Req F external-content-as-text (settings pane adversarial render test)

`packages/news/src/settings/index.tsx` ~:451/:507 render `{source.label}` / `{source.canonicalDomain}`
/ `{topic.label}` / `{topic.guidance}` as plain spans (verified — no anchor in that range).
**Open question: locate the outbound-link render site** — check
`packages/news/src/settings/add-source.tsx` (AddSourceFlow preview/candidates likely renders
homepage/candidate links). Then add an adversarial test (extend
`tests/unit/news-settings-pane.test.tsx`, currently 15 tests): feed a source whose label is
HTML-entity/script-y (e.g. `&lt;img src=x onerror=…&gt;<script>` mix) AND whose URL is
`javascript:`/`data:`; assert the label renders as LITERAL plain text (no decode-after-strip
reordering — textContent equals the raw string, no element injection) and any outbound link's
scheme is dropped/neutralized (no `href` starting `javascript:`/`data:`). If NO anchor exists
anywhere (settings + add-source), assert that instead and say so in the PR body.

## Then (wrap-up)

1. `prettier --write` every touched test file before committing. Stage **explicit paths only** —
   NEVER `git add -A`; never stage `.claude/context-meter.log` or `docs/coordination/handoff-news-s4.md`.
2. Affected suites first, then full gates:
   `pnpm verify:foundation > /tmp/cb-vf.log 2>&1; echo "VF_EXIT=$?"` and
   `pnpm audit:release-hardening > /tmp/cb-audit.log 2>&1; echo "AUDIT_EXIT=$?"` (never pipe to tail).
3. Update PR body: `gh pr view 977 --json body` → add a "Sentinel approach (council re-run)"
   section describing the sentinel + three surfaces + positive control, so each lens can re-run it.
   `gh pr edit 977 --body-file <file>`.
4. Pre-push trio + `git fetch origin main && git rebase origin/main`, push.
5. Message Coordinator (herdr-pane-message, caveman-terse): gaps fixed, exit codes, PR updated,
   ready for re-council. Then STOP.

## Traps

- fast-json-stringify strips undeclared response fields — not in play here (test-only), stay alert.
- foundation.test.ts asserts the FULL migration list — no migration changes allowed anyway.
- Herdr reads: `--source recent --lines 12`. Busy agent queues messages = delivered, don't resend.
- Relay at meter 70% warning → next Fable successor, same worktree, point at this doc + your delta.

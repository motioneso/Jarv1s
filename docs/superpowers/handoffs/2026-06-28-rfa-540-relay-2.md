# Relay 2 — rfa-540-safe-automation-audit-log

**Relay reason:** context at 70%+ after writing plan — relaying before compaction.
**Branch / worktree:** `rfa-540-safe-automation-audit-log` / `~/Jarv1s/.claude/worktrees/rfa-540-safe-automation-audit-log`
**Spec:** `docs/superpowers/specs/2026-06-28-safe-automation-audit-log.md`
**Plan:** `docs/superpowers/plans/2026-06-28-rfa-540-safe-automation-audit-log.md` ← **READ IN FULL**
**Coordinator label:** `Coordinator` (session id: `5e1a6b62-a480-4b5c-9706-e476cfe77044`, pane `w1:p59`)
**Migration slot:** placeholder `XXXX` in filename; coordinator assigns **0128** at merge.

## What's done

- `pnpm install` complete (node_modules present — skip)
- Spec verified against branch: all premises current (no audit log code exists anywhere)
- All relevant source read: gateway, policy, repository, routes, SQL patterns, db types, manifest, module-registry, briefings/jobs, worker, foundation.test.ts, data-export, query-keys, client, settings-page
- **Plan written** to `docs/superpowers/plans/2026-06-28-rfa-540-safe-automation-audit-log.md` (12 tasks + self-review)
- **Coordinator notified** with message: "plan ready for rfa-540 — approve or flag"
- **WAITING FOR COORDINATOR APPROVAL** — do NOT write code until approved

## What's next (START HERE)

**STEP 1 — CHECK FOR APPROVAL.**
Run `herdr pane read --label Coordinator --source recent --lines 12` and look for coordinator's reply.

- If APPROVED → proceed to STEP 2
- If NOT YET → wait and re-check periodically; message coordinator "[RFA-540] still waiting on plan approval" if >10 min

**STEP 2 — Build per plan (only after coordinator approval).**
Resume via `coordinated-build` skill.
Use `superpowers:test-driven-development`. Commit green per task. Explicit `git add` paths only — never `git add -A`.

Task order:

1. SQL migration (`packages/ai/sql/XXXX_jarvis_action_audit_log.sql`)
2. DB types (`packages/db/src/types.ts`)
3. Repository methods (`packages/ai/src/repository.ts`)
4. AI jobs module (`packages/ai/src/jobs.ts` — new)
5. Manifest + module-registry updates
6. Gateway wiring (`packages/ai/src/gateway/gateway.ts`)
7. Shared types + manifest route + API route
8. Data export (`packages/settings/src/data-export.ts`)
9. Frontend client + query keys
10. Settings Activity pane (new `apps/web/src/settings/settings-activity-pane.tsx`)
11. Wire pane into settings-page
12. Tests (foundation.test.ts + new integration tests)

**STEP 3 — Wrap up.**
`coordinated-wrap-up` → PR → report to coordinator.

## Key architectural decisions (embedded in plan)

- `recordAudit` private method on `AssistantToolGateway`; called at THREE sites:
  1. `callTool()` auto-run path — after `runHandler()`, only when `risk !== "read"`
  2. `confirmAndRun()` denied/cancelled/timeout branch — before return
  3. `confirmAndRun()` confirmed+ran branch — after `runHandler()`
- Audit failure NEVER changes action result — try/catch + swallow in `recordAudit`
- pg-boss purge job in new `packages/ai/src/jobs.ts`; registered via `registerAiMaintenanceWorkers`
- Module-registry AI entry needs `queueDefinitions: AI_QUEUE_DEFINITIONS` + `registerWorkers: (boss, deps) => registerAiMaintenanceWorkers(boss, deps.rootDb)`
- Purge uses raw Kysely (rootDb) — calls SECURITY DEFINER function, no actor context needed
- FORCE RLS + ENABLE RLS; `jarvis_app_runtime` gets SELECT+INSERT only (no UPDATE/DELETE)
- DataContextDb for all runtime reads/writes; assertDataContextDb guard in all repo methods
- Migration named `XXXX_jarvis_action_audit_log.sql` — coordinator assigns 0128 at merge

## Key invariants

- Metadata-only rows — no input_summary, content, prompt, secrets ever stored
- Only gateway writes the table; no other module/route/job can bypass
- Do NOT touch: packages/chat, packages/shared/src/chat-api.ts, packages/people, packages/briefings
- Lane DB for integration tests: `JARVIS_PGDATABASE=jarvis_build_540 pnpm verify:foundation`
- Never git add -A; explicit paths only

## Collision notes

- Migration 0128 is reserved slot; use XXXX placeholder during dev
- Parallel in-flight: #538 (packages/people), #539 (packages/notes), #541 (data freshness)
- You own `packages/ai/src/gateway/` exclusively

## Gate command

```bash
pnpm format:check && pnpm lint && pnpm typecheck
pnpm test:ai && pnpm test:settings && pnpm test:api && pnpm test:web
JARVIS_PGDATABASE=jarvis_build_540 pnpm test:integration
```

# #1110 app-map relay — checkpoint 22

Branch `build/1110-app-map` @ `fe032169` (clean, pushed to origin). PR #1122. `node_modules` present — do NOT `pnpm install`.

## Mandate (from Ben via coordinator, still standing)

Root-cause TWO CI failures on PR #1122, fix, push, fresh VF, report with real evidence.
**No `ci.yml` timeout-minutes edits, no blind reruns, no widening.**

## Task #27 — auth-settings.test.ts (13/23 CI fail) + auth-bootstrap-recovery.test.ts (5/5 CI fail), ~10.9-13.7s, "pg-boss timeout signature"

**Root cause found this segment (NOT yet fixed).** The #1124/#1128 `connectionTimeoutMillis: 25_000` override on `boss` is correctly wired — traced pg-boss@12.18.2 source end to end, no gap. The real suspect is a **different, unrelated** client: both files also build
`appDb = createDatabase({ connectionString, maxConnections: 1 })` — **no `connectionTimeoutMillis` override**. `createDatabase()` (`packages/db/src/database.ts:14-26`) defaults that to `JARVIS_DB_CONNECT_TIMEOUT_MS ?? 5000`; that env var is unset everywhere in CI. With `max: 1`, node-postgres applies `connectionTimeoutMillis` to *queued* `pool.connect()` waiters too, not just new physical connections — so any 2 overlapping queries against `appDb` during `createApiServer()`'s `server.ready()` boot can produce a 5000ms-ceiling timeout whose error shape is easily mistaken for pg-boss's (same underlying `pg.Pool` mechanism). Two stacked ~5s waits plausibly explains the 10.9-13.7s clustering.

Full trace + citations: `memory_smart_search("Task #27 pg-boss appDb maxConnections")`.

**Next step (not started):** reproduce — bump `appDb`'s `connectionTimeoutMillis` (and/or `maxConnections` above 1) in both test files, rerun locally/CI, confirm failure disappears. Then apply the real fix (widen `appDb`'s timeout/pool size, test-infra only — zero prod code touched, same pattern as the existing `#1124` comment for `boss`).

## Task #28 — app-map.json missing-artifact theory (`docs/coordination/AWAITING-BEN.md`, branch `coord/settings-host-cleanup@48f97e0e`, not mergeable into build branches — read via `git show 48f97e0e:docs/coordination/AWAITING-BEN.md`)

**Confirmed real, independent bug** (unrelated to #27 — do not try to unify them, that hypothesis was tested and rejected this segment): `loadAppMap()` (`packages/settings/src/app-map.ts:27-33`) unguarded `JSON.parse(readFileSync(...))` throw, called unconditionally from `registerBuiltInApiRoutes` (`packages/module-registry/src/index.ts:2090`) on every `createApiServer()` boot. `build:app-map` is wired only into `apps/api/package.json`'s `dev`/`start` — confirmed **absent** from `verify:foundation` and from `.github/workflows/ci.yml` (line 42 is a bare `pnpm verify:foundation`). Fresh CI checkout has no `dist/app-map.json` (gitignored) → throws. Local green is a stale leftover artifact.

**The AWAITING-BEN.md "vitest retries with backoff → ~11.1s × 46 files" explanation is UNVERIFIED and probably wrong** — grepped `vitest.config.ts` + `scripts/test-integration.ts` for `"retry"`: zero hits, no retry/backoff config exists. A sync throw in `beforeAll` fails fast, not after ~11s. Flag this gap explicitly when reporting — don't claim the mechanism is proven.

**Fix (authorized by Ben, not yet applied):** wire `build:app-map` into `verify:foundation`'s chain (or a global `pretest`/`globalSetup`). Do this on its own merits regardless of whether it explains the 46-file pattern.

## Next actions

1. Reproduce #27 locally (see above), confirm, apply minimal fix to both test files.
2. Apply #28 fix: add `build:app-map` step to `verify:foundation` chain in root `package.json`.
3. Push, run fresh full VF (`test:uat-seed` will still hit the known pre-existing #1087 guard.test.ts flake — that's expected/unrelated, don't chase it; run `test:integration` separately if the chain halts there).
4. Report to coordinator via `herdr-pane-message` — **re-resolve the coordinator's pane label fresh** via `herdr pane list` first, do not trust any previously-cached label.
5. Hold for coordinator instruction after reporting.

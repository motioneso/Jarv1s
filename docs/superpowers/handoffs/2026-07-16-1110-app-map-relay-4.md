# #1110 app-map — relay checkpoint 4

Successor to `2026-07-16-1110-app-map-relay-3.md`. Task 3 now done. Task 4 build **paused on an
open escalation** — read that section before writing any code.

**Branch/worktree:** `build/1110-app-map` at `/home/ben/Jarv1s/.claude/worktrees/build-1110-app-map`
(reuse in place, `[ -d node_modules ] || pnpm install` — should already exist, skip).
**Plan:** `docs/superpowers/plans/2026-07-16-1110-app-map-plan.md` (headers at lines
51/244/428/540/1083/1538/1698/1775). Task 4 section: lines 540-1083.
**Spec:** `docs/superpowers/specs/2026-07-16-1110-app-map-design.md`.
**Coordinator label:** `Coord-1109-1110` — confirm exactly one pane holds it via `herdr pane list`
before messaging (per `herdr-pane-message` skill). Last confirmed: pane `w1:pRV` (resolve fresh —
pane numbers reflow), session `ed4b90d8-5e7f-40b1-b395-5e2beb2bd006`.

## Done

- **Task 1 committed: `2ffadbf1`**, **Task 2: `432fe68e`** (see relay-2/-3 docs for detail).
- **Task 3 committed: `25e44f86`** "feat(news): drive model availability from manifest" — files:
  `packages/news/src/manifest.ts` (`newsAddSourceRequirement` const + `features` array on the
  manifest), `packages/news/src/index.ts` (export), `packages/module-registry/src/index.ts`
  (News `hasJsonModel` closure now consumes `newsAddSourceRequirement.{service,capability,tier}`
  instead of hardcoded literals), `tests/unit/app-map-contract.test.ts` (identity test using
  `toBe`). Verified: `pnpm vitest run tests/unit/app-map-contract.test.ts tests/unit/news-routes.test.ts`
  (38 passed) + `pnpm typecheck` exit 0.

## RESOLVED — Task 4 settings-page.tsx drift (identity section)

**Coordinator (`Coord-1109-1110`) APPROVED option (a) — proceed, no further check needed.**

Verdict: drop `"identity"` from `CORE_APP_SETTINGS` (`packages/shared/src/app-map-core.ts`) and
skip the `"identity"` entry in `ADMIN_SECTIONS` entirely. Describe registration policy truthfully
under the existing `"people"` section (the `getRegistrationSettings`/`putRegistrationSettings`
"Allow new registrations" toggle already lives in `PeoplePane`, in
`apps/web/src/settings/settings-admin-panes.tsx`). Do **not** build an `IdentityPane` — new UI is
out of #1110 scope (spec-before-build hard invariant; spec never mentions "identity").

**General rule for Task 4 (coordinator's words, binding):** the app-map MUST mirror CODE reality,
not the plan's Task-4 literals. Use the real section ids, real labels (e.g. `profile` =
"Account & preferences", not the plan's "Profile & account"), and the real grouped structure
(`flattenSettingsGroups` over `ADMIN_GROUPS`/`PERSONAL_GROUPS`). Reconcile ALL drifted plan
labels/sections to what actually exists in `settings-page.tsx` — inventing sections defeats the
feature (spec anti-hallucination §3/§4). This is an in-scope plan-bug fix, not a new spec.
Confirmed on-branch by coordinator: `IdentityPane` = zero refs in `apps/web/src`; `AdminSectionId`
is exactly `people|aiproviders|instmods|audit|oversight|host` (6, no identity); `ADMIN_SECTIONS` is
`flattenSettingsGroups(ADMIN_GROUPS)`, not a flat literal — read the actual current
`PERSONAL_GROUPS`/`ADMIN_GROUPS` arrays in `settings-page.tsx` for the real id/label list rather
than trusting the plan's snippet or the partial list drafted in the prior (open-escalation) version
of this doc.

## Efficiency (carry forward — copy this section into every future relay doc)

Coordinator guidance (agent 3 didn't propagate it — CARRY THIS FORWARD, don't drop it again):
1. Read the plan ONLY for the current task — never re-read the whole 2162-line file or completed tasks.
2. Trust the relay doc's "verification done" — do NOT re-run spec-vs-branch checks; agent 1 verified all 8, no drift (except where a later relay doc explicitly reopens one, e.g. this doc's Task 4 note above).
3. Keep relay docs lean: state + next task + gotchas, not a plan restatement.
4. Aim 2-3 green tasks per context; relay at ~70% meter, not earlier.

## Task 4 (plan lines 540-1083) — other premises verified this session, still valid

- None of `packages/shared/src/app-map-core.ts`, `scripts/build-app-map.ts`,
  `tests/unit/app-map-build.test.ts` exist yet — all create-new per plan.
- `packages/shared/src/ai-types.ts` already defines/re-exports `AiModelCapability`/`AiModelTier`
  from `@jarv1s/module-sdk` — plan's `app-map-core.ts` import of these from `./ai-types.js` is valid.
- `packages/module-registry/src/index.ts` exports `getBuiltInModuleManifests()` already (confirmed,
  ~line 1811) — plan's `scripts/build-app-map.ts` import is valid as written.
- `apps/web/src/app-route-metadata.ts`: `todayNavEntry` (line 33) is also consumed elsewhere
  (relay-1 doc flagged `shell/command-palette-model.ts` as a second usage — re-check that import
  still resolves after deriving `todayNavEntry` fields from `CORE_APP_SCREENS`, since the core
  screen declaration has no `icon`/`order` fields — keep those two hardcoded, only derive
  `id`/`label`/`path` from the core declaration per plan's loose instruction, it's not a full
  literal snippet for this file).
- Root `package.json`: **no** `build:app-map` script yet (create). `build:api` is currently
  `"tsx scripts/build-app.ts api"` already (i.e. already at the plan's END state before the plan's
  own "add prefix then remove it" churn — you can skip that churn and go straight to the final
  form: add the `buildTarget` guard in `scripts/build-app.ts`, add the `build:app-map` script, leave
  `build:api` as-is, no back-and-forth edit needed).
- `apps/api/package.json` `dev` is currently `"tsx watch src/server.ts"` (needs the
  `pnpm --dir ../.. build:app-map &&` prefix per plan).
- `scripts/build-app.ts`: `buildTarget()` function at line 76; insertion point plan describes
  ("immediately before esbuild in buildTarget", target === "api" branch) not yet inspected in
  full — read that function before editing.

## Tasks 5, 7 — already verified clean, do not re-verify

Per original relay doc `2026-07-16-1110-app-map-relay.md`. Proceed straight to build when reached.

## Tasks 6, 8 — verify premises just-in-time, right before starting each

Unchanged from relay-2/-3 docs:
- **Task 6** (plan lines 1538-1697): re-check `packages/shared/src/news-api.ts`,
  `packages/news/src/personalization-routes.ts`, `packages/news/src/settings/add-source.tsx`
  (plan claims plain `alert()`, no `ClosedWriteGate` — confirm still true).
- **Task 8** (plan lines 1775-end): re-check `tests/uat/seed/{types.ts,chunks/ai.ts,levels.ts,cli.ts}`,
  `tests/uat/provisioner.ts`, `tests/uat/run-uat.ts`, `packages/news/src/routes.ts` +
  `personalization-routes.ts` (`previewOverride` insertion points), `docs/WHATS_NEW.md` top content.

## After all 8 tasks green

`pnpm verify:foundation` → pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck`) →
`git fetch origin main && git rebase origin/main` → `coordinated-wrap-up` (PR + report to
`Coord-1109-1110`). Never merge/board/close — coordinator's job.

## Process notes

- TDD strict: red → green → commit per task, `git add` exact file list only, never `-A`.
- Relay again on next context-meter 70% warning or compaction summary — commit first, write doc,
  spawn successor, message coordinator, request reap.
- Read plan/spec by section for the task at hand — never front-to-back.
- This relay triggered at the 70% context-meter warning, mid-Task-4 verification (before any
  Task 4 code was written) — clean state, nothing uncommitted except this doc.

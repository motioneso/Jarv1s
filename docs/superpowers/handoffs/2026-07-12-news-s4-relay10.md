# News Slice 4 — relay continuation #10 (issue #975, epic #954)

Tenth relay successor, same worktree/branch `news-s4`. Plan:
`docs/superpowers/plans/2026-07-12-news-s4-chat-actions-revalidation-notifications.md` — read BY
TASK only (T8=:301, T9=:318, T10=:335). Never the spec in full. Coordinator label `Coordinator`
(session authority `58a78927-385c-4b1d-8fa0-94db20255d6f`), caveman-terse via `herdr-pane-message`.
Plan approval GRANTED — do not re-ask. Directives: ONE summary notification (done, T4); T9 = wire
EXISTING Settings scaffolds + Retry button only; 6 chat tools total, source edit + exclusion
removal stay REST-only. Build per `coordinated-build` + TDD; commit green per task; explicit-path
`git add`; relay ONLY at 70% meter warning, successor MUST be Fable.

## State: Tasks 1–7 ✅. Resume at Task 8 RED

Task 7 committed `b0cdfa87` (`feat(news): assistant preview/confirm source tools (#975)`), 4/4
chat-tool tests + 7/7 REST regression green, lint/typecheck 0.

## Verified facts from Task 7 (do NOT re-derive)

- `packages/news/src/chat-tools.ts` (175 lines — READ IT FIRST, it is the pattern for T8):
  deferred-deps seam (`configureNewsChatTools`, `requireDeps()`); deps already include
  `repository: NewsPersonalizationStore`, `boss: PgBoss|null`, `previews`, `discovery`,
  `availability`. T8's four executes go in this file; benign failures return
  `{data:{error:"...")}}`, never throw; security violations THROW (gateway → sanitized
  `Tool <name> failed` + audit `failed`).
- `personalization-routes.ts` exports `confirmSourceFromPreview`, `NewsPersonalizationStore`,
  `NewsSourcePreviewStore`, `PersonalizationRouteDependencies`, `triggerNewsRefresh(scopedDb,
repository, boss, actorUserId, afterBump?, logger?)`. T8 helpers (`cleanTopic`,
  `validateTopic`, `pruneSnapshotDomain` afterBump, `normalizePublisherDomain`) live in/near this
  file or `discovery/` — grep export status first; if local, export them (mirror the
  `confirmSourceFromPreview` extraction — REST behavior must stay byte-identical).
- `manifest.ts` assistantTools: three entries exist (`news.topHeadlinesToday`,
  `news.previewSource`, `news.confirmSource`). T8 adds four write tools after them: risk `write`,
  NO `actionFamilyId` (never auto-approvable), permissionId `news.prefs`, each with `summarize`
  from INPUT only.
- CYCLE TRAP (cost this session hours): `NEWS_MODULE_ID` lives in `module-id.ts`; jobs.ts imports
  it from there, manifest re-exports it. NEVER add an import that closes
  manifest→chat-tools→personalization-routes→jobs→manifest — under tsx the cycle leaves manifest
  `execute` bindings `undefined` and the gateway silently filters the tools
  ("Tool not available"). If a new tool shows that error, check import cycles first.
- `routes.ts` boots the wiring: ONE `createPreviewStore()` shared by REST + chat;
  `configureNewsChatTools` called before `registerNewsPersonalizationRoutes`.
- Test harness: `tests/integration/news-chat-tools.test.ts` has the full gateway setup, confirm/
  approve flow, and `parseToolText` (strips the `<tool_result>` envelope). EXTEND it for T8; do
  not weaken existing assertions. Focused run:
  `pnpm exec tsx scripts/test-integration.ts tests/integration/news-chat-tools.test.ts`.
- Secrets rule: `validationFingerprint`/`feedUrl` NEVER in tool output (T7 already complies).

## Task 8 design (from plan :301 — re-read the section, then build)

Four tools, execute via existing repository/route helpers only:

- `news.removeSource {sourceId}` → find in `listCustomSources` (friendly not-found; cross-owner id
  must behave as not-found with NO effect) → `deleteCustomSource` → `triggerNewsRefresh` with
  `pruneSnapshotDomain` afterBump (mirror DELETE route).
- `news.addTopic {label, guidance?}` → `cleanTopic` → `hasWebSearch` gate → `validateTopic`
  (unavailable/rejected → friendly errors) → `createCustomTopic` → refresh; map
  `NewsPersonalizationLimitError`/`NewsDuplicateSourceError` to friendly strings.
- `news.removeTopic {topicId}` → `deleteCustomTopic` + refresh.
- `news.addExclusion {domain}` → `normalizePublisherDomain` → `createExclusion` → refresh.

RED first: each tool confirm-gates (pending request, no write before confirm; after confirm → row
- audit `approval_mode=confirmed`); limit breach → friendly error, no row; removeSource cross-owner
  → not-found + positive control. Commit:
  `feat(news): assistant topic/exclusion/removal tools (#975)` + user-facing summary line. Then T9
  (:318 — EXISTING scaffolds + Retry only), T10 (:335).

## Traps (carried)

- `ai-tools.test.ts` ~:458 asserts FULL queue list — no new queues. NO new `ALLOWED_PAYLOAD_KEYS`
  (`packages/jobs/src/pg-boss.ts:76`). fast-json-stringify strips undeclared REST response fields
  (chat-tool output NOT route-serialized). `herdr pane read` MUST use `--source recent
--lines 12`. Prettier every doc AND test file before committing. Never `sed -i` a file you'll
  Edit. agentmemory smart_search returns empty here — don't block. Pre-push gate:
  `pnpm format:check && pnpm lint && pnpm typecheck` + rebase origin/main. T10/PR body must state
  migration 0161 RLS approach (column-scoped worker grants, owner-scoped policy) + notification
  privacy test, and note if no e2e news settings spec exists (don't invent a harness).
- `docs/coordination/handoff-news-s4.md` is coordinator-owned — never `git add` it. Stage explicit
  paths only; shared tree.

## Boot

1. `[ -d node_modules ] || pnpm install` (no-op expected).
2. Message Coordinator: "relay #10 driving, Task 8 RED starting"; ask reap of relay9 pane (label
   `News S4 relay9`, session `25434003-1503-4650-9fe8-f4cc6eb1126b`).
3. Read the plan Task 8 section (:301) + `chat-tools.ts` + the test file, then BUILD. Reading is
   not progress.

# Handoff — Slice D IMAP Send (R3 → R4)

**Branch:** `coord/647-imap-send`  
**Worktree:** `~/Jarv1s/.claude/worktrees/647-imap-send`  
**Issue:** #647  
**Risk tier:** security  
**Status:** Tasks 1-6 complete, green, committed. Task 7 ready.

## Completed (R1-R3)

- ✅ Task 1-3: EmailWriteProvider abstraction, Google/IMAP providers, dispatcher
- ✅ Task 5: IMAP threadId derivation (Message-ID/References headers)
- ✅ Task 6: Unit tests for providers (16 tests passing, typecheck green)

## Task 7: GreenMail integration tests

**Goal:** Test IMAP provider against real GreenMail server in Docker.

**Requirements:**
- Start/stop GreenMail container programmatically
- Test saveDraft (APPEND to \Drafts) with real IMAP protocol
- Test send (SMTP + APPEND to \Sent) with real SMTP/IMAP
- Verify credentials never leak in responses/logs
- Verify threadId headers preserved in saved messages
- Clean shutdown (container removal)

**Reference:** See existing GreenMail patterns in codebase (search for GreenMail usage)

**Constraints:**
- SecurityQA + Ben sign-off required before merge (Task 9)
- No production code changes except test infrastructure
- Explicit git add paths only (no git add -A)
- Test files in `tests/integration/`

## Files of interest

- `packages/connectors/src/imap-email-write-provider.ts` (GREEN - needs integration testing)
- `tests/unit/imap-email-write-provider.test.ts` (GREEN - 9 tests)
- `packages/email/src/reply-mime.ts` (GREEN - IMAP threadId derivation)

## Next exact step (R4)

1. Find existing GreenMail test patterns in codebase
2. Create `tests/integration/imap-email-write-provider.integration.test.ts`
3. Implement GreenMail lifecycle (start/stop/cleanup)
4. Test saveDraft + send with real protocol
5. Clean git commit with explicit paths
6. Continue to Task 8 (security QA)

(End of handoff)
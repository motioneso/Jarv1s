# Relay 14 — #1265 module content self-operation

**Read this doc only.** Do not read relay-9/relay-12/relay-13 front-to-back.

- Worktree/branch: `1265-module-content-self-operation` (this dir), `node_modules` present, skip install.
- PR **#1273** OPEN, https://github.com/motioneso/Jarv1s/pull/1273. Do not open new, do not merge.
- Coordinator label `Coordinator`, agent name `coord-1262`. **Resolve pane fresh** via `herdr pane
  list`/`herdr agent list` before messaging. Talk caveman.
- Issue #1265, epic #1262. Risk tier: **security**.
- All 4 code findings are DONE and committed, tree clean except this doc + the PR-body draft file
  (also new, untracked): commits `aadf6c73` (ESPN timeout), `42966446` (news maxLength),
  `56c33266` (u-flag docstring+test, filed #1274), `6314a847` (trust-boundary comment, filed
  #1275). **Coordinator already reviewed and ACCEPTED all four in the tree directly** (message
  received this relay) — do not re-litigate them.

## What's left (in this exact order — Coordinator was explicit about order)

1. **Coordinator's one addition to the PR body**, already drafted: the ESPN `fetchJson` timeout
   is on the SHARED fetch chokepoint, so it also bounds ordinary ESPN read paths (schedules,
   scores, league team lists), not just `followTeam`'s write-path roster check. Read-path
   trade-off: an ESPN response that would have succeeded past 8s now degrades to empty/stale
   instead of waiting — "a merely slow scoreboard becomes an empty scoreboard." Coordinator
   approves this trade but wants it named in the PR body, not left for a reviewer to find in the
   diff. **Already written into the draft** at
   `docs/superpowers/handoffs/2026-07-27-1265-pr1273-body-draft.md` (full replacement body, ready
   to paste — see its "Five things worth calling out explicitly" section item 1-5 plus the "Post-QA
   follow-up" section which has the ESPN read-path paragraph already in it).
2. **Fresh gate — NOT YET RUN.** Postgres is a Docker container, not local `psql`/`pg_ctl`:
   container name `jarv1s-postgres` (confirmed via `docker ps`; do not confuse with
   `jarv1s-prod-postgres-1`, never touch prod). Use whatever the repo's normal
   drop/create-database convention is (grep `package.json` `db:` scripts / `pnpm --filter` docs, or
   `docker exec jarv1s-postgres psql -U postgres -c "DROP DATABASE IF EXISTS jarvis_gate_1265e;
   CREATE DATABASE jarvis_gate_1265e;"`) — **do NOT reuse `jarvis_gate_1265d`** (relay-13/prior PR
   body already cites that name as used) or any prior name; use a brand new one, e.g.
   `jarvis_gate_1265f` if `...e` is already claimed, check first.
   - `JARVIS_PGDATABASE=<new-name> pnpm verify:foundation` — capture real exit code, never through
     `tail`/`head`/`grep` as the final pipe stage.
   - `JARVIS_PGDATABASE=<new-name> pnpm audit:release-hardening` — same.
3. **Pre-push trio:** `pnpm format:check && pnpm lint && pnpm typecheck`.
4. **Rebase:** `git fetch origin main && git rebase origin/main`.
5. **Push** to `origin/1265-module-content-self-operation`.
6. **PATCH the PR body** — fetch current live body fresh first (`gh pr view 1273 --json body -q
   .body`, don't assume nothing else changed), then reconcile with the draft file (draft was
   written against the body as of this relay's start — check for drift before overwriting), then
   `gh api repos/motioneso/Jarv1s/pulls/1273 -X PATCH -F body=@<path>` (capital `-F`, reads the
   file — `-f` would set the literal string `@path`).
7. **Report to Coordinator** (`coord-1262`, re-resolve pane fresh): all 4 findings already
   accepted by Coordinator directly (no need to re-report those), PR body updated with the
   ESPN-read-path callout, gate DB name + both exit codes, pre-push trio clean, rebase status, push
   confirmed (commit sha).

## Hard reminders from Coordinator (still stand)

- PR does **NOT merge tonight** — security tier, parks on Ben's hands-on LAN UAT pass.
- Do **NOT** describe the UAT `test.fixme` blocks as discharging the spec's exit criterion — the
  draft body's item 1 already states this correctly (unmet, plainly), don't soften it.
- Do **NOT** assert which lane (this one or #1264) rebases the self-operation inventory count
  assertion — still unsettled, leave as-is in the draft (draft already hedges this correctly).
- Do not merge, move board, or invoke `coordinated-wrap-up` unless Coordinator asks again — this
  round's finish line is the report.

## Model

Boot on **Sonnet** explicitly (`--model sonnet`).

## Relay note

Relayed at context-meter 70% warning, immediately after receiving Coordinator's accept message +
one PR-body addition request — zero gate/push work done yet this relay (psql wasn't on PATH,
about to pivot to `docker exec` when the meter fired). PR body fully drafted and saved to
`docs/superpowers/handoffs/2026-07-27-1265-pr1273-body-draft.md` (untracked, not yet committed —
successor should commit it alongside this doc). Successor should go straight to gate → push → PATCH
→ report, in that order per Coordinator's explicit instruction.

# Handoff — JS-01 Module skeleton · clean-slate reset · landing · full-screen chat (task #1232)

**You are the JS-01 build worker.** Model: gpt-5.6-luna @ xhigh. Build **only Phase JS-01**.
JS-00 (surface-scoped chat sessions) is already merged on this branch — build on top of it.

## Authoritative refs (read in full first)
- Plan: `docs/superpowers/plans/2026-07-22-job-search.md` → **Phase JS-01** (scope, tasks,
  invariants, tests, and the `⛔ UAT GATE JS-01 (Ben)` checklist). This is your contract —
  follow its six tasks exactly.
- Spec: `docs/superpowers/specs/2026-07-22-job-search.md` — design **§1 landing** and
  **§2 full-screen chat** are the visual contract for Tasks 4–5.
- GitHub task: **#1232** (Part of epic #1230).
- **Format template:** the **finance** module (`external-modules/finance/`) is the shape
  reference for manifest / worker / web seams. Finance is not fully validated — **prove each
  platform seam when you first touch it, never presume it works.**

## What JS-01 is
A fresh user opens **Job Search** in the nav → authored landing page → "Start a new search" →
chats in the module's **own isolated full-screen session**. Six tasks: (1) clean-slate reset
of `external-modules/job-search/` + idempotent `job-search.reset` reconcile job; (2) manifest
v1 (KV namespaces, nav entry, `profiles.list` read tool, **no owned tables**, excluded from
core image); (3) worker skeleton (finance shapes: WorkerPorts / wrap / validate);
(4) web skeleton + landing (spec §1: returning-user keyline grid + first-run hero, skeletons
while loading); (5) full-screen chat page (spec §2) driven by the **host chat routes with
`surface: "job-search"`** — this is the JS-00 seam; seed once per surface-session with the
job-search opener (resume-first); (6) web run-now plumbing (finance `invokeTool`/`runQueue`).

## Guardrails (blockers if violated)
- **Never reference the prior job-search implementation.** The reset replaces
  `external-modules/job-search/` wholesale; the old tree is NOT a design or code reference.
- **Module isolation:** module code imports only `@jarv1s/module-sdk`; the web bundle carries
  no own React and no `@jarv1s/*` runtime leaks; never import host internals or query host
  tables.
- **Isolation is live:** the chat page rides host routes with `surface: "job-search"` — do not
  spin a second chat engine. Absent-surface still means `"drawer"`.
- **Private by default:** all KV namespaces user-scoped, riding existing owner-only RLS.
- **Design system — Park Press:** token vars from `apps/web/src/styles/tokens.css` only (no
  hex literals in module CSS), serif headings / mono eyebrows / sans body, `jds-*` + local
  primitives, authored empty/loading/error states (skeletons, **no spinners**), no curved
  accent left-border cards. React Query keys `["job-search", …]`.
- **AI:** none used in JS-01 (the chat page rides the host engine). Do not hardcode a provider.
- Respect all CLAUDE.md hard invariants. House rules: why-comments citing #1232/spec ids;
  `check:file-size` ≤ 1000 lines; prettier before every commit; explicit `git add <paths>`
  (never `-A` — another session has uncommitted docs); one commit per task; each commit body
  carries a one-line release-note summary.

## Exit criteria — then STOP
1. **Unit:** manifest test (real validator, pinned namespaces/tools/nav), bundle test (CJS
   worker self-contained, ESM web, no `@jarv1s/*` runtime leaks, not in the core image),
   reset-job idempotency over a fake kv.
2. **Integration** (`tests/integration/external-module-job-search.test.ts`, rebuilt from
   scratch): install/enable through the real registration path; `profiles.list` through the
   real worker runtime for a seeded user; reset job wipes seeded stale keys.
3. **e2e #1000-harness Playwright** on a real dev instance (finance's `docker cp` + restart
   recipe): nav shows Job Search → first-run hero → "Start a new search" → full-screen chat
   renders → send a turn → reply streams. Mind uat-spec-gotchas (seeded owner lands on
   onboarding: Skip setup → Skip anyway; `getByLabel` needs `{ exact: true }`).
4. `pnpm verify:foundation` exit 0 on a **fresh gate DB** (DROP/CREATE first — see memory
   `verify-foundation-fresh-gate-db`).
5. **STOP at the JS-01 UAT gate. Do NOT start JS-02.** Post a comment on #1232 summarizing
   what changed + the JS-01 gate click-through (from the plan). Ben UATs over LAN and records
   sign-off on #1232; JS-02 does not begin until he clears it.

## Start
1. `pnpm install` (fresh worktree).
2. Read the plan's Phase JS-01 in full; write your task checklist (one todo per task).
3. Build with TDD — manifest/bundle/reset unit tests early. Commit in small steps with
   `#1232`-referencing messages.

# News Slice 4 — Relay #12 Handoff (2026-07-12)

You are the twelfth relay successor for News Slice 4 (issue #975, epic #954), branch
`news-s4`, worktree `~/Jarv1s/.claude/worktrees/coord-2026-06-30-rfa-fleet/.claude/worktrees/news-s4`
(shared with predecessors by design — relay chain, not parallel work).

## State

- **Tasks 1–9 committed green.** Last commit: `6018805b`
  `feat(news): settings add/remove/retry flows (#975)`.
- **Resume at Task 10** — the FINAL task. Plan:
  `docs/superpowers/plans/2026-07-12-news-s4-chat-actions-revalidation-notifications.md`
  read **:335 to end only** (never front-to-back; never the spec in full).
- Plan is coordinator-approved — do NOT re-ask.

## Task 10 = full gates + PR

1. `pnpm verify:foundation` (full local gate) and full `pnpm test:integration`.
   - `tests/integration/foundation.test.ts` asserts the FULL migration list with
     `toEqual` — Slice 4 added migration **0161**; if it fails, the fix is adding the
     row there, not touching migrations.
2. Pre-push gate: `pnpm format:check && pnpm lint && pnpm typecheck`, then rebase on
   `origin/main` (re-run gates if the rebase pulled anything).
3. Push `news-s4`, open the PR (base `main`, `Part of #975` / epic #954). PR body MUST
   state, in release-note language plus these review facts:
   - **Migration 0161 RLS approach**: column-scoped worker grants + owner-scoped
     policies (no BYPASSRLS, admin has no private-data bypass).
   - **Notification privacy test** exists (notifications carry counts/kinds only, no
     private content).
   - **e2e note**: NO news e2e spec exists under `tests/e2e/` — say so plainly in the
     PR body; do NOT invent a harness.
4. Message the Coordinator (below) that the PR is up, then await QA/merge direction.

## Verified facts (do not re-derive)

- Task 9 shipped: 6 REST client helpers in `packages/news/src/web/news-client.ts`;
  `AddSourceFlow` in `packages/news/src/settings/add-source.tsx` (preview → candidate
  pick → confirm); topic form + per-row Remove + owner-wide Retry in
  `packages/news/src/settings/index.tsx`; 15/15 tests in
  `tests/unit/news-settings-pane.test.tsx`.
- Preview body field is `input` (not `source`); REST confirm body =
  `{confirmationId, candidateId?}` only (no tamper fields — chat tools carry those).
- `healthStatus` is `"available" | "unavailable"` — no `"degraded"`.
- Error copy maps machine keys client-side; raw user input and machine keys are never
  echoed (`PREVIEW_REJECTION_COPY`, `topicCreateErrorMessage`).

## Traps

- **Shared tree**: stage explicit paths only; NEVER `git add -A`/`git add .`. Never
  stage `.claude/context-meter.log` or coordinator-owned
  `docs/coordination/handoff-news-s4.md`.
- Prettier every doc AND test file before committing (`pnpm exec prettier --write`).
- Read before Edit; never `sed -i` a file you will Edit.
- fast-json-stringify strips undeclared response fields — new REST fields must also go
  in `packages/shared/src/*-api.ts`.
- `herdr pane read` MUST use `--source recent --lines 12`.
- Full `test:integration` can contend with other agents' Postgres runs — if it crashes
  with recovery-mode signatures, check for a concurrent run before assuming regression.

## Coordinator

Label `Coordinator`, session authority `58a78927-385c-4b1d-8fa0-94db20255d6f` — resolve
the pane fresh by label via `herdr pane list` (pane ids reflow). Messages caveman-terse
via the `herdr-pane-message` skill.

## Relay rule

Fresh full budget. Relay ONLY at the 70% context-meter warning; successor MUST be
Fable. Pointer-style handoff <6KB, prettier'd, committed.

# Relay: #964 module distribution & install (relay 2)

## Where things are

- Worktree: `/home/ben/Jarv1s/.claude/worktrees/mod-dist-964`, branch `mod-dist-964`.
- Spec: `docs/superpowers/specs/2026-07-12-module-distribution-install.md` (approved, council waived by Ben — do not stop at DRAFT header).
- Plan (task-by-task source of truth): `docs/superpowers/plans/2026-07-12-module-distribution-install.md`. **Read ONE task section at a time, never the whole file.**
- Handoff contract (constraints, security invariants, deviations): `docs/coordination/handoff-mod-dist-964.md` — read in full, never edit/stage it.
- Prior relay: `docs/superpowers/handoffs/2026-07-12-mod-dist-964-relay.md` (Task 1-2 done).
- Coordinator: Herdr label `Coordinator` (session `58a78927-385c-4b1d-8fa0-94db20255d6f`) — re-resolve pane fresh by label before messaging, confirm exactly one match.
- Risk tier: **security**. You build to green; coordinator runs the adversarial security council and owns merge/board/close. Never merge yourself.

## Done (commits on this branch)

- Task 1 — registry index schema + ensure-list parsing: `b4af4976`.
- Task 2 — manifest `database.ownedTables` declaration + `sql/**` in package hash: `02dc9f5c`.
- Task 3 — migration + staged/purge repository state: `a56a644b`.
  - **IMPORTANT deviation from the written plan text:** the plan says migration `0161`. The
    coordinator flagged a real collision mid-build — News S4 (#977, branch `news-s4`) already
    claimed `0161_news_revalidation.sql` and lands ahead of #964 in the merge queue. **Use `0162`,
    not `0161`, for everything in this build** — already applied: file is
    `packages/settings/sql/0162_external_module_distribution.sql`, foundation-catalog test row is
    `{ version: "0162", name: "0162_external_module_distribution.sql" }` (with a comment noting the
    0161 gap is reserved for News S4, not present on this branch), and the `types.ts` comment says
    "distribution columns by 0162". **If any later task (4-10) or the plan's own prose references
    0161 for this feature, treat it as stale — 0162 is correct and already committed.**

Task 3 also: TDD red→green confirmed (5/5 tests + foundation-catalog + external-modules-repository
suites all green), typecheck clean (`@jarv1s/db`, `@jarv1s/settings`), file-size gate clean, staged
by explicit path (never `-A`), committed with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`.

## Next: Task 4 onward

Read the plan's Task 4 section only (`docs/superpowers/plans/2026-07-12-module-distribution-install.md`,
starts at line 1285: "Publish script + rolling-release GitHub workflow"), then continue the same
cadence through Task 10:

4. Publish script + rolling-release GitHub workflow — **IN PROGRESS, not committed.** I had just
   started verifying premises (checked `scripts/build-external-module.ts` exists with
   `buildExternalModule(moduleDir)` at line 14 — confirmed; checked `packages/module-registry/src/node.ts`
   re-exports — confirmed `export * from "./external/hash.js"` and `"./external/validate.js"` etc.
   are present, but I had NOT yet grepped for `ARTIFACT_FILENAME_RE` / `REGISTRY_INDEX_SCHEMA_VERSION`
   / `validateRegistryIndex` / the `ModuleRegistryArtifactRef`/`Entry`/`Index` types inside
   `packages/module-registry/src/external/*.ts` and `distribution/*.ts` — do that grep first, they
   should exist from Task 1 but re-verify locations/exact names before writing the test import.
   No files created yet for Task 4 — start clean from Step 1 of the plan section.
5. Download → verify → extract → stage pipeline
6. Shared contracts, lifecycle derivation, admin registry routes
7. Boot-time module reconcile
8. Boot & compose wiring
9. Admin web UI — module registry section
10. Integration suite, docs, spec-example fix, full gates (`pnpm verify:foundation` + full `pnpm test:integration`)

After Task 10 green: `coordinated-wrap-up` → open PR (Part of #964) → report to Coordinator for the security council. Do not merge, move the board, or close issues.

## Standing constraints (from handoff/CLAUDE.md — don't re-litigate)

- Stage explicit paths only, never `git add -A` / `git add .` / repo-wide `pnpm format` (shared tree).
- Never touch `docs/coordination/`, the project board, milestones, or merge.
- No secrets in any doc/payload/log/prompt.
- Never edit an applied migration — new migration file only; module SQL lives in the owning module's `sql/`, never `infra/`.
- **Migration numbers are globally assigned by landing order — always confirm with the coordinator before creating a new migration file if there's any chance of collision** (proven necessary this relay: 0161→0162 rename mid-Task-3).
- `@jarv1s/settings` must NOT import `@jarv1s/module-registry`.
- File-size gate: all source (incl. CSS) ≤1000 lines.
- Every new Fastify response field must be declared in `packages/shared/src/*-api.ts` (schema strips undeclared fields) — test via `app.inject`.
- Prove security invariants (input validation/allowlisting, hash verification fail-closed, no admin RLS bypass, metadata-only job payloads) via tests, not prose.
- 3 intentional spec deviations already blessed — don't re-litigate: (1) `app.external_modules.last_install_error` mirror column instead of reading FORCE-RLS `module_installs`; (2) dev-boot parity via root `db:reconcile` script, no `scripts/dev.ts`; (3) spec's `jarv1s.job-search`-style example ids get fixed to bare kebab in Task 10.
- Relay again on the 70% context-meter warning or immediately on seeing a compaction summary — message Coordinator first (re-resolve pane by label + session id, confirm exactly one), then use `relay` skill.

## Bootstrap for the next session

`[ -d node_modules ] || pnpm install` (should already exist — skip). Read this doc in full, then
read Task 4's section of the plan (line 1285 onward, stop before Task 5 at line 1693), verify its
premises against the actual branch (grep the cited files/exports — especially the module-registry
exports noted above as unverified), and continue via `coordinated-build`. Remember: 0162 not 0161.

# Relay 12 — #1265 module content self-operation

**Read this doc only.** Do not read relay-7..11 front-to-back. Everything open is below.

- Worktree/branch: `1265-module-content-self-operation` (this dir), `node_modules` present, skip install.
- PR **#1273** OPEN on this branch, url https://github.com/motioneso/Jarv1s/pull/1273. Do not open
  new, do not merge, do not close/reopen.
- Coordinator label `Coordinator`, agent name `coord-1262`. Resolve pane fresh via `herdr pane list`
  before messaging — confirm exactly one match. Talk caveman.
- Issue #1265, epic #1262. Risk tier: **security**. Tree clean except this doc.

## DONE this relay (commits, all green, do not redo)

- `2d1037e6` — fixed a real gate failure found running Task 6's own gate: an earlier branch commit
  (`bc506b6a`) had pushed `tests/unit/mcp-gateway-units.test.ts` to 1024 lines, tripping
  `check:file-size`. Split the "tool input validation" + "gateway tool output sanitization" describe
  blocks into new `tests/unit/mcp-gateway-validation.test.ts` (455 lines) — original now 577 lines.
  No test content changed. Ran both files: 57/57 pass. format/lint clean.
- Fresh gate DB `jarvis_gate_1265d` (drop+create, not reused from any prior relay):
  - `JARVIS_PGDATABASE=jarvis_gate_1265d pnpm verify:foundation` → **exit 0**
  - `pnpm audit:release-hardening` → **exit 0**
- Pre-push trio (`format:check && lint && typecheck`) → clean.
- `git fetch origin main && git rebase origin/main` → already up to date, nothing to rebase.
- **Pushed** `2d1037e6` to `origin/1265-module-content-self-operation` (14 commits ahead of what was
  there before this relay). PR #1273 stays open, same branch.

## OPEN — pick up here

### Task 6 remainder — PR body + report (last task)

PR #1273's body currently has a **stale** "Four things worth calling out" section (SSRF/install-grant/
DELETE-route/inventory-count — those were from an earlier relay's four-item draft). **Replace that
section** with the actual required four callouts (binding, from relay-10's coordinator ruling,
carried forward unchanged by relay-11):

1. **Exit-criterion-unmet language** — state plainly, against the approved spec's Exit Criteria
   (`docs/superpowers/specs/2026-07-26-module-self-operation-content-commands.md`), what this PR
   does and does not fully close. (Check the spec's Exit Criteria section only — don't read the
   whole spec.)
2. **Pattern-anchoring note** — `validateToolInput` (in `packages/ai`, changed in `bc506b6a`) now
   enforces `minLength`/`maxLength`/`pattern` for every module's tool input schema, and patterns are
   anchored to the **whole string**, not a substring match. This is a real behavior change to a
   shared gateway chokepoint, not just the sports/news tools this PR adds.
3. **External-module blast-radius note** — because the enforcement lives in the shared gateway
   validator, it applies to **installed external modules too**, not only built-in ones. Any external
   module whose manifest declared `minLength`/`maxLength`/`pattern` bounds that were previously
   decorative (never actually checked) will now have them enforced for real.
4. **`packages/settings/src/app-map-tool.ts` naming** — confirmed: this file's tool input schema
   declares `maxLength` on `screenId`/`settingId`/`errorCode`/`query` (lines 8-11) that were **never
   enforced before this PR** (validator didn't parse those keywords pre-`bc506b6a`). Name this file
   explicitly as the one in-tree file whose bounds go from decorative to enforced — a live behavior
   change outside this PR's nominal scope, not left for a reviewer to find. Do not drop this line.

Use `gh pr edit 1273 --body-file <file>` (or `gh api`) to replace the body — keep the rest of the
existing body (Summary, Verification, Release notes) intact, only swap the callouts section content
and update the Verification section's gate DB name/exit codes to `jarvis_gate_1265d` / this relay's
runs (see DONE above for the numbers).

**Shared-surface note:** the self-operation tool inventory count in the PR body (previously stated
"31/5/4 = 40") is a shared surface with #1264 (settings self-operation, separate worktree/branch).
**Recompute it fresh from THIS tree** with an exact `toBe` before writing it into the body — do not
trust the old number, and do not hard-code an assumption about which of #1264/#1265 lands first.
Search `tests/**/self-operation*` or grep the manifest classification test for the current asserted
total.

**After the PR body is updated:**
- Report to Coordinator: gate exit codes (verify:foundation 0, audit:release-hardening 0 on
  `jarvis_gate_1265d`), the file-size fix commit, PR body updated with all four correct callouts +
  recomputed inventory number, push confirmed (`2d1037e6` on origin).
- Then invoke `coordinated-wrap-up`. **Do not merge. Do not move the board.**
- Ben's manual sign-off remains a merge blocker regardless of gate color — PR will not merge tonight
  even fully green. Build to done, get CI green, get the QA verdict (qa-1265 worktree, held alive for
  the delta re-review — this push is the one it reviews), then stop and report.

## Model

Boot on **Sonnet** explicitly (`--model sonnet`). Confirm status bar.

## Relay note

Relayed at context-meter 71% warning, immediately after pushing `2d1037e6` (gate fix + full gate
green + push). Tree clean, nothing in-flight except this doc (uncommitted at write time, commit it
next). Only the PR-body callout rewrite + inventory recompute + report remain — no code changes
expected, no open design questions.

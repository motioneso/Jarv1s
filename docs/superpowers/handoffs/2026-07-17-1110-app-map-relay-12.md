# #1110 app-map relay checkpoint 12

Branch: `build/1110-app-map`, worktree: this one. `node_modules` present — do NOT `pnpm install`.
Coordinator: label `Coord-1109-1110-g3` (re-resolve fresh via `herdr pane list`, never reuse a pane id from this doc).

## Done this session (committed)

- `34457186` — module-sdk regression fix (node:crypto leak into apps/web bundle via
  `AI_MODEL_CAPABILITIES`). Fully verified: typecheck clean, real `pnpm --filter @jarv1s/web build`
  succeeds, grepped dist bundle confirms node:crypto/rate-limit-key/randomBytes absent. Coordinator
  acked it — not a CI landmine, no further action needed. Followup tracked in #1120.
- (Already committed pre-session: `bcc74686`, `6c4ae260` — unrelated infra fixes.)

## Task 8 — ONLY remaining step: fix the UAT spec, then ship

`tests/uat/specs/app-map-grounding.uat.spec.ts` — its `signIn()` helper (line 14-26) is missing the
onboarding-skip steps every other UAT spec has. All 4 tests fail identically at
`.jds-usermenu__trigger` not visible (seeded owner lands on onboarding first).

**Exact fix** — insert into `signIn()` right after the `Sign in` click (line 24), before the
`.jds-usermenu__trigger` assertion (line 25). Pattern copied verbatim from
`tests/uat/specs/cli-terminal.uat.spec.ts` lines 36-39:
```ts
await page.getByRole("button", { name: "Skip setup" }).click();
await page.getByRole("button", { name: "Skip anyway" }).click();
```
(Skip setup → confirmation dialog "Skip setup without connecting a provider?" → Skip anyway.)

After that fix:
1. Re-run `pnpm test:uat -- app-map-grounding` — must pass all 4 tests. Coordinator confirmed
   docker is free (UX-1117 hold pane gone from fleet).
2. `pnpm verify:foundation`.
3. Single commit with Task 8's file list (already staged/uncommitted, unchanged all session):
   `tests/uat/specs/app-map-grounding.uat.spec.ts`, `tests/uat/run-uat.ts`,
   `tests/uat/provisioner.ts`, `tests/uat/seed/types.ts`, `tests/uat/seed/chunks/ai.ts`,
   `tests/uat/seed/levels.ts`, `tests/uat/seed/cli.ts`, `packages/news/src/routes.ts`,
   `packages/news/src/personalization-routes.ts`, `packages/module-registry/src/index.ts`,
   `docs/WHATS_NEW.md`.
4. Pre-push trio: `pnpm format:check && pnpm lint && pnpm typecheck`, then
   `git fetch origin main && git rebase origin/main`.
5. `coordinated-wrap-up` skill → PR → report to coordinator (label above). Never merge/board/close.

## Notes

- Local-only repair (not committed, not needed elsewhere): this worktree's
  `packages/shared/node_modules/@jarv1s/module-sdk` symlink was missing and I manually recreated it
  to unblock the vite build without `pnpm install`. Coordinator confirmed CI is unaffected (fresh
  install regenerates it normally). No action needed unless node_modules gets reset in this worktree.
- Read this doc in full (it's short). Do NOT re-read the original spec/plan docs from scratch —
  everything needed for the remaining step is above. Build, don't re-derive.

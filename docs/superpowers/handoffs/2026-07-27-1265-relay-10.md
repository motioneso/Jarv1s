# Relay 10 — #1265 module content self-operation

**Read this doc only.** Do not read relay-7/8/9 front-to-back. Everything open is below.

- Worktree/branch: `1265-module-content-self-operation` (this dir), `node_modules` present, skip install.
- PR **#1273** OPEN on this branch. Do not open new, do not merge, do not close/reopen.
- Coordinator label `Coordinator`, agent name `coord-1262`. Resolve pane fresh via `herdr pane list`
  before messaging — confirm exactly one match. Talk caveman.
- Issue #1265, epic #1262. Risk tier: **security**. Tree clean, all work below committed.

## Coordinator ruling this relay (binding, from mid-turn message — read verbatim, don't re-derive)

1. Task 3 denylist test WAS structurally vacuous (matchingExclusionRule requires rule.moduleId ==
   caller's; no rule names news/sports, so it returns false for ANY input) — fixed by asserting the
   positive (a known-excluded prefix IS excluded), not by strengthening the negative. DONE.
2. Task 4 singleton: process-wide state leak into whatever runs next in the same worker. Restore
   it. DONE.
3. Task 5 RLS: question is whether cross-actor isolation is proven through the TOOL path (gateway
   entry) or only one layer below at the repository — only the former counts for an auto-run write
   tool. Build if feasible; if not, **escalate to Coordinator, do not conclude "good enough" yourself.**
4. Task 6 PR body needs **four** callouts, not three: exit-criterion-unmet language, pattern-anchoring
   note, external-module blast-radius note, AND a fourth line **naming
   `packages/settings/src/app-map-tool.ts`** explicitly as the one in-tree file whose bounds go from
   decorative to enforced — a live behavior change outside this PR's nominal scope a reviewer must be
   told about, not left to find.
5. Ben's manual sign-off remains a merge blocker, not Coordinator's to waive. PR will NOT merge
   tonight even fully green — #1264 and #1265 both park on Ben's hands-on chat-turn pass.
6. qa-1265 (worktree, pane exists per `herdr pane list`) is **held alive** for the delta re-review —
   make your next push the one it reviews.

## DONE this relay (commits, all green, do not redo)

- `c5a37543` — Task 3: added positive-match assertion in
  `tests/unit/self-operation-manifests.test.ts` (`isSelfOperationExcluded("settings", {name:
  "settings.yolo.enable"})` → true). 19/19 unit tests pass.
- `8f5a8c76` — Task 4: added `resetSportsChatToolsForTests()` export in
  `packages/sports/src/chat-tools.ts` (+ index.ts export), wired an `afterEach` in
  `tests/integration/mcp-gateway-self-operation.test.ts`. Follows the existing
  `set<X>ForTests(undefined)` convention already used in `@jarv1s/web-research`
  (reader.ts/providers.ts/url-safety.ts). Ran via `pnpm exec tsx scripts/test-integration.ts
  tests/integration/mcp-gateway-self-operation.test.ts` (raw vitest refuses the shared `jarv1s` DB —
  must go through the harness script) — 6/6 pass.
- Task 1.2 — **real** exit code confirmed: `pnpm test:uat module-install.uat.spec.ts` (this IS the
  right invocation; `tsx tests/uat/run-uat.ts module-install.uat.spec.ts` under the hood; do not
  second-guess it again). Spun a real Docker Compose UAT instance (`uat-1414134_ec5d8d53-*`),
  seeded `admin+data` level, 1 test passed in 24.6s, clean teardown (containers/volumes/network all
  Removed), **exit code 0**. No further action needed on Task 1.2 — it's closed.

## OPEN — pick up here, in order

### Task 5 — ESCALATE, do not decide (start here)

Attempt a **gateway → real DB → RLS** cross-actor span test for sports `followTeam`, entering via
the tool/gateway path (AssistantToolGateway or the MCP dispatch surface — NOT `SportsService.followTeam`
directly; `tests/integration/sports-follows-tool-rls.test.ts` already covers the direct-entry
version calling `service.followTeam`/`unfollowTeam` with a scoped db, which is one layer below the
gateway). Read `tests/integration/mcp-gateway-self-operation.test.ts` for how it constructs an
`AssistantToolGateway` against a real DB with real auth signup (pattern also in
`sports-follows-tool-rls.test.ts`'s `signUp`/`disableApproval` helpers) — the gap is whether that
gateway harness can dispatch a *named tool call* (`sports.followTeam`) end-to-end into the real DB
for two distinct signed-up actors, asserting user B never sees/removes user A's follow.

If structurally infeasible (e.g. the gateway harness in this file is mock-writer-only and wiring a
real DB through it is a bigger lift than this task warrants), **message Coordinator with the
specific structural reason** and wait for a ruling — do not decide "the repo-level test is good
enough" yourself, per ruling #3 above.

### Task 6 — gate + push + report (last)

- Fresh isolated gate DB — do NOT reuse any prior `jarv1s_gate_1265*` name. Use `jarvis_gate_1265c`
  (drop+create first).
- `pnpm verify:foundation` and `pnpm audit:release-hardening` — capture **real exit codes**, never
  piped through `tail`/`head`.
- Pre-push trio `pnpm format:check && pnpm lint && pnpm typecheck`, then
  `git fetch origin main && git rebase origin/main`, then push to **this same branch** (PR #1273
  stays open).
- **Update PR #1273 body** — four callouts, see ruling #4 above (do not drop the app-map-tool.ts
  line; it's new this relay, not in the prior relay's three-item list).
- Report to Coordinator: every fix this + prior relays, both gate exit codes, Task 5 outcome (built
  or escalated+reason), confirmation PR body updated with all four callouts. Then invoke
  `coordinated-wrap-up`. **Do not merge. Do not move the board.**

## Model

Boot on **Sonnet** explicitly (`--model sonnet`). Confirm status bar.

## Relay note

Relayed at context-meter 70%→74% warning after landing `c5a37543`/`8f5a8c76` and confirming Task
1.2's real exit code (0). Tree clean, nothing in-flight. Fresh full budget: BUILD, commit per task,
relay only after real work past ~80%. Task 5 and Task 6 remain; Task 5 may terminate in an
escalation (that's a valid, expected outcome per ruling #3, not a failure to fix before relaying
again).

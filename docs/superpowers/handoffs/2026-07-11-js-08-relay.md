# JS-08 relay — BUILD IN PROGRESS, Tasks 1–6 committed, resume at plan Task 7

**You are the successor build agent for JS-08 (task issue #937).** Model MUST be Fable 5 (Ben's
scoped exception — check your own model line; if it says Sonnet/Opus, stop and tell the
Coordinator). Same worktree/branch: `feat/js-08-opportunity-feed`, rooted at `c23a93b8`.

**First:** read `docs/coordination/2026-07-11-js-08-build-handoff.md` (short; bans + routing +
security tier), then continue the TDD build reading
`docs/superpowers/plans/2026-07-11-js-08-opportunity-feed.md` **BY TASK starting at Task 7**.
Do NOT re-do grounding below — verified current on this branch. Do NOT rewrite the plan.

## State (updated by relay 4, 2026-07-11)

- **Plan APPROVED** by Coordinator (independent Opus adjudication). Flag rulings: Flag 1
  (decide `reason`) approved as optional free text, 500 B cap, owner-private; Flags 2–6 approved
  as proposed. Two REQUIRED additions are already folded into the plan (commit `533ec41d`):
  (A) plan T7e — direct test that a CONFIRMED decision writes an owner-attributed audit record and
  decisions survive module disable/re-enable; (B) fail-closed citation = `routes.ts:602-627`.
- **decisionReason ruling (Coordinator, 2026-07-11 — supersedes the earlier "never echoed" line):**
  INCLUDE `decisionReason` in `opportunities.get` (owner-own content, owner-only read; the owner's
  assistant may see it). It must NEVER hit shared/structured logs, pg-boss payloads, shared-model
  training, other-owner context, or non-owner export surfaces. `opportunity.decide` responses and
  ERROR messages still never echo it. Web detail screen renders it as TEXT. **PR body must flag
  this exposure explicitly for the security-tier Opus QA.**
- **Committed so far (per green task):** `533ec41d` plan edits → `5122c2c2` Task 1
  (`src/domain/decisions.ts`: `decideOpportunity`, `OPPORTUNITY_DECISIONS`, reason validated
  pre-write, feed rebuilt) → `56df47c0` Task 2 (`opportunities.list` handler + `readInt` in
  `validate.ts`) → `9d215ce5` Task 3 (`opportunities.get`) → `6e2824ea` Task 4
  (`opportunity.decide` handler). All in
  `external-modules/job-search/src/worker/handlers/opportunities.ts` +
  `tests/unit/external-module-job-search-handlers-opportunities.test.ts` (27 tests green;
  `pnpm check:external-modules`, eslint, prettier all clean at `6e2824ea`).
- **Implementation deviations to flag in the PR body (all strictly-tightening):**
  1. List budget backstop — plan's caps can't guarantee 14,000 B at 15 maxed cards; handler strips
     advisory `topEvidence`/`topGap` from the LAST (lowest-ranked) cards until it fits.
  2. Escape-aware `fitDescription` (get) — the plan's byte-allowance rule ignores JSON escaping;
     a convergence loop cuts the allowance by the exact overshoot per pass (pinned by a
     `'"\n'`-heavy test).
  3. decisionReason exposure in get per the ruling above.
- **Task 5 committed `0f905123`** — registry stubs → real factories; manifest strict inputSchema +
  full outputSchema on all 3 tools (envelope keys `status`/`code`/`message`/`question` optional,
  `required: ["status"]` only; `decisionReason` declared in get; decide outputSchema deliberately
  OMITS reason — Coordinator confirmed correct). Manifest + registry + bundle pinning tests updated.
- **Task 6 committed `e984d6c4`** — extended
  `tests/integration/external-module-job-search-kv-isolation.test.ts` with describe
  `"job-search opportunity feed + decision isolation (#937)"` (24/24 green): A-positive control;
  B/admin denials (feed null before self-heal, get/decide `missing_record`); B's denied decide
  leaves A's row BYTE-IDENTICAL (bootstrap SQL compare — denial before write); A's decide ack never
  echoes reason; feed index value never contains reason. DEVIATION (Coordinator ACCEPTED): plan's
  literal "B list → 0" unachievable (B owns a row from the #931 test); asserted the STRONGER
  "B sees only B's own hash, never A's, and A's private summary absent from B's serialized page".
- **Coordinator acks received:** T5+T6 acked; decide-outputSchema-omits-reason confirmed correct;
  stronger B assertion accepted; directive "Proceed to Task 7 REST invoke tests (app.inject, not
  service). Keep going."
- **NEXT: plan Task 7 (lines 273–298)** — extend `tests/integration/js06-module-surface.test.ts`
  (194 lines, well under cap; harness: `signUp` returns `{cookie, userId}`, `setEnabled`,
  `invokeTool(name)` posts `payload:{input:{}}` — add an input-accepting variant). Seed the admin
  user's KV via the kv-isolation harness's `kvForActor` pattern (`connectionStrings.worker` +
  `DataContextRunner`) with job + evaluation + `rebuildFeed`, then:
  (a) list via `app.inject` → `invocation.result.opportunities[0]` keeps
  title/fitBand/freshness/topEvidence (fast-json-stringify trap test);
  (b) get → posting + evaluation survive, description present + bounded;
  (c) decide → 403 `confirmation_required` + `actionRequestId`, follow-up list status UNCHANGED;
  (d) disable → invoke 404 fail-closed, re-enable → data intact;
  (e) real confirm path — see recipe below. Commit:
  `test(job-search): REST invoke surface — declared fields survive, decide stays confirm-gated`.
- **Task 7e recipe (reachability SOLVED — no escalation needed):** the REST resolve route
  (`POST /api/ai/assistant-actions/:id/resolve`) only flips the row via
  `repository.resolveAssistantAction` and NEVER executes — so (e) needs a live gateway waiter.
  Copy `tests/integration/external-module-gateway.test.ts` VERBATIM as the skeleton: construct
  `AssistantToolGateway` directly (real `AiRepository`, `DataContextRunner(appDb)`,
  `SessionTokenRegistry`, `ConfirmationRegistry`, notifier→array, `confirmTimeoutMs: 5_000`),
  mint a token, `const pending = gateway.callTool(token, "<tool>", input)` → poll notifier for
  `action_request` → `gateway.resolveActionRequest(userId, actionRequestId, "confirmed")` →
  `await pending` → POLL `app.jarvis_action_audit_log` (fire-and-forget write; 100×5 ms) for
  owner-attributed `{outcome:"success"}`. That reference test uses a FAKE execute fn; for the REAL
  job-search worker execute, replicate `apps/api/src/external-module-tools.ts`
  (`createExternalToolManifests(discoveries, executeFn)` where executeFn =
  `createExternalModuleRpcHandler({module, toolRisk, actorUserId, requestId, workerDataContext,
  cipher: createModuleCredentialSecretCipher(), isActorAdmin})` +
  `new ExternalModuleWorkerRuntime({logger}).invoke(module, tool.handler, input, rpc)`) — all
  exported from `@jarv1s/module-registry/node`; check how the kv-isolation harness builds its
  discovery/module object and reuse it. Verify the gateway-side tool NAME (module-prefixed vs
  bare) from `createExternalToolManifests` before asserting. After confirmed decide: assert
  decided status + stored decisionReason survive disable→re-enable.
- Coordinator label `Coordinator`, session id `58a78927-385c-4b1d-8fa0-94db20255d6f` — verify
  exactly ONE pane with that label, fresh `herdr pane list` every time. Your pane label lineage:
  `JS-08 build 4` (predecessor). Announce yourself to the Coordinator on arrival.
- `node_modules` present — skip install.

## Extra verified findings (resolved the doc's old UNFINISHED check — trust these)

- `invocation.result` REST schema = `nullableJsonObjectSchema` (`packages/shared/src/schema-fragments.ts`,
  `additionalProperties: true`, comment says "Do NOT tighten") → NO fast-json-stringify strip at
  the shared layer and NO packages/shared change needed.
- The real allow-list is `sanitizeAssistantToolResult(manifestTool.outputSchema, result)`
  (`packages/ai/src/gateway/output-validation.ts`, called at `packages/ai/src/routes.ts:668-669`):
  recursive projection to schema-declared keys; throws on missing required key. Then
  `boundedAssistantToolResultData` degrades the whole result to `{text: capped}` if
  `renderToolResult` > 16,000 chars — hence the plan's `RESPONSE_BUDGET_BYTES = 14_000`.
- Manifest validator passes `outputSchema` through
  (`packages/module-registry/src/external/tool-manifests.ts:41`).
- `validate.ts` has NO int reader — plan Task 2 adds `readInt`.
- Circular-import: `feed.ts` imports `opportunities.ts`, so decide+rebuild lives in NEW
  `src/domain/decisions.ts` (retention.ts precedent imports both).
- Integration harnesses to extend: `tests/integration/external-module-job-search-kv-isolation.test.ts`
  (real RPC handler + RLS; note its describe DELETES userA at the end — order new tests before
  that) and `tests/integration/js06-module-surface.test.ts` (booted server + `server.inject`,
  signUp/setEnabled helpers; the REST-invoke precedent incl. write-403 assert).
- Unit harness pattern: `tests/unit/external-module-job-search-handlers-monitor.test.ts`
  (`createMemoryKv` from `./helpers/job-search-memory-kv.js`, `portsAt(kv, now)`, seed via domain).

## Verified premises (all confirmed on this branch — cite, don't re-read broadly)

- **Manifest** (`external-modules/job-search/jarvis.module.json`) already declares the three JS-08
  tools: `opportunities.list` / `opportunities.get` (read), `opportunity.decide` (write) — with
  placeholder `{"type":"object"}` input schemas. Tightening those schemas is JS-08 work.
  `tests/unit/external-module-job-search-manifest.test.ts` pins the manifest; registry test pins
  handler keys — both will need updating.
- **Worker registry stubs** at `external-modules/job-search/src/worker/registry.ts:44-46`
  (`notImplemented`). Handler pattern to copy: `src/worker/handlers/monitor.ts` (ports.kv,
  `validate.ts` readers, `{status:"ok"|"question"}` returns, scrub-by-construction responses,
  error messages never echo private values).
- **Feed index (JS-07)** `src/domain/feed.ts`: `FeedEntry {h,r,s,e?,b?,c?}` single-char codes;
  `FEED_GATE_CODES`/`FEED_BAND_CODES`/`FEED_CONFIDENCE_CODES` exported for readers to decode;
  `readFeedOrRebuild(kv, now)` self-repairs. Freshness/postedAt NOT stored in index — compute via
  `freshnessOf(job)` from the job record.
- **Opportunities repo** `src/domain/opportunities.ts`: `OpportunityRecord` (status
  `new|saved|active|passed|stale`, `statusAt`, posting w/ 16 KB-capped `description` +
  `descriptionTruncated`), `getOpportunity`/`listOpportunities`/`setOpportunityStatus(kv,h,status,now)`
  all exist — decide handler composes these, no new repo needed.
- **Evaluations** `src/domain/evaluations.ts`: `EvaluationRecord` has `fitBand, recommendation,
  evidence[{requirement,evidence,source}], blockers, gaps, unknowns, preferenceMatches,
  preferenceConflicts, postingConfidence, overallConfidence, summary, inputs, createdAt`;
  `isOutdated(record, current)` computed on read (needs active profile+resume revisions).
- **Retention** `src/domain/retention.ts`: saved/active NEVER evicted (protected);
  passed/stale age-evicted after 30 d + tombstoned. Setting status via decide gets protection
  for free. Retention already rebuilds the feed; decide must also rebuild after status change.
- **CRITICAL — confirm-gate reality:** REST invoke route
  (`packages/ai/src/routes.ts:576-686`) executes READ tools only; a write tool gets
  `createPendingAssistantAction` + 403 `confirmation_required` and there is **no waiter** —
  a REST-initiated write can NEVER execute (ConfirmationRegistry settles only live gateway/chat
  calls; fail-closed guard `packages/ai/src/gateway/gateway.ts:281-299`). So `opportunity.decide`
  executes ONLY through the assistant chat confirm flow (gateway `confirmAndRun` + audit). The
  module web client comment (`src/web/api.ts:5-6`) codifies the standing Coordinator ruling:
  web UI invokes risk:read tools only. Plan accordingly: UI = reads + decision-state display.
- **Web surface (JS-06):** screens use `useToolQuery` (`src/web/store.ts`) → `invokeTool` →
  `/api/ai/assistant-tools/:name/invoke`; `opportunities.tsx` is a ready route shell with
  buckets `new|saved|passed|stale`; authored states in `states.tsx` (`EmptyState`,
  `DisabledState`, `outcomeGate`, `announce`); `invalidateQueries()` exists for refetch. JSX
  auto-escape = external text renders as text; NO `dangerouslySetInnerHTML` anywhere — keep that,
  and add the #960-style test (entity-encoded markup stays literal text; sanitize decode-after-strip
  lives in `src/adapters/sanitize.ts`, already strip→decode ordered).
- **Zero migration confirmed** — everything is `ctx.kv` (`module_kv`); namespaces in manifest
  (`job-search.opportunities`, `job-search.feed`, …). Cross-owner isolation tests exist at
  `tests/integration/external-module-job-search-kv-isolation.test.ts` — extend for feed/decide.
- **UNFINISHED check (do this before designing tool result shapes):**
  `invokeAiAssistantToolResponseSchema` in `packages/shared/src/ai-api.ts` — confirm whether
  `invocation.result` passes arbitrary fields or strips undeclared ones
  (fast-json-stringify silent-drop trap, see MEMORY). Test via `app.inject`, never the service.
  Also check `sanitizeAssistantToolResult`/`boundedAssistantToolResultData`
  (`packages/ai/src/routes.ts:668-669`) for output caps your list responses must fit.

## Flag at plan-approval time (don't decide alone)

1. Spec open review question: decide `reason` — bounded optional free text vs enum vs omit.
   Lean: bounded optional free text (~500 char cap, stored owner-private, never in errors/logs).
2. "Monitor health summary" tool (spec Tools list): propose REUSING existing `monitor.list` +
   `monitor.get` (already return health timestamps) — no new tool. Confirm with Coordinator.
3. UI decide affordance: given the REST write dead-end, propose NO web write path — UI shows
   decision state; decides happen in assistant chat (confirm + audit + survive disable/re-enable).
   This matches spec ("canonical state comes from the confirmed tool result") but say it out loud.

## Bans / reminders

- SECURITY tier: cross-owner denial tests with positive controls; bounded assistant context; no
  secrets anywhere; external text as text.
- Never touch `docs/coordination/`; `git add` explicit paths only; no board/merge actions.
- Migration believed needed → STOP, escalate `[DESIGN-FORK]`.
- Prettier this repo checks handoff docs: run `pnpm prettier --write` on files you add before commit.
- If YOU relay: successor must be `--model fable`, same worktree, verify its model line.

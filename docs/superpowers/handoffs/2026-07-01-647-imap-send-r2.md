# Relay Handoff — Slice D IMAP Send (R2)

**Branch:** `coord/647-imap-send`  
**Worktree:** `~/Jarv1s/.claude/worktrees/647-imap-send`  
**Issue:** #647  
**Risk tier:** security  
**Status:** Tasks 1-3 complete, green, committed. Tasks 4-12 pending.

## Commits landed this session

1. `057a2c1a` feat(email): add EmailWriteProvider abstraction
2. `726ad06f` feat(connectors): extract GoogleEmailWriteProvider  
3. `e36c9f9a` feat(connectors): add ImapEmailWriteProvider
4. `3dcdb4f5` feat(chat): dispatch email write by provider type

## Current state

**Green:** Typecheck passes. Tasks 1-3 core implementation complete.
- EmailWriteProvider interface exists
- GoogleEmailWriteProvider extracted (Gmail behavior unchanged)
- ImapEmailWriteProvider added (SMTP + APPEND)
- email-write-impl.ts dispatches by provider type
- Repository extended with getAccountProviderType()

**Pending:** Tasks 4-12 from plan
- Task 4: No new SMTP dep needed (nodemailer already exists) — SKIP
- Task 5: ThreadId derivation for IMAP messages (reply-mime.ts)
- Task 6: Unit tests for both providers  
- Task 7: GreenMail integration tests
- Task 8: Security sanitizer tests
- Task 9: Package exports
- Task 10: Documentation
- Task 11: Final gate verification
- Task 12: Wrap-up + PR

## Next exact step

**Task 5: Add threadId derivation for IMAP messages**

Current `deriveReplyTarget()` in `packages/email/src/reply-mime.ts` assumes Gmail `threadId` in `external_metadata`. IMAP needs different logic:

```typescript
// IMAP: derive threadId from Message-ID + References headers in cached external_metadata
// threadId = first Message-ID in thread, or computed from normalized references
```

Files: `packages/email/src/reply-mime.ts` or new dedicated file.

## Security constraints (never violate)

- **Secrets never escape:** SMTP/IMAP credentials stay in-memory, never in logs/payloads/responses
- **Provider-agnostic AI:** Tools unchanged; model sees only `cacheMessageId` + `body`  
- **Metadata-only:** Send is synchronous; body rides stream, not DB
- **Error sanitization:** Map SMTP/IMAP library errors to fixed human-safe strings
- **Cross-provider security QA + Ben sign-off required before merge**

## Provider rotation note

Coordinator confirmed: use existing nodemailer/imapflow/mailparser. No new SMTP dependency needed.

## Dirty status

Clean. All changes committed.

## Handoff to successor

Spawn in agents tab `w1:tC`, same worktree/branch. Load coordinated-build skill. Continue from Task 5.

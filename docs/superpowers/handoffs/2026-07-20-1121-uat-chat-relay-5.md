# #1121 relay-5 — pointer handoff

Supersedes relay-4 (read relay-4 only for the original task-by-task spec if
this doc is somehow insufficient — it shouldn't be needed).

Branch `fix/1121-uat-scriptable-chat`, worktree
`~/Jarv1s/.claude/worktrees/fix-1121-uat-chat`.

## Done, committed at `5a75deb7`

Tasks 1-6 of the Coordinator-approved 7-task plan (TDD, all green):

1. Root `package.json`/`pnpm-lock.yaml`: `@jarv1s/cli-runner` workspace dep.
2. `packages/cli-runner/src/index.ts`: barrel-exports provider-token-store.
3. `infra/docker-compose.prod.yml`: `seed` service gets its own `env_file:`
   list (existing anchor path + new opt-in `JARVIS_UAT_REAL_CHAT_ENV_FILE`,
   default `/dev/null`, both `required: false`) and mounts
   `jarv1s-cli-auth:/data/cli-auth`. `jarv1s` service block untouched.
4. `tests/unit/compose-seed-real-chat-token.test.ts`: regression proving 3.
5. `tests/uat/provisioner.ts`: `validateSingleTokenEnvContent` (exported,
   pure — exactly-one `CLAUDE_CODE_OAUTH_TOKEN` key or throw) +
   `writeUatRealChatEnvFile()` (opt-in, triggered by
   `JARVIS_UAT_REAL_CHAT_TOKEN_FILE` pointing at a GPG-encrypted file; decrypts
   via `execFile("gpg", [...paths only...])` into a mode-0700 dir/mode-0600
   file; fails closed + cleans up on invalid shape; sets
   `JARVIS_UAT_REAL_CHAT_ENV_FILE` only once validated). Tested with a real
   ephemeral GPG keypair round-trip in
   `tests/unit/uat-real-chat-token-env.test.ts` (9 tests, all green).
6. `tests/uat/seed/cli.ts`: `maybePersistRealChatToken()` (exported), called
   in `main()` strictly *after* the existing `assertTargetIsEphemeral` guard;
   no-op unless `CLAUDE_CODE_OAUTH_TOKEN` is set. Added the standard
   `import.meta.url === resolve(process.argv[1])` guard so importing this
   file in tests no longer auto-runs `main()`. Tested in
   `tests/unit/uat-seed-real-chat-token.test.ts` (2 tests, green).

Full focused suite (18 tests / 5 files) green as of the commit.

## Remaining — Task 7 (not started)

New Playwright spec, e.g. `tests/uat/specs/real-chat-onboarding.uat.spec.ts`:

- Reuse `signIn(page)` from `tests/uat/specs/runtime-context.uat.spec.ts:31-46`
  — don't reinvent.
- `page.request.post` to `/api/onboarding/provider-login/begin` with
  `{providerKind: "anthropic"}` (route:
  `packages/settings/src/onboarding-routes.ts:697-728`, manifest
  `packages/settings/src/manifest.ts:389`, admin-gated).
- Poll `GET /api/ai/models` (schema `listAiConfiguredModelsResponseSchema`,
  `packages/shared/src/ai-api.ts:457`) with backoff — no fixed sleeps — until
  a row has `capabilities.includes("chat")` and active status.
- Drive an actual chat turn, assert a real model reply.
- Gate the whole spec to skip/`test.fixme` when no real-chat token is
  configured for the run — default CI must stay credential-free.

Then: run the focused checks + `pnpm format`, `pnpm lint`, `pnpm typecheck`;
push the branch; open a PR (**do not merge, do not touch the board, do not
edit other coordination docs**). One real dedicated-account token UAT run is
mandatory before merge (no secret output) — likely a Coordinator/Ben action,
not this session's.

## ⚠️ Live merge-conflict warning (Coordinator, this session)

PR #1217 landed on `main` at `4a2020686d7822dc6ba368aa31300c702cb9d259`,
modifying `infra/docker-compose.prod.yml`'s `seed` service by adding a
runtime uid/gid `user:` field — same service block this branch's Task 3 also
edited (different lines: uid/gid vs. `env_file:`/`volumes:`). **Before the
final rebase onto `main`, preserve #1217's `user:` line exactly** while
keeping this branch's `env_file:`/`volumes:` additions; report any
substantive conflict beyond a clean line-level merge back to the
Coordinator. Do not push a stale (pre-rebase) result.

## Standing constraints (still binding)

Never log/display/store the real token; never put it in argv; preserve
`.claude/context-meter.log` but never commit it (already excluded from the
task-6 commit).

# Handoff — UAT harness plumbing + seed cleanups (#1047 + #1034 combined)

**Tier:** routine. **Model:** Codex `gpt-5.6-sol`. **Coordinator:** label `Coordinator`, session
`58a78927-385c-4b1d-8fa0-94db20255d6f`.

**Worktree / branch:** `.claude/worktrees/uat-plumbing-1047-1034` / `uat-plumbing-1047-1034`
(off `origin/main` @ `313c194c`).

**Why combined:** both issues touch `tests/uat/` (+ `provisioner.ts`), so they MUST land as one PR
to avoid a self-collision. Read both issue bodies in full on GitHub before coding — they carry the
exact file:line pointers.

## Scope — do exactly these, nothing more

### #1047 — harness spec-filter + seed plumbing
- `run-uat.ts` currently **ignores a forwarded spec filter** and **hardcodes the `admin+data` seed
  level + job-search exclusion**, so `pnpm test:uat -- <spec>` cannot run a single named spec.
  Fix it to **honor the forwarded spec filter** and stop hardcoding the seed level/exclusion —
  derive the seed level from the spec's `uatLevel` export (harness spec §5 convention:
  `export const uatLevel = { level: "...", without: [...] } as const`).
- The **job-search spec has no `uatLevel` named export** — add it so the derived-level path works
  for it (use the level it currently runs at; `without: ["job-search"]` maps to that spec's own
  exclusion). Verify against the approved harness spec §5.

### #1034 — non-blocking seed cleanups (all three) + test placement
- `tests/uat/seed/tasks.ts:~37` — re-seed inserts **duplicate task rows** (latent). Make it
  idempotent (guard / upsert / clear-before-insert consistent with the other seeders).
- `tests/uat/seed/connections.ts:~33` — DB pool **not closed on the error path**. Ensure the pool
  is closed in a `finally` (or equivalent) so an error can't leak a connection.
- `tests/uat/provisioner.ts:~430` — **unreachable `--excludeChunks` branch**. Remove the dead
  branch (or make it reachable if it was meant to be — check the issue's intent; default = remove).
- **Test placement:** move the **DB-free `describe` block** in `tests/uat/seed/admin.test.ts` to
  `tests/unit/` (it's a pure unit fence check that shouldn't sit in the DB-integration suite). Keep
  any DB-requiring cases where they are. (This is my filed follow-up on #1034 — do it in this PR.)

## Guardrails (HARD)

- **`docs/coordination/` is coordinator-only — never edit it.**
- **No repo-wide `pnpm format` / `git add -A`** — stage explicit paths only (shared tree).
- Do not change the #1040 fence semantics (`JARVIS_UAT_SEED_CONFIRM`, prod-compose token default,
  admin.ts log-site re-check). If a cleanup touches that code, preserve the fail-closed behavior.
- **Never edit an applied migration** — none expected here; if you think you need one, escalate.
- Keep the change minimal and mechanical. No feature additions, no refactors beyond the listed items.

## Exit criteria

- `pnpm test:uat -- tests/uat/specs/<some-single-spec>` runs **that one spec** (filter honored) and
  seeds at the spec's declared `uatLevel` — prove it in the PR body.
- Re-running a seed does **not** create duplicate task rows.
- The full local gate is green: `pnpm verify:foundation` (or record the exact commands + exit codes
  if CI is the gate). File-size + format:check + typecheck + lint must pass.
- `admin.test.ts` DB-free describe now lives under `tests/unit/` and runs there.

## Process

- STEP 1: `pnpm install`. STEP 2: read this handoff + both issues (#1047, #1034) in full, then
  follow the **coordinated-build** skill: escalate your PLAN to label `Coordinator` before building.
- Open a PR titled for #1047 + #1034; include the release-note-language user summary (CLAUDE.md
  rule) — here it's dev-only tooling, so say "developer-facing test harness only, no user-visible
  change." Report PR # + your green evidence to label `Coordinator` when done.

# PR #1065 live UAT handoff

## Scope

- PR: #1065, branch `ux/993-host-truth`, security tier.
- This worktree starts at exact PR head `13e6352c0dff500cf389c7d72bc677003238abe9`.
- Verify the user-facing host-truth slice in a live dev instance. Do not edit feature code.
- Use the `webwright` skill and keep all generated proof under `webwright-proof-1065/`.

## Required live path

1. Start the real local stack from this checkout using the repository's documented dev commands.
2. In a fresh Firefox browser session, create a fresh owner account.
3. Navigate through the real UI to Settings → Advanced host setup.
4. Capture the host-health summary before action.
5. Exercise the one-click **Install Herdr** control through the UI and record the displayed structured
   result (`installed`, `failed`, or `timeout`). Do not parse or expose installer stdout/stderr.
6. Capture the resulting host-health summary and verify the UI/API exposes no secrets or raw error
   output.

## Evidence contract

- Follow Webwright's `plan.md`, `final_runs/run_<id>/final_script.py`, screenshots, and action-log
  contract exactly; Firefox headless, 1280×1800 viewport, no full-page screenshots.
- Visually inspect every cited PNG and mark each critical point only with unambiguous evidence.
- If the live path is blocked, record the exact repeated UI evidence and report RED; never simulate
  success.
- Post a durable live-UAT verdict on PR #1065 with links to the run log and screenshots. Follow an
  existing repository/PR convention if one exists. Otherwise commit only `webwright-proof-1065/`
  to this UAT branch, push it, and link GitHub blob URLs from the PR comment.
- Do not modify the PR branch, feature code, board, or merge state.

## Start

1. Run `[ -d node_modules ] || pnpm install`.
2. Read the `webwright` skill in full and use only the relevant UAT/exit-criteria sections of
   `docs/superpowers/specs/2026-07-15-settings-host-account-truth.md`.
3. Execute the live path, self-verify all evidence, post the PR comment, then message exact label
   `UX Coordinator` with only: GREEN/RED, proof commit/link, PR-comment link, and any blocker.

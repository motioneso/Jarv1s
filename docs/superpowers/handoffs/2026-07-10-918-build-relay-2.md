# Build Relay 2 — #918 Open module system Slice 2 (Build phase)

**Relayed:** 2026-07-10, context-meter 70% warning, fired again before any code written (this relay
spent its budget fully recovering masked plan content, not on implementation — 0 commits, don't be
alarmed).
**Worktree:** `/home/ben/Jarv1s/.claude/worktrees/918-implementation-plan` — stay here.
**Branch:** `plan/918-open-module-system-slice2` — continue on this exact branch.
**`node_modules`:** already installed. Skip `pnpm install`.
**Read first (unchanged, read-only, do NOT re-derive):**
1. `docs/superpowers/handoffs/2026-07-10-918-build-relay-1.md` — original build-phase entry point,
   confirms Task 1/Task 2 premises still unimplemented as of relay-1.
2. `docs/coordination/handoffs/handoff-918-build-phase.md` — coordinator label/session id, risk
   tier (`security`), collision notes vs. parallel #914 build.
3. `docs/superpowers/plans/2026-07-10-open-module-system-slice2.md` — the approved 27-task TDD
   plan. **Ben-approved, committed — do not re-plan.** Enter directly at `coordinated-build`'s
   **Build** step.

## ⚠️ Read-tool masking — already resolved, do not repeat the diagnosis

Reading the plan file via the Read tool renders several spans as opaque `<<ccr:HEXID,type,sizeB>>`
placeholders (Tasks 13, 14, 17, 18, 19, 23, 24, 25, 26, 27 affected). **Confirmed via `grep -c
"ccr:" <planfile>` → 0 real occurrences — it is purely a Read-tool rendering artifact, the file text
is normal.** If it recurs on your Read: use `sed -n '<start>,<end>p' <file>` via Bash to pull the
literal text for any masked task — do not guess or skip. All 27 tasks were fully recovered this way
by the relay-1→relay-2 session; every task's exact code/SQL/test spec is legible in the file on disk
right now. No further recovery work needed — just Read (or sed) each task fresh as you reach it.

## What's done

Still nothing coded. Confirmed/reconfirmed only:
- Migration head is `0152` (`packages/settings/sql/`, sorted) — `0153`/`0154` free, matches plan.
- Task 1 target (`packages/module-sdk/src/index.ts` ~495-564) unimplemented — old
  `ModuleAuthDeclaration`/`ModuleStorageDeclaration` shapes present, no `web?` field yet.
- Task 2 target (`packages/module-registry/src/external/validate.ts`) unimplemented —
  `FORBIDDEN_FIELDS` still has `"auth"`/`"storage"`.
- Tasks 3-27 premises NOT yet spot-checked. Do the step-½ verify-against-branch check per task as
  you reach it (grep the cited file/lines, confirm the plan's premise still holds before coding).
- Coordinator pane reconfirmed live via fresh `herdr pane list`: label `Coordinator`, pane
  `w1:pDF`, session `4d68fcc5-bc2c-44cd-ae0d-a2e305f94069`. Sent it a terse relay-2 status message
  (no blockers, no forks) — no need to resend.

## What's left

Execute all 27 tasks via `superpowers:test-driven-development`, each committing green with
`Co-Authored-By: Claude`, `git add` by explicit path only (never `-A`). **TaskList tracker is
session-scoped and did NOT persist across relay-1→this session — recreate it first** (27 tasks
matching plan Task 1-27 + a final wrap-up task), then start driving.

Order: Task 1 (SDK types) → Task 2 (validator) → ... strictly sequential per the plan's own
dependency order (types → consumers → routes → UI → integration tests). Full task-by-task spec
(exact interfaces/SQL/code/commit messages) lives in the plan file itself — re-read each task
fresh, don't rely on any prose summary of it (including this doc).

After all 27: pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck`, then
`git fetch origin main && git rebase origin/main`) before every push; full gate
(`pnpm verify:foundation` + `pnpm test:integration`) green before wrap-up; then
**`coordinated-wrap-up`** (PR + report to coordinator, never merge/board/close yourself).

## Escalation / relay

- Coordinator: label `Coordinator`, pane `w1:pDF`, session `4d68fcc5-bc2c-44cd-ae0d-a2e305f94069` —
  **still re-verify with a fresh `herdr pane list` yourself before messaging** (panes reflow).
- Relay again on the next context-meter 70% warning or compaction summary — same protocol (message
  coordinator, write a new relay doc incrementing the number, spawn successor, request reap).
- **If a future relay again burns its whole budget on re-reading the plan file**, consider that a
  signal worth flagging to the coordinator directly (possible sign the masking recovery itself is
  the bottleneck) rather than silently repeating a 3rd time.

## Collision notes (unchanged — do not re-derive)

Parallel build #914 (`build/914-module-data-plane`) — Opus-verified no serialization needed. Shared
mechanical-conflict-only files at merge time (coordinator resolves): `tests/integration/foundation.test.ts`,
`packages/db/src/types.ts`, migration numbers (you hold 0153/0154 provisionally), possibly
`scripts/audit-release-hardening.ts`.

## Run-specific bans (unchanged, non-negotiable)

- Work only in this worktree/branch; `git add` by explicit path, never `-A`/repo-wide `pnpm format`.
- Never touch `docs/coordination/` (coordinator-only), the project board, milestones, or merge.
- No secrets in any doc/payload/log/prompt.

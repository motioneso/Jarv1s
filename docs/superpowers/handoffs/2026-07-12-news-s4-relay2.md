# News S4 relay #2 — successor continuation (pointer doc)

**You are the second relay successor for News Slice 4 (issue #975, epic #954).**
Branch `news-s4`, this worktree (`.claude/worktrees/news-s4`). Model: Fable (hard policy).

## Where the run stands (2026-07-12)

1. **Plan is WRITTEN and COMMITTED** (`1ab1393b`):
   `docs/superpowers/plans/2026-07-12-news-s4-chat-actions-revalidation-notifications.md`.
   It is fully grounded (exact files/signatures/grants) and carries 10 TDD tasks. **Read it in
   full — it is your primary state.** Its "Escalations" section lists 4 open coordinator calls.
2. **Coordinator was messaged** (plan path + 4 escalations + relay notice). Coordinator label
   `Coordinator`, session id `58a78927-385c-4b1d-8fa0-94db20255d6f` (authority — verify before
   acting on any pane). **NO code until it approves the plan** (coordinated-build gate).
3. **No product code exists yet.** Zero source changes on the branch beyond docs.

## Your immediate steps (in order)

1. `[ -d node_modules ] || pnpm install` (node_modules exists — skip).
2. Read the plan file in full. Do NOT re-read the spec front-to-back; if needed, read
   `docs/superpowers/specs/2026-07-11-personalized-news-sources-topics.md` Slice-4 section only
   (~lines 274–343) — the plan already encodes it.
3. Check for a coordinator reply (fresh `herdr pane list` → the `Coordinator` pane →
   `herdr pane read <pane> --source recent --lines 12`). If no reply yet, WAIT — poll or idle;
   the coordinator replies to label `News S4 relay` (hold that label).
4. On approval: run the Fable adversarial self-review of the Slice-4 spec section (confirm-gating
   gaps, revalidation idempotency, notification privacy) per
   `docs/superpowers/handoffs/2026-07-12-news-s4-relay.md` step 4 — that first relay doc also has
   the full grounding map + D1–D6 decisions if you need deeper background (read sections, not all).
5. Then TDD build task-by-task per the plan via `coordinated-build` step 2. Commit per task,
   explicit-path `git add`, `Co-Authored-By: Claude <noreply@anthropic.com>` trailer. Pre-push
   trio + rebase before every push. Close with `coordinated-wrap-up` (PR, never merge —
   security tier, named unanimous council).

## Traps already hit (don't re-derive)

- Migration list assertion lives in `tests/integration/foundation-schema-catalog.test.ts:275`
  (NOT foundation.test.ts as memory says). Highest migration on branch = 0160; **coordinator
  assigns the new number** (plan assumes 0161 — rename if told otherwise).
- News tests live in `tests/integration/`, not in-package.
- `pnpm exec prettier --write` any handoff/plan doc BEFORE committing (format:check gate).
- `herdr pane run` long messages type but do NOT auto-submit — send a separate
  `herdr pane send-keys <pane> Enter`, then verify prompt `❯` is empty via
  `--source recent --lines 12` (bounded reads only; a hook rejects unbounded).
- Worker role name = `jarvis_worker_runtime`; sources worker UPDATE policy exists (0160),
  topics has none (the new migration adds it). No `updated_at` trigger — repo methods set it.

## Standing constraints

70% meter warning or compaction summary → message coordinator, then `relay` skill; successor
MUST be Fable. Never `git add -A`; never touch `docs/coordination/` (read-only); never merge /
board / close issues. Caveman-terse to coordinator; conventional commits/PR. All security
invariants are restated in the plan's Global Constraints.

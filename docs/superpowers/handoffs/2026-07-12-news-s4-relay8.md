# News Slice 4 — relay continuation #8 (issue #975, epic #954)

You are the eighth relay successor building News Slice 4. Same worktree
(`~/Jarv1s/.claude/worktrees/coord-2026-06-30-rfa-fleet/.claude/worktrees/news-s4`), branch
`news-s4`. Relay, not parallel work — predecessors are gone once reaped.

## Primary state

1. This doc (short).
2. `docs/superpowers/plans/2026-07-12-news-s4-chat-actions-revalidation-notifications.md` — THE
   plan. Read BY TASK, only the task you are on (T7=~:255, T8=~:301, T9=~:318, T10=~:335). Never
   front-to-back. Do NOT read the spec in full.
3. `docs/coordination/handoff-news-s4.md` — coordinator-owned, READ-ONLY, never `git add` it.

## Coordinator + protocol

- Label `Coordinator` (resolve pane fresh via `herdr pane list`; session id authority
  `58a78927-385c-4b1d-8fa0-94db20255d6f`). Caveman-terse messages via `herdr-pane-message`.
- Build per `coordinated-build`; TDD per `superpowers:test-driven-development`; commit green per
  task, `Co-Authored-By: Claude <noreply@anthropic.com>`; explicit-path `git add` only.
- Relay at the 70% context-meter warning via the `relay` skill. **Successor MUST be Fable.**
- Pre-push: `pnpm format:check && pnpm lint && pnpm typecheck` + rebase on origin/main.
- Closeout = `coordinated-wrap-up` (PR only). Merge gate = named-unanimous Opus+Codex+Gemini. PR
  body MUST state the migration RLS approach (column-scoped grants, owner-scoped policy) and the
  notification privacy test.
- Plan approval ALREADY GRANTED — do not re-ask. Directives: (1) migration 0161 done; (2) ONE
  summary notification, spec wording; (3) Task 9 = wire EXISTING Settings scaffolds + Retry
  button only; (4) 6 chat tools, edit+unexclude stay REST/Settings-only.

## Done so far (commits on news-s4)

- Tasks 1–4 ✅ (`bd8c8241`, `decd1725`+`d3348a10`, `4c7cbb97`, `616c34fe`); Task 5 ✅
  (`8e2dd24a`+`eed0142d`, schedule key = bare actorUserId — pg-boss rejects colons, deviation
  already reported); Task 6 ✅ (`b7f76737`, POST /api/news/revalidation).
- Task 7: NOT started in code — relay #7 spent its leg on precedent study. All findings below;
  do NOT re-read the gateway/harness files, just build.

## Resume here — Task 7 RED (news.previewSource / news.confirmSource)

Read plan "### Task 7" (~:255) for the interface, then write
`tests/integration/news-chat-tools.test.ts` RED and build GREEN.

**HARNESS CORRECTION (supersedes relay7's pointer to ai-tools.test.ts):** the REST
`/api/ai/assistant-tools/.../invoke` route NEVER executes writes on confirm (403 + audit-state
only). The confirm-executes path is the chat gateway. Copy
`tests/integration/js08-decide-confirm-audit.test.ts`: construct `AssistantToolGateway` directly
(`{ resolveActiveModules: async () => [newsModuleManifest], repository: new AiRepository(),
runner: new DataContextRunner(appDb), tokens: new SessionTokenRegistry(), confirmations: new
ConfirmationRegistry(), notifier: { emit: (_s, r) => emitted.push(r) }, confirmTimeoutMs: 5_000 }`),
mint token `{ actorUserId, chatSessionId, allowedToolNames: null }`, `gateway.callTool(...)`
unawaited for writes → poll `emitted` for the `action_request` → assert nothing executed →
`gateway.resolveActionRequest(actorUserId, actionRequestId, "confirmed")` → await result → poll
`app.jarvis_action_audit_log` (~100×5ms loop, fire-and-forget write) for
`approval_mode='confirmed'`, `outcome='success'`. Imports from `@jarv1s/ai`.

**Gateway facts (verified this leg):**

- `GatewayToolResponse = {ok:true, data} | {ok:false, denied:true, reason} | {ok:false, error}`.
  Execute throw → sanitized `{ok:false, error:"Tool <name> failed"}` + audit outcome=failed.
- Gateway renders execute output via `renderAndCap`
  (`packages/ai/src/gateway/output-validation.ts:78`) → `data` becomes `{ text: string }`
  (trust-boundary-wrapped when `externalContent:true`). **One unverified detail:** behavior of
  `sanitizeAssistantToolResult` with undefined outputSchema + `renderToolResult`'s text format —
  check that file BEFORE finalizing assertions. Robust pattern from js08: `toMatchObject({ok:true})`
  + `JSON.stringify(result)` contains/not-contains; extracting `confirmationId` for the follow-up
  confirm call is the one place structure matters (regex over `data.text`, or declare an
  outputSchema on previewSource so the shape survives).
- Write tools with NO `actionFamilyId` are always confirm-gated (email precedent
  `packages/email/src/manifest.ts:218`). Both tools `permissionId:"news.prefs"`.

**Test deps (decided):** call `configureNewsChatTools` in the test with stub
discovery/availability copied from `tests/integration/news-personalization-routes.test.ts`
(:17 feed const, :53-77 stubs — URL input `https://example.com/feed.xml` → one candidate, domain
`example.com`), fresh `createPreviewStore()`, real `NewsPersonalizationRepository`, `boss: null`
(type deps `boss: PgBoss | null` to match `triggerNewsRefresh`).

**Error-style split (decided):** benign failures (availability gate, expired, ambiguous,
duplicate, limit) → tool-error STRINGS in returned data; security violation (resubmitted
label/domain ≠ stored candidate) → execute THROWS → `{ok:false, error:"Tool news.confirmSource
failed"}` + audit outcome=failed.

**RED list:** previewSource runs unconfirmed (read risk) + returns candidates; confirmSource
unconfirmed → pending action request, NOT executed (no source row); confirm → source row + audit
confirmed/success; tampered domain → error, no row; cross-owner confirmationId replay → expired,
no row; no provider/model string in any tool output.

**Build (plan Task 7):** `packages/news/src/chat-tools.ts` deferred-deps pattern per
`briefing-tool.ts` (`configureNewsChatTools({previews, discovery, availability, boss,
repository})`, throw "(composition-root bug)" if unconfigured, `assertDataContextDb`);
extract confirm-route logic (`personalization-routes.ts` ~:287) into exported
`confirmSourceFromPreview(scopedDb, deps, actorUserId, {confirmationId, candidateId?})` →
`{ok:true, source} | {ok:false, reason:"expired"|"ambiguous"|"duplicate"|"limit"}`;
`PersonalizationRouteDependencies` gains `previews?` (default `createPreviewStore()`); routes.ts
creates ONE shared store + calls `configureNewsChatTools`; manifest gets both assistantTools
(`news.previewSource` risk read, `externalContent:true`; `news.confirmSource` risk write,
summarize from INPUT label/domain only, execute cross-checks stored candidate, writes ONLY the
stored candidate, then `triggerNewsRefresh`); export from index.ts. Commit
`feat(news): assistant preview/confirm source tools (#975)` + user-facing summary line.

Then Tasks 8–10 in order (T8 = removeSource/addTopic/removeTopic/addExclusion; T9 = Settings
wiring per directive #3; T10 = full gate + `coordinated-wrap-up`). Re-read the plan's task
section fresh before each.

## Traps (carried)

- Focused integration test: `pnpm exec tsx scripts/test-integration.ts
  tests/integration/<file>.test.ts`. `pnpm test:integration -- <name>` does NOT filter (~11 min).
- `tests/integration/ai-tools.test.ts` (~:458) asserts the FULL queue-name list — no new queues
  for Tasks 7–9, leave it.
- fast-json-stringify strips undeclared response fields — declare new REST fields in
  `packages/shared/src/news-api.ts`; test via inject. (Chat-tool output is NOT route-serialized.)
- NO new keys in `ALLOWED_PAYLOAD_KEYS` (`packages/jobs/src/pg-boss.ts:76`).
- `herdr pane read` MUST use `--source recent --lines 12` (hook rejects `visible`).
- `has_table_privilege` ignores column grants — worker-posture `[true,false,false,false]` stays.
- Prettier every doc AND test file BEFORE committing.
- agentmemory `memory_smart_search` returns empty in this env — don't block.
- Never `sed -i` a file you plan to Edit. Outside watcher may touch files — re-read if stale.

## Boot sequence for you

1. `[ -d node_modules ] || pnpm install` (no-op expected).
2. Write the RED tests (`tests/integration/news-chat-tools.test.ts`) using the facts above —
   the only file left to check is `packages/ai/src/gateway/output-validation.ts` (sanitize with
   undefined schema + render format). BUILD — reading is not progress.
3. Message Coordinator: "relay #8 driving, Task 7 RED starting" and ask it to reap predecessor
   pane (label `News S4 relay7`, session `d49fe0bd-7a4b-43e3-802b-9de5f22f1bd3`).

# News S4 relay15 — council rework: tests DONE + green, finish wrap-up only

You are relay15 (**Fable**), branch `news-s4`, same worktree. Read
`docs/superpowers/handoffs/2026-07-12-news-s4-relay13.md` "Then (wrap-up)" + "Traps" sections
ONLY (the mandate/design sections are complete — do not re-read or re-derive). Resume via
`coordinated-build`. You do NOT merge.

## Done (do not redo)

- Both council gaps are WRITTEN, COMMITTED (`6815f526`), and GREEN:
  - GAP 1 (Req C): sentinel scan in `tests/integration/news-revalidation.test.ts` — `SENTINEL`
    const + `sentinelFetch` (module scope), parameterized `seedValidationRows`, widened
    `registerWorkers` (fetch/logger), `sentinelLeaks()` collector (scans pgboss.job data+output
    + captured logs + notifications), negative test with non-vacuity anchors, positive-control
    test planting the sentinel on all three surfaces (expects
    `["jobRows","revalidationLogs","notifications"]`).
  - GAP 2 (Req F): adversarial describe in `tests/unit/news-settings-pane.test.tsx` — hostile
    label/guidance escape assertions incl. double-escape + `&quot;` attribute check, zero-anchor
    assertion under `allOn`; under `allOff` BOTH PrereqGates render (index.tsx:488 + :578 —
    verified), so the href assertion dedupes: `[...new Set(hrefs)]` equals
    `["/settings?section=assistant"]`, none `javascript:`/`data:`. Mention this
    both-gates/dedupe point in the PR body's GAP-2 statement.
- Suites: unit 17/17 passed; FULL `pnpm test:integration` (sanctioned runner — bare
  `pnpm vitest run` on integration files REFUSES the shared DB reset, don't use it) =
  **150 files / 1613 passed, 2 skipped**, exit 0. Prettier already run on both test files.

## Remaining (relay13 doc steps 2–5, in order)

1. Full gates: `pnpm verify:foundation > /tmp/cb-vf.log 2>&1; echo "VF_EXIT=$?"` and
   `pnpm audit:release-hardening > /tmp/cb-audit.log 2>&1; echo "AUDIT_EXIT=$?"`.
2. PR #977 body: add "Sentinel approach (council re-run)" section — sentinel string
   `SENTINEL-975-PRIVATE-e5b1c9`, 3 scanned surfaces (pg-boss data+output, worker+revalidation
   logs, notifications), positive control, and the GAP-2 statement: no user/model-derived anchor
   exists in the settings pane; only anchor = static PrereqGate `/settings?section=assistant`.
3. Pre-push trio (`pnpm format:check && pnpm lint && pnpm typecheck`) +
   `git fetch origin main && git rebase origin/main`, push (includes `6815f526` + this doc).
4. `coordinated-wrap-up`, then herdr-pane-message to Coordinator — label `Coordinator`, session
   `58a78927-385c-4b1d-8fa0-94db20255d6f` (verify session id after resolving by label; never
   trust a pane number) — caveman-terse: gaps done, exit codes, PR updated, ready for re-council.
   Then STOP.

## Traps

- Never stage `.claude/context-meter.log` or `docs/coordination/*` — explicit paths only.
- Herdr reads: `--source recent --lines 12`.
- Working tree should be clean except `.claude/context-meter.log` — if anything else is dirty,
  investigate before staging.

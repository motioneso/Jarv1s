# News S4 relay14 — council rework: WRITE the two tests (design done)

You are relay14 (**Fable**), branch `news-s4`, same worktree. **FIRST read
`docs/superpowers/handoffs/2026-07-12-news-s4-relay13.md` in full** — mandate, wrap-up steps,
traps, coordinator label/session all live there. This doc is only relay13's delta: relay13
verified all ground truth and finalized the test design below. Do NOT re-derive; write the code.
`[ -d node_modules ]` is true — no install. Test-only changes; you do NOT merge.

## Tree state (uncommitted, intentional)

`tests/integration/news-revalidation.test.ts:16` already has the new import
`import type { NewsCompilationLogFields } from "../../packages/news/src/compilation/compile.js";`
(currently unused → lint red until you write Test A). Only other dirty file:
`.claude/context-meter.log` — never stage it.

## GAP 1 — sentinel scan (tests/integration/news-revalidation.test.ts, 720 lines)

Verified facts: `registerWorkers` helper ~:408 (hardcodes `fetch: fetchOk`, `logger: {info:()=>{}}`
— widen with optional `fetch?: NewsSafeFetchPort` and `logger?: {info(f: NewsCompilationLogFields): void}`).
`seedValidationRows(fingerprint)` ~:340 does two bootstrap INSERTs with literal label/guidance.
Queues pre-exist via `migratePgBoss` inside `resetFoundationDatabase` (raw `appBoss.send` works —
that's exactly the sendJob-guard bypass a regression would use). `NEWS_REVALIDATE_QUEUE` policy
`exclusive` = max 1 queued+active, fine since Test B enqueues nothing else. Worker `output`
column = `{transitionedToAttention}` — scan it too. Revalidation flow: fetch(feedUrl) body →
sampleFeedHeadlines → decideSourcePolicy(description=label, sampleHeadlines) → allowed:false ⇒
rejected ⇒ ONE notification (counts-only metadata).

Write:
1. Module scope near `FEED_BODY` ~:115: `const SENTINEL = "SENTINEL-975-PRIVATE-e5b1c9";`,
   `SENTINEL_FEED_BODY` (same rss shape, headline carries SENTINEL), `sentinelFetch` stub.
2. Parameterize `seedValidationRows(fingerprint, opts: {sentinel?: string} = {})` — move
   label/guidance to SQL params: source label `The Example Times[ +sentinel]`, topic label
   `AI Safety[ +sentinel]`, guidance `focus on policy[ +sentinel]`. Existing callers unchanged.
3. Inside jobs describe, collector:
   `async function sentinelLeaks(captured: Record<string, unknown>): Promise<string[]>` —
   `SELECT data, output FROM pgboss.job` via bootstrap → surfaces = `{jobRows: rows, ...captured}`
   → return names where `(JSON.stringify(v) ?? "").includes(SENTINEL)`.
4. **Test A (negative)**, append after "refresh run does not enqueue…": seed w/ sentinel; capture
   `workerLogs: unknown[]` + `revalidationLogs: NewsRevalidationLogFields[]` via registerWorkers
   `{ai: makeWorkerAi({fingerprint:"fp-new", allowed:false}), fetch: sentinelFetch, logger, revalidationLogger}`;
   `enqueueNewsRevalidation(appBoss, ids.userA)`; waitFor notification (copy pattern ~:446).
   Non-vacuity anchors: `sources[0].label`/`topics[0].label`/`topics[0].guidance` contain SENTINEL
   (via repository list calls under asActor), `sources[0].validationStatus === "rejected"`,
   revalidationLogs toContainEqual `{event:"news_revalidation_run", sourcesChecked:1, topicsChecked:1,
   sourcesNeedingAttention:1, topicsNeedingAttention:1}`. Then
   `expect(await sentinelLeaks({workerLogs, revalidationLogs, notifications: visible.notifications})).toEqual([])`.
5. **Test B (positive control)**: plant on all three surfaces — (a) raw
   `appBoss.send(NEWS_REVALIDATE_QUEUE, {actorUserId: ids.userA, kind:"revalidate",
   idempotencyKey:\`news-revalidate:${ids.userA}\`, leakedLabel:\`The Example Times ${SENTINEL}\`})`;
   (b) `const revalidationLogs: unknown[] = [{event:"debug", detail:\`checking ${SENTINEL}\`}]`;
   (c) notification with SENTINEL in body via
   `workerContext.withDataContext({actorUserId: ids.userA, requestId: crypto.randomUUID()}, (db) => notifications.create(db, {moduleId:"news", title:"News sources need attention", body:\`…${SENTINEL}…\`, metadata:{kind:"news_revalidation"}, urgency:"normal"}))`
   (worker role writes notifications in prod path; app-role INSERT unverified — use workerContext).
   Read back via asActor listVisible. Expect
   `sentinelLeaks({revalidationLogs, notifications: listed.notifications})` toEqual
   `["jobRows","revalidationLogs","notifications"]`.
KEEP every existing test/assertion byte-identical. Rich why-comments citing Req C + council re-run.

## GAP 2 — adversarial render (tests/unit/news-settings-pane.test.tsx, 327 lines)

Verified ground truth (state in PR body): **no user/model-derived anchor exists anywhere in the
settings pane.** Only anchor in `packages/news/src/settings/**` = static PrereqGate
`href="/settings?section=assistant"` (index.tsx:189, renders only when a prereq is missing).
Stored source rows :451-452 and topic rows :507-509 are plain `<span>`; add-source.tsx candidate
label/domain :179-182 plain `<label>/<span>` (unreachable via renderToString anyway — mutation
state). news-mosaic/today-widget anchors = news page, outside settings scope.

Write new describe (harness: existing `render()` + `storedSource`/`storedTopic` spreads):
- Fixtures: source `label: '<img src=x onerror=alert(1)>&lt;script&gt;"quoted'`,
  `homepageUrl: "javascript:alert(document.cookie)"`, `canonicalDomain: 'evil.example"'`; topic
  `label: "<script>alert(2)</script>"`, `guidance: "data:text/html,<script>alert(3)</script>"`.
- Render with `availability: allOn` (no PrereqGate ⇒ ZERO anchors expected):
  `expect(html).not.toContain("<script")`, `not.toContain("<img")`,
  `toContain("&lt;img src=x onerror=")` (escaped literal), `toContain("&amp;lt;script&amp;gt;")`
  (entity-mix double-escape ⇒ no decode-after-strip), `not.toContain("<a ")` (no anchor at all ⇒
  javascript:/data: URLs can never become hrefs), aria-label quote escaping: raw `"quoted` never
  appears inside an attribute unescaped — assert `toContain("&quot;quoted")`.
- Second render with `availability: allOff`, same adversarial rows: collect
  `[...html.matchAll(/href="([^"]*)"/g)].map(m => m[1])` → toEqual(["/settings?section=assistant"])
  and none matches `/^(javascript|data):/i`.

## Then

Follow relay13 doc "Then (wrap-up)" EXACTLY (prettier on the two test files, affected suites →
full gates w/ exit codes, PR #977 body: add "Sentinel approach (council re-run)" section — sentinel
string, 3 surfaces incl. worker logs, positive control, AND the GAP-2 no-anchor statement above —
pre-push trio + rebase, push, herdr-pane-message to Coordinator label `Coordinator`, session
`58a78927-385c-4b1d-8fa0-94db20255d6f`, caveman-terse, then STOP). Affected suites:
`pnpm vitest run tests/integration/news-revalidation.test.ts` (needs test DB up) and
`pnpm vitest run tests/unit/news-settings-pane.test.tsx`.

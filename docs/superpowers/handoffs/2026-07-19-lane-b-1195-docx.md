# Lane B handoff — DOCX resume extraction in chat attachments

**Issue:** task #1195 (Part of feature #1193) · **Spec (source of truth):**
`docs/superpowers/specs/2026-07-19-job-search-embedded-onboarding.md` §Resume upload, item 6.
Read `AGENTS.md` and the issue body first.

## Mission

Add DOCX text extraction to the existing #1133 chat-attachment pipeline
(`packages/chat/src/attachments-service.ts`):

- `mammoth.extractRawText` for text extraction, feeding the same engine-manifest path as PDF
  (`ATTACHMENT_TEXT_CAP_CHARS` applies unchanged).
- Whitelist the docx mime type; magic-byte gate = `PK\x03\x04` sniff **plus** a
  `word/document.xml` zip-entry check so renamed xlsx/pptx are rejected.
- Extraction-failure note mirroring the existing PDF one
  ("[PDF text extraction failed for this attachment]" precedent).
- Commit a real `.docx` test fixture.

## Exit criteria (from issue #1195)

Unit tests incl. renamed-xlsx rejection and extraction-failure note; full gate green. Size S,
1 PR, base `main`.

## Process

- Work ONLY in your assigned worktree/branch; never touch the shared checkout `~/Jarv1s`;
  stage explicit paths only — never `git add -A`.
- First step: `pnpm install` (fresh worktree).
- No migrations, no frontend changes in this lane.
- Generous why-comments citing issue #1195/#1193. PR body carries the user-facing summary
  ("you can now attach Word (.docx) documents in chat and Jarvis reads their text") and
  "Part of #1193".
- `pnpm verify:foundation` green (real exit code) before the PR.
- **Do NOT merge your PR** — push, open PR, report done; coordinator runs independent QA and
  merges.

## Start

1. `pnpm install`
2. Read spec §Resume upload + issue #1195 + `attachments-service.ts` (PDF path is the template).
3. Test-first: fixture + rejection cases, then implementation.

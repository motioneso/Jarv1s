# mod-dist-964 relay 4

Issue: #964 (module distribution & install, epic #860). Security risk tier.
Coordinator label: **Coordinator** — re-resolve pane fresh via `herdr pane list`
before messaging (never trust a baked-in pane number).

Spec: `docs/superpowers/specs/` (module distribution — see relay-3 for exact
filename if needed). Plan: `docs/superpowers/plans/2026-07-12-module-distribution-install.md`.
Worktree/branch: this worktree, branch `mod-dist-964`.

## Standing constraints (still in force)

- Migration 0162 already applied — **do not touch**.
- `capabilities.ownsTables` is `readonly string[]`, NOT `boolean` (fixed in `c0c55a8d`).
- Explicit-path staging only (`git add <paths>`, never `-A`/`.`).
- Never touch `docs/coordination/` or the GitHub board/issues — Coordinator-owns those.
- No secrets ever logged/returned. Never edit an applied migration.
- `@jarv1s/settings` must not import `@jarv1s/module-registry`.
- 1000-line file-size gate on every source file (incl. CSS).
- Fastify response schemas need every emitted field declared explicitly (silent drop otherwise).
- Relay again at the 70% context-meter warning or immediately on seeing a compaction summary.
- Read the plan **by section** for your current task only — never front-to-back.

## Commits so far (Tasks 1-5 partial)

- Task 1: `b4af4976`
- Task 2: `02dc9f5c`
- Task 3: `a56a644b`
- `ownsTables` bug fix: `c0c55a8d`
- Task 4 steps 1-4: `8a784b60`
- Task 4 step 5 (rolling-release registry publish workflow, `.github/workflows/modules-registry.yml`): `b21bdda2`
- **Task 5 checkpoint (this relay): `1394d3fe`** — download/verify/extract/stage pipeline files
  written, typechecked, `check:package-deps` clean. `extract.ts`'s
  `safeExtractModuleTarball` is fully implemented and TESTED GREEN
  (`tests/unit/module-distribution-extract.test.ts`, 5/5 pass) — including the real
  node-tar v7.5.16 bug found and fixed this session (throwing synchronously inside
  `tar.t()`'s `onReadEntry` skips node-tar's auto `entry.resume()`, causing a 30s hang
  + uncaught exception instead of a promise rejection; fix = record the violation,
  return normally, throw once after `tar.t()` resolves — see the comment block in
  `extract.ts`). `pipeline.ts`, `registry-source.ts`, `stage.ts` are typechecked and
  wired into `packages/module-registry/src/node.ts` but have **no unit test yet**.

## Next steps (Task 5 remainder, plan lines ~1693-2487)

1. Create `tests/unit/module-distribution-pipeline.test.ts` per plan Step 5
   (plan lines ~1984-2188) — tests `resolveRegistryIndexUrl`, `fetchRegistryIndex`,
   `downloadAndStageModule` against a fake host-pinned fetch + crafted fixtures.
2. Run it; fix anything real it surfaces in `pipeline.ts`/`registry-source.ts` (neither
   has been exercised by a test yet, only typechecked — treat their correctness as
   unverified until this test is green).
3. Run `pnpm vitest run tests/unit/module-distribution-extract.test.ts tests/unit/module-distribution-pipeline.test.ts` together — expect all PASS (plan Step 8).
4. Re-run `pnpm --filter @jarv1s/module-registry typecheck` and `pnpm check:package-deps`
   (both were green as of `1394d3fe`; re-check after any pipeline-test-driven fixes).
5. Commit Task 5's remainder: `git add tests/unit/module-distribution-pipeline.test.ts`
   plus any fix files, message: `test(modules): pipeline integration coverage (#964)`
   (or fold fixes into this commit if `pipeline.ts`/`registry-source.ts` need changes).
6. Continue to **Task 6** onward per the plan (contracts/admin registry routes, boot
   reconcile, boot/compose wiring, admin web UI, integration suite, full gates) —
   read each task's plan section fresh, verify premises against branch state before
   building (same discipline applied this session: grep-verify every cited
   export/signature before trusting the plan).
7. After the final task's gates are green: invoke `coordinated-wrap-up` → open PR
   ("Part of #964") → report to Coordinator with verified evidence. Never merge, move
   the board, or close issues — that's the Coordinator's job exclusively.

## Task boundaries in the plan (for quick section reads)

Confirmed via `grep -n "^### Task" docs/superpowers/plans/2026-07-12-module-distribution-install.md`:
Task5=1693, Task6=2488, Task7=3609, Task8=4207, Task9=4442, Task10=4884.

# #1110 app-map relay checkpoint 13

Branch: `build/1110-app-map`, this worktree. `node_modules` present — do NOT `pnpm install`.
Coordinator: label `Coord-1109-1110-g3` (re-resolve fresh via `herdr pane list`; session
`cb9ca6a3-11c9-40b6-944f-6bf3713e8536` is authority — never reuse a `…-N` pane id).
Predecessor context on relay-12 doc: `2026-07-17-1110-app-map-relay-12.md` (background only).

## What changed since -12 (the important part)

The relay-12 doc under-scoped Task 8. Investigation found the 3 admin tests asserted **real
grounded-chat LLM output the UAT harness deliberately never produces** (fake provider by design,
no CLI engine in the image, no assistant/chat model seeded — `ai.ts` binds only `module.news`).
Coordinator ROUTED the **deterministic path** (drop LLM-phrase assertions, keep only surfaces
#1110 owns; defer real-LLM e2e to issue **#1121**).

**DONE this session (on disk, UNCOMMITTED):** rewrote
`tests/uat/specs/app-map-grounding.uat.spec.ts` per that routing. All the reasoning the coordinator
asked to see is **in-file as comments** — read the spec, don't re-derive:
- **test1** `no_json_model` → tied to #1110 **`withoutNewsJsonBinding` seed threading** (not
  previewOverride, not tautological: if threading regressed news binds → gate passes → error never
  renders → red).
- **test3** `data-jarvis-error-class="transient"` → tied to #1110 **`buildUatNewsPreviewOverride`
  specifically** (without override, `uat-transient.invalid` falls to `no_json_model`/prerequisite,
  so `class=transient` can only come from the override).
- **test2 DELETED** with a `#1121` comment (honest-unknown = pure chat, no deterministic surface).
- **test4 kept** + in-file `#1121` note that it's a NEGATIVE assertion, vacuous until #1121.
- Both admin tests now `goto` the settings deep-link
  `/settings?section=modules&module=news` — kills the choose-sources-empty-state timeout AND the
  News-nav 5-match strict-mode bug. `signIn()` conditional onboarding-skip kept.

Verified `tests/uat/seed/admin.ts` exports `UAT_SECOND_OWNER_EMAIL/PASSWORD` (imports OK).

## Remaining steps (all mechanical — NOT yet run)

1. `pnpm test:uat -- app-map-grounding` — must pass all 3 remaining tests. Docker is FREE.
   **First execution of the rewrite** — a selector may still miss; you have full headroom to debug.
   Keep tool output small (pipe to a file, read tail/grep for pass/fail).
2. `pnpm verify:foundation`.
3. Single commit, **explicit `git add`** (NEVER `-A`; NEVER `.claude/context-meter.log`):
   `tests/uat/specs/app-map-grounding.uat.spec.ts`, `tests/uat/run-uat.ts`,
   `tests/uat/provisioner.ts`, `tests/uat/seed/types.ts`, `tests/uat/seed/chunks/ai.ts`,
   `tests/uat/seed/levels.ts`, `tests/uat/seed/cli.ts`, `packages/news/src/routes.ts`,
   `packages/news/src/personalization-routes.ts`, `packages/module-registry/src/index.ts`,
   `docs/WHATS_NEW.md`.
4. Pre-push trio `pnpm format:check && pnpm lint && pnpm typecheck`, then
   `git fetch origin main && git rebase origin/main`.
5. `coordinated-wrap-up` → PR → report PR to coordinator label above. **Never merge/board/close.**
   PR BODY must call out: **deferred real-LLM e2e + UAT harness gap + issue #1121**, and the
   **test4 negative-assertion caveat** (vacuous until #1121) — so Ben can rule at review. Do NOT
   block on Ben; ship the deterministic PR. Merge sign-off is the coordinator's job.

## Notes
- Local-only symlink repair from -12 may still apply if node_modules resets:
  `packages/shared/node_modules/@jarv1s/module-sdk` (CI unaffected). See relay-12 doc.
- module-sdk blocker fix already committed (`34457186`); followup #1120. HEAD=`7735f192`.

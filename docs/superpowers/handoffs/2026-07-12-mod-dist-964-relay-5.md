# mod-dist-964 relay 5

Issue: #964 (module distribution & install, epic #860). Security risk tier.
Coordinator label: **Coordinator** — re-resolve pane fresh via `herdr pane list`
before messaging (never trust a baked-in pane number).

Plan: `docs/superpowers/plans/2026-07-12-module-distribution-install.md`.
Worktree/branch: this worktree, branch `mod-dist-964`.

## Standing constraints (still in force)

- Migration 0162 already applied — **do not touch**.
- `capabilities.ownsTables` is `readonly string[]`, NOT `boolean` (fixed in `c0c55a8d`).
- Explicit-path staging only (`git add <paths>`, never `-A`/`.`).
- Never touch `docs/coordination/` or the GitHub board/issues — Coordinator-owns those.
- No secrets ever logged/returned. Never edit an applied migration.
- `@jarv1s/settings` must not import `@jarv1s/module-registry` (structural mirror types only).
- 1000-line file-size gate on every source file (incl. CSS).
- Fastify response schemas need every emitted field declared explicitly (silent drop otherwise).
- Relay again at the 70% context-meter warning or immediately on seeing a compaction summary.
- Read the plan **by section** for your current task only — never front-to-back.

## Commits so far (Tasks 1-5 done, Task 6 in progress)

- Task 1-5 commits: `b4af4976`, `02dc9f5c`, `a56a644b`, `c0c55a8d`, `8a784b60`, `b21bdda2`,
  `1394d3fe`.
- **Task 5 COMPLETE: `c13aeb4d`** — `tests/unit/module-distribution-pipeline.test.ts` added,
  15/15 tests pass (`module-distribution-extract.test.ts` + `module-distribution-pipeline.test.ts`
  together), `@jarv1s/module-registry` typecheck clean, `check:package-deps` clean.

## Task 6 status: plan read in full (lines 2488-3609), premises partly verified, NO code written yet

Task 6 = shared registry contracts + lifecycle derivation + admin registry routes + server wiring.
Files: `packages/shared/src/platform-api-modules.ts` (append), NEW
`packages/settings/src/module-registry-rows.ts`, NEW `packages/settings/src/routes-module-registry.ts`,
modify `packages/settings/src/repository-external-modules.ts` (add `markExternalModuleRemoved`),
modify `packages/settings/src/routes.ts` (new `ModuleDistributionDependencies` port + registration
call ~L806), modify `apps/api/src/server.ts` (10-min index cache + port wiring), test
`tests/unit/module-registry-rows.test.ts`.

**Verified against branch (confirmed still accurate):**
- `packages/shared/src/platform-api-modules.ts` is 383 lines; `adminModuleParamsSchema` +
  `errorResponseSchema` already exist and are imported — Step 1's append is safe as written in
  the plan.
- `packages/settings/src/repository-external-modules.ts` is 348 lines. Task 3 already landed
  `ExternalModuleAdminState`, `listExternalModuleAdminStates`, `updateExternalModuleStaging`,
  `setExternalModulePurgeRequested` — all match the plan's assumed signatures exactly (checked
  full source). `markExternalModuleRemoved` does **not** exist yet — Step 6(a)'s code is correct
  to add verbatim; the `updated_at: new Date()` idiom in the plan matches what's already in the
  file (no adjustment needed).

**NOT yet verified — do this before trusting the plan's exact code for each:**
- `packages/settings/src/routes.ts`: `ExternalModulesDependencies` interface location (~L131-150),
  `SettingsRoutesDependencies`, the `registerModuleRoutes(...)` call site (~L806), the import
  block (~L84), and whether line numbers have drifted.
- `packages/settings/src/routes-modules.ts`: confirm `ModuleRoutesContext` is exported with the
  shape the plan assumes (`{ dependencies, repository, assertAdminUser, requireRequestId }`).
- `packages/settings/src/route-error.ts`: confirm `HttpError` + `handleRouteError` exports match.
- `@jarv1s/module-sdk/core-version`: confirm `compareJarvisVersions` + `satisfiesCoreVersion`
  exports/signatures match Step 4's usage.
- `apps/api/src/server.ts`: confirm the `@jarv1s/module-registry/node` import site (~L55,
  `getExternalModuleRegistrations`), `externalRuntimeEnabled` (~L200), `apiServerConfig.externalModulesDir`,
  the `registerSettingsRoutes` dependencies object (~L525), and existing `node:fs/promises` /
  `node:path` imports.
- `packages/settings/src/index.ts`: confirm the re-export pattern for `routes.js` so
  `ModuleRegistryEntryLike` can be exported the same way.

## Next steps (Task 6, follow plan steps 1-10 in order)

1. Step 1: append shared contracts to `platform-api-modules.ts` (code is fully spelled out in the
   plan at lines 2534-2726 — copy verbatim, no changes needed).
2. Step 2-3: write `tests/unit/module-registry-rows.test.ts` (plan lines 2732-2940), confirm it
   fails (module doesn't exist yet).
3. Step 4-5: implement `packages/settings/src/module-registry-rows.ts` (plan lines 2952-3072),
   confirm the 14-test suite passes.
4. Step 6: (a) add `markExternalModuleRemoved` to `repository-external-modules.ts` (plan lines
   3088-3132, verified accurate above); (b) add `ModuleDistributionDependencies` port to
   `routes.ts` (plan lines 3142-3194) — **verify the L131/L806/L84 line numbers first**, they may
   have drifted from other work landing on main/this branch.
5. Step 7: implement `packages/settings/src/routes-module-registry.ts` (plan lines 3198-3469) —
   full code given; verify `ModuleRoutesContext`/`HttpError`/`handleRouteError` imports resolve
   before pasting.
6. Step 8: wire the port in `apps/api/src/server.ts` (plan lines 3476-3585) — **verify all cited
   line numbers/imports first**, this file changes most across the whole repo.
7. Step 9: typecheck (`shared`, `settings`, `api`) + `vitest run tests/unit/module-registry-rows.test.ts`
   + `check:package-deps` — all must pass.
8. Step 10: commit exactly the file list in the plan (line 3603) with the given message.
9. Continue to **Task 7** onward (plan lines 3609+: boot reconcile, boot/compose wiring, admin
   web UI, integration suite, full gates) — read each task's section fresh, verify premises.
10. After the final task's gates are green: invoke `coordinated-wrap-up` → open PR ("Part of
    #964") → report to Coordinator with verified evidence. Never merge, move the board, or close
    issues — Coordinator-only.

## Task boundaries in the plan

Task6=2488, Task7=3609, Task8=4207, Task9=4442, Task10=4884 (confirmed via
`grep -n "^### Task" docs/superpowers/plans/2026-07-12-module-distribution-install.md`).

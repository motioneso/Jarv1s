# JS-07 relay continuation (relay doc — committed per relay skill; fine to keep in the PR branch)

You are FABLE, successor build agent for JS-07 (#936), branch `feat/js-07-freshness-dedup-fit`,
THIS worktree. Relay #3 (2026-07-11): predecessor completed Steps 1–2 end-to-end and relayed at
the 70% meter. Tree is clean; start at Step 3 immediately.

## State: PLAN APPROVED — do NOT re-request approval

- Handoff (read in full): `docs/coordination/2026-07-11-js-07-build-handoff.md` (read-only, never commit).
- Plan (read BY SECTION per current step only): `docs/superpowers/plans/2026-07-11-js-07-freshness-dedup-fit.md`.
  Steps live at lines ~63–170 (Step 3 gate ~121, Step 4 ~129, Step 5 ~136, Step 6 ~151,
  Step 7 ~157, Step 8 ~165); also read "Hard constraints" + "Key ABI additions" (lines ~28–62)
  once — they bake in the schemaVersion-1 additive-only rule and the eval/evalBudget key ABI.
- Coordinator: label `Coordinator`, session `58a78927-385c-4b1d-8fa0-94db20255d6f`. Approval,
  premise verification, and #962 fold (items 1–2 → Step 8) all done — see handoff.
- Zero migration; if one seems needed STOP + `[DESIGN-FORK]`.
- Task list may not carry over: recreate tasks mirroring plan Steps 3–8 if empty.

## DONE (do not redo)

- **Step 0 committed `1539fb6a`** — ctx.ai wired into the queue path (ai-bridge, job-handler
  extraction, worker.ts composition, `tests/integration/module-worker-queue-ai.test.ts` 7/7).
- **Step 1 committed `7773be1e`** — structured posting facts + sourceKey through ingestion:
  - `domain/keys.ts`: `sourceKey(adapterId, board)` = sha256Hex32 of `` `${adapterId}\0${board}` ``.
  - `domain/opportunities.ts`: `PostingFacts` (publishedAt/workMode/employmentType/compensation,
    all optional), `OpportunityRecord.sourceKey?`, `OpportunityInput.sourceKey?`,
    `buildStoredPosting` spreads facts only-when-present; `sourceBinding` spread on ALL THREE
    upsert branches (old records gain sourceKey on any re-sighting; sourceKey-less input
    preserves stored value).
  - `worker/handlers/run.ts`: `postingToOpportunity(adapterId, board, posting)` 3-arg, stamps
    sourceKey + facts (adapters already sanitize/cap at normalize time — pass-through correct).
  - Tests: kv-opportunities +4 (facts stored, hash stable, fact-only change re-hashes but keeps
    saved status, old-shape records fine), handlers-run +3 (mapping, no undefined placeholders,
    e2e discovery persists facts).
- **Step 2 committed `9fcc1b41`** — freshness transitions per source board:
  - NEW `domain/freshness.ts`: `Freshness` ("active"|"uncertain"|"stale"), `freshnessOf`
    (absent → "uncertain"), `transitionFreshness` (pure; seen → active+lastLivenessAt; unseen
    same-sourceKey → stale, status auto-follows ONLY from new/passed — saved/active never
    clobbered; other board / no sourceKey → null), `markFreshnessAfterRun(kv, ctx)` → counts.
  - `domain/opportunities.ts`: additive `freshness?` + `lastLivenessAt?` on OpportunityRecord.
  - Exports in `domain/index.ts`. NEW `tests/unit/external-module-job-search-kv-freshness.test.ts`
    7/7 (active marking, stale marking, user-status preservation, cross-monitor
    non-contamination, no-sourceKey untouched, idempotency, purity).
  - DECISION (noted in code): plan-exact — seen path touches freshness/lastLivenessAt only,
    never revives status (stale→re-seen does NOT resurrect passed jobs). QA may flag; escalate
    to coordinator if challenged.
- Gates at `9fcc1b41`: targeted units 20/20 green, prettier clean on touched files, typecheck
  green. (Full unit suite was 2915 green at `7773be1e`.)

## NEXT: Step 3 — deterministic gate (TDD, isolated commit)

Read plan "Step 3" section (~line 121) first. Creates `domain/gate.ts` — the free deterministic
filter that decides which opportunities are even eligible for AI evaluation (budget economy:
EVAL_DAILY_CAP=25/day, PER_INVOCATION_EVAL_MAX=6). Then Steps 4–8 strictly per plan (read each
section when you reach it). Step 7 wires freshness into `runMonitorDiscovery`
(`worker/handlers/run.ts` — after ingestion loop + cursor save, before `runRetentionPass` at
~line 201; collect seen identity hashes from the ingestion loop, ctx.sourceKey =
`sourceKey(adapter.id, boardConfig.board)`).

## Verified premise facts (confirmed by opening files — trust these)

- `records.ts` hard-pins schemaVersion 1 read+write → ALL record changes additive-optional.
- `module_kv` RLS = `packages/settings/sql/0154_module_kv.sql` (per-owner policies present).
- `isFeedEntry` (`src/domain/feed.ts:29`) checks only h/r/s → additive feed fields safe.
- `DEFAULT_TIMEZONE="UTC"` + `localDateAndTime` in `src/domain/schedule.ts`.
- Module ai port exists: `src/worker/ai-port.ts` (JobSearchAi, WorkerPorts.ai nullable, wraps
  throws → `provider_error`). Step 5 budgets: daily 25, PER_INVOCATION_EVAL_MAX=6 (< platform
  cap 8 — never raise the cap).
- Retention: `domain/retention.ts` — Step 4 extends `evictOpportunity` to delete `eval/<h>` and
  prune `evalBudget/*` older than 7 days; `runRetentionPass` already ends with `rebuildFeed`.
- Test scoping: `pnpm exec tsx scripts/test-integration.ts <file>` runs ONE file;
  `pnpm test:integration <file>` runs the WHOLE suite + file (script arg appends). Runner
  auto-creates isolated DB when JARVIS_PGDATABASE unset — safe beside other sessions.
- `dist/` under external-modules/job-search is untracked build output — ignore stale grep hits.
- Pre-existing dirty `.claude/context-meter.log` is NOT ours — never stage it.

## Rules (unchanged)

- Caveman-terse comms to Coordinator; conventional commits/PR. `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer.
- `git add` explicit paths only. Pre-push trio + rebase before every push. Full gate at wrap-up
  (`coordinated-wrap-up`, PR `Closes #936`).
- Relay again at 70% meter: spawn successor as FABLE, same worktree, update THIS file first.

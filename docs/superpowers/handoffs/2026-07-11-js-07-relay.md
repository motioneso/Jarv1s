# JS-07 relay continuation (relay doc — committed per relay skill; fine to keep in the PR branch)

You are FABLE, successor build agent for JS-07 (#936), branch `feat/js-07-freshness-dedup-fit`,
THIS worktree. Relay #8 (2026-07-11): predecessor completed Step 7 (committed `1ffa33fd`, all
gates green — full unit suite 3000 passed | 2 skipped, format/lint/typecheck exit 0) and relayed
at the 70% meter right after starting Step 8 recon. Resume at Step 8 (last code step).

## State: PLAN APPROVED — do NOT re-request approval

- Handoff (read in full): `docs/coordination/2026-07-11-js-07-build-handoff.md` (read-only, never commit).
- Plan (read BY SECTION per current step only): `docs/superpowers/plans/2026-07-11-js-07-freshness-dedup-fit.md`.
  Steps live at lines ~63–170 (Step 5 ~136, Step 6 ~151, Step 7 ~157, Step 8 ~165);
  also read "Hard constraints" + "Key ABI additions" (lines ~28–62) once — schemaVersion-1
  additive-only rule and the eval/evalBudget key ABI.
- Coordinator: label `Coordinator`, session `58a78927-385c-4b1d-8fa0-94db20255d6f`. Approval,
  premise verification, and #962 fold (items 1–2 → Step 8) all done — see handoff.
- Zero migration; if one seems needed STOP + `[DESIGN-FORK]`.
- Task list may not carry over: recreate tasks mirroring plan Steps 7–8 if empty.

## DONE (do not redo)

- **Steps 0–2 committed** `1539fb6a` (ctx.ai queue path), `7773be1e` (posting facts + sourceKey),
  `9fcc1b41` (freshness transitions). Details in relay #3 (git history of this file).
- **Step 3 committed `abd84bd6`** — deterministic gate `domain/gate.ts` `applyGate(profileFields,
  record)`; 27/27 green. Decisions in code comments (sponsorship/industry permanently unknown at
  gate; compensation needs confirmed currency + annual periodicity). Details in relay #4 (git
  history of this file).
- **Step 4 committed `cfe7cb2f`** — evaluation records + daily budget:
  - NEW `domain/evaluations.ts`: `EvaluationInputs`, `EvaluationRecord` (schemaVersion 1,
    evaluationId, identityHash, fitBand strong|possible|low, recommendation review|watch|pass,
    evidence[], blockers/gaps/unknowns, preferenceMatches/Conflicts, posting+overall confidence,
    summary, inputs, createdAt), `EvalBudgetRecord {schemaVersion,date,used}`; fns
    `getEvaluation`, `saveEvaluation` (EVALUATION_MAX_BYTES cap, `oversize_value`, writes
    nothing), `isOutdated` (computed on read), `budgetDateFor` (UTC via
    localDateAndTime+DEFAULT_TIMEZONE), `takeBudget(kv,date,n,cap=EVAL_DAILY_CAP)` (grants
    remainder, 0 when exhausted, fail-closed on corrupt ledger), `readBudgetUsed`.
  - `limits.ts`: `EVALUATION_MAX_BYTES=24_576`, `EVAL_DAILY_CAP=25`, `PER_INVOCATION_EVAL_MAX=6`,
    `EVAL_BUDGET_RETENTION_DAYS=7`. `keys.ts`: `keys.evaluation(h)=eval/<h>`,
    `keys.evalBudget(date)=evalBudget/<date>`.
  - `retention.ts`: eviction order tombstone → eval delete → job delete (convergent);
    Step 4b prunes `evalBudget/*` strictly older than 7 days (boundary day kept);
    `RetentionReport.prunedBudgets`. `assertHash` now exported from `opportunities.ts` (reused
    by evaluations). Barrel exports updated.
  - Tests: `tests/unit/external-module-job-search-kv-evaluations.test.ts` (NEW) + retention suite
    additions — full unit suite 2968 passed; format/lint/typecheck exit 0 at commit.

- **Step 5 committed `baf81efa`** — AI fit-band evaluator:
  - NEW `worker/evaluate.ts`: `EVALUATION_OUTPUT_SCHEMA` (JSON schema, additionalProperties:false,
    all 11 fields required), `parseEvaluationOutput` (strict field-by-field rebuild, unknown keys
    dropped, null on any deviation), `buildEvaluationPrompt` (profile JSON + resume trusted;
    posting fenced `BEGIN/END UNTRUSTED JOB POSTING`), `runEvaluationSweep(ports) →
    {gateExcluded, evaluated, evalPending}`. Flow: profile/resume null → all-zero no-op;
    listOpportunities → applyGate (excluded skip) → candidate if no eval or isOutdated → sort
    oldest firstSeenAt, hash tie-break → ai null → all pending → takeBudget BEFORE calls
    (fail-closed: attempted call spends budget even if output rejected) → generateStructured
    (`tierHint:"interactive"`, maxOutputTokens 4096) → parse → saveEvaluation (KvError → pending,
    other errors rethrow). Identity hashes MODULE-authored, never model-echoed.
  - Tests: `tests/unit/external-module-job-search-worker-evaluate.test.ts` (21 tests incl.
    injection state-delta fixture + provider-name source-scan regex). Full unit suite 2989 passed;
    format/lint/typecheck exit 0 at commit.

- **Step 6 committed `2e20a6b0`** — evaluation-aware feed ordering (`domain/feed.ts`):
  - `rebuildFeed(kv, now)` signature unchanged; now reads profile/resume/evaluations internally
    and emits the spec sort: eligibility → fit band → confidence → freshness → newest
    (`posting.publishedAt ?? firstSeenAt`) → identityHash asc tie-break.
  - **BYTE-BUDGET AMENDMENT vs the plan's field list** (plan hard-constraint 4 forced it — full
    strings overflow KV_VALUE_MAX_BYTES=65535 at the designed 510-protected-record envelope):
    additive fields are SINGLE-CHAR codes `e?: e|f|x`, `b?: s|p|l`, `c?: h|m|l` (exported maps
    `FEED_GATE_CODES`/`FEED_BAND_CODES`/`FEED_CONFIDENCE_CODES` for JS-08 readers). Freshness and
    postedAt are NOT stored — derivable from job records, computed only for the sort at rebuild.
    Sort semantics identical to plan. 510-entry oversize regression test added. **Coordinator FYI
    sent at relay #7 — do not re-raise unless coordinator objects.**
  - Ranks: e eligible 0/flagged 1/absent 2/excluded 3; b strong 0/possible 1/low 2/absent
    (pending) 3; c high 0/medium 1/low 2/absent 3; f active 0/uncertain 1/stale 2. b/c stamped
    ONLY from a current (non-`isOutdated`) eval AND only when profile+resume both approved; no
    profile → e/b/c all absent. `isFeedEntry` still checks only h/r/s; old index reads cleanly.
  - Tests: feed suite 12 green (ordering matrix, no-profile, profile-no-resume, old-index,
    510-envelope). Full unit suite 2994 passed; trio exit 0 at commit.

- **Step 7 committed `1ffa33fd`** — pipeline wiring in `worker/handlers/run.ts`
  (`runMonitorDiscovery`): seen-set collected during ingestion (tombstone-suppressed postings
  COUNT as seen — deletion is a status decision, not liveness), then after cursor save:
  `markFreshnessAfterRun` (board-scoped `sourceKey(adapter.id, boardConfig.board)`) →
  `runRetentionPass` (never spend eval budget on about-to-evict records) → `runEvaluationSweep` →
  `rebuildFeed` again (retention's internal rebuild predates this run's evals; rebuild is a cheap
  in-memory sort — chosen over reordering retention internals, plan allowed either). Run `counts`
  extended (numbers only): `staleMarked, gateExcluded, evaluated, evalPending`. Tests: 6 new in
  `tests/unit/external-module-job-search-handlers-run.test.ts` (25/25) — evaluate+budget+feed
  codes; run-twice-identical → evaluated 0, ai.calls unchanged; changed content re-evals only
  that job; AI failure leaves prior eval `toEqual(before)` + evalPending; absence → stale →
  gate-excluded → feed `e:"x"` ranked last, third run staleMarked 0 (idempotent); ai null →
  evalPending, budget 0. Full unit suite 3000 passed | 2 skipped; trio exit 0 at commit.

## NEXT: Step 8 — integration + security tests (TDD, isolated commit). Recon ALREADY DONE — trust this:

Plan Step 8 (~line 165, read it) + #962 items 1–2 (folded in via handoff; items 3–4 NOT ours).

- **Queue-path ctx.ai e2e is ALREADY COVERED** by Step 0's
  `tests/integration/module-worker-queue-ai.test.ts` (committed `1539fb6a`): real worker ai
  bridge on queued-jobs path — typed `needs_config` fail-closed, composition guard e2e,
  per-invocation cap `usage_limited`, handler-without-ai fail-closed. Do NOT duplicate; cite it
  as satisfying that plan bullet in the commit/PR.
- **Everything else goes in `tests/integration/external-module-job-search-kv-isolation.test.ts`**
  (478 lines; read it in full once — it IS the harness). Facts verified by predecessor's full
  read: `kvForActor(actorUserId, {toolRisk, admin})` builds a JobSearchKv over
  `createExternalModuleRpcHandler` with the real parsed manifest (scope "user");
  `workerQuery(actorUserId, moduleId, query)` = worker-role SQL with `app.actor_user_id` /
  `app.current_module_id` GUCs in a rolled-back txn; ids from `./test-database.js`
  (`ids.userA/userB/adminUser`); `resetFoundationDatabase()` + module row bootstrap in beforeAll.
  **Describes run SEQUENTIALLY and order is load-bearing**: #931 lifecycle describe ENDS BY
  DELETING userA; #932 re-creates userA in its beforeAll; #934 schedule-state isolation is LAST
  (both users exist at suite end). **Append new JS-07 describe(s) AFTER the #934 describe.**
- Step 8 checklist for the new describe(s):
  1. Cross-owner denial on `eval/*` + `evalBudget/*` (module_kv RLS): A writes via
     `saveEvaluation`/`takeBudget` over kvA; kvB get → null / list → empty; admin ALSO denied
     (no private-data bypass); `workerQuery` as B → 0 rows. SECURITY tier: prove denial as
     0-rows/null, not absence-of-error.
  2. Full pipeline fixture at module level over real RPC kv: build WorkerPorts
     `{kv: kvForActor(A), ai: stub, fetch: injected, now}` → `runMonitorDiscovery` → assert gate
     verdicts, evaluation persisted, feed order + `e/b/c` codes (mirror the unit fixture shapes
     from `tests/unit/external-module-job-search-handlers-run.test.ts`).
  3. Run-twice dedup at module level: second identical run → evaluated 0, ai call count
     unchanged, budget unchanged.
  4. **#962 item 1** — handler-level cross-owner run-now denial: actor A saves a monitor; actor
     B invokes the run-now handler with A's monitorId → `monitor_not_found`, and NOTHING written
     against A (check A's kv unchanged). Read `worker/handlers/run.ts` + the monitors handler for
     the exact run-now entry point first.
  5. **#962 item 2** — schedule-state UPDATE-side cross-owner clobber assert: A has schedule
     state for monitor id M; B writes schedule state for the SAME id M → A's row byte-identical
     after (B gets own row; extend the existing #934 pattern, UPDATE side).
- Runner (ONE file): `pnpm exec tsx scripts/test-integration.ts
  tests/integration/external-module-job-search-kv-isolation.test.ts`. TDD; trio; commit isolated
  (test file only).
- After Step 8: `coordinated-wrap-up` (full gate incl. `pnpm verify:foundation`, pre-push trio +
  rebase, push, PR `Closes #936` noting the Step 6 byte-budget amendment, report to Coordinator).
  Never merge/board/close. Task list: #2 Step 8 in_progress, #3 wrap-up pending — recreate if
  empty.

## Verified premise facts (confirmed by opening files — trust these)

- `records.ts` hard-pins schemaVersion 1 read+write → ALL record changes additive-optional.
- `module_kv` RLS = `packages/settings/sql/0154_module_kv.sql` (per-owner policies present).
- `isFeedEntry` (`src/domain/feed.ts:29`) checks only h/r/s → additive feed fields safe.
- `DEFAULT_TIMEZONE="UTC"` + `localDateAndTime` in `src/domain/schedule.ts`.
- Platform `AI_CALLS_PER_INVOCATION_CAP = 8` (repairs included) — never raise it; module caps
  (25/day, 6/invocation) live above it.
- `keys.ts`: `assertId` = `/^[A-Za-z0-9_-]{1,64}$/`; `evaluationIdentity({opportunityContentHash,
  profileRevisionId, resumeRevisionId})` implemented. Keys carry only ids/hashes — never prose.
- Errors: reuse `JobSearchKvError` codes `oversize_value` / `invalid_record`; messages NEVER
  embed record content.
- Test scoping: `pnpm exec tsx scripts/test-integration.ts <file>` runs ONE file;
  `pnpm test:integration <file>` runs the WHOLE suite + file. Runner auto-creates isolated DB
  when JARVIS_PGDATABASE unset — safe beside other sessions.
- Unit tests live at repo-root `tests/unit/` (NOT under external-modules/); memory-kv helper =
  `tests/unit/helpers/job-search-memory-kv.ts` (`createMemoryKv()`, has `failAfterSets(n)` +
  `dump()` for convergence asserts). Unit runner: `pnpm vitest run tests/unit/<file>`.
- `dist/` under external-modules/job-search is untracked build output — ignore stale grep hits.
- Pre-existing dirty `.claude/context-meter.log` is NOT ours — never stage it.

## Rules (unchanged)

- Caveman-terse comms to Coordinator; conventional commits/PR. `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer.
- `git add` explicit paths only. Pre-push trio + rebase before every push. Full gate at wrap-up
  (`coordinated-wrap-up`, PR `Closes #936`).
- Relay again at 70% meter: spawn successor as FABLE (`--model fable`), same worktree, update
  THIS file first.

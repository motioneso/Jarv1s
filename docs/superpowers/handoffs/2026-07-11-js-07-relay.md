# JS-07 relay continuation (relay doc — committed per relay skill; fine to keep in the PR branch)

You are FABLE, successor build agent for JS-07 (#936), branch `feat/js-07-freshness-dedup-fit`,
THIS worktree. Predecessor relayed at context 70% with ZERO code written — no re-verification
needed, start coding immediately at Step 0.

## State: PLAN APPROVED — do NOT re-request approval

- Handoff (read in full): `docs/coordination/2026-07-11-js-07-build-handoff.md` (read-only, never commit).
- Plan (read BY SECTION per current step only): `docs/superpowers/plans/2026-07-11-js-07-freshness-dedup-fit.md`.
- Coordinator (label `Coordinator`, session `58a78927-385c-4b1d-8fa0-94db20255d6f`) APPROVED the
  adopted plan 2026-07-11 after my premise report: all premises HOLD on this branch, zero drift
  from JS-06/News-S2 (domain/worker last touched by JS-05 `9d4589d1`), #962 items 1–2 (test-only)
  folded into plan Step 8, items 3–4 out of scope. QA gate will hunt: proven cross-owner denial,
  provider-agnostic fit bands, metadata-only payloads/UNTRUSTED prompts, response-schema
  declaration (no REST changes planned → moot unless you add one). Zero migration; if one seems
  needed STOP + `[DESIGN-FORK]`.
- `pnpm install` done. Tasks #1–#9 in task list mirror plan Steps 0–8; Task #1 in_progress.

## Verified premise facts (all confirmed by opening files — trust these)

- `postingToOpportunity` (`external-modules/job-search/src/worker/handlers/run.ts:69`) drops
  publishedAt/workMode/employmentType/compensation; stale-marking deferral comment at run.ts:12–15.
- `opportunityIdentity`/`evaluationIdentity` live in `src/domain/keys.ts`; `records.ts` hard-pins
  schemaVersion 1 read+write → ALL record changes additive-optional.
- `module_kv` RLS = `packages/settings/sql/0154_module_kv.sql` (per-owner policies present).
- `isFeedEntry` (`src/domain/feed.ts:29`) checks only h/r/s → additive feed fields safe.
- `DEFAULT_TIMEZONE="UTC"` + `localDateAndTime` in `src/domain/schedule.ts`.
- Module ai port already exists: `src/worker/ai-port.ts` (JobSearchAi, WorkerPorts.ai nullable,
  aiFromWorkerContext wraps throws → `provider_error`).

## Step 0 design (researched, ready to implement — TDD, ISOLATED COMMIT)

Wire `ctx.ai` into queue path:
1. NEW `apps/worker/src/external-module-ai-bridge.ts` — mirror
   `apps/api/src/external-module-ai-bridge.ts` (40 lines) near-verbatim: returns
   `(scopedDb, moduleId, request) => ExternalModuleAiResult`; constructs ONE `createAiSecretCipher()`
   inside; drops usage/model/provider on ok; catch → `provider_error`.
2. Wire in `buildWorker` (`apps/worker/src/worker.ts`, externalConfig block ~line 204): ONE
   `new AiRepository()` (from `@jarv1s/ai`) + ONE bridge at composition time. Handler adapts 3-arg
   bridge to host's 2-arg dep exactly like `apps/api/src/external-module-tools.ts:57`:
   `ai: (db, req) => bridge(db, module.id, req)`.
3. TESTABILITY REFACTOR: extract the inline per-job closure (worker.ts:245–294) into exported
   factory, NEW `apps/worker/src/external-module-job-handler.ts`, behavior-identical
   (assertModuleJobPayload → active-user check → module state check → build rpc → runtime.invoke).
   Deps injected: module, queue, runtime, workerDb, dataContext, cipher, discoveryById,
   listActiveUserIds, ai (3-arg bridge, optional).
4. Host dep shape (verified): `createExternalModuleRpcHandler` accepts optional
   `ai?: (scopedDb, request)` — `packages/module-registry/src/external/worker-rpc-host.ts:78`;
   cap `AI_CALLS_PER_INVOCATION_CAP=8`, composition guard via resolvedSecrets, envelope rebuild,
   read-risk forbidden — ALL already host-tested in `tests/integration/module-worker-rpc.test.ts`
   (mirror its harness: seeded acme-a module, `connectionStrings.worker`, `ids`).
5. PLAN TRAP NUANCE (state in commit msg): plan says "reuse the cipher already constructed in
   worker.ts" — mechanically impossible: that's the ModuleCredentialCipher (settings). AI secrets
   use `createAiSecretCipher()` (`packages/ai/src/crypto.ts:16`, env JARVIS_AI_SECRET_KEY, dev
   fallback exists → no boot risk). Honor the INTENT: single cipher+repository instances at
   composition time, none per-job.
6. NEW TEST `tests/integration/module-worker-queue-ai.test.ts` — queue-path acceptance proofs:
   build handler via the new factory with real DataContextRunner + real cipher + REAL bridge;
   stub `runtime.invoke` capturing the `rpc` arg; drive captured rpc:
   (a) composition guard e2e: `auth.getCredential` then ai prompt containing secret →
   `forbidden_secret_in_ai_input`; (b) cap: 9th call → `usage_limited`; (c) fail-closed: no
   provider configured → `{ok:false,error:"needs_config"}` typed, NO throw; (d) metadata-only
   payload: assertModuleJobPayload rejects payloads with extra fields (unchanged invariant).
   Success-path e2e (optional, nice-to-have): stub `globalThis.fetch` returning anthropic
   tool_use shape (parse: `packages/ai/src/adapters/http-api-structured.ts:137–157`), seed
   provider/model/binding via admin routes like `tests/integration/ai-structured.test.ts`
   (JARVIS_AI_SECRET_KEY env pinned in its beforeAll — copy that pattern).
7. `generateStructured`: `packages/ai/src/structured/generate-structured.ts` — resolves via
   `repository.resolveModelForService(scopedDb, input.service…)`; bridge passes
   `service: module.<moduleId>`. `apps/worker` already depends on `@jarv1s/ai`.

## Then Steps 1–8 strictly per plan doc (read each section when you reach it)

## Rules (unchanged)

- Caveman-terse comms to Coordinator; conventional commits/PR. `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer.
- `git add` explicit paths only. Pre-push trio + rebase before every push. Full gate at wrap-up
  (`coordinated-wrap-up`, PR `Closes #936`).
- Relay again at 70% meter: spawn successor as FABLE, same worktree, update THIS file first.

# JS-07 relay continuation (relay doc — committed per relay skill; fine to keep in the PR branch)

You are FABLE, successor build agent for JS-07 (#936), branch `feat/js-07-freshness-dedup-fit`,
THIS worktree. Relay #2 (2026-07-11): predecessor completed Step 0 end-to-end and relayed at the
72% meter. Tree is clean; start at Step 1 immediately.

## State: PLAN APPROVED — do NOT re-request approval

- Handoff (read in full): `docs/coordination/2026-07-11-js-07-build-handoff.md` (read-only, never commit).
- Plan (read BY SECTION per current step only): `docs/superpowers/plans/2026-07-11-js-07-freshness-dedup-fit.md`.
  Steps live at lines ~63–170; also read "Hard constraints" + "Key ABI additions" (lines ~28–62)
  once — they bake in the schemaVersion-1 additive-only rule and the eval/evalBudget key ABI.
- Coordinator: label `Coordinator`, session `58a78927-385c-4b1d-8fa0-94db20255d6f`. Approval,
  premise verification, and #962 fold (items 1–2 → Step 8) all done — see handoff.
- Zero migration; if one seems needed STOP + `[DESIGN-FORK]`.
- Task list may not carry over: recreate tasks mirroring plan Steps 1–8 if empty.

## DONE (do not redo)

- **Step 0 committed `1539fb6a`** — ctx.ai wired into the queue path:
  - NEW `apps/worker/src/external-module-ai-bridge.ts` (real @jarv1s/ai generateStructured,
    one `createAiSecretCipher()` — NOT the ModuleCredentialCipher; commit msg documents the
    plan's "reuse the cipher" trap resolution).
  - NEW `apps/worker/src/external-module-job-handler.ts` (inline closure extracted verbatim from
    worker.ts + optional 3-arg `ai` dep adapted to the host's 2-arg shape).
  - `apps/worker/src/worker.ts` composes one AiRepository + bridge and passes `ai` to the handler.
  - NEW `tests/integration/module-worker-queue-ai.test.ts` 7/7: metadata-only input (exact
    toEqual), undeclared-key reject, inactive/drifted skip, needs_config fail-closed,
    composition-guard e2e (`forbidden_secret_in_ai_input`), cap 8→`usage_limited`,
    no-ai-dep→`invalid_rpc`. Proves plan acceptance invariants 1,3,4,5; acceptance 2
    (provider-agnostic) lands in Step 5.
- All gates green at commit: typecheck, lint, format:check, test:unit (2908), FULL
  test:integration (1553 passed / 145 files).

## NEXT: Step 1 — structured posting facts through ingestion (TDD, isolated commit)

Read plan "Step 1" section, then: extend `OpportunityInput.posting` + `OpportunityRecord.posting`
(additive optional `publishedAt`, `workMode`, `employmentType`, `compensation`) + top-level
`sourceKey`; update `postingToOpportunity` (`external-modules/job-search/src/worker/handlers/run.ts:69`)
to map them and compute `sourceKey` from `(adapterId, boardConfig.board)` — `board` is in scope in
`runMonitorDiscovery`, thread it through. Content hash already covers the posting object. Tests:
mapping, hash stability for unchanged postings, old records (no new fields) read fine.
Then Steps 2–8 strictly per plan (read each section when you reach it).

## Verified premise facts (confirmed by opening files — trust these)

- `records.ts` hard-pins schemaVersion 1 read+write → ALL record changes additive-optional.
- `opportunityIdentity`/`evaluationIdentity` in `src/domain/keys.ts` (evaluationIdentity unused so far).
- `module_kv` RLS = `packages/settings/sql/0154_module_kv.sql` (per-owner policies present).
- `isFeedEntry` (`src/domain/feed.ts:29`) checks only h/r/s → additive feed fields safe.
- `DEFAULT_TIMEZONE="UTC"` + `localDateAndTime` in `src/domain/schedule.ts`.
- Module ai port exists: `src/worker/ai-port.ts` (JobSearchAi, WorkerPorts.ai nullable, wraps
  throws → `provider_error`). Step 5 budgets: daily 25, PER_INVOCATION_EVAL_MAX=6 (< platform
  cap 8 — never raise the cap).
- Test scoping: `pnpm exec tsx scripts/test-integration.ts <file>` runs ONE file;
  `pnpm test:integration <file>` runs the WHOLE suite + file (script arg appends). Runner
  auto-creates isolated DB when JARVIS_PGDATABASE unset — safe beside other sessions.

## Rules (unchanged)

- Caveman-terse comms to Coordinator; conventional commits/PR. `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer.
- `git add` explicit paths only. Pre-push trio + rebase before every push. Full gate at wrap-up
  (`coordinated-wrap-up`, PR `Closes #936`).
- Relay again at 70% meter: spawn successor as FABLE, same worktree, update THIS file first.

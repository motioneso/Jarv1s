# JS-07 relay continuation (relay doc — committed per relay skill; fine to keep in the PR branch)

You are FABLE, successor build agent for JS-07 (#936), branch `feat/js-07-freshness-dedup-fit`,
THIS worktree. Relay #4 (2026-07-11): predecessor completed Step 3 and the Step 4 red phase
(evaluations test file written to disk, UNTRACKED — see NEXT), then relayed at the 70% meter.
Resume mid-Step 4 immediately.

## State: PLAN APPROVED — do NOT re-request approval

- Handoff (read in full): `docs/coordination/2026-07-11-js-07-build-handoff.md` (read-only, never commit).
- Plan (read BY SECTION per current step only): `docs/superpowers/plans/2026-07-11-js-07-freshness-dedup-fit.md`.
  Steps live at lines ~63–170 (Step 4 ~129, Step 5 ~136, Step 6 ~151, Step 7 ~157, Step 8 ~165);
  also read "Hard constraints" + "Key ABI additions" (lines ~28–62) once — schemaVersion-1
  additive-only rule and the eval/evalBudget key ABI.
- Coordinator: label `Coordinator`, session `58a78927-385c-4b1d-8fa0-94db20255d6f`. Approval,
  premise verification, and #962 fold (items 1–2 → Step 8) all done — see handoff.
- Zero migration; if one seems needed STOP + `[DESIGN-FORK]`.
- Task list may not carry over: recreate tasks mirroring plan Steps 4–8 if empty.

## DONE (do not redo)

- **Steps 0–2 committed** `1539fb6a` (ctx.ai queue path), `7773be1e` (posting facts + sourceKey),
  `9fcc1b41` (freshness transitions). Details in relay #3 (git history of this file).
- **Step 3 committed `abd84bd6`** — deterministic gate:
  - NEW `external-modules/job-search/src/domain/gate.ts`: `applyGate(profileFields, record)` →
    `{verdict: "eligible"|"excluded"|"flagged", reasons}`. Excluded (structured/confirmed both
    sides): `stale_posting`, `excluded_company`, `employment_type_incompatible`,
    `work_mode_incompatible`, `compensation_below_minimum`. Flagged (deterministic signal, text
    can't confirm impossibility): `dealbreaker_match:<term>` (title+description, term ≥3 chars),
    `location_mismatch` (ONLY when posting.workMode === "onsite", substring overlap either
    direction). Missing/unparseable = unknown, never rejection; empty profile passes all.
  - DECISIONS (in code comments, within plan latitude): sponsorship + industry exclusion have no
    structured posting-side fact → permanently unknown at gate (AI eval sees them via profile);
    compensation compared only with confirmed currency (code or symbol; bare "$" presumed USD);
    non-annual periodicity (hourly/daily/weekly/monthly) → unknown; annualMax < 1000 refused.
  - Exports in `domain/index.ts`. NEW `tests/unit/external-module-job-search-gate.test.ts` 27/27
    green; prettier + typecheck green at commit.

## NEXT: finish Step 4 — evaluation records + daily budget (TDD, isolated commit)

Read plan Step 4 (~line 129) first. Red phase HALF done:

- **`tests/unit/external-module-job-search-kv-evaluations.test.ts` EXISTS on disk, UNTRACKED,
  intentionally red** (imports don't exist yet). Read it — it IS the Step 4 contract:
  - `domain/evaluations.ts` exports: types `EvaluationInputs {opportunityContentHash,
    profileRevisionId, resumeRevisionId}`, `EvaluationRecord {schemaVersion:1, evaluationId
    (= evaluationIdentity(inputs)), identityHash, fitBand "strong"|"possible"|"low",
    recommendation "review"|"watch"|"pass", evidence[{requirement,evidence,source}], blockers,
    gaps, unknowns, preferenceMatches, preferenceConflicts, postingConfidence,
    overallConfidence ("high"|"medium"|"low"), summary, inputs, createdAt}`; fns
    `getEvaluation(kv, hash)` (null when absent, JobSearchKvError on bad hash),
    `saveEvaluation(kv, record)` (EVALUATION_MAX_BYTES cap → `oversize_value`, writes nothing),
    `isOutdated(record, currentInputs)` (computed on read, any of 3 inputs differing → true),
    `budgetDateFor(now)` (= `localDateAndTime(now, DEFAULT_TIMEZONE).date`, UTC per plan Open
    decision 2), `takeBudget(kv, date, n, cap=EVAL_DAILY_CAP)` → granted count (remainder near
    cap, 0 when exhausted, per-date ledgers, zero-request writes nothing, non-`assertId` date
    rejects), `readBudgetUsed(kv, date)`.
  - `limits.ts` additions: `EVALUATION_MAX_BYTES=24_576`, `EVAL_DAILY_CAP=25`,
    `PER_INVOCATION_EVAL_MAX=6`, `EVAL_BUDGET_RETENTION_DAYS=7`.
  - `keys.ts` additions to `keys` object: `evaluation: (h) => eval/<h>`,
    `evalBudget: (date) => evalBudget/<date>`. `evaluationIdentity` already exists.
- **Remaining red phase**: add retention cases to
  `tests/unit/external-module-job-search-kv-retention.test.ts` (conventions: `seedJob`,
  `daysAgo`, NOW fixture — read file first): (a) evicting a job also deletes `eval/<h>`
  (order tombstone → eval delete → job delete so interrupted passes converge); (b)
  `evalBudget/*` older than EVAL_BUDGET_RETENTION_DAYS pruned, recent ledgers kept; (c)
  `RetentionReport` gains `prunedBudgets` count.
- Then: run both suites red → implement (`limits.ts`, `keys.ts`, NEW `domain/evaluations.ts`
  with budget record `{schemaVersion:1, date, used}`, `retention.ts` extension, `domain/index.ts`
  exports) → green + prettier + lint + typecheck + adjacent suites → commit
  `feat(job-search): evaluation records + daily AI budget ledger (#936 Step 4)` with the
  Fable trailer, explicit paths only.
- Then Steps 5–8 strictly per plan (read each section when reached). Step 7 wires into
  `runMonitorDiscovery` (`worker/handlers/run.ts` — after ingestion loop + cursor save, before
  `runRetentionPass` ~line 201; ctx.sourceKey = `sourceKey(adapter.id, boardConfig.board)`).
  Step 8 = integration + security tests (cross-owner denial on eval/*, evalBudget/*; queue-path
  ctx.ai e2e; run-twice dedup; #962 items 1–2).

## Verified premise facts (confirmed by opening files — trust these)

- `records.ts` hard-pins schemaVersion 1 read+write → ALL record changes additive-optional.
- `module_kv` RLS = `packages/settings/sql/0154_module_kv.sql` (per-owner policies present).
- `isFeedEntry` (`src/domain/feed.ts:29`) checks only h/r/s → additive feed fields safe.
- `DEFAULT_TIMEZONE="UTC"` + `localDateAndTime` in `src/domain/schedule.ts`.
- Module ai port exists: `src/worker/ai-port.ts` (JobSearchAi, WorkerPorts.ai nullable, wraps
  throws → `provider_error`). Step 5 budgets: daily 25, PER_INVOCATION_EVAL_MAX=6 (< platform
  cap 8 — never raise the cap).
- `keys.ts`: `assertId` = `/^[A-Za-z0-9_-]{1,64}$/` (dates like `2026-07-11` pass; `2026/07/11`
  rejects). `evaluationIdentity({opportunityContentHash, profileRevisionId, resumeRevisionId})`
  already implemented. Keys carry only ids/hashes — never prose.
- Retention: `evictOpportunity` is private in `domain/retention.ts`, writes tombstone BEFORE
  deleting job key; `runRetentionPass` already ends with `rebuildFeed`. Retention test file read
  in full — helpers `seedJob(kv, externalId, status, seenAt)` + `daysAgo(days)`.
- Errors: reuse `JobSearchKvError` codes `oversize_value` / `invalid_record`; messages NEVER
  embed record content.
- Test scoping: `pnpm exec tsx scripts/test-integration.ts <file>` runs ONE file;
  `pnpm test:integration <file>` runs the WHOLE suite + file. Runner auto-creates isolated DB
  when JARVIS_PGDATABASE unset — safe beside other sessions.
- Unit tests live at repo-root `tests/unit/` (NOT under external-modules/); memory-kv helper =
  `tests/unit/helpers/job-search-memory-kv.js` (`createMemoryKv()`).
- `dist/` under external-modules/job-search is untracked build output — ignore stale grep hits.
- Pre-existing dirty `.claude/context-meter.log` is NOT ours — never stage it.

## Rules (unchanged)

- Caveman-terse comms to Coordinator; conventional commits/PR. `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer.
- `git add` explicit paths only. Pre-push trio + rebase before every push. Full gate at wrap-up
  (`coordinated-wrap-up`, PR `Closes #936`).
- Relay again at 70% meter: spawn successor as FABLE (`--model fable`), same worktree, update
  THIS file first.

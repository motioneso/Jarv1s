# OTNR Tasks 142 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Discharge still-reproducing #142 task-module MED/LOW findings that are safe in the tasks lane.

**Architecture:** Keep fixes inside `packages/tasks` and focused integration tests. Move temporal task classification into a pure helper, push list filters into `TasksRepository.listFiltered`, validate recurrence JSON at route/repository boundaries, narrow recurrence unique-violation handling to PG `23505` + `tasks_recurrence_occurrence_idx`, and remove the false assistant write tool until job-backed execution is designed.

**Tech Stack:** TypeScript, Kysely, Fastify route helpers, Vitest integration tests.

---

## Relevance Check

- Still reproduces: `tasks.updateStatus` has no `execute`; fix by adding a real executor while keeping REST write invocation confirmation-gated by the AI route.
- Still reproduces: `tasks.list` and GET `/api/tasks` load broad rows then filter in memory; fix with `TasksRepository.listFiltered`.
- Still reproduces: quadrant logic reads `Date.now()` and duplicates the 48h constant; fix with injected `now` and shared `TASK_URGENCY_WINDOW_HOURS`.
- Still reproduces: recurrence casts unchecked JSON and accepts invalid `freq`/`interval`/`occurrence_date`; fix with parser/guard.
- Still reproduces: recurrence unique handling string-matches `"unique"`; fix with PG code + constraint guard.
- Still reproduces: route-side deferred payload metadata re-check and double-casts; fix by keeping worker-side guard only and typing guards as `unknown`.
- Still reproduces: preferences `getOrCreate` recursive lost-race retry; fix with terminal re-select.
- Still reproduces: focus/at-risk/overdue manifest response schemas reach into route schemas; fix by importing named response schema exports.
- Skip: full `description` in AI tool outputs requires product/security decision about shared-task AI exposure and truncation policy.
- Skip: `actor_kind = system` for cascade/auto-close/recurrence requires product provenance semantics beyond the issue text.

### Task 1: Failing Tests

**Files:**

- Modify: `tests/integration/tasks-tools.test.ts`
- Modify: `tests/integration/tasks.test.ts`

- [ ] **Step 1: Add focused failing tests**

Add tests proving:

- `tasks.updateStatus` is not advertised without an executor.
- `tasks.list` delegates filters to repository SQL by asserting list/status/tag filters return the intended row set.
- `getQuadrant(task, now)` is deterministic across injected times.
- invalid recurrence bodies get `400`.
- non-target unique recurrence errors are re-thrown by the exported guard path.
- extra deferred payload keys are rejected by workers through the `unknown` guard.

- [ ] **Step 2: Run tests to verify RED**

Run:

```bash
pnpm test:tasks-tools -- --runInBand
pnpm test:tasks -- --runInBand
```

Expected: at least one new assertion fails against current code.

### Task 2: Classification + SQL Filtering

**Files:**

- Create: `packages/tasks/src/classification.ts`
- Modify: `packages/tasks/src/drift.ts`
- Modify: `packages/tasks/src/serialize.ts`
- Modify: `packages/tasks/src/repository.ts`
- Modify: `packages/tasks/src/routes.ts`
- Modify: `packages/tasks/src/tools.ts`
- Modify: `packages/tasks/src/index.ts`

- [ ] **Step 1: Add pure classification helper**

Create `TASK_URGENCY_WINDOW_HOURS`, `TASK_URGENCY_WINDOW_MS`, `TaskQuadrant`, and `classifyTaskQuadrant(task, now)`.

- [ ] **Step 2: Use helper from serializers and drift**

`getQuadrant(task, now)` and `filterByQuadrant(tasks, quadrant, now)` delegate to the helper. `drift.ts` imports `TASK_URGENCY_WINDOW_HOURS`.

- [ ] **Step 3: Add repository `listFiltered`**

Add criteria for `listId`, `tagId`, `status`, `priority`, `dueBefore`, `dueAfter`, `quadrant`, and `now`. Push all predicates into SQL, including quadrant urgency via the shared window.

- [ ] **Step 4: Reuse from route and tool**

GET `/api/tasks` and `tasks.list` call `repository.listFiltered`, then fetch tags for returned rows only.

- [ ] **Step 5: Run focused tests**

Run:

```bash
pnpm test:tasks-tools -- --runInBand
pnpm test:tasks -- --runInBand
```

Expected: relevant tests pass.

### Task 3: Recurrence Validation + Error Guard

**Files:**

- Modify: `packages/tasks/src/recurrence.ts`
- Modify: `packages/tasks/src/repository.ts`
- Modify: `packages/tasks/src/routes.ts`
- Modify: `tests/integration/tasks.test.ts`

- [ ] **Step 1: Add recurrence parser/guard**

Export `parseRecurrenceSpec(value)` and `isTasksRecurrenceOccurrenceConflict(error)` from `recurrence.ts`.

- [ ] **Step 2: Validate route input**

`optionalRecurrence` returns `RecurrenceSpec | null | undefined` and rejects invalid freq, non-positive/non-integer interval, and malformed `YYYY-MM-DD`.

- [ ] **Step 3: Remove unchecked reads**

Repository inputs use `RecurrenceSpec`; `rollForwardRecurringSeries` and `generateNext` parse stored JSON instead of double-casting.

- [ ] **Step 4: Use exact PG guard**

`generateNext` and roll-forward catch only PG `23505` on `tasks_recurrence_occurrence_idx`.

- [ ] **Step 5: Run focused tests**

Run:

```bash
pnpm test:tasks -- --runInBand
```

Expected: recurrence tests pass.

### Task 4: Manifest, Metadata, Preferences

**Files:**

- Modify: `packages/tasks/src/manifest.ts`
- Modify: `packages/tasks/src/jobs.ts`
- Modify: `packages/tasks/src/routes.ts`
- Modify: `packages/tasks/src/preferences.ts`
- Modify: `tests/integration/tasks-tools.test.ts`
- Modify: `tests/integration/tasks.test.ts`

- [ ] **Step 1: Wire the write tool executor**

Add `taskUpdateStatusExecute` in `packages/tasks/src/tools.ts` and wire it from the `tasks.updateStatus` manifest entry.

- [ ] **Step 2: Use named response schemas**

Import `focusTasksResponseSchema`, `atRiskTasksResponseSchema`, and `overdueTasksResponseSchema`; use them for manifest route/tool response schemas.

- [ ] **Step 3: Keep worker metadata guard only**

Change metadata guard parameters to `unknown`; remove producer re-check and casts from route.

- [ ] **Step 4: Replace preferences recursion**

After insert conflict, select and `executeTakeFirstOrThrow()`.

- [ ] **Step 5: Run focused tests**

Run:

```bash
pnpm test:tasks-tools -- --runInBand
pnpm test:tasks -- --runInBand
```

Expected: focused tests pass.

### Task 5: Verification + Closeout

**Files:**

- All owned paths only.

- [ ] **Step 1: Run checks**

Run:

```bash
pnpm test:tasks-tools
pnpm test:tasks
pnpm format:check
pnpm lint
pnpm check:file-size
pnpm typecheck
```

- [ ] **Step 2: Commit owned paths**

Stage explicit files only and commit with co-author trailer.

- [ ] **Step 3: Pre-push + PR**

Run pre-push trio, rebase on `origin/overnight-batch-2026-06-16`, push `otnr-tasks-142`, open PR with base `overnight-batch-2026-06-16`.

- [ ] **Step 4: Report Coordinator**

Use `herdr pane list`, confirm exactly one `Coordinator`, then `herdr pane run` compact done report with PR URL and evidence.

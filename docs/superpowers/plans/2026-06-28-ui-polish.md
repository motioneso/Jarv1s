# Plan — ui-polish (#480 #512)

**Branch:** ui-polish | **Date:** 2026-06-28

## Scope

Two isolated UI-layer fixes. No schema, auth, or API changes.

---

## Task 1 — Remove medication nudge (#480)

**File:** `apps/web/src/today/today-page.tsx`

Remove three items, all confined to that file:

1. Line 122 — `useState(false)` for `medReminderDismissed` (no longer needed)
2. Line 135 — `showMedReminder` const (no longer needed)
3. Lines 448-463 — the `well__nudge` JSX block (the persistent nudge card)

**Invariants:** `medsAllTaken`, `medsNoneLogged`, `medTotal`, `medTaken` are kept — they drive
the wellness summary line (lines 428-447), which is NOT the nudge.

**Verify:** no `showMedReminder` or `medReminderDismissed` references remain.

**Commit:** `fix(ui): remove Today medication nudge card (#480)`

---

## Task 2 — Chat approve/reject spacing and copy (#512)

**Files:**

- `apps/web/src/chat/action-request-card.tsx`
- `apps/web/src/styles/kit-chat.css`

Changes:

1. In `action-request-card.tsx`: remove `<p className="action-request-tool">{props.toolName}</p>`
   — it renders raw tool names ("browser_use", "web.search") as always-visible debug text.
   The `action-request-summary` already describes the action in user terms.
2. In `kit-chat.css`: add CSS block for `.action-request-card` and `.action-request-actions`
   with `display: flex; gap: 8px;` on the actions container.

**Verify:** no raw CSS colors introduced; uses design tokens only; `kit-chat.css` stays under
1000 lines (currently 694).

**Commit:** `fix(ui): chat approve/reject button spacing, remove debug tool-name text (#512)`

---

## Exit Criteria

- [ ] Today page shows no nudge when meds are outstanding.
- [ ] Approve / Reject buttons have visible gap between them.
- [ ] No raw tool-name text above the buttons.
- [ ] `pnpm format:check && pnpm lint && pnpm typecheck` all exit 0.
- [ ] No raw colors outside `tokens.css`.
- [ ] Both commits clean; PR open.

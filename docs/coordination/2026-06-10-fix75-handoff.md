# Build Handoff — fix75-users-select

**Spec (approved):** GitHub issue #75 (verified Fable H2 audit finding) — for remediation fixes the
verified issue is the spec, per the #72–#74 precedent. The governing RLS intent is the approved
auth-secret-RLS work that produced migrations 0045/0046 (owner-only users SELECT).
**GitHub issue:** #75
**Risk tier:** `security` (RLS + auth surface ⇒ cross-model Opus QA + Ben merge sign-off; build to that bar.)
**Worktree:** /home/ben/Jarv1s/.claude/worktrees/fix75-users-select **Branch:** fix75-users-select (off origin/main)
**Build skill path (absolute):** /home/ben/Jarv1s/.claude/skills/coordinated-build/SKILL.md
**Coordinator label:** `Coordinator` (UNIQUE — escalate via `herdr-pane-message`; before messaging, verify `herdr pane list` shows EXACTLY ONE pane with this label. Never guess a `…-N` pane-id — they reflow when panes close.)
**Relay threshold:** countable events — ~80–100k tokens OR a compaction summary in your own context (then relay immediately).

## Task

Migration 0045 left `users_app_runtime_select ... USING(true)`: any app-runtime query (even
GUC-less) can read every user row → identity enumeration; deviates from the approved owner-only
intent and makes `count_all_users()` redundant.

**Locked direction (coordinator):** TIGHTEN — users SELECT policy becomes self-row only
(`id = app.current_user_id()` or this repo's equivalent GUC accessor), with targeted
`SECURITY DEFINER` helper functions for the genuine cross-user admin/system paths you find.
Audit every current consumer of `users` SELECT before writing the policy; list them in your plan.
If tightening breaks a substantial legitimate path that helpers can't cleanly cover, STOP and
escalate `[DESIGN-FORK]` to the coordinator — do not silently keep USING(true).
Decide `count_all_users()`: keep it only if a real caller needs it post-tighten; otherwise drop it
in the same migration (No Stale Concepts).

**Regression test must FAIL on origin/main** (proving the hole) and pass on your branch. NEW RIGOR:
QA will check out main and run it RED, then GREEN on your branch — build that test first.

## Start

1. **Resolve your skills.** Confirm you can invoke `coordinated-build` by name; if not, open the
   absolute **Build skill path** above and follow it directly.
2. `[ -d node_modules ] || pnpm install` (worktrees share the pnpm store).
3. Read issue #75 IN FULL (`gh issue view 75`), plus migrations 0045/0046 and the RLS test suites
   they shipped with.
4. Invoke the **`coordinated-build`** skill and follow it: write the plan → escalate it to the
   coordinator for approval → on approval, build TDD/green → run the pre-push trio
   (`pnpm format:check && pnpm lint && pnpm typecheck`) + fresh rebase before every push → close out
   with **`coordinated-wrap-up`** (PR + report to the coordinator).

## Your compact (non-negotiable)

- Work **only** in this worktree/branch. Commit green per task; `git add` only that task's files
  (`Co-Authored-By: Claude Sonnet 4.6`).
- Plan approval comes from the **coordinator**, not a human gate. Do not write code before it.
- **Escalate to coordinator label `Coordinator`** the moment you hit: a blocker, a plan ready for
  approval, a design fork outside this spec, a review request, or done. Tag security questions
  `[SECURITY]`/`[RLS]`.
- **Never touch** the project board, milestones, or merge — those are the coordinator's.
- **Self-monitor your context on countable events**, not a felt %. At ~80–100k tokens, or the
  moment you see a compaction summary in your own context: message the coordinator, then use the
  **`relay`** skill — write a continuation handoff, `herdr-handoff` your successor, and let the
  coordinator reap you.
- Honor every CLAUDE.md Hard Invariant. No secrets in any doc, payload, log, or prompt.
- **Caveman mode** for all status/escalations to the coordinator (terse, no filler, full technical
  accuracy — saves tokens). Commit messages, PR bodies, and code stay normal/conventional.

## Collision notes (from the coordinator)

- **Your migration number is 0047** — you are the only migration-bearing lane in flight; latest on
  main is 0046. Follow the 0045/0046 placement precedent (app-level auth SQL in
  `infra/postgres/migrations/`). Never edit an applied migration.
- Use your own database: `JARVIS_PGDATABASE=jarvis_fix75` (set in your spawn env) for all
  DB-touching tests.
- fix76 (compose restart policy) runs in parallel — it touches `docker-compose`/infra only, no SQL
  overlap. No shared files expected; escalate if you need to touch compose files.
- Run the required agentmemory recalls before RLS work: `"jarv1s RLS shareability policy"` and
  `"jarv1s migration hash placement"`.

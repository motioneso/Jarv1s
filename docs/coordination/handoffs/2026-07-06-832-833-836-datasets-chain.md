# Build Handoff — datasets host-pinning hardening chain (#832 → #833 → #836)

**Spec (approved):** docs/superpowers/specs/2026-07-04-module-dataset-connector-sdk.md
**GitHub issues (do all three, IN THIS ORDER, one PR each):**
1. **#832** — log host-pinning violations distinctly instead of silent `catch {}`
2. **#833** — constrain header forwarding across host-pinned redirect hops
3. **#836** — 303 redirect method downgrade + cache-key user-scoping guard

**Risk tier:** #832 `routine`, #833 `sensitive` (security-adjacent header handling — build to a
higher bar, this guards the future api-key path), #836 `routine`.
**Worktree:** `/home/ben/Jarv1s/.claude/worktrees/832-datasets-host-pinning`
**Branch:** `832-datasets-host-pinning` off `origin/main` @ `616b9ed1`
**Build skill path (absolute):** `/home/ben/Jarv1s/.claude/worktrees/coord-2026-06-30-rfa-fleet/.claude/skills/coordinated-build/SKILL.md`
**Coordinator label:** `Coordinator` — escalate via `herdr-pane-message`; verify `herdr pane list`
shows EXACTLY ONE pane with this label, resolved fresh (never a cached pane number).
**Coordinator session id:** `f64fd971-3fad-4880-a2fd-6dbb7aba935e`
**Relay trigger:** context-meter 70% warning, or a compaction summary in your own context →
message the coordinator, then use the `relay` skill immediately.

## Why these three are bundled as a chain, not 3 worktrees

All three rewrite the same function: `createHostPinnedFetch`'s redirect loop in
`packages/datasets/src/host-pinning.ts` (and #832/#836 also touch `client.ts`). Building them as
independent worktrees would produce unmergeable conflicts. Build and land them **sequentially in
this one worktree, one PR per issue**, rebasing the branch forward after each merges:

1. Build #832 first (establishes a pinning-error class/marker at the throw sites in
   `host-pinning.ts` + fixes the bare `catch {}` in `client.ts`). PR, wait for coordinator merge.
2. After #832 merges, `git fetch origin && git rebase origin/main`, then build #833 (strip
   sensitive headers across redirect hops, same redirect loop). PR, wait for merge.
3. After #833 merges, rebase again, then build #836 (303→GET method downgrade in the same loop +
   `buildCacheKey` user-scoping comment in `client.ts`). PR, wait for merge.

Do not batch all three into one PR — the coordinator wants three independently reviewable PRs,
landed in this order, each rebased on the previous.

## Start (per issue, repeat 3x)

1. `[ -d node_modules ] || pnpm install` (first time only; worktrees share the pnpm store).
2. Read the issue body in full: `gh issue view <NN> --repo motioneso/jarv1s`.
3. Read the spec above IN FULL.
4. Invoke **`coordinated-build`** and follow it end-to-end: verify the issue's acceptance
   criteria against your actual branch → plan → coordinator approval (do NOT write code before
   it) → TDD build → **`coordinated-wrap-up`** (PR + report). Escalation rules, gate commands,
   and comms style are defined there — this doc does not restate them.
5. After the coordinator confirms merge, rebase and move to the next issue in the chain.

## Run-specific bans (non-negotiable)

- Work ONLY in this worktree/branch; `git add` by explicit path — never `git add -A` or repo-wide
  `pnpm format`.
- Never touch `docs/coordination/` (coordinator-only), the project board, milestones, or merge.
- No secrets in any doc, payload, log, or prompt.

## Collision notes (from the coordinator)

- You own `packages/datasets/src/host-pinning.ts` and `client.ts` for the whole run — no other
  agent touches these files. #834/#835/#837 (running in parallel in other worktrees) are fully
  disjoint from your files, confirmed by the Phase-0 collision map in the run manifest.

# Build Handoff — #665 chat stop should not kill chat session

**Spec (approved):** `docs/superpowers/specs/2026-06-24-chat-heartbeat-stop.md`
**GitHub issue:** [#665](https://github.com/motioneso/Jarv1s/issues/665)
**Risk tier:** `sensitive` — touches chat turn lifecycle, RPC/API behavior, and web UI stop behavior.
**Worktree:** `~/Jarv1s/.claude/worktrees/665-chat-stop-esc`
**Branch:** `coord/665-chat-stop-esc` from `origin/main` at `9dac3861`
**Build skill path:** `~/Jarv1s/.claude/skills/coordinated-build/SKILL.md`
**Coordinator label:** `Coordinator`
**Coordinator session id:** `019f22a1-d0c6-73c3-8914-7f326c41dd39`
**Relay threshold:** countable events — ~80-100k tokens or compaction summary.

## Start

1. `[ -d node_modules ] || pnpm install`.
2. Read this handoff and the approved spec section for Stop in `docs/superpowers/specs/2026-06-24-chat-heartbeat-stop.md`.
3. Invoke/follow `coordinated-build`: verify premise, write a compact plan, escalate to `Coordinator` for approval before code.

## Issue

#665: "The stop button shouldn't kill the chat session entirely, it is meant to just be like pressing esc in the terminal."

Known current graph hits:

- `apps/web/src/chat/chat-drawer.tsx`: `stop()` and `stopSending()`.
- `packages/chat/src/live/chat-session-manager.ts`: `ChatSessionManager.stopTurn()`.
- Existing e2e coverage: `tests/e2e/chat-drawer.spec.ts`.

## Scope

Fix the root cause once, at the shared stop path:

- Stop should cancel/end only the in-flight turn, equivalent to terminal Esc.
- Stop must not kill or discard the whole reusable chat session.
- The turn lock must release and the user must be able to send the next message immediately.
- Preserve wedge recovery/orphan cleanup behavior outside the user Stop path.

Do not broaden into the rest of #456 unless the bug requires it.

## Verification

Smallest checks that prove the regression:

- A focused unit/integration test for the session manager or live route path showing user stop does not destroy the session.
- Existing chat drawer stop e2e/unit coverage, or a focused addition if the current test misses this bug.
- `pnpm format:check && pnpm lint && pnpm typecheck`.

## Collision Notes

- Do not touch `docs/coordination/` from the build branch.
- No repo-wide `git add .` or `git add -A`.
- No project board / milestone / merge actions; coordinator owns those.
- Keep staging explicit and scoped to #665.

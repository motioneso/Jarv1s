# Build Handoff — 647 Generic IMAP Send

**Spec:** `docs/superpowers/specs/2026-06-30-generic-imap-email-connector-design.md`
**GitHub issue:** #647
**Risk tier:** `security`
**Provider:** GLM / opencode
**Worktree:** `~/Jarv1s/.claude/worktrees/647-imap-send`
**Branch:** `coord/647-imap-send` off `origin/main` `73625702`
**Build skill path:** `~/Jarv1s/.claude/skills/coordinated-build/SKILL.md`
**Coordinator label:** `Coordinator`
**Coordinator session id:** `019f1f06-e858-7862-9245-ae8a22ea968c`

## Current State

- Task 0 is done: restored approved generic IMAP spec from commit `4bfa0221`.
- Task 0 commit on this branch: `6f8a6aac`.
- #629/#660 made reply write tooling work for Google only.
- #647 implements Slice D: IMAP credential accounts send via SMTP behind `EmailWriteProvider`.

## Scope

- Implement send path behind `EmailWriteProvider`.
- Reuse the existing app-password preset registry from generic IMAP read path.
- Support IMAP/non-Google messages that currently return unsupported in `packages/chat/src/email-write-impl.ts`.
- Keep Outlook/XOAUTH2 deferred.

## Security Constraints

- App passwords and connector credentials never reach frontend, logs, pg-boss payloads, exports, AI prompts, or PR text.
- Preserve provider-agnostic AI.
- Metadata-only job payload rule holds.
- Use existing encrypted credential paths; do not invent a new secret store.
- This is security-tier: PR needs cross-provider security QA and Ben sign-off before merge.

## Coordination

- Do not touch `docs/coordination/`.
- No broad `git add .` or `git add -A`.
- No repo-wide format; format and stage only changed files.
- Escalate plan for coordinator approval before implementation.

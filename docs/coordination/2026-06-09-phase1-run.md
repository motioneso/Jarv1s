# Coordination Run — phase1 (LIVE)

**Date:** 2026-06-09
**Coordinator lock:** label `Coordinator` = pane `w6539dd8e4d1621-6` (env `HERDR_PANE_ID=p_7`) — the
single coordinator for this run; agents escalate to the label `Coordinator` (exactly one pane holds it).
**Merge policy:** autonomous-after-verified-QA (coordination mode)
**Context self-handoff threshold:** ~70%
**Epic:** #46 Phase 1 · Foundation Hardening

> GitHub is the source of truth for spec/issue/board status; this file holds in-flight operational
> state only. Two-coordinator incident (2026-06-09) → single-coordinator lock now enforced.

## Done (merged to main)

- **#51** unit-tests-in-CI — PR #62? no, **PR #63** squash `bfe3613`. Board Done, epic box ✓.
- **#58** native-build pin — **PR #62** squash `62df10c`. Board Done, epic box ✓.

## In flight (Wave 1 — 3 concurrent, 1 reserve)

| Cluster | Issues    | Pane / label       | Worktree / branch  | DB         | Status                          |
| ------- | --------- | ------------------ | ------------------ | ---------- | ------------------------------- |
| SEC     | #52       | pane 1 / build-52  | p1-auth-secret-rls | jarvis_p52 | building (owns migration ~0045) |
| API     | #54 → #53 | pane 3 / build-api | p1-api-hardening   | jarvis_api | building (one PR for both)      |
| AI      | #57 → #55 | pane 2 / build-ai  | p1-ai-isolation    | jarvis_ai  | building (one PR for both)      |

Reserve: pane 4. (build-ai pane 2 reads the skill files directly — its session predates the
coordinator skills.)

## Held (queued)

- **#56** scheduled backups — independent; launch when a slot frees.
- **#60** UI honesty — independent (apps/web); launch when a slot frees.
- **#59** workspace-vocab removal — **must land AFTER the AI cluster** (its #57 edits the same
  calendar/email/notifications `manifest.ts` files); rebase on that PR.

## Merge order / collisions

- Only **#52** carries a migration (no ordering contention).
- Clusters are mutually independent (db/sql · apps/api · packages/ai) → merge in any order as each
  goes QA-green. #59 after AI cluster.

## Outstanding escalations

- (none yet — awaiting the three plan-approval escalations)

## Reaped

- Duplicate coordinator pane 5 (closed). build-51 pane reused as build-52; its worktree removed.

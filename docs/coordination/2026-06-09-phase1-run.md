# Coordination Run — phase1 + remediation (LIVE) — COORDINATOR RELAY IN PROGRESS

**Date:** 2026-06-09
**Coordinator lock:** label `Coordinator` = pane `w6539dd8e4d1621-2` (Fable/Sonnet relay 2026-06-10, session `2d36b1d7-a372-458d-b85a-79eed5e85f4a`). **Pane ids REFLOW on pane close** — authority anchors are terminal `term_653dd5a7b87d19` + agent session `2d36b1d7-a372-458d-b85a-79eed5e85f4a`; re-resolve `herdr pane get "$HERDR_PANE_ID"` against these before every merge. Pane ids reflowed twice: original -6→-5 (outgoing Sonnet reaped), then -2 (Ben closed idle build-fix73/56/fix74 panes -2/-3/-4, coordinator shifted to -2).
**Successor model:** **Sonnet** (resident dispatch loop is mechanical — per the perf audit P-C2). Use **Opus only** for the v2 rewrite + any genuine design-fork adjudication.
**Merge policy:** autonomous-after-verified-QA for `routine` tier; **`security` tier needs Ben's explicit merge sign-off** (new rigor — see below).
**Self-handoff threshold:** relay every **~80–100k tokens / 2–3 merges** (perf audit P-C1 — do NOT drag a fat context).

> **RELAY TRIGGERED:** Fable coordinator compacted — relay in progress. Sonnet successor: read this
> file IN FULL, claim the lock, re-adopt the live fleet (herdr pane list), confirm you're driving,
> reap the old coordinator pane.

> **Messaging:** use `herdr pane run <pane_id> "<text>"` to send messages to agents (Ben's preferred
> method 2026-06-10). `herdr agent send` queues but may not auto-submit for long messages; if using
> it, follow with `herdr pane send-keys <pane_id> Enter`.

## Phase 1 — BUILT 10/10 (all merged to main)

#51 #58 #53 #54 #55 #57 #59 #60 #52 #56 — all on `main` (head ~e828a9d).

## Remediation — COMPLETE (2026-06-10)

| Issue    | What                            | PR  | Merged           | Opus QA verdict  |
| -------- | ------------------------------- | --- | ---------------- | ---------------- |
| #72 (C1) | rate-limit XFF bypass           | #77 | 2026-06-10 02:23 | MERGE-READY HIGH |
| #73 (C2) | key rotation bricks legacy      | #78 | 2026-06-10 02:29 | MERGE-READY HIGH |
| #74 (H1) | open auth_sessions bearer table | #79 | 2026-06-10 02:34 | MERGE-READY HIGH |

**Queued / remaining:** #75 + #76 DISPATCHED 2026-06-10 ~03:02 — see Fleet (live) below.

## NEW RIGOR — apply to every fix PR before merge (this is why we're remediating)

These are `security` tier. For each fix PR (#77 first):

1. **Independent review** — a fresh agent (NOT the author). Prefer **cross-model** (Opus or `/codex-review`) for the security review — same-lens Sonnet missed the original flaws.
2. **Verify the regression test actually FAILS on origin/main** (don't trust "fails-by-design" — check out main, run it, confirm RED, then confirm GREEN on the branch). The whole point is the hole gets locked.
3. **Confirm ALL GitHub checks green** (`gh pr checks`) — `compose-smoke` is pre-existing-red (#67), the only acceptable waiver, recorded here.
4. **Get Ben's explicit sign-off** before merging a security-tier PR (new rule — H1/C1 of the safety audit).
5. Post the QA verdict as a **PR comment** (durable) before merging.

## Coordinator v2 rewrite — COMPLETE (commit d43849a)

Risk tiering, compaction tripwire, CI waiver protocol, pane-id authority, model tiering table,
QA-trusts-CI, pre-push trio, guarded install — all 7 skill files updated. rtk hook deferred.

## Queued for next coordinator (v2 rules apply)

| Issue    | What                     | Tier       | Unblocked?                                        |
| -------- | ------------------------ | ---------- | ------------------------------------------------- |
| #75 (H2) | users-SELECT tighten     | `security` | YES — 0046 on main; serialize after #74 migration |
| #76 (M2) | container restart policy | `routine`  | YES — independent                                 |

**Stale doc follow-up:** DONE 2026-06-10 (commit 0b7c582) — `packages/db/src/keyring.ts` comment now
describes `legacyCandidates`; the reserved-`"legacy"`-id text is gone (PR #78 Opus finding).

## Fleet (live) — 2026-06-10 relay state

**Pane IDs as of relay:** Ben closed idle panes -2/-3/-4 (build-fix73/build-56/build-fix74) —
coordinator reflowed from -5 → -2; fix75 reflowed from -6 → -3; fix76 reflowed from -7 → -4.

| Lane  | Label       | Current Pane    | Terminal (stable)   | Branch / worktree                                             | Tier       | Status                                                                                                                                                                      |
| ----- | ----------- | --------------- | ------------------- | ------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| fix75 | build-fix75 | **-3** (stable) | term_653dd78c7df35a | fix75-users-select / .claude/worktrees/fix75-users-select     | `security` | **plan NEEDS-CHANGES** (Opus verdict sent 2026-06-10) — agent amending plan, will relay to fresh successor for build; DO NOT approve plan until it re-escalates with fixes  |
| fix76 | build-fix76 | **-4** (stable) | term_653dd78f9f537b | fix76-restart-policy / .claude/worktrees/fix76-restart-policy | `routine`  | plan APPROVED — **building** (compose restart+healthcheck, smoke --wait, ops docs; no migrations). Flake root cause: pnpm install >60s health deadline, addressed in fix76. |

**DO NOT REAP build-fix72** (pane -1, Ben, 2026-06-10).

Handoffs: `docs/coordination/2026-06-10-fix75-handoff.md` / `2026-06-10-fix76-handoff.md`.
Migration 0047 assigned to fix75 (sole migration lane). Merge order: independent — no serialization.
fix75 is `security` tier ⇒ cross-model Opus QA + posted `gh pr comment` verdict + **Ben merge sign-off**.

### Fix75 Opus review findings (NEEDS-CHANGES — 2026-06-10)

Sent to build-fix75 agent. Two blockers:

- **[CRIT]** `packages/connectors/src/routes.ts:253-272` — `requireAdmin()` reads `app.users` via
  raw `appDb` (no GUC set). Under self-row policy, `current_actor_user_id()` = NULL → 0 rows → every
  admin call 401s. Consumer audit missed this. Fix: route `requireAdmin` through `app.get_user_by_id`
  or `withDataContext`.
- **[HIGH]** Regression test does NOT go RED on main. `release-hardening.test.ts` uses
  `resetEmptyFoundationDatabase` (zero users seeded) → `count(*) = 0` passes on main regardless of
  policy. Fix: seed 2 users, assert user A can't see user B's row (count = 1 not 2).

When fix75's successor re-escalates plan v2 (with these fixed): spawn a fresh Opus subagent to re-verify, then approve and send to the new pane.

**ci_waivers:** `Compose deployment smoke` — red on main; pre-existing per issue #67 (Ben-approved
waiver); being addressed in fix76 lane. `Verify foundation and app` GREEN on main @ 66a7877.

## Live tooling notes

- build-fix72 pane -1: **DO NOT REAP** (Ben, 2026-06-10).
- build-fix73 / build-56 / build-fix74: REAPED by Ben (pane IDs reflowed — see above).
- **Messaging (Ben, 2026-06-10):** use `herdr pane run <pane_id> "<text>"` as the primary send method. `herdr agent send "<label>" "<text>"` + `herdr pane send-keys <pane_id> Enter` also works but can have submit races on long messages.
- **Tab discipline (Ben, 2026-06-10):** spawn all build/QA agents in **window 1** (`--tab <ws>:1`); coordinator's window is coordinator-only except relay successor. fix75/fix76 are grandfathered in tab `:2`.
- ccusage installed (`ccusage daily`); rtk installed but hook NOT wired (deferred to v2 follow-up).
- Open follow-ups: #67 (compose-smoke red, addressed in fix76), #70 (backup:db host pg_dump), #75/#76 (in progress).
- CI waiver on file: `compose-smoke` (#67) — proven pre-existing, Ben-approved.

## Reaped

- Original Opus coordinator pane …-6 (reaped on session exit). Outgoing Sonnet coordinator pane (old `-5`) reaped by Fable successor 2026-06-10. Idle panes build-fix73/-56/fix74 reaped by Ben 2026-06-10. Fable coordinator pane (compaction hit) relaying to Sonnet successor.

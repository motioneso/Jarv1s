# Coordination run manifests

Operational state for in-flight coordinator runs (one file per run: `<run-id>.md`).
GitHub remains the source of truth for spec/issue/board status; these files hold only
the in-flight operational state GitHub does not track. See the `coordinate` skill.

# Coordination Run — 2026-07-09-next-wave

**Date:** 2026-07-09
**Coordinator lock:** label `Coordinator`, **stable anchor = Claude session id
`71f71d9a-bb4e-4089-9c8b-0af25068c346`** (match `agent_session.value` in `herdr pane list`).
Adopted from predecessor `dd8b3920-6924-4eaf-b2bf-4120f187c7a3` (pane `w1:pBR`, closed) via a
clean relay handoff — see "Relay checkpoint" section below for the state that was carried
forward verbatim.
Single-coordinator lock — exactly one pane labelled `Coordinator` whose session id matches this
anchor holds authority for the life of the run. ⚠️ **Pane numbers (`w…-N`) reflow on every
restart/split/reap — do NOT trust any pane number written in this file as an identifier; resolve
the pane fresh by label+session at read time.** Agents escalate to the **label** (routing,
re-claimable); the coordinator merges only when its own pane's **session id** (immutable, NOT the
pane number) matches this recorded anchor.
**Merge policy:** autonomous-after-verified-QA for `routine`/`sensitive`; **`security`-tier needs
Ben's explicit merge sign-off** — N/A this wave, all three items are `routine`. Standing override
from Ben (see `docs/coordination/2026-07-07-issue-queueing-pass.md`): any GREEN QA verdict merges
immediately, no pause-and-ask, security tier included — carried forward, not re-litigated here.
**Relay threshold:** security-tier merge → relay immediately after Phase 3 step 7;
routine/sensitive `merges_since_relay` ≥ 2 → relay. No deferral. Compaction summary = already
past safe → relay, merge nothing.
**merges_since_relay:** 0

**Predecessor run:** `docs/coordination/2026-07-07-issue-queueing-pass.md` — carried #866/#855/#858
readiness forward from there; this file is the fresh manifest for the actual build wave so the
predecessor doc doesn't grow past what's useful. Reference it for full history/context.

**Main CI gate — fix landed, confirming green:** `main`'s `Verify foundation and app` check failed
twice in a row (`886985fb` PR #893, then `1cf017a2` PR #886/#874) — root cause identified: PR #893
landed two spec files (`docs/superpowers/specs/2026-07-08-{herdr-install-and-attach-hint,sports-
followed-team-dedupe}.md`) without running them through prettier first (known trap — see
agentmemory `handoff-doc-prettier-trap`); `pnpm format:check` fails on them, blocking every later
commit's gate regardless of its own content. Trivial whitespace-only fix — not invoking the
CI-waiver protocol. This coordinator opened a fix PR (#894), but a separate concurrently-active
agent session ("Jim (hive agent)", Opus 4.8) independently direct-pushed the identical fix to
`main` first as commit `33270eef` (byte-identical 2-file/2-line diff). PR #894 closed as
duplicate/superseded, its branch and worktree removed. Verifying CI run for `33270eef` now; **do
not spawn any build agent until that run is confirmed green.**

**Scope expansion (Ben, 2026-07-09, live instruction):** full autonomous pipeline authorized for
this wave — Opus 4.8 one-shot review of this manifest + the 3 specs stands in for the Phase-0
"present to Ben, pause" gate; once Opus approves, build all three items through to merge without
further pause; QA approval gate uses **Fable 5** (`model: "fable"`) instead of the tier-default
model; green Fable verdict merges immediately per the existing standing override. After all three
land, deploy to prod (`docker compose pull` + `up -d` in `/home/ben/JarvisProd`, verify digest +
health) once CI auto-publishes the updated `:edge` image.

> This is the coordinator's externalized memory. Keep it CURRENT — it is what lets a fresh
> coordinator adopt this run after a self-handoff. GitHub is the source of truth for
> spec/issue/board status; this file holds only in-flight operational state.

## Queue

| Spec | Issue | Tier | Status | Agent label | Pane | Branch | PR |
| ---- | ----- | ---- | ------ | ----------- | ---- | ------ | -- |
| docs/superpowers/specs/2026-07-08-herdr-install-and-attach-hint.md | #866 | routine+sec-QA | building | Build-866 | w1:pBT | build/866-herdr-install | — |
| docs/superpowers/specs/2026-07-08-sports-followed-team-dedupe.md | #855 | routine | building | Build-855 | w1:pBV | build/855-sports-dedupe | — |
| (none — exempted as tracked-debt, Ben's call 2026-07-09) | #858 | routine | queued (waiting on #855 merge) | — | — | — | — |

Agents tab: `w1:t1E` (label "agents"), created 2026-07-09. Both worktrees branched off
`origin/main` @ `33270eef` (confirmed green CI run 28995848259).

Risk tier: #855, #858 = `routine` (standard QA, auto-merge after green). **#866 upgraded to
`sensitive`-rigor QA** (still auto-merges after green — not gated on Ben sign-off, this is a QA
depth upgrade, not a tier promotion to `security`): per Opus 4.8 manifest review 2026-07-09, #866's
entire spec is a privilege-boundary decision (no web-triggered install/exec, config-power-only) +
supply-chain surface (pinned-checksum binary fetch) — routine `/code-review` could miss a subtle
boundary violation, so its Fable QA pass must run `/security-review` in addition to
`/code-review` + exit-criteria.

**Opus 4.8 manifest review verdict (2026-07-09): APPROVE-WITH-CONDITIONS.** Collision map, merge
order, and #858 exemption all confirmed sound. Conditions applied to this manifest:
1. #866 QA = `/security-review` + `/code-review` (above). If the build agent adds any
   privileged install/exec endpoint against the spec's explicit "no route" decision, or the UI
   references an install script path that doesn't ship in the same PR (with pinned per-arch
   checksums) — that is a **blocking finding, halt for Ben**, not an auto-merge waiver.
2. #858's build agent must rebase onto post-#855 `main` and check what #855 already subsumes
   (#855 already dedups `stories` by URL) — must not re-introduce id-keying or double-fix the
   service layer; reduce #858a to remaining web-layer `key={}` fixes + #858b (fetch timeout).
3. Minor, non-blocking, flag to QA: #855's "most recently created follow" primary-selection
   assumes a `created_at`-style ordering column on `sports_follows` (0133) — confirm it exists.
   #858b's `AbortController` touches the shared datasets fetch path (all connectors) — confirm a
   sane default deadline doesn't break long-running sources.

**#858 spec-gate note:** #858 carries the `RFA` label but has zero spec file and zero approval
comment anywhere (confirmed: not on `origin/main`, not in the live `/home/ben/Jarv1s` checkout,
absent from that checkout's `docs/superpowers/handoffs/2026-07-08-needs-spec-queue.md`). Surfaced
to Ben; he confirmed **exempt as tracked-debt** (small bug-fix/hardening follow-up from #857's
Fable review, not a new feature/module) — build directly from the issue body, no spec doc
required, routine tier.

## Dependency / merge order

Opus one-shot collision map (2026-07-09, grounded on `origin/main`), full verdict on file in
run history — summary:

- **#866** — fully isolated (`packages/settings/*`, `packages/ai/src/adapters/multiplexer-resolve.ts`,
  `packages/module-registry/src/chat-multiplexer.ts`, `infra/docker-compose.prod.yml`). No overlap
  with sports. **Parallel-safe with everything.**
- **#855 ↔ #858 — CONFLICT.** Both edit `packages/sports/src/sports-service.ts`'s `getOverview()`
  region (~lines 188–347): #855 rewrites the merge/dedup grouping structurally; #858a re-keys the
  same id-based dedup/caps (`new Set(...map(h => h.id))`, `pickedIds`, `topStoryIds`) to
  `url`/composite keys. Parallel builds from the same base guarantee a merge conflict and semantic
  drift in one hot function. **Must serialize: #855 lands first, then #858 rebases onto the new
  structure.**
  - #858 also has a second, unrelated part (#858b: per-fetch timeout in
    `packages/datasets/src/{host-pinning.ts,client.ts}` via `createHostPinnedFetch`) that has zero
    overlap with sports or #866 — could run fully parallel if #858 were split. **Not splitting for
    this wave** (single issue, single build agent, simpler to serialize the whole thing after #855
    lands) — revisit only if #855 stalls and #858b is worth decoupling.
- **No migration-numbering risk** — all three items explicitly add zero migrations.

**Parallel group 1:** #866 (solo) + #855 — launch together, both from fresh worktrees off
`origin/main`, once main CI confirmed green.
**Serialized chain A:** #855 → #858 (#858 build agent spawns only after #855's PR merges to main;
its worktree branches from the post-#855 `main`).
**Merge order:** #866 and #855 in either order (independent); #858 last, after #855 is on `main`.

## CI waivers

| Check | PR | Proven red on `main` @ SHA | Proof | Ben-approved |
| ----- | -- | --------------------------- | ----- | ------------- |
| <none> | — | — | — | — |

## Outstanding escalations

- [ ] None — all three items cleared for spawn pending main CI green.

## Reaped sessions

- Build-866 original session `b4e78394-4bb9-4528-9977-dde3683467c7` (pane `w1:pBT`) — self-relayed
  at context 70% during Phase-1 spec/branch grounding (no code written yet, worktree clean).
  Successor `b60e33e6-b483-4ee1-ae76-658eb69a6fdb` confirmed driving in the same worktree
  (`build/866-herdr-install`), pane renamed back to `Build-866` (`w1:pBW`). Old pane closed.

## Relay checkpoint (coordinator session `dd8b3920-6924-4eaf-b2bf-4120f187c7a3`, context-meter 70%)

**Ben's live scope expansion (this turn, not written above yet):** "Have opus 4.8 review manifest
and plan, once approved build all the way, fable approval, merge and deploy." Applied: Opus 4.8
manifest review = APPROVE-WITH-CONDITIONS (already folded into Queue/tiers above). Standing
authorization: build all 3 items through to merge with **no further Ben pause**; QA approval gate
for every PR uses **Fable 5** (`Agent(model: "fable", ...)`) instead of tier-default model —
green Fable verdict merges immediately (existing standing override already covers routine tiers).
**After all three land, deploy to prod**: `docker compose pull` + `up -d` in `/home/ben/JarvisProd`
using `--env-file env.production.local`, verify digest + health, once CI auto-publishes the
updated `:edge` image. This is the authoritative scope for the rest of this run — do not
re-confirm with Ben unless a genuine security/design fork appears (per standing coordinate rules).

**Fleet state right now:**
- **Build-866** (label `Build-866`, resolve fresh — do NOT trust a cached pane id) — grounding
  complete, no drift vs spec/branch. **BLOCKED, unanswered, needs Ben's real-world answer**: repo
  has ZERO Herdr release-binary/checksum source anywhere (grepped whole tree) — spec requires a
  pinned per-arch URL+SHA256 in `scripts/install-herdr.sh`, agent won't fabricate one. **Ask Ben:
  where do Herdr release artifacts actually live (GitHub org/repo, private registry, etc)?** Also
  confirmed via Dockerfile: runtime image has NO curl/wget (only tmux/git/ca-certificates/
  bubblewrap) but does have node — agent proposed using node's `https` module instead of shelling
  out; fine to approve unless Ben has a preference. **Successor's first action: get Ben's answer,
  then `herdr-pane-message` it to label `Build-866`** (resolve pane fresh by label at send time).
- **Build-855** (label unset on the pane record but worktree `855-sports-dedupe`, branch
  `build/855-sports-dedupe`) — status `working`, no blockers reported yet, no escalation.
- **#858** — still queued, waits for #855's PR to merge to `main` before its worktree is created.
- **merges_since_relay:** 0 — nothing has merged yet this run.
- Agents tab: `w1:t1E`. Liveness monitor was running as a persistent background Monitor in the
  relayed-from session (task id not portable across sessions — successor must start its own).

**Not yet done:** no PR open for either build. No QA spawned yet (Fable model). No deploy started.
Do not skip the CI-waiver/QA/merge-order protocol just because Ben authorized full autonomy —
autonomy removes the *pause*, not the *verification*.

**Addendum — Build-855 also self-relayed during this same checkpoint window:** old session
`ad38e790-50e0-4555-92c0-3f0e45285d7c` (pane `w1:pBV`, closed/reaped) → successor session
`2958adbf-689d-478b-a67e-bf30e417cbc9`, pane `w1:pBX`, label `Build-855` — confirmed driving
(reading handoff+relay doc) before reap. Relay happened at ctx 71%, **before any plan file was
written**: spec verified clean against branch (createdAt exists on SportsFollowDto/migration 0133
— clears Opus's non-blocking QA flag #3; espnSport+sourceTeamId confirmed on catalog/source types;
followedTeamCardSchema already has nextMatch/resultMatch/todayGameState so **no shared-api schema
change needed**), design worked out (grouping module + `Across`-suffixed helper refactor in
`sports-service.ts`) but not yet committed to a plan doc — successor's first job is writing that
plan and sending it to `Coordinator` for approval (grounding already done, don't re-verify).

**Live pane map at handoff time** (resolve fresh anyway — do not trust these numbers):
`Coordinator` = `w1:pBR` / session `dd8b3920-6924-4eaf-b2bf-4120f187c7a3` (this session, relaying
now). `Build-866` = `w1:pBW` / session `b60e33e6-b483-4ee1-ae76-658eb69a6fdb`, `agent_status:
done` (expected — it's idle waiting on the Herdr-artifact answer above, not actually finished).
`Build-855` = `w1:pBX` / session `2958adbf-689d-478b-a67e-bf30e417cbc9`, `agent_status: working`.
Agents tab `w1:t1E` also has a root placeholder pane `w1:pBS` (`agent_status: unknown`, no label)
— not a build agent, ignore it in liveness monitoring.

**Coordinator self-relay in progress:** this session (`dd8b3920-...`) is spawning its successor
now in the same tab (`w1:t15`) per the coordinate skill's self-handoff protocol. Successor's first
actions, in order: (1) confirm driving + reap this pane, (2) get Ben's answer to the Build-866
Herdr-artifact question (AskUserQuestion — not yet asked), (3) relay that answer to `Build-866` via
`herdr-pane-message`, (4) start a fresh persistent liveness Monitor scoped to tab `w1:t1E` filtered
by label (`Build-855`/`Build-866`, excluding `w1:pBS`), (5) resume normal supervision.

# Build Handoff — fix76-restart-policy

**Spec (approved):** GitHub issue #76 (verified Fable M2 audit finding) — for remediation fixes the
verified issue is the spec, per the #72–#74 precedent.
**GitHub issue:** #76
**Risk tier:** `routine` (compose/infra deployment config; no schema/auth/secret surface — standard QA, auto-merge after green.)
**Worktree:** ~/Jarv1s/.claude/worktrees/fix76-restart-policy **Branch:** fix76-restart-policy (off origin/main)
**Build skill path (absolute):** ~/Jarv1s/.claude/skills/coordinated-build/SKILL.md
**Coordinator label:** `Coordinator` (UNIQUE — escalate via `herdr-pane-message`; before messaging, verify `herdr pane list` shows EXACTLY ONE pane with this label. Never guess a `…-N` pane-id — they reflow when panes close.)
**Relay threshold:** countable events — ~80–100k tokens OR a compaction summary in your own context (then relay immediately).

## Task

Issue #76 (M2): crash-exit handlers cause permanent outage — the compose deployment has no
container restart policy and no healthcheck, so a crashed API/worker stays down until a human
notices. Add appropriate `restart:` policies and container healthchecks to the deployment compose
configuration (and any docs in `docs/operations/` that describe the deployment), so a crash-exit
self-heals.

**Heads-up:** the `Compose deployment smoke` CI job has been flaky on recent main runs
(`Timed out waiting for http://localhost:3099/health`, scripts/smoke-compose.ts:132) — your change
touches exactly this surface. Run `pnpm smoke:compose` locally and make sure your change leaves it
reliably green; if you find the flake's root cause, report it (it may belong in issue #67 follow-up
rather than your PR).

## Start

1. **Resolve your skills.** Confirm you can invoke `coordinated-build` by name; if not, open the
   absolute **Build skill path** above and follow it directly.
2. `[ -d node_modules ] || pnpm install` (worktrees share the pnpm store).
3. Read issue #76 IN FULL (`gh issue view 76`), plus `docs/operations/release-hardening.md` and the
   compose files.
4. Invoke the **`coordinated-build`** skill and follow it: write the plan → escalate it to the
   coordinator for approval → on approval, build TDD/green → run the pre-push trio
   (`pnpm format:check && pnpm lint && pnpm typecheck`) + fresh rebase before every push → close out
   with **`coordinated-wrap-up`** (PR + report to the coordinator).

## Your compact (non-negotiable)

- Work **only** in this worktree/branch. Commit green per task; `git add` only that task's files
  (`Co-Authored-By: Claude`).
- Plan approval comes from the **coordinator**, not a human gate. Do not write code before it.
- **Escalate to coordinator label `Coordinator`** the moment you hit: a blocker, a plan ready for
  approval, a design fork outside this spec, a review request, or done.
- **Never touch** the project board, milestones, or merge — those are the coordinator's.
- **Self-monitor your context on countable events**, not a felt %. At ~80–100k tokens, or the
  moment you see a compaction summary in your own context: message the coordinator, then use the
  **`relay`** skill — write a continuation handoff, `herdr-handoff` your successor, and let the
  coordinator reap you.
- Honor every CLAUDE.md Hard Invariant. No secrets in any doc, payload, log, or prompt.
- **Caveman mode** for all status/escalations to the coordinator (terse, no filler, full technical
  accuracy — saves tokens). Commit messages, PR bodies, and code stay normal/conventional.

## Collision notes (from the coordinator)

- No migration for this issue. fix75 (users-SELECT RLS) runs in parallel — SQL/RLS only, no compose
  overlap. No shared files expected; escalate if your scope grows beyond compose/infra/ops docs.
- Use your own database: `JARVIS_PGDATABASE=jarvis_fix76` (set in your spawn env) for any
  DB-touching tests.

# Awaiting Ben

Decisions that need Ben and only Ben. Each entry says what is blocked and what the options are.
Remove an entry once he rules and the ruling is recorded where the work lives.

## No open items

Both 2026-07-27 entries (rescued from the pre-reset working tree) are resolved:

- **The live-path gate** — Ben ruled on 2026-07-27: adopt it. Recorded as a hard invariant in
  `CLAUDE.md`, stated in full in `docs/DEVELOPMENT_STANDARDS.md` → **Live-Path Gate**, and enforced
  at the merge step in `.claude/skills/coordinate/SKILL.md`.
- **The voice/STT spec approval** — not a judgement call after all. Issue #874 closed as *completed*
  on 2026-07-09 and the feature is on `main`, so the spec was approved and built; only its status
  line was stale. Corrected in `docs/superpowers/specs/2026-07-08-voice-stt-settings.md`.
